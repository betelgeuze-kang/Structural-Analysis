# Betelgeuze State

Status: created
Mode: deep
Risk: R3
Goal: Close gaps from docs/commercial-structural-solver-product-gap-ledger.md and docs/structural-analysis-ai-engine-gap-ledger.md until commercial structural solver and AI engine readiness gates are evidence-backed
Done criteria: Verify the requested outcome and hand off exact gaps.

Notes:
- Keep this concise. Do not store secrets.
- 2026-06-03: Reported parser/runtime/CI/path fixes reconfirmed; G1 uncoarsened boundary P-Delta claim boundary narrowed to accepted `0.45`, first failed `0.455` without closure promotion.
- 2026-06-03: G1 uncoarsened boundary P-Delta accepted-state checkpoint/resume tooling added and real-MGT smoke-tested (`0.05` checkpoint -> resumed `0.1` accepted); full G1 closure remains open.
- 2026-06-03: Durable G1 `0.45` checkpoint replayed in official sidecar; `0.45025` still fails fixed-point increment (`1.246e-04 > 1e-04`) despite residual pass, so G1 remains partial.
- 2026-06-03: G1 relaxed checkpoint continuation aggregate advanced uncoarsened boundary P-Delta frontier to `0.5`; `0.55` still fails fixed-point increment (`1.470e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: Reported parser/runtime/CI/gap/path issues reverified; delivery status snapshot commands now use a shared explicit `--productization-dir`/`--output-json` helper, and focused regression suite passed `20` tests in `6.17s`.
- 2026-06-03: G1 relaxed checkpoint continuation advanced again to accepted `0.54`; `0.545` fails fixed-point increment (`1.904e-04 > 1e-04`) despite residual pass, so G1 remains partial.
- 2026-06-03: G1 relaxed checkpoint continuation bracket narrowed again: accepted frontier remains `0.54`, first failed is now `0.5425` with fixed-point increment `1.239e-04 > 1e-04`; a 32-iteration retry timed out and was not promoted.
- 2026-06-03: G9 ROCm residual-row Kaczmarz correction added; shell stays `3.603 N`, coupled improves slightly from `0.188749 N` to `0.188666 N` but still misses the `0.05 N` gate, so G9 remains partial.
- 2026-06-03: G9 Kaczmarz damping refined to `0.00048828125`; shell improved from `3.602922 N` to `3.598564 N`, coupled remained `0.188666 N`, and G9 remained partial.
- 2026-06-03: G9 Kaczmarz damping refined again to `0.000030517578125`; shell improved to `3.598281 N`, coupled remained `0.188666 N`, and G9 remains partial.
- 2026-06-03: User-reported parser/runtime/perf/gap/path-contract issues rechecked; focused regression suite passed `24` tests in `6.61s`, confirming fixes without promoting G1/G9 closure.
- 2026-06-03: G1 relaxed checkpoint continuation advanced to accepted `0.5406`; `0.5407` still fails fixed-point increment (`1.676e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 relaxed checkpoint continuation failed bracket narrowed to `0.54061`; accepted frontier remains `0.5406`, while `0.54061` passes residual (`8.12e-06 N`) but fails fixed-point increment (`1.296e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 relaxed checkpoint continuation failed bracket narrowed again to `0.540601`; accepted frontier remains `0.5406`, while `0.540601` passes residual (`2.72e-05 N`) but fails fixed-point increment (`1.159e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 secant-seed sidecar added; accepted `0.54 -> 0.5406` state extrapolation seeds `0.540601` but still fails fixed-point increment (`1.161e-04 > 1e-04`), so a linear predictor alone is not closure.
- 2026-06-03: G1 seed alpha-scan globalization advanced the accepted frontier to `0.54061`; `0.54062` still fails fixed-point increment (`1.910e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 alpha-scan plus short relaxed remap continuation advanced accepted frontier to `0.585`; `0.586` still fails fixed-point increment (`1.701e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 alpha-scan/remap continuation advanced accepted frontier to `0.621`; `0.6215` still fails fixed-point increment (`1.572e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 alpha-scan/remap continuation advanced accepted frontier to `0.635`; `0.64` still fails fixed-point increment (`4.438e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: User-reported parser/runtime/perf/gap/path-contract issues rechecked again; focused regression suite passed `24` tests in `6.68s`, and native reanalysis CI smoke alone passed `3` tests in `3.71s`.
- 2026-06-03: G1 alpha-scan/remap continuation advanced accepted frontier to `0.64`; `0.6425` still fails fixed-point increment (`1.501e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 alpha-scan/remap continuation advanced accepted frontier to `0.645`; `0.6475` still fails fixed-point increment (`5.050e-04 > 1e-04`), so G1 remains partial.
- 2026-06-03: G1 fine alpha-scan/remap continuation replaced the old failed `0.6475` segment and advanced accepted frontier to `0.6475`; first failed bracket is now `null`, but full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 fine alpha-scan/remap continuation advanced accepted frontier to `0.65`; first failed bracket remains `null`, but full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation advanced accepted frontier to `0.651`; a direct larger `0.651` step failed increment, but micro-steps accepted it. First failed bracket remains `null`; full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation advanced accepted frontier to `0.652`; first failed bracket remains `null`, but full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation evidence is now integrated at accepted frontier `0.653`; first failed bracket remains `null`, and full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation advanced accepted frontier to `0.654`; first failed bracket remains `null`, and full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation advanced accepted frontier to `0.655`; first failed bracket remains `null`, and full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 finer alpha-scan/remap continuation advanced accepted frontier to `0.656`; first failed bracket remains `null`, and full-load `1.0` consistent Newton/material closure remains open.
- 2026-06-03: G1 direct residual Newton bridge is now explicit; `R(u,lambda)` at checkpoint `0.656` has physical direct residual `4.28e4 N` versus fixed-point receipt residual `1.10e-04 N`, and current-tangent trust-region Newton is non-descent, so G1 remains partial.
- 2026-06-03: G1 direct residual Newton bridge now records micro trust-region descent from `4.28e4 N` to `1.55e4 N` at alpha `0.0005`, and `ai_physics_guard_execution` enforces `blocked_from_promotion`; G1 remains partial because residual and increment gates still fail.
- 2026-06-03: G1 direct residual Newton bridge now runs `3` accepted micro trust-region iterations by default, lowering direct residual from `4.28e4 N` to `1.554e4 N` with alpha sequence `0.0005 -> 0.00025 -> 0.000125`; G1 remains partial because the residual gate is still far from tolerance and the third-step increment is still slightly above `1e-4`.
- 2026-06-03: G9 ROCm sparse probe now includes post-coarse micro residual-row Kaczmarz; shell residual frontier moved `3.5983 -> 3.5942 N` and coupled `0.188666 -> 0.187518 N`, but coupled still exceeds the `5e-2 N` gate so G9 remains partial.
- 2026-06-03: G1 direct residual Newton now includes directional finite-difference residual-Jacobian alpha candidates and 6 accepted trust iterations; direct residual improved `4.275e4 -> 1.553959e4 N`, and later iterations pass increment gate, but residual gate remains open so G1 is still partial.
- 2026-06-03: G1 direct residual contract now fixes `lambda*F_ext` to a reference-configuration external load vector instead of reassembling it with displacement; final residual stays about `1.553959e4 N`, confirming load-vector drift was not the main blocker.
- 2026-06-03: G1 accepted-history secant subspace globalization found a residual-only candidate `1.550832e4 N`, but relative increment `1.80e-03` fails the gate, so the candidate is recorded and blocked from final promotion.
- 2026-06-03: G1 gate-aware secant promotion now selects alpha `0.015625`, reducing final direct residual to `1.553862e4 N` while passing relative increment `5.64e-05`; residual gate remains open.
- 2026-06-03: G1 gate-aware secant promotion is now iterative for two passes; final direct residual moved to `1.553732e4 N` with relative increment `7.48e-05`, but residual gate remains open.
- 2026-06-04: G1 gate-aware secant promotion default limit is now four passes; official receipt promotes three passes and stops on pass 4, moving final direct residual to `1.553579e4 N` while keeping relative increment under `1e-4`; residual gate remains open.
- 2026-06-04: G1 finer secant alpha grid adds `0.0078125` and promotes pass 4; final direct residual is now `1.553490e4 N` with relative increment `5.18e-05`; residual gate remains open.
- 2026-06-04: G1 gate-limited secant alpha candidates now promote six passes; final direct residual is `1.552967e4 N` with final relative increment `9.97e-05`, so the increment/remap gate is respected but the direct residual gate remains open.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace now promotes one JVP candidate; final direct residual is `1.552803e4 N` with relative increment `9.499e-05`, but the residual gate remains open.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace promotion is now iterative for two passes; final direct residual is `1.552638e4 N` with relative increment `9.499e-05`, but the residual gate remains open and commercial gap ledger stays `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace promotion now runs four passes; final direct residual is `1.552309e4 N` with relative increment `9.499e-05`, but the residual gate remains open and commercial gap ledger stays `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace promotion now runs six passes by default; final direct residual is `1.551980e4 N` with relative increment `9.499e-05`, but the residual gate remains open and commercial gap ledger stays `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace promotion now runs eight passes by default; final direct residual is `1.551651e4 N` with relative increment `9.499e-05`, but the residual gate remains open and commercial gap ledger stays `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G1 matrix-free consistent-Jacobian subspace now evaluates basis `1/2` candidates per pass; selected basis sequence is `[2,1,2,2,2,2,2,1]`, final direct residual is `1.551637e4 N`, and commercial gap ledger stays `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G9 ROCm sparse probe now includes residual-row block-LS correction after micro Kaczmarz; shell frontier is `3.557612 N` and coupled frontier is `0.185501 N`, with no host dense-solve fallback, but both sparse gates remain open.
- 2026-06-04: G9 residual-row block-LS now requests and accepts `12/12` passes; shell frontier is `3.549514 N` and coupled frontier is `0.185191 N`, with no host dense-solve fallback, but both sparse gates remain open.
- 2026-06-04: G9 residual-row block-LS now requests up to `24` adaptive passes with a `1e-5` relative-improvement floor; shell frontier is `3.548140 N` after `18` accepted improving passes and coupled frontier is `0.185166 N` after `14` accepted passes, with no host dense-solve fallback, but both sparse gates remain open.
- 2026-06-04: G1 basis-width sensitivity check compared matrix-free basis `3` for `4` promotions in `/tmp`; pass-4 residual improved only `15523.020524613024 -> 15523.013372327177 N`, so it was not promoted to official productization evidence. Official frontier remains basis `1/2`, `8/8` promotions, final direct residual `15516.365848834645 N`.
- 2026-06-04: G1 direct residual matrix-free loop now records per-candidate/per-pass improvement, applies a `1e-5` minimum relative-improvement governance floor, and stops with `max_promotions_exhausted` after official `8/8` gate-eligible promotions. Final direct residual remains `15516.365848834645 N`; G1 remains partial.
- 2026-06-04: G9 residual-row block-LS now includes support-connected equation expansion and post-block-LS candidate fusion. Official ROCm receipt accepts `24/24` block-LS passes and moves shell `3.548140 -> 3.255231 N`, coupled `0.185166 -> 0.181876 N`, with no host dense-solve fallback. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: G9 support-connected residual-row block-LS request was extended to `36` passes. Official ROCm receipt accepts `36/36` passes and moves shell `3.255231 -> 3.207407 N`, coupled `0.181876 -> 0.180883 N`; post-block-LS fusion selects the block-LS state, with no host dense-solve fallback. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: G1 direct residual max-16/basis-4 sensitivity runs were not promoted: both exceeded 12 minutes before writing receipt, and the remaining max-16 run was terminated at 18 minutes. This suggests the next G1 move should improve subspace/Jacobian quality rather than merely raising promotion count.
- 2026-06-04: G9 support-connected residual-row block-LS request was extended to `48` passes. Official ROCm receipt moves shell `3.207407 -> 3.167197 N` with `48/48` accepted and coupled `0.180883 -> 0.180554 N` with `46/48` accepted before the minimum-improvement stop; no host dense-solve fallback. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: G1 current-tangent residual-row least-squares correction was added as an opt-in experiment (`--enable-current-tangent-residual-row-correction`) but not promoted to the default official receipt because even a reduced grid pushed the direct probe beyond 6 minutes before completion. G1 remains partial and needs better residual/Jacobian/globalization quality rather than a heavier default probe.
- 2026-06-04: G9 support-connected residual-row block-LS request was extended to `60` passes. Official ROCm receipt moves shell `3.167197 -> 3.124107 N` with `60/60` accepted; coupled remains `0.180554 N` with `46/60` accepted before the minimum-improvement stop. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: G9 support-connected residual-row block-LS minimum relative-improvement floor was lowered from `1e-5` to `1e-6`. Official ROCm receipt keeps shell at `3.124107 N` with `60/60` accepted and moves coupled `0.180554 -> 0.180403 N` with `60/60` accepted; G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: G9 support-connected residual-row block-LS was extended to `72` requested passes and a `1e-7` minimum relative-improvement floor. Official ROCm receipt moves shell `3.124107 -> 3.100591 N` and coupled `0.180403 -> 0.179855 N`, both with `72/72` accepted and no host dense-solve fallback. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- 2026-06-04: ROCm/HIP official solver lane guard added after CPU diagnostic confusion. `solver_runtime_backend_policy.json` is `ready`, sets `official_solver_compute_backend=amd_rocm_hip`, forbids CPU fallback/diagnostic promotion for G1/G9 closure, and the direct residual CLI now requires explicit `--allow-cpu-diagnostic`.
- 2026-06-04: G9 post-block-LS residual-row Kaczmarz polish added on the official ROCm/HIP path. Official receipt stays `partial`, but shell moves `3.100591 -> 3.097603 N` and coupled moves `0.179855 -> 0.179618 N`; post-block-LS fusion selects the Kaczmarz state, no CPU fallback is promoted, and G9 remains open against `1e-3 N` shell / `5e-2 N` coupled gates.
- 2026-06-04: G9 post-Kaczmarz residual-row block-LS refinement added on the official ROCm/HIP path. Official receipt stays `partial`, but shell moves `3.097603 -> 3.092444 N` and coupled moves `0.179618 -> 0.179433 N`; fusion selects `post_kaczmarz_residual_row_block_lstsq_refinement`, no CPU fallback is promoted, and G9 remains open against `1e-3 N` shell / `5e-2 N` coupled gates.
- 2026-06-04: G9 support-expanded post-Kaczmarz residual-row block-LS refinement added on the official ROCm/HIP path. Secondary support expansion from support-connected rows is capped at `96` DOF and contributes accepted updates. Official receipt stays `partial`, but shell moves `3.092444 -> 3.089210 N` and coupled moves `0.179433 -> 0.179239 N`; no CPU fallback is promoted, and G9 remains open against `1e-3 N` shell / `5e-2 N` coupled gates.
- 2026-06-04: G9 ROCm sparse pytest default now validates the existing productization receipt instead of regenerating the full GPU probe. Full regeneration remains available with `RUN_MGT_ROCM_HEAVY_GPU_TEST=1`; default validation now passes in `1.12s`, and the focused regression bundle passes in `16.03s`. This is CI/runtime scale progress, not solver closure.
- 2026-06-04: Productization validation now hard-fails solver runtime policy drift: official backend must stay `amd_rocm_hip`, GPU must be required for commercial solver closure, `nvidia-smi` remains unnecessary on the AMD ROCm lane, and CPU diagnostic/fallback paths cannot promote G1/G9. Delivery bundle summary exposes these fields. Validator passes, commercial ledger remains `open closed=16/20 partial=3 external_blocked=1`.
- 2026-06-04: G9 post-refinement residual-row Kaczmarz polish added on the official ROCm/HIP path after support-expanded post-Kaczmarz block-LS refinement. Official receipt stays `partial`, but shell moves `3.089209942 -> 3.087244829 N` and coupled moves `0.179239215 -> 0.179195231 N`, both with `8/8` accepted ROCm CSR replay improvements. Fusion now selects `post_refinement_residual_row_kaczmarz_polish`; G9 remains open against shell `1e-3 N` and coupled `5e-2 N` gates.
- 2026-06-04: G9 post-polish support-expanded residual-row block-LS refinement is now documented as the superseding ROCm/HIP frontier. Official receipt stays `partial`, but shell moves `3.087244829 -> 3.082478328 N` and coupled moves `0.179195231 -> 0.179127039 N` with `8/8` accepted ROCm CSR replay improvements, secondary support depths `[1,2,4]`, and a `128` DOF cap. Fusion selects `post_polish_residual_row_block_lstsq_refinement`; G9 remains open against shell `1e-3 N` and coupled `5e-2 N`.
- 2026-06-04: G9 post-fusion support-expanded residual-row block-LS refinement now starts from the post-block-LS fusion best state on the official AMD ROCm/HIP path. Official receipt stays `partial`, but shell moves `3.082478328 -> 3.079314601 N` and coupled moves `0.179127039 -> 0.179026677 N` with `6/6` accepted ROCm CSR replay improvements, secondary support depths `[1,2,4,8]`, a `160` DOF cap, and host dense-solve fallback count `0`. G9 remains open against shell `1e-3 N` and coupled `5e-2 N`.
- 2026-06-04: G9 large-component coarse correction now tests residual-hotspot aggregate ordering in addition to free/global ordering on the official ROCm/HIP path. Both shell and coupled run `8` device coarse solves in the first pass and both orderings fail to reduce the full ROCm CSR residual (`3.598281468473605 N` shell, `0.18866597120746142 N` coupled at that stage), with host dense fallback `0`. This strengthens the counter-evidence: simple aggregate reordering is not enough; next G9 work should move to real AMG/Schur/domain-decomposition style preconditioning.
- 2026-06-04: G9 overlapping Schwarz patch correction was added on the official AMD ROCm/HIP path. The Python host process orchestrates HIP kernels, and `rocm-smi` confirmed live Navi 21 activity around `64-66%` GPU during the run. Official receipt stays `partial`; shell moves only `3.079314601 -> 3.079200257 N`, coupled remains `0.179026677 N`, device patch solves are `112` shell / `56` coupled, and host dense fallback count is `0`. This is counter-evidence against naive local patch correction, not G9 closure.
- 2026-06-05: G9 additive Schwarz-Krylov correction was added on the official AMD ROCm/HIP path. `rocm-smi` confirmed Navi 21 activity around `54-67%` GPU and up to about `109W` during the run. Official receipt stays `partial`; shell remains `3.079200257 N`, coupled moves only `0.179026677 -> 0.179026341 N`, device patch/Krylov solves are `90/4` for shell and `180/8` for coupled, and host dense fallback count is `0`. G9 remains open against shell `1e-3 N` and coupled `5e-2 N`.
- 2026-06-05: G1 matrix-free JVP probe-scale selection was added as an opt-in CPU diagnostic. Full default scale-sweep regeneration was terminated as too heavy, so default remains the legacy single probe scale. Reduced smoke receipt `mgt_direct_residual_probe_scale_sweep_smoke.json` uses `[0.125,0.25]`, selects `0.25`, records `promotion_count=0` / `stop_reason=no_residual_descent`, and stays partial at final direct residual `15543.349715 N`. This must not promote G1; official commercial closure lane remains AMD ROCm/HIP.
- 2026-06-05: G9 deflated Jacobi-Krylov correction was added on the official AMD ROCm/HIP path after additive Schwarz-Krylov. It combines global Jacobi, raw residual, residual-hotspot, and Jacobi-preconditioned Krylov directions, solves candidate-space systems on HIP, and accepts only full CSR replay improvements. Official receipt remains partial: shell stays `3.079200257 N`, coupled stays `0.179026341 N`, both stop after one pass with `no_deflated_jacobi_krylov_candidate_improved_residual`, and host dense fallback remains `0`. This is counter-evidence against cheap global residual-space correction; G9 still needs AMG/Schur/domain-decomposition or a stronger ROCm sparse backend.
- 2026-06-05: G9 structural-node coarse correction was added on the official AMD ROCm/HIP path. It uses `free_global_dof`/`dof_per_node` to build piecewise-constant node/DOF aggregate modes over the dominant component and solves reduced Galerkin systems on HIP. Official receipt remains partial but moves shell `3.079200257 -> 3.079200245 N` and coupled `0.179026341 -> 0.179026340 N`, both with `24` device coarse solves, host dense fallback `0`, and `structural_node_coarse_min_improvement_reached`. This is real but tiny physics-aware coarse evidence, not G9 closure.
- 2026-06-05: ROCm/HIP runtime-lane evidence was hardened after CPU/GPU ambiguity review. `solver_runtime_backend_policy.json` now exposes both `official_solver_compute_backend` and `official_solver_backend` as `amd_rocm_hip`, records `official_solver_backend_family=rocm_hip`, marks PyTorch ROCm `cuda:0` as a compatibility alias, and sets `cpu_solver_fallback_detected=false`. Productization validation hard-fails backend alias drift, backend-family drift, CPU diagnostic/fallback promotion, and any detected CPU solver fallback. Commercial ledger remains `open`; this is evidence-boundary hardening, not G1/G9 numerical closure.
- 2026-06-05: Gap closure rollup was compacted after regeneration duplicated the full commercial ledger rows into `gap_closure_status.json`. The rollup now stores 20 compact ledger requirement summaries and leaves full evidence in `commercial_gap_ledger_status.json`; regenerated rollup is 337 lines, validator remains `pass missing=0`, and focused policy/validator/bundle/commercial/gap tests pass. This addresses receipt operability while preserving open G1/G9 status.
- 2026-06-05: G9 enriched structural-node coarse correction was added after structural-node coarse on the official AMD ROCm/HIP path. It extends aggregate modes from constant node/DOF modes to constant + node-order linear ramp + residual-signed weight modes at aggregate counts `[8,16]`. Official ROCm receipt regenerated with `rocm-smi` observing about `63-69%` GPU, up to `117W`, and VRAM up to `21%`; shell/coupled each ran `16` HIP coarse solves with host dense fallback `0`, but neither improved. Frontier remains shell `3.0792002451710445 N` and coupled `0.17902634022354746 N`; next G9 work should move beyond cheap aggregate bases to true AMG/Schur/domain-decomposition or a stronger ROCm sparse backend.
- 2026-06-05: G9 bounded Schur-interface correction was added after enriched structural-node coarse on the official AMD ROCm/HIP path. An initial wider interface attempt was terminated as too slow for the official probe, then bounded to `partition_counts=[8]`, `max_interface_dof_count=96`, `max_equation_row_count=256`, and one correction pass. The official ROCm receipt regenerated with `rocm-smi` observing about `61-67%` GPU; the stage itself runs `4` HIP interface solves for shell and coupled, host dense fallback `0`, device residency `1.0`, and finishes in about `0.5s` per row. It does not improve residuals: shell remains `3.0792002451710445 N`, coupled remains `0.17902634022354746 N`. Commercial ledger remains open (`16/20` closed, `3` partial, `1` external blocked), with next locally closable gaps `G1` and `G9`.
- 2026-06-05: G9 focused rocALUTION diagnostics added. The new C++ bridge compiles against `/opt/rocm` and returns ROCm/HIP library solver solutions for full Python CSR residual replay. Same-assembly shell CPU sparse direct remains mathematically closed at `4.256728516338626e-07 N`, but this is non-promoting CPU reference evidence. rocALUTION multi-colored ILU does not close shell (`4936.275753520634 N`, `383.9171239394624 N`, `504.62667877072585 N` for tested GMRES ILU families), and default SAAMG currently replays to the initial residual. G9 is now classified as a GPU preconditioner/backend-quality gap rather than a residual-polishing gap.
- 2026-06-05: G9 CPU/GPU boundary corrected after user review. CPU sparse direct is now documented strictly as diagnostic reference evidence; a long coupled rocALUTION run reached `99%` GPU utilization in `rocm-smi`, confirming the ROCm bridge can exercise the AMD GPU. The bridge removed the invalid GMRES infinity-norm setting and added standard ILU/ILUT/manual-SAAMG probes, but focused shell evidence still fails: best rocALUTION row remains `383.9171239394624 N` versus the `1e-3 N` gate, standard ILU is `532.8228836243367 N` or worse, and SAAMG/ILUT do not produce useful shell iteration. Ginkgo HIP configure was attempted in `/tmp` and blocked because the workstation lacks `rocm-device-libs` / `AMDDeviceLibsConfig.cmake` and ROCm device bitcode; sudo password is unavailable. G9 remains open and now needs ROCm dev-library repair plus Ginkgo/AMGCL HIP, or a prebuilt rocSPARSE/hipSPARSE preconditioned Krylov route.
- 2026-06-06: G9 no-new-dependency route explored. Local extraction of `rocm-device-libs_1.0.0.50701-98~22.04` allowed Ginkgo HIP configure for `gfx1030`, but Ginkgo build still fails in HIP CSR kernel instantiation on bfloat16 helper compatibility with the installed ROCm 6.0.2 headers. Added `hipsparse_ilu_bicgstab_solve.cpp`, a standalone prebuilt HIP runtime + hipSPARSE bridge using CSR SpMV, `csrilu02` ILU(0), and `csrsv2` triangular solves. Tiny CSR smoke closes to machine precision, but official shell focused evidence diverges to `1.502116910094704e+73 N`; rocALUTION IC also misses (`CG+IC=55.310924450017765 N`). G9 remains open; next implementation should be true multilevel AMG/V-cycle or Schur/domain-decomposition, not another one-level ILU/IC/Jacobi variant.
- 2026-06-06: G9 two-level smoothed aggregation PCG added as the first no-new-dependency AMG-like experiment. It builds RCM aggregates, optional damped-Jacobi-smoothed prolongation, Galerkin coarse operator, and applies the preconditioner inside PCG using ROCm torch sparse/dense kernels. Focused shell evidence does not close: base `[128,256,512]` aggregate rows all fail, and sensitivity best is `31.997625165812096 N` (`aggregate_count=256`, no smoothing, no Jacobi mix) versus the `1e-3 N` shell gate. This rules out naive two-level aggregation and points next to recursive multilevel hierarchy, better near-nullspace modes, or Schur/domain-decomposition.
- 2026-06-06: G9 projected recursive V-cycle added to test the old orthogonal-projection/distributed-node residual-correction idea in a solver-consistent location. The first level uses distributed structural node/DOF projection modes from `free_global_dof`, then recursive RCM aggregates and ROCm torch V-cycle PCG/FGMRES. PCG is unstable; FGMRES improves focused shell residual from `32.47377433333335 N` to `22.769081918072025 N`, still far from the `1e-3 N` gate. Projection is retained as a near-nullspace basis ingredient, not a closure mechanism.
- 2026-06-06: G9 energy-smoothed projected V-cycle tested. The hierarchy setup can now apply damped `P <- P - omega D^-1 A P` smoothing with column normalization and records projector nnz/level metadata. Focused shell evidence does not improve the unsmoothed frontier: best smoothed FGMRES row is `24.324310976328043 N`, while the overall best remains `22.769081918072025 N`. Next G9 route should move to shell/6-DOF block Schur physics or a stronger smoother/coarse solve.
- 2026-06-06: G9 coordinate-aware rigid-body projector tested. The first-level projector can now encode aggregate rigid translations/rotations and rotation-induced translation terms from node coordinates. Focused shell evidence reaches only `24.25603853096381 N`, so the compact rigid-body near-nullspace does not beat the existing node/DOF projection frontier. G9 remains open and should move to stronger block/Schur or production multilevel preconditioning.
- 2026-06-06: G9 translation/rotation DOF block Schur FGMRES added. The focused shell solver splits free DOF into translational/rotational subblocks, applies approximate block triangular Schur preconditioning with ROCm sparse subblock matvecs and Jacobi inner solves, and wraps it in FGMRES. Best focused shell residual is now `15.396808816819568 N`, improving the projected frontier but still far from the `1e-3 N` gate. Next work should strengthen the subblock solves or nest multilevel preconditioners inside the Schur blocks, then test coupled focused before official integration.
- 2026-06-06: G9 nested PCG subblock solves tested inside block Schur FGMRES. The new `subblock_solver='pcg'` option uses bounded diagonal-preconditioned CG for TT/RR solves, but focused shell best is only `23.90068961216808 N`; the Jacobi-Schur frontier remains `15.396808816819568 N`. The next blocker is likely Schur complement/block coupling quality rather than independent subblock accuracy alone.
- 2026-06-06: G9 diagonal Schur complement correction tested. The `schur_diagonal_correction` option applies diagonal `K_tr D_r^-1 K_rt` / `K_rt D_t^-1 K_tr` complement terms, but focused shell best is only `30.26888443952602 N`. The frontier remains Jacobi-Schur `15.396808816819568 N`; next route needs sparse/low-rank Schur action or nested multilevel around the coupled operator, not diagonal-only scaling.
- 2026-06-06: G9 repeated coupled block-Schur sweeps tested. The `block_schur_sweeps` option alternates translational and rotational block solves inside the preconditioner, but focused shell best is `16.17976916961354 N`, still behind the one-pass Jacobi-Schur frontier `15.396808816819568 N`. Simple block Gauss-Seidel repetition is not enough; next route should approximate sparse/low-rank Schur complement action or build true multilevel Schur preconditioning.
- 2026-06-06: G9 static coupling-hotspot dense correction tested inside block Schur FGMRES. The `coupling_hotspot_correction_size` option selects largest `K_tr`/`K_rt` coupling-strength DOF and solves a small ROCm dense residual correction, but focused shell best is `16.02124789197754 N`. The frontier remains `15.396808816819568 N`; useful low-rank Schur work now needs residual-adaptive or algebraic basis selection, not static coupling magnitude alone.
- 2026-06-06: G9 initial RHS/residual-hotspot Schur selection tested. The `coupling_hotspot_selection` option can now choose `rhs_residual` or `mixed` subspaces for the dense Schur correction, but focused shell best is `19.10982584203132 N`. The frontier remains `15.396808816819568 N`; one-shot initial residual magnitude is not enough, so next useful route needs Krylov-state adaptive or algebraic Schur basis construction.
- 2026-06-06: G9 Krylov-state residual hotspot Schur correction tested. The `krylov_adaptive_hotspot_size` option selects top current FGMRES residual DOF and accepts only replay-improving dense corrections, but focused shell accepts `0` corrections and reaches only `17.38041804427162 N`. The frontier remains `15.396808816819568 N`; top-coordinate adaptive residual selection is insufficient, so the next useful route is algebraic/projected Schur basis construction or true multilevel Schur preconditioning.
- 2026-06-06: G9 projected algebraic Schur-basis correction tested. The `schur_basis_aggregate_count` option builds translation/rotation aggregate basis columns from approximate Schur energy scores and applies a `B^T A B` projected correction inside ROCm FGMRES, but focused shell best is `20.329296601244092 N` at `4` aggregates / `8` columns. The frontier remains `15.396808816819568 N`; simple aggregate projected Schur spaces are insufficient, so the next local route needs true multilevel Schur preconditioning, stronger smoother/coarse-operator quality, or a production AMG backend.
- 2026-06-06: G9 TT/RR multilevel subblock V-cycle tested inside block Schur FGMRES. The `subblock_solver='multilevel_vcycle'` option builds RCM Galerkin hierarchies for translation and rotation diagonal blocks, but focused shell best is `22.975876831045937 N` and coupled best is `703.9110398140688 N` versus the `5e-2 N` coupled gate. The frontier remains `15.396808816819568 N`; independent TT/RR hierarchy quality is not the closure blocker. The next useful route must approximate the full coupled Schur complement or use a production AMG/domain-decomposition backend.
- 2026-06-06: G1 configurable active-set current-tangent residual-row correction controls added and zero-trust focused evidence generated. The direct residual probe accepts target row counts, support column counts, and tangent-graph support expansion depth for opt-in row corrections. A trust-first widened run exceeded interactive runtime, but the zero-trust focused run (`max_trust_iterations=0`, target rows `[2]`, support columns `[8]`, depth `0`) completed in `75.41s` and lowered direct residual `42754.66805918372 -> 17540.09324314082 -> 15553.630703419272 N` over two promotions while keeping the increment gate passed. G1 remains open; next useful work should compose this early descent with matrix-free/consistent Jacobian globalization or checkpointed/resumable probing.
- 2026-06-06: G1 active-set frontier is now durable/resumable. The focused receipt writes `mgt_direct_residual_row_active_set_zero_trust_final_checkpoint.npz` using schema `mgt-direct-residual-newton-state.v1`; replay receipt `mgt_direct_residual_row_active_set_checkpoint_replay.json` disables trust/secant/matrix-free/row corrections and starts from the same direct residual `15553.630703419272 N`. G1 remains open, but the next consistent-Jacobian/globalization experiment can seed from this verified state instead of recomputing the active-set descent.
- 2026-06-06: G1 direct-residual checkpoints now preserve accepted state/residual history. The regenerated active-set checkpoint has `accepted_history_count=3`, and its replay still starts at `15553.630703419272 N`. A row-active continuation from that checkpoint applies four more promotions and lowers the durable frontier to `13698.533336101626 N`, with a replay-verified continuation checkpoint carrying `accepted_history_count=7`. Secant/matrix-free can now consume checkpoint history, but the active-set checkpoint composition gives no residual descent and a longer continuation-history composition exceeded the `180s` diagnostic timeout; G1 still needs a stronger consistent-Jacobian/globalization model.
- 2026-06-06: G1 row-active correction now has an opt-in directional residual-Jacobian alpha probe. Focused evidence from the `13698.533336101626 N` checkpoint with probe alpha `0.03125` lowers the durable frontier to `12855.687023192413 N` and verifies replay from an `accepted_history_count=9` checkpoint. The directional alpha candidates were near `1.0`, but fixed alpha `0.03125` remained the accepted best row, so this is progress plus counter-evidence: scalar row-direction alpha fitting is not enough for G1 closure.
- 2026-06-06: G1 row-active alpha candidates are now configurable. Widened alpha evidence lowered the replay-verified durable frontier from `12855.687023192413 N` to `10426.970626342814 N` using `[0.0625,0.03125,0.015625]`, then to `8424.675514897524 N` using `[0.125,0.0625,0.03125,0.015625]`. An aggressive `[0.25,...]` two-pass probe only accepted one smaller-alpha promotion to `8330.920342893796 N` and then stopped with no residual descent. G1 remains open; this is state-aware scalar globalization evidence, not a consistent-Jacobian closure.
- 2026-06-06: G1 fine row-alpha continuation shows diminishing returns. A four-pass fine-alpha continuation timed out at `180s`; a bounded two-pass smoke lowers `8330.920342893796 -> 8261.85717314353 N` using `0.00390625` and `0.0078125`, with replay verified from an `accepted_history_count=20` checkpoint. G1 remains open and should move from scalar row-active descent toward a stronger consistent residual/Jacobian/globalization model.
- 2026-06-06: G1 expanded row-active support improves the direct-residual frontier more than further fine alpha polishing. From the `8261.85717314353 N` checkpoint, `target_row_count=4` and `support_column_count=16` lower residual to `8003.652871020366 N` in one smoke promotion, then to `7511.203920880909 N` over two continuation promotions, replay verified with `accepted_history_count=23`. G1 remains open; next work should use richer active subspace/Jacobian construction rather than merely tuning scalar alpha.
- 2026-06-06: G1 expanded-support follow-up shows the `[4]/[16]` active subspace is exhausted at `7511.203920880909 N` (`no_residual_descent`), but `[8]` target rows with `[16]` support columns accepts one small-alpha promotion to `7452.444439515273 N`, replay verified with `accepted_history_count=24`. G1 remains open; broader/adaptive active subspaces are useful but still diagnostic.
- 2026-06-06: G1 active-subspace sweep from `7452.444439515273 N` shows `[8]/[16]` remains the best candidate among `[8,12]` target rows and `[16,24]` support columns; it lowers residual to `7219.400801402895 N`. One more `[8]/[16]` continuation lowers the replay-verified frontier to `7106.522708708845 N` with `accepted_history_count=26`. G1 remains open; adaptive subspace selection matters more than monotone widening.
- 2026-06-06: G1 residual-weighted row support selection was implemented and tested. From the `7106.522708708845 N` frontier, both `[8]/[16]` and compact `[8]/[8]` residual-weighted support fail with `no_residual_descent`; a same-state row-strongest follow-up also fails. The latest frontier remains `7106.522708708845 N`, and G1 should move beyond scalar row-active support scoring toward stronger residual/Jacobian/globalization work.
- 2026-06-06: G1 node-block support expansion was implemented and tested. Same-node DOF expansion from the `7106.522708708845 N` frontier fails for `[8]/[16]` (`52 -> 120` support columns) and `[4]/[16]` (`37 -> 90` support columns), both with `no_residual_descent`. The row-active family remains locally exhausted; next work should use element/block residual equations or a coupled Jacobian action instead of just widening support columns.
- 2026-06-06: G1 residual-node block target selection was implemented and tested. `--current-tangent-residual-row-target-mode residual_node_blocks` targets all free 6DOF rows on high-residual nodes, but from `7106.522708708845 N` both coarse and fine alpha smokes worsen the residual (`7119.692693978816 N` and `7108.168956867663 N`). The latest frontier remains `7106.522708708845 N`; the older distributed-node/orthogonal-projection idea should stay as preconditioner/coarse-basis support, not closure.
- 2026-06-06: G1 finite-difference residual-row Jacobian mode was implemented and tested. The wide `[4]/[8]` measured residual-JVP subspace worsens to `8884.487342922255 N`, but compact `[2]/[4]` lowers the replay-verified frontier from `7106.522708708845 N` to `7092.642781544098 N`. G1 remains open and CPU diagnostic only; next useful work should scale measured residual-Jacobian action beyond tiny active rows without exploding assembly cost.
- 2026-06-06: G1 global/reverse directional row-alpha candidates were implemented and tested. From the `7092.642781544098 N` FD frontier, plain compact FD continuation worsens to `7133.944505406227 N`; global L2 alpha is negative (`-0.055637562526651044`), but the reverse signed candidate worsens to `8004.657412096502 N`. Latest G1 frontier remains `7092.642781544098 N`; the next route needs broader measured-Jacobian subspace selection, not repeat `[2]/[4]`.
- 2026-06-06: G9 one-hop harmonic GENEO transfer extension was implemented and tested on the ROCm/HIP focused shell route. Extension weight `0.5` lowers the saturated GENEO single-level frontier from `22.6733256606884 N` to `22.593705557868677 N` with `26189` extension DOFs, while full weight worsens. G9 remains open; this confirms transfer/operator-space quality is the lever, not load partition or scalar pass tuning.
- 2026-06-06: G9 harmonic GENEO transfer depth is now governed and tested. With extension weight `0.5`, depth `1` remains best at `22.593705557868677 N`; depth `2` and `3` worsen to `22.899531912544845 N` and `22.993533780768566 N` despite larger supports. G9 remains open; next route should be stabilized multilevel transfer/interface-DD coupling rather than deeper single-level Jacobi extension.
- 2026-06-06: G9 harmonic GENEO coarse-basis QR stabilization is implemented and tested. QR slightly improves depth `1` to `22.5936722466134 N` and depth `2` to `22.899515893609532 N`, but the gains are tiny and G9 remains open. QR should stay as conditioning hygiene; next work needs true multilevel/interface Schur/DD quality.
- 2026-06-06: G9 load-restriction target control is implemented and tested. Harmonic GENEO + QR keeps best `22.5936722466134 N` when restricting the load vector, but current-residual restriction worsens to `23.26506328336595 N`. G9 remains open; preserve load-target restriction and move to stronger multilevel/interface Schur/DD coupling.
- 2026-06-06: G1 finite-difference residual JVP column caching was implemented and tested. Cached active-subspace sweeps over `[1,2,4]` target rows and `[4,8]` support counts lower the replay-verified frontier from `7092.642781544098 N` to `7078.789963612223 N`, then to `7071.877082788062 N`. G1 remains open and CPU diagnostic only, but refreshed measured-Jacobian active sweeps are now the strongest local residual-closure direction.
- 2026-06-06: G1 fixed-pattern FD-Jacobian continuation was tested. Repeating only the latest winning `[4]/[4]` active block from `7071.877082788062 N` fails with `no_residual_descent` and worsens best candidate to `7216.035042010209 N`. Latest G1 frontier remains `7071.877082788062 N`; active-set refresh is required for further progress.
- 2026-06-06: G1 refreshed broader FD target sweep was tested. `[4,8]` target rows and `[4,8]` support counts from `7071.877082788062 N` accept `[8]/[8]` and lower the replay-verified frontier to `7064.970952826998 N`. Repeating the same family from that checkpoint fails with `no_residual_descent` and best `7094.058778498981 N`. Latest G1 frontier is `7064.970952826998 N`; next work needs broader/global measured-Jacobian construction.
- 2026-06-06: G1 broader measured-Jacobian sweep was tested. `[8,12]` target rows and `[8,12]` support counts from `7064.970952826998 N` accept `[12]/[8]` and lower the replay-verified frontier to `7051.172181441225 N`. G1 remains open and CPU diagnostic only, but broadening measured residual-JVP active subspaces continues to move the frontier.
- 2026-06-06: G1 wider measured-Jacobian sweep was tested. `[12,16]` target rows and `[8,12]` support counts from `7051.172181441225 N` accept `[12]/[8]` and lower the replay-verified frontier to `7037.400360781316 N`. G1 remains open and CPU diagnostic only; active measured-Jacobian broadening is still productive.
- 2026-06-06: G1 very-wide measured-Jacobian sweep was tested. `[16,24]` target rows and `[8,12]` support counts from `7037.400360781316 N` accept `[16]/[8]` and lower the replay-verified frontier to `7023.655438207834 N`; `[24]` target rows are unstable and worsen to `1748394.335228357 N`. G1 remains open and CPU diagnostic only; broader active JVP needs conditioning/governance.
- 2026-06-06: G1 ridge-damped row least-squares was implemented and tested. Ridge factor `1e-6` reduces the unstable `[24]` block from `1748394.335228357 N` to `195251.9798241862 N`, but does not make it useful; the best ridge candidate does not beat the unregularized frontier. Latest G1 frontier remains `7023.655438207834 N`.
- 2026-06-06: G1 stable-target broader-support measured-Jacobian sweep was tested. Keeping target count `16` and widening support counts to `[16,20]` from `7023.655438207834 N` accepts `[16]/[16]`, support size `36`, alpha `0.001953125`, and lowers the replay-verified direct-residual frontier to `7009.937361185119 N`. G1 remains open and CPU diagnostic only; the measured-JVP active route still moves, but serial row polishing is not a physical residual closure strategy.
- 2026-06-06: G1 stable-target widest-support measured-Jacobian sweep was tested. Keeping target count `16` and widening support counts to `[24,32]` from `7009.937361185119 N` accepts `[16]/[24]`, support size `48`, alpha `0.001953125`, and lowers the replay-verified direct-residual frontier to `6996.246077280766 N`. G1 remains open and CPU diagnostic only; 48 FD columns cost about `365s` for one `13.69 N` step, so the next implementation should pivot toward full residual/Jacobian or GPU large-component preconditioning.
- 2026-06-06: G9 post-Schur residual-row block-LS refinement was added as an opt-in hook but disabled by default after a bounded official integration attempt triggered a ROCm memory access fault (`Page not present or supervisor privilege`). `mgt_rocm_post_schur_block_lstsq_refinement_safety_receipt.json` records the safety boundary, keeps CPU/host fallback promotion disallowed, and leaves the official G9 frontier at shell `3.0792002451710445 N` / coupled `0.17902634022354746 N`. G9 remains open; next work should move to robust AMG/Schur/domain-decomposition backend quality rather than more post-Schur residual polishing.
- 2026-06-06: G1 accepted-history secant-family globalization was added as an opt-in CPU diagnostic Newton auxiliary. From the `6996.246077280766 N` frontier, windows `[4,8,16,32]` with ridge `[0,1e-8]` promote `window=4`, `ridge=0`, `alpha=1.0` to a replay-verified `6949.9113895643495 N` with relative increment `3.02e-07`; the immediate same-family continuation is blocked by the `1e-5` relative-improvement floor. G1 remains open and non-promoting; projection/history correction is useful as a gated subspace seed, not standalone closure.
- 2026-06-06: G9 block-Schur FGMRES now has dynamic recycled-Krylov residual correction on the ROCm/HIP path. Undamped residual/preconditioned recycling and solution-update recycling are counter-evidence, but damped residual-only recycling lowers the focused shell frontier from `15.396808816819568 N` to `14.728535946405538 N`; multi-pass replay lowers it to `14.030266570199556 N`; basis-width sensitivity lowers it again to `13.984934900412316 N` with residual-only basis size `16`. G9 remains open; this is a useful Schur/FGMRES ingredient, not the missing multilevel/coupled Schur closure.
- 2026-06-06: G9 recycled-Krylov block-Schur focused runner now supports the coupled frame-shell 6DOF matrix family using the official coupled assembly. Coupled evidence improves over the earlier TT/RR subblock multilevel coupled counter-evidence (`703.9110398140688 N`) but remains far from closure: best `49.81599342638738 N` in `mgt_dof_block_schur_recycled_krylov_coupled_deep_inner_probe.json` versus the `5e-2 N` coupled gate, with a longer run plateauing at `49.85823142913492 N`. G9 remains open; the next route still needs true coupled Schur/AMG/domain-decomposition backend quality.
- 2026-06-06: G9 Schur/FGMRES now includes a damped node-local 6DOF block smoother that applies same-node translation/rotation correction after the Schur step on ROCm torch tensors. Focused shell improves from `13.984934900412316 N` to `13.670481762504783 N` at one sweep / weight `0.1`; focused coupled improves from `49.81599342638738 N` to `36.848427883622634 N` at one sweep / weight `0.15`. G9 remains open because shell still misses `1e-3 N` and coupled still misses `5e-2 N`; the useful route is now local 6DOF block smoothing inside a larger hierarchy/domain decomposition.
- 2026-06-06: G9 node-aggregate 6DOF coarse correction added inside Schur/FGMRES to test a minimal hierarchy over the node-local smoother. The current constant aggregate basis is counter-evidence: shell agg32/agg64/V-cycle reach `14.792773605311131 N`, `14.57075984773655 N`, and `14.393511104295039 N`; coupled agg64 reaches `39.253358162057566 N`. All are worse than smoother-only shell `13.670481762504783 N` / coupled `36.848427883622634 N`, so the next hierarchy needs better prolongation/domain decomposition, not plain node constants.
- 2026-06-06: G9 energy-smoothed node-aggregate prolongation was tested inside Schur/FGMRES. Damped `P <- P - omega D^-1 A P` with column normalization does not rescue the constant aggregate coarse space: shell `omega=0.1` reaches `14.645940256098811 N`, shell `omega=0.33` reaches `17.3820734477982 N`, and coupled `omega=0.1` reaches `39.44310265127285 N`, all worse than smoother-only. The next G9 hierarchy route should change coarse-space/domain partition quality rather than tune smoothing weights.
- 2026-06-06: G9 matrix-adjacency RCM node partition option added for node-aggregate 6DOF coarse correction. It replaces sorted node-id chunks with RCM-ordered node chunks, but focused evidence is worse: shell RCM agg64 reaches `20.71995361193298 N` and coupled RCM agg64 reaches `46.83877198477329 N`. The next route needs real graph clustering/interface-aware domain decomposition, not simple graph ordering plus contiguous chunks.
- 2026-06-06: G9 connected graph-BFS node partition option added for node-aggregate 6DOF coarse correction. Shell BFS agg64 worsens to `26.030906950015893 N`, but coupled BFS agg64 improves the focused coupled frontier from `36.848427883622634 N` to `7.566263063217367 N`; coupled agg128 worsens to `17.778351028061582 N`. G9 remains open, but connected subdomains are now a productive coupled direction and should be combined with richer shell/coarse/interface treatment next.
- 2026-06-06: G9 damped graph-BFS node coarse correction added via `node_block_coarse_weight`. Weight `0.25` improves focused shell from `13.670481762504783 N` to `12.74233293760405 N` and focused coupled from `7.566263063217367 N` to `5.815998578901954 N`; coupled weights `0.5` and `0.125` are weaker. G9 remains open against shell `1e-3 N` and coupled `5e-2 N` gates, but under-relaxed connected-subdomain coarse correction is now the strongest dependency-free Schur/coarse frontier.
- 2026-06-06: G9 focused Schur runner now supports `--node-block-coarse-weights` and records `node_block_coarse_weight_candidates` so damping sweeps are reproducible in one receipt. Smoke receipt `mgt_dof_block_schur_bfs_node_coarse_weight_sweep_smoke.json` verifies two rows (`0.25`, `1.0`) and best-row selection without promoting G9 closure.
- 2026-06-06: G9 interface-aware graph-BFS coarse modes added and measured. `interface_split` separates interior/boundary aggregate modes and reaches only `17.088544552184835 N` on the shell focused run; `interface_boundary` keeps boundary modes only and its shell weight sweep reaches `14.697228607242236 N` at weight `0.0625`, still worse than damped constant graph-BFS `12.74233293760405 N`. Coupled boundary smoke is far worse (`11339.570007567216 N`). G9 remains open; simple boundary splitting is counter-evidence, not closure.
- 2026-06-06: G9 coordinate-aware rigid-body graph-BFS coarse mode added inside Schur/FGMRES. The focused assembler now passes `node_xyz` into the ROCm Schur solver, and `node_block_coarse_mode=rigid_body` builds aggregate-centered translational/rotational columns. Focused shell improves `12.74233293760405 -> 12.530892150341918 N` at weight `0.125`; focused coupled improves `5.815998578901954 -> 5.405688506206863 N` at weight `0.25`. G9 remains open; geometry-aware coarse modes are useful but not enough.
- 2026-06-06: G9 coordinate affine-DOF graph-BFS coarse mode added and measured. `node_block_coarse_mode=affine_dof` builds constant plus local coordinate ramp columns per aggregate/local DOF and creates `1821` shell coarse columns, but focused shell reaches only `23.489876634475685 N` at weight `0.03125`. This is counter-evidence against wide coordinate-ramp enrichment; rigid-body coarse remains the stronger geometry route.
- 2026-06-06: G9 rigid-body plus constant graph-BFS coarse union added and measured. `node_block_coarse_mode=rigid_body_plus_constant` creates `1072` shell coarse columns but reaches only `24.457691968098256 N` at weight `0.0625`. This rules out simple basis concatenation; compact rigid-body remains the strongest geometry-aware coarse route.
- 2026-06-06: G9 focused Schur runner now supports `--node-block-coarse-smoothing-weights`; rigid-body graph-BFS smoothing evidence shows one Jacobi smoothing step is worse than unsmoothed rigid-body. Shell smoothing best is `12.883446086856926 N` at smoothing weight `0.1`, while unsmoothed rigid-body remains `12.530892150341918 N`.
- 2026-06-06: G9 focused Schur runner now supports `--node-block-coarse-aggregate-counts`; rigid-body graph-BFS aggregate-count sweeps show shell requests `[32,64,128]` keep request `64` best at `12.530892150341918 N`, while coupled requests `[32,64,128]` improve the focused coupled frontier from `5.405688506206863 N` to `4.681515148086191 N` at request `32`. G9 remains open against shell `1e-3 N` and coupled `5e-2 N` gates.
- 2026-06-06: G9 residual/RHS-weighted projected Schur-basis selection added and measured. `--schur-basis-selection=rhs_weighted` with Schur basis aggregate `8` improves the focused shell frontier from `12.530892150341918 N` to `12.466549536358821 N` and the focused coupled frontier from `4.681515148086191 N` to `4.37671406620138 N`. G9 remains open by a wide margin; this is Schur basis-selection evidence, not closure.
- 2026-06-06: G9 projected Schur-basis correction weight sweep added and measured. `--schur-basis-weights` shows full weight `1.0` remains best for shell (`12.466549536358821 N`) and coupled (`4.37671406620138 N`); under-relaxed weights are worse. G9 remains open; the blocker is not scalar over-application of the projected Schur basis.
- 2026-06-06: G1 matrix-free direct-residual JVP now supports central difference. Central and forward one-pass comparison smokes both keep stable free-DOF sets, both stop with `promotion_count=0` / `no_residual_descent`, and both leave final direct residual at `15543.349715249427 N`; central JVP infinity norm is `42792.44427210307 N` versus forward `42794.61590178555 N`. G1 remains open; differencing symmetry is diagnostic coverage, not closure.
- 2026-06-06: G1 matrix-free direct-residual JVP now supports opt-in `current_newton` basis source and per-direction probe step caps. Uncapped current-Newton basis evidence shows the legacy scale probes about `0.1015 m` and spikes probe residual to `729713.957247828 N`; capped evidence limits the step to `6.75e-05 m` but still stops with `promotion_count=0`, `no_residual_descent`, and final direct residual `15543.349715249427 N`. G1 remains open; this is basis-scaling instrumentation and counter-evidence, not closure.
- 2026-06-06: G1 matrix-free direct-residual JVP basis least-squares now supports ridge-factor sweeps. Current-Newton capped ridge smoke tests `[0,1e-8,1e-6]`; all factors keep the same non-descending best residual `15543.349727522387 N`, `promotion_count=0`, and `no_residual_descent`. G1 remains open; ridge conditioning is available for broader bases but does not close the current Newton basis.
- 2026-06-06: G1 matrix-free direct-residual JVP now supports opt-in signed-alpha scanning. Current-Newton signed-alpha smoke finds a tiny negative-alpha descent from `15543.349715249427 N` to `15543.343432173222 N` at alpha `-1.0` with relative increment `3.6565e-07`, but this required zero relative-improvement floor and remains far above the residual gate. G1 remains open; signed alpha is a globalization clue, not closure.
- 2026-06-06: G1 signed-alpha current-Newton continuation smoke shows the negative-alpha micro-descent repeats for three promotions: `15543.349715249427 -> 15543.343432173222 -> 15543.330866082199 -> 15543.305733582718 N`. Relative improvements remain below the default `1e-5` floor and the residual gate remains far open, so this is repeatable globalization evidence, not closure.
- 2026-06-06: G1 matrix-free signed-alpha now supports opt-in max-alpha above the legacy `1.0` clamp. Alpha16 improves by `0.10052923294642824 N`; alpha32 with default `1e-5` floor promotes negative alpha `-32` and lowers direct residual to `15543.148656784975 N` with relative improvement `1.293533685695564e-05`. G1 remains open, but the alpha cap is confirmed as a real globalization bottleneck.
- 2026-06-06: G1 current-Newton signed-alpha near gate-limited smoke promotes negative alpha `-256` and lowers direct residual to `15541.741247508204 N`; checkpoint follow-up promotes gate-limited alpha `-1.0075474192347846` and lowers it again to `15540.108614387922 N`. G1 remains open; this is repeatable CPU-diagnostic globalization progress, not residual-gate closure.
- 2026-06-06: G1 second signed-alpha checkpoint follow-up finds only sub-floor micro-descent: best candidate `15540.095861749318 N` at alpha `-0.00390625`, relative improvement `8.206e-7`, so no promotion and durable frontier remains `15540.108614387922 N`. G1 remains open; current-Newton signed-alpha repetition is locally weakening and needs richer basis/Jacobian work.
- 2026-06-06: G1 measured residual-row FD-Jacobian solve now supports truncated-SVD governance through condition/relative cutoff controls. From the secant-family frontier `6949.9113895643495 N`, `mgt_direct_residual_row_fd_jacobian_svd_truncation_smoke.json` with condition cap `1e6` promotes `[16]/[8]`, support size `28`, alpha `0.001953125`, and lowers the replay-verified frontier to `6936.337343886427 N`. The `[24]` target candidates drop one singular direction and avoid active-block blow-up but do not win. G1 remains open and CPU diagnostic; runtime is `436.33s`, so this is governance/frontier evidence rather than closure.
- 2026-06-06: G9 ROCm/HIP Schur projected basis now supports signed RHS/residual aggregate modes (`rhs_signed_weighted`, `mixed_rhs_signed`). Focused shell and coupled receipts show counter-evidence: shell keeps unsigned `rhs_weighted` best at `12.466549536358821 N` versus signed `13.18924047920264 N` / `16.248662930527985 N`; coupled keeps unsigned `rhs_weighted` best at `4.37671406620138 N` versus signed `6.847400559043649 N` / `4.8103591251878015 N`. G9 remains open; residual-sign aggregate weights are weaker than the existing Schur basis.
- 2026-06-06: G9 `rhs_weighted` Schur projected-basis aggregate-count sweep tested `[4,8,16,32]` on the ROCm/HIP focused shell and coupled paths. Both keep aggregate `8` best: shell `[12.639024094175015, 12.466549536358821, 12.772640011564716, 13.350879149999306] N`, coupled `[6.7092752223624075, 4.37671406620138, 6.204393286835398, 6.36603252400505] N`. G9 remains open; simple low-rank Schur basis width tuning is exhausted.
- 2026-06-06: G9 node-block coarse partitioner now supports `rhs_graph_bfs`, a connected graph-BFS partition seeded and ordered by RHS hotspot magnitude. Focused ROCm receipts are counter-evidence: shell worsens to `15.392137365307763 N` versus high-degree graph-BFS `12.466549536358821 N`; coupled worsens to `4.65869423252434 N` versus `4.37671406620138 N`. G9 remains open; RHS-only domain seeding is weaker than degree-connected graph partitioning.
- 2026-06-06: G9 node-block coarse operator now supports graph-BFS overlap depth for halo aggregate basis construction. Depth `1` is counter-evidence: shell worsens to `15.278792855657246 N` with `3365` overlap nodes / `571` coarse columns, and coupled worsens to `5.055470397196352 N` with `3015` overlap nodes / `1136` coarse columns. G9 remains open; simple halo-overlapped rigid-body coarse basis is weaker than the non-overlap frontier.
- 2026-06-06: G9 focused ROCm/HIP block-Schur runner now exposes coupling-hotspot size/selection sweeps under the current best Schur/coarse settings. Shell focused frontier moves `12.466549536358821 -> 12.099760245908454 N` with size `64` / `rhs_residual`; coupled moves `4.37671406620138 -> 3.699645345812989 N` with size `64` / `coupling_strength`. Size sweeps over `[32,64,96,128]` keep `64` best for both. G9 remains open; this is focused Schur/interface preconditioner progress, not official full sparse closure.
- 2026-06-06: G9 focused Schur preconditioner now supports opt-in post-interface coupling-hotspot passes after Schur basis/coarse/smoother stages. Shell improves `12.099760245908454 -> 11.09282697775784 N` with one post pass, while coupled post-pass rows worsen to about `5.27 N` and keep the no-post-pass `3.699645345812989 N` row as best. G9 remains open; post-interface polish is a shell-only ingredient, not a coupled closure mechanism.
- 2026-06-06: G9 focused Schur runner now supports separate post-interface hotspot size/selection. Coupled split-post evidence is counter-evidence: with pre-hotspot fixed at size `64` / `coupling_strength`, post `coupling_strength`, `rhs_residual`, and `mixed` reach `5.273263330318059 N`, `8.041777475205777 N`, and `5.285900152662634 N`, all worse than the no-post `3.699645345812989 N` baseline. Coupled G9 now needs a main coupled Schur/coarse operator change, not final hotspot polishing.
- 2026-06-06: G9 focused Schur runner now supports batched translation-rotation coupling-pair smoothing as a main preconditioner ingredient. Shell improves slightly `11.09282697775784 -> 11.018219447125434 N` with `128` pairs / weight `0.1`, but coupled keeps the no-pair `3.699645345812989 N` baseline and all tested pair rows worsen (`4.027417777564491` to `5.09044963551105 N` for `64` pairs, `4.961935669314357` / `4.778060260239954 N` for `128` pairs). G9 remains open; pair-local smoothing is weak shell-only evidence, not coupled closure.
- 2026-06-06: G9 coupling-pair projected basis added to re-test the older orthogonal-projection/distributed residual-correction idea inside the ROCm/HIP Schur preconditioner. The new `coupling_pair_basis_*` device-resident `B^T A B` correction is counter-evidence: shell keeps no-basis `11.018219447125434 N` and coupled keeps no-basis `3.699645345812989 N`; all tested pair-basis rows worsen. G9 remains open and should route next to stronger Schur/domain-decomposition or production AMG quality.
- 2026-06-06: G9 bounded graph-BFS subdomain Schwarz smoother added on the ROCm/HIP Schur path. It reuses connected graph-BFS aggregates, truncates oversized local supports by RHS magnitude to width `48`, batches local dense inverses on device, and applies additive local residual correction after Schur/coarse/smoother stages. Focused evidence is counter-evidence: shell keeps weight `0` best at `11.018219447125434 N` while `0.025/0.05` worsen to `12.505130230810465/12.724763989404131 N`; coupled keeps weight `0` best at `3.699645345812989 N` while `0.025/0.05` worsen to `4.126192494233578/7.435020056198596 N`. G9 remains open; current local Schwarz is not enough without better interface/coarse coupling quality.
- 2026-06-06: G1 direct-residual row correction now supports element-connected target equations via `residual_element_blocks`. Focused CPU-diagnostic evidence from the `6936.337343886427 N` checkpoint shows the one-element block is unstable, but the two-element shell block (`21535`, `20846`) with `36` target rows, `16` measured FD residual-JVP columns, and SVD condition cap `1e6` lowers the replay-verified frontier to `6931.641138710459 N`. G1 remains open; this is useful element/block residual evidence, not residual-gate closure or official ROCm/HIP promotion.
- 2026-06-06: G1 element-connected target equations now support shared-node neighbor expansion. One-hop element patch evidence from the `6931.641138710459 N` checkpoint expands one seed to `21` elements / `114` rows but worsens the best candidate to `6931.882795083492 N`; replay keeps the frontier at `6931.641138710459 N`. G1 remains open; broad element patches need matching Jacobian coverage or sparse/consistent operator quality, not a fixed `16` measured-column cap.
- 2026-06-06: G9 ROCm bridge runtime safety hardened. A full official rerun reached ROCm execution but timed out at `420s` and left a `rocalution_sparse_solve` child alive; the child was terminated, SAAMG candidates were moved behind explicit `MGT_ROCALUTION_ENABLE_SAAMG_SWEEP=1`, and rocALUTION/hipSPARSE bridges now run in their own process group with timeout kill metadata. The standalone ROCm receipt was restored from embedded G9 ledger evidence and remains `partial`; this is safety/evidence-boundary progress, not G9 closure.
- 2026-06-06: G9 focused rocALUTION candidate sweep now covers the previously missing cheap preconditioner rows. `mgt_rocalution_shell_preconditioner_sweep.json` shows `GMRES+MultiColoredILU(p=1,q=1)` replays to `32.47378268212585 N`, `GMRES+ILUT(q=15)` exits at `0` iterations and replays to `32.47377433333335 N`, and `GMRES+IC` also returns to the initial residual after `3` iterations; none closes the `1e-3 N` shell gate. Opt-in `mgt_rocalution_shell_saamg_debug_sweep.json` shows `GMRES+SAAMG` GPU memory-faults, while standalone default-smoother SAAMG builds `2-3` levels but explodes to `3.0e10-5.0e10 N`. G9 remains open; cheap rocALUTION wrapper enumeration is now counter-evidence, and the next route should be real RAS/AMG/domain-decomposition operator quality.
- 2026-06-06: G9 streamed large-subdomain RAS was implemented and measured as a no-new-dependency domain-decomposition check. The shell probe uses streamed per-subdomain GPU dense inverses for `57` graph-BFS local blocks, max width `4096`, with `device_residency_ratio=1.0` and no host dense fallback. Weight `0` remains best at `14.744813152434435 N`; nonzero weights worsen to `15.60196509888516 N` and `19.80249934320678 N`. The current-frontier `512/1024` comparison also keeps weight `0` best at `11.018219447125434 N`. G9 remains open; the current blocker is interface/coarse coupling quality for the dominant shell component, not local GPU dense-solve availability or tiny patch size.
- 2026-06-06: G9 subdomain Schwarz now supports multiplicative/swept residual updates. Focused shell smoke `mgt_dof_block_schur_multiplicative_ras_shell_smoke.json` compares additive and multiplicative update modes with `57` graph-BFS subdomains, max width `1024`, `device_residency_ratio=1.0`, and no host dense fallback. Weight `0` remains best at `16.622547625913917 N`; additive weight `0.001` worsens to `17.229920506541845 N`, and multiplicative weight `0.001` is slightly worse at `17.229922490627388 N`. G9 remains open; stale additive residual application is not the missing closure mechanism.
- 2026-06-06: G9 interface-edge coarse basis mode was added. `node_block_coarse_mode=interface_edge` builds projected coarse columns per aggregate-interface pair, recording `81` graph-cut interface pairs, `1708` boundary nodes, and `411` coarse columns on the shell smoke. The reduced smoke moves weight `0` `16.622547625913917 N` to weight `0.01` `16.594877302281674 N`, but the stronger current-style probe keeps weight `0` best at `14.744813152434435 N` and worsens at `0.01/0.025`. G9 remains open; pair-boundary coarse modes are useful instrumentation, not standalone closure.
- 2026-06-06: G9 smoothed interface-edge coarse basis was measured. One smoothing step improves the reduced shell smoke from unsmoothed `16.594877302281674 N` to `16.584897457260737 N` at smoothing `0.05` and `16.571199149536795 N` at smoothing `0.1`. The stronger current-style probe is counter-evidence: zero coarse remains `14.744813152434435 N`, unsmoothed `0.01` reaches `14.745128722655913 N`, and smoothed `0.01/0.1` worsens to `15.09784789888118 N`. G9 remains open; smoothing pair-boundary modes is an ingredient signal, not closure.
- 2026-06-06: G9 repeated interface-edge coarse correction was measured. The ROCm/HIP path now can reapply the pair-boundary projected correction multiple times with residual recomputation; reduced shell smoke improves to `16.525447145065655 N` with three passes and smoothing `0.1`. The stronger current-style probe still keeps zero coarse best at `14.744813152434435 N`, with repeated nonzero rows worse (`14.860352857620295 N` to `15.359900620053148 N`). G9 remains open; the bottleneck is still true hierarchy/interface Schur quality, not the number of single-level pair-boundary passes.
- 2026-06-06: G9 RHS-weighted interface-edge coarse columns added and measured. `node_block_coarse_mode=interface_edge_rhs_weighted` weights pair-boundary DOF columns by RHS magnitude before the ROCm projected `B^T A B` correction. The current-style shell weight sweep improves zero coarse `14.727081739528142 N` to `14.717569494056175 N` at weight `0.005` and smoothing `0.1`, while weight `0.01` worsens to `14.804693730008754 N`. G9 remains open; RHS-aware interface prolongation is a useful route signal, not closure.
- 2026-06-06: G9 constant+RHS enriched interface-edge basis tested. `node_block_coarse_mode=interface_edge_rhs_enriched` adds an orthogonalized RHS-magnitude column beside each constant pair-boundary column, increasing shell coarse columns from `411` to `564`, but the current-style sweep keeps zero coarse best at `14.727081739528142 N` and worsens every nonzero row. G9 remains open; wider pair-boundary enrichment is counter-evidence, so the useful route is selective RHS-aware prolongation inside a stronger hierarchy/interface Schur operator.
- 2026-06-06: G9 signed RHS interface-edge weighting measured. `node_block_coarse_mode=interface_edge_rhs_signed` improves the current-style shell zero-coarse row to `14.720757625786018 N` at weight `0.005` and smoothing `0.1`, but it is weaker than magnitude-only RHS weighting at `14.717569494056175 N`; signed weight `0.01` worsens to `14.892200906801715 N`. G9 remains open; local pair-boundary prolongation should prefer magnitude/RHS-energy features over signed RHS columns.
- 2026-06-06: G9 load-restricted enriched interface-edge correction implemented and measured. `node_block_coarse_mode=interface_edge_rhs_enriched_restricted` keeps the enriched `564`-column projected basis but removes the `411` constant pair-boundary coarse components from the preconditioner load/residual vector before forming the coarse residual. The current-style shell sweep improves from the previous RHS-weighted frontier `14.717569494056175 N` to `13.824047006173632 N` at weight `0.0025`, three passes, and no smoothing; a fine weight sweep reproduces the same best row. G9 remains open against the `1e-3 N` shell gate, but the active route is now load-restricted enriched interface prolongation inside a stronger multilevel/interface Schur operator.
- 2026-06-06: G9 load-restricted enriched coupled smoke and single-row current-strength probe measured. A full multi-row current-best coupled sweep with the restricted mode exceeded the bounded timeout before writing a receipt, so it was not promoted. A reduced coupled smoke records device residency `1.0`, no host dense fallback, and improves its local zero-coarse row `62.44090595153894 N` to `61.45142142272349 N` at weight `0.0025`. A single-row full-strength coupled probe completes at `27.963756977175404 N`, better than the reduced smoke but weaker than the existing coupled frontier `3.699645345812989 N`. G9 remains open; coupled needs a coupled-specific hierarchy/Schur combination rather than a direct shell-route transplant.
- 2026-06-06: G9 secondary restricted-interface hybrid was implemented and tested. The focused ROCm/HIP Schur preconditioner can now keep primary `rigid_body` graph-BFS coarse correction and append `interface_edge_rhs_enriched_restricted` as a secondary projected correction with its own weight/pass controls. Coupled evidence is counter-evidence: full-strength hybrid overran before writing a receipt, reduced smoke baseline without secondary is `719.4718284713658 N`, secondary `0.0025` / `3` passes worsens to `727.2150871516874 N`, and tiny secondary `0.00025` / `1` pass still worsens to `719.8017291511305 N`. G9 remains open; the next route should change the coupled interface/coarse operator itself or move to backend-quality DD/AMG rather than appending the shell-winning correction.
- 2026-06-06: G1 element-block FD32 follow-ups tested the "more measured columns" hypothesis from the latest replay-verified `6931.641138710459 N` checkpoint. `mgt_direct_residual_row_element_patch_fd32_smoke.json` keeps the one-neighbor `21`-element / `114`-row patch and raises FD support to `32` columns, but worsens to `6931.900944921984 N`. `mgt_direct_residual_row_element_block_fd32_followup_smoke.json` applies `32` measured columns to the compact two-element / `36`-row target and worsens to `7225.620217042061 N`. G1 remains open; serial local FD column widening is now counter-evidence, and the next implementation route should move toward sparse/consistent residual Jacobian or GPU-preconditioned Newton/Krylov.
- 2026-06-06: G9 full enriched-load orthogonalization was implemented and measured. `node_block_coarse_mode=interface_edge_rhs_enriched_orthogonalized` projects the load/residual away from all enriched pair-boundary coarse columns, not only constant columns. The projection is active (`564/564` shell coarse/restriction columns and `2188/2188` coupled), but it worsens residuals: shell best `15.396808816819568 N` versus restricted shell `13.824047006173632 N`, coupled best `189.36077165845572 N` versus restricted coupled smoke `61.45142142272349 N`. G9 remains open; full RHS projection removes useful load, so the next route should preserve selective restriction and change the coupled interface/coarse or AMG/DD operator.
- 2026-06-06: G1 global matrix-free residual-JVP Krylov bridge was implemented and measured. `--enable-matrix-free-global-krylov` uses a GMRES `LinearOperator` whose matvec evaluates full direct residual finite-difference JVPs, then replays line-search candidates through the direct residual and increment gates. From the replay-verified `6931.641138710459 N` element-block frontier, `mgt_direct_residual_global_matrix_free_krylov_smoke.json` performs `3` stable JVP matvecs but stops with `no_residual_descent`; best candidate worsens to `6931.7024565551865 N`, final remains `6931.641138710459 N`, and JVP probe residuals jump to `7.0e6-3.1e8 N`. G1 remains open; the next G1 route should keep whole-system residual-JVP Krylov but add nondimensionalization/preconditioning/globalization rather than repeating an unscaled FD-GMRES smoke.
- 2026-06-06: G1 global Krylov now supports residual/displacement scaling and signed alpha replay. Positive scaled Krylov (`D_r J D_u`, `D_u=1e-4 m`) reduces the unscaled miss to a `6.36e-7 N` worsening but still has `no_residual_descent`. Signed scaled Krylov accepts alpha `-1.0` with zero improvement floor and lowers the replayed residual to `6931.641118344325 N`, only `2.04e-5 N` below base. G1 remains open; scaling/sign are necessary ingredients but not enough without a real preconditioner or trust/arc-length style globalization.
- 2026-06-06: G1 global Krylov now has a current-tangent right-preconditioned mode. The focused CPU-diagnostic smoke maps Krylov directions through the current regularized tangent, performs `3` stable JVP matvecs and `4` tangent solves, and lowers `6931.641138710459 -> 6931.640219069869 N` at alpha `1.0` while passing the increment gate. The smoke uses a zero improvement floor and the relative improvement `1.33e-7` is below the default `1e-6` governance floor. G1 remains open; this is a stronger whole-system preconditioned-JVP signal, not residual-gate or official ROCm/HIP closure.
- 2026-06-06: G1 global Krylov max-alpha control added and verified. Opening the tangent-preconditioned global Krylov line search to max alpha `8` lets the route pass the default `1e-6` relative-improvement floor: first receipt lowers `6931.641138710459 -> 6931.632714519975 N` at alpha `8.0`, and checkpoint follow-up lowers `6931.632714519975 -> 6931.610704092173 N` at alpha `-8.0`. G1 remains open; this is repeatable CPU-diagnostic preconditioned-JVP progress, not residual-gate or official ROCm/HIP closure.
- 2026-06-06: G1 alpha8 preconditioned global Krylov passed the default floor a third time. A second checkpoint follow-up from `6931.610704092173 N` promotes alpha `-2.0` and lowers residual to `6931.566973287116 N` with relative improvement `6.31e-6`. G1 remains open; the route is now repeatable CPU-diagnostic progress and should be automated or ported to the ROCm/HIP closure lane.
- 2026-06-06: G9 residual-region diagnostic added after the Galerkin-coarse question. The Schur node-block coarse operator is already `P^T A P` (`galerkin_ptap`). New shell diagnostic receipt shows the residual is concentrated on interface/coarse support and translation DOFs: primary support contains `65.6%` of top-64 residual rows and `94.2%` of global residual infinity norm, while translation DOFs contain all top-64 residual rows and rotation DOFs are much weaker. G9 remains open; next useful work is interface/translation operator or energy-minimizing basis quality, not more load-partition tuning.
- 2026-06-06: G9 local energy-restricted interface basis implemented and measured. `node_block_coarse_mode=interface_edge_energy_restricted` builds local interface stiffness eigenmodes as Galerkin coarse columns and uses constant interface columns as selective load restriction. Focused shell smoke records `244` energy modes and improves zero coarse `23.23643604628073 N` to `23.130067918481974 N` at weight `0.0025` with three passes. G9 remains open; this confirms basis/operator quality is the useful axis and points next to GENEO/multilevel interface Schur/DD work.
- 2026-06-06: G9 GENEO-style generalized interface basis implemented and measured. `node_block_coarse_mode=interface_edge_geneo_restricted` diagonal-scales local interface stiffness blocks before extracting low generalized modes, then applies them through the existing ROCm Galerkin projected correction with selective constant-load restriction. Focused shell smoke records `22` interface pairs, `244` generalized energy modes, and improves zero coarse `23.23643604628073 N` to `22.826183134567387 N`, beating plain energy modes while still missing the `1e-3 N` gate. G9 remains open; next route is multilevel GENEO/interface Schur/DD.
- 2026-06-06: G9 GENEO basis-width sweep added and measured. The solver/focused runner now expose `node_block_coarse_energy_modes_per_dof`; focused shell sweep `[1,2,3,4]` finds `3` modes/DOF best at `22.673608427637085 N` with `362` energy modes, while `4` modes/DOF worsens to `22.83171465230876 N`. G9 remains open; basis width is non-monotone, so next work should be selective multilevel GENEO/interface Schur/DD rather than more columns by default.
- 2026-06-06: G9 GENEO local mode ordering controls added and measured. `node_block_coarse_energy_mode_selection` can now choose `low_eigen`, `rhs_projection`, or `rhs_energy_score`; focused shell evidence keeps low-eigen best at `22.673608427637085 N`, while RHS-ranked variants worsen above `23.18 N`. G9 remains open; simple one-level load-projection mode ranking is counter-evidence, so next work should change multilevel/interface Schur/DD structure.
- 2026-06-06: G9 GENEO scalar weight/pass sensitivity measured. With modes/DOF `3` and low-eigen ordering fixed, focused sweep improves only to `22.6733256606884 N` at weight `0.0015` and five passes, less than `1e-3 N` beyond the prior frontier; aggressive repeated correction worsens to `26.122705369264658 N`. G9 remains open; cheap single-level tuning is exhausted and next work must change multilevel/interface Schur/DD structure.
- 2026-06-06: G9 Schur-after-GENEO cycle hook implemented and measured. The Schur approximation can now be reapplied to the residual after GENEO coarse correction and followed by another coarse pass, but focused shell evidence is counterproductive: zero-cycle keeps `22.6733256606884 N`, while all nonzero cycles worsen above `23.20 N`. G9 remains open; simply alternating the same approximate Schur and GENEO blocks is not the missing domain-decomposition/complement operator.
- 2026-06-06: G9 interface-pair DD smoother builds `22` local interface blocks with max width `128`, halo depths `[0,1]`, device residency `1.0`, and host fallback `0`, but nonzero weights worsen; best remains disabled smoother `22.5936722466134 N`, best nonzero is `22.81499423648211 N`. G9 remains open; bounded additive interface-pair local solves are counter-evidence, so next work needs stable overlapped DD, multilevel interface coupling, or AMG-grade preconditioning.
- 2026-06-06: G9 swept interface-pair DD update mode added and measured. Multiplicative residual-recomputed application slightly improves the nonzero local-solve row (`22.814994235978013 -> 22.814749673790256 N`) but still loses to disabled smoother `22.5936722466134 N`. G9 remains open; stale additive residual application is not the primary blocker.
- 2026-06-06: G9 post-interface coarse rebalance hook added and measured. Reapplying the same harmonic GENEO coarse correction after multiplicative interface-pair DD worsens to `32.01348863445636 N` / `32.00781560811737 N`, while no rebalance keeps `22.5936722466134 N`. G9 remains open; same-coarse-after-local-DD cycling is counter-evidence.
- 2026-06-06: G9 signed post-DD coarse rebalance sweep measured. With multiplicative interface smoothing fixed at `0.001`, no rebalance remains best at `22.814749673790256 N`; signed rebalance weights `[-0.01,-0.001,0.001,0.01]` all worsen, best rebalance `23.015726093014894 N`. G9 remains open; this is not a scale/sign-only issue.
- 2026-06-06: G9 harmonic GENEO local DOF filtering added and measured after residual-region diagnostics showed translation DOF concentration. The filter is active (`362` all-DOF columns, `167` translation-only, `195` rotation-only), but all DOFs remain best at `22.5936722466134 N`; translation-only worsens to `23.128330475949674 N` and rotation-only to `23.246764238877844 N`. G9 remains open; the residual location is diagnostic, while closure still needs a stronger transfer/coarse hierarchy, interface Schur complement, or AMG/DD-grade preconditioner.
- 2026-06-06: G9 harmonic GENEO Galerkin-energy orthogonalization added and measured. `node_block_coarse_basis_orthogonalization=energy` diagonalizes `B^T A B` and rescales the same `362`-column subspace, but worsens shell residual from QR `22.5936722466134 N` to `22.59379546634976 N`. G9 remains open; same-subspace conditioning is now counter-evidence, and the next route should change hierarchy/interface Schur/DD operator quality.
- 2026-06-06: G1 alpha8 current-tangent preconditioned global residual-JVP Krylov continuation ran one more checkpoint follow-up. `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_followup_3_smoke.json` starts from `6931.566973287116 N`, promotes alpha `-0.25`, and lowers the CPU-diagnostic direct residual to `6931.557151848129 N` with relative improvement `1.4169e-6`. G1 remains open; the preconditioned global Krylov route is repeatable but tapering and still far above the physical residual gate.
- 2026-06-06: G1 alpha8 current-tangent preconditioned global residual-JVP Krylov continuation hit its same-operator boundary. `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_followup_4_smoke.json` starts from `6931.557151848129 N`, finds best candidate `6931.562985874174 N`, and records `matrix_free_global_krylov_accepted=false` with `stop_reason=no_residual_descent`. G1 remains open; next work should change/adapt the basis, preconditioner, or globalization instead of repeating alpha8.
- 2026-06-06: G1 broader global Krylov basis retry measured. `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_broader_basis_smoke.json` raises the same current-tangent preconditioned global Krylov basis from `3` to `6` iterations at the `6931.557151848129 N` checkpoint, but best candidate worsens to `6931.573327681425 N`. G1 remains open; broader same-operator GMRES is counter-evidence, so next work needs different basis sources, preconditioner quality, or globalization.
- 2026-06-06: G1 tangent-preconditioner regularization sensitivity measured. At the `6931.557151848129 N` checkpoint, regularization factor `1e-9` worsens to `6931.578307076875 N`, while factor `1e-7` finds raw best `6931.538073710218 N` and promotes the gate-limited candidate to `6931.541434355825 N`. G1 remains open, but adaptive preconditioner/globalization is the live route after same-operator alpha8 and basis-width-only retries saturated.
- 2026-06-06: G1 `reg1e7` follow-up measured. From the new `6931.541434355825 N` frontier, the same current-tangent preconditioned global residual-JVP Krylov operator finds only sub-floor descent to `6931.539418181269 N` with relative improvement `2.9087e-7`; governance leaves the durable frontier unchanged and records `relative_improvement_floor_not_met`. G1 remains open; next work should adapt regularization/globalization or move to a production-lane residual/Jacobian operator, not replay the same setting.
- 2026-06-06: G1 stronger regularization sweep measured from the same `6931.541434355825 N` frontier. Factor `3e-7` improves to `6931.536916621216 N` but remains below the `1e-6` floor; factor `1e-6` passes the floor and promotes the durable frontier to `6931.50857647534 N`. G1 remains open and CPU diagnostic, but adaptive tangent-preconditioner regularization is now confirmed as a productive axis.
- 2026-06-06: G1 `reg1e6` checkpoint continuation measured twice. From `6931.50857647534 N`, follow-up 1 promotes alpha `2.0` to `6931.46276643071 N`; follow-up 2 promotes alpha `1.0` to `6931.432267962403 N`. G1 remains open and far above the physical residual gate, but repeated floor-passing CPU-diagnostic progress now supports automating adaptive preconditioner/globalization continuation.
- 2026-06-06: G1 current `reg1e6` boundary measured and improvement plan documented. Follow-up 3 promotes alpha `0.25` to `6931.422474165393 N`, but follow-up 4 records `no_residual_descent` and stronger `3e-6` also worsens. `docs/solver-consistent-newton-krylov-improvement-plan.md` now records the active rule: AI/row/FD/secant corrections are candidate generators only; closure requires solver-consistent residual/Jacobian/globalization and backend gates.
- 2026-06-06: G1 adaptive preconditioned global Newton/Krylov controller added. `mgt_direct_residual_adaptive_preconditioned_global_newton_smoke.json` wraps the existing direct-residual global Krylov probe with factor rows and boundary detection; from `6931.422474165393 N`, factors `[1e-6,3e-6]` both worsen and the controller records `promotion_count=0`, `stop_reason=no_regularization_factor_promoted`. G1 remains open; next work should feed new basis/preconditioner/globalization sources into this controller.
- 2026-06-06: G1 secant-family seed added to the adaptive controller and measured. `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_smoke.json` uses secant-family windows `[4,8,16,32]` as candidate generation before global Krylov replay and promotes one step to `6931.387293436975 N`; both seed and global Krylov components are accepted. G1 remains open, but the new architecture is working: residual memory helps as a seed under direct residual/increment gates.
- 2026-06-06: G1 secant-seeded adaptive controller continuation measured. `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_followup_smoke.json` promotes another step from `6931.387293436975 N` to `6931.346282307703 N` with both secant-family and global Krylov accepted. G1 remains open; route repeatability is confirmed, but one-step CPU diagnostic runtime is about `650 s`.
- 2026-06-06: G1 adaptive controller runtime budget governance added. `mgt_direct_residual_adaptive_preconditioned_global_newton_runtime_budget_smoke.json` records the zero-budget boundary with `runtime_budget_exceeded=true`, row count `0`, and promotion count `0`; child runtime fields are now available for launched rows. G1 remains open, but future continuation sweeps can be sliced through auditable budget launch boundaries instead of unbounded CPU-diagnostic loops.
- 2026-06-06: G1 budgeted adaptive controller follow-up 2 measured. `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_followup_2_smoke.json` resumes from `6931.346282307703 N`, runs under a `720 s` budget, completes in about `670 s`, and promotes the frontier to `6931.331952798518 N`. G1 remains open; the accepted component is matrix-free global Krylov, not secant-family, so the route is tapering and the next useful work is changed residual/Jacobian operator quality or ROCm/HIP production-lane transfer.

# Betelgeuze Harness State

- Status: active
- Mode: Deep
- Risk: R3, local artifact/productization work
- Goal: keep recursively improving the workstation-based structural delivery service until locally closable gaps are eliminated.
- Current boundary: EB/RH strict independent-product evidence remains external and is not claimed as locally closable.

## Completed In This Track

- Added workstation hardware profile, service budget, delivery package, client input validation, and workstation delivery readiness gates.
- Generated local workstation artifacts and package zip.
- Verified workstation delivery readiness passes locally.
- Kept independent commercial product readiness blocked by EB/RH only.
- Upgraded the integrated single HTML viewer with Material Model Lock and selected-member nonlinear material state evidence.
- Representative package `viewer.html` now comes from `structural_viewer_midas33_optimized_singlefile.html`, with material library and per-member material assignment parity at 100% and only section/drawing assignment optimization changes.
- Added Material-Force Interaction evidence so source-backed load-combination force rows map back to the locked material ids with selected-combination sync, governing material, max D/C, and axial/shear/moment bars.
- Added Member Section Capacity evidence so the selected member's KDS demand/capacity rows are paired with locked material id, locked section id, parsed section geometry, fy/fc strength, KDS source capacity, section-screening estimates, max D/C, and synchronized load-combination/member focus.
- Added Story Force Flow Ledger evidence so active load-combination force rows are grouped by building story/source level with source row counts, member counts, axial/shear/moment totals, governing story, governing D/C, representative member focus, and story-clip behavior.
- Added Material Model Signature Ledger evidence so every source material row keeps its raw signature locked with zero changed signatures, including concrete, steel, composite SRC, and rigid-link constraint models.
- Added Stage Story Force Flow Bands so story-level axial/shear/moment force evidence projects into the 3D viewport with source-backed row counts, D/C tone, governing member, and story-clip synchronization.
- Added Stage Force Demand Contour so selected load-combination source-backed member force demand rows project into the 3D viewport as D/C contour markers with material id/model, component, material lock status, and click-to-focus synchronization.
- Added Material Model Demand Atlas so every locked source material model remains visible while selected load-combination force rows show mapped/source counts, force-backed material count, governing member/component, max D/C, and lock status.
- Added Material Model Force Envelope so every locked source material model is compared across all load combinations with SVG D/C traces, selected/governing points, governing combination/member/component, axial/shear/moment bars, and 100% material/member parity.
- Added Stage Material Model Demand Badges so every locked source material model is also visible in the 3D viewport, with force-backed badges anchored to active force-demand material evidence and non-governing material models docked without changing original material parity.
- Added Stage Material Force Ribbons so material-force interaction groups project into the 3D viewport with axial/shear/moment bars, selected-combination sync, governing member/material identity, and D/C evidence.
- Added Stage Material Force Envelope so the all-combination Material Model Force Envelope projects into the 3D viewport with SVG D/C traces, selected/governing points, governing combination/member/component, axial/shear/moment bars, mapped/source force rows, and 100% material/member parity receipts.
- Added Stage Member Material State Badge so the selected member's locked material id, section id, nonlinear material state, D/C, demand ratio, source-backed rows, and fy/fc receipt project directly into the 3D viewport.
- Added Drawing Material Parity Ledger so the active drawing handoff proves original-vs-optimized material libraries and member material ids remain at 100% while section/drawing assignment edits carry the optimization scope.
- Added Drawing Force Handoff Ledger so the active drawing sheet also carries selected load-combination, selected member, source-backed force row count, max D/C, and axial/shear/moment/check evidence from the same Load Combination Force Matrix as the cockpit.
- Added Drawing Material Model Matrix so every source material model row remains visible inside the drawing rail, including lock status, raw signature token count, selected-combination demand state, and force-backed D/C where available.
- Added Drawing Sheet Force Matrix so visible drawing sheets can be selected and compared against the same selected load combination, selected member, source-backed force count, max D/C, and material-lock state.
- Added Drawing Source Detail Ledger so original drawing detail/source-sheet links, active sheet, material lock, force overlay, and section/drawing-only optimization scope are visible together while source material models stay locked.
- Added Drawing Sheet Detail Matrix so every visible drawing sheet can be reviewed against source linkage, revision/callout, material lock, D/C force overlay, and drawing-only optimization scope before opening the sheet.
- Added Drawing Capacity Handoff Ledger so the active drawing sheet carries selected load-combination, member, section, source/estimated capacity counts, demand/capacity, max D/C, min margin, and 100% material/member parity from the same Material Model Capacity Envelope as the cockpit.
- Added Drawing Material Constitutive Register so drawing review now lists every locked source material law, including concrete damage-plasticity, steel bilinear, composite steel-concrete interaction, and rigid-link constraint models, while drawings/section assignments remain the optimization scope.
- Added Drawing Material Curve Evidence so drawing review now renders source stress-strain curves, yield markers, and selected-combination demand markers for the same locked material laws.
- Added Drawing Force Vector Evidence so drawing review now renders selected load-combination axial/shear/moment/check force rows as SVG vectors with source-backed counts, D/C, active sheet/member/combination sync, and material-lock receipt.

## Current Recursive Gap

Resolved local productization gaps:

- `build_workstation_delivery_package.py` now writes `implementation/phase1/workstation_jobs/<job_id>/`.
- The job folder contains `input_manifest.json`, `run_log.jsonl`, `output_manifest.json`, and `checksums.sha256`.
- `check_workstation_delivery_readiness.py` now includes `Job reproducibility contract`.
- Delivery package builder now verifies `manifest.json` output rows against the zip's actual bytes/SHA-256 rows.
- Restore smoke now verifies the restored `viewer.html` has a viewer shell marker.
- Delivery package now includes `DELIVERY_INDEX.md`, `REVISION_HISTORY.md`, and `data/revision_policy.json`.
- Restore smoke now verifies the delivery index marker and revision policy.
- Added `workstation-job-retention-policy.v1` with explicit-confirmation cleanup policy.
- `check_workstation_delivery_readiness.py` now includes `Job retention and cleanup policy`.
- Added a read-only `cleanup_preview` that lists stale job folders without deleting.
- Restore smoke now checks PDF header, manifest report/viewer references, and manifest claim boundary.
- Added `ACCEPTANCE_PACKET.md` with customer acceptance/rejection and engineer-review acknowledgement markers.
- Added `data/redelivery_comparison_manifest.json` to link current package/job to previous delivery history.
- Restore smoke now verifies acceptance packet markers, manifest acceptance references, and redelivery comparison policy.
- Added `DELIVERY_QA_SUMMARY.md` with customer-visible QA, included-check, and hidden/internal marker checks.
- Added `data/report_metadata.json` with report SHA-256/bytes, fallback source, manifest/revision/QA summary links, and current job id.
- Restore smoke and readiness now verify QA summary markers and report metadata cross-reference.
- Added `data/signing_manifest.json` as an unsigned offline-signing skeleton with no embedded key material/private key.
- Restore smoke and readiness now verify signing manifest schema, `unsigned_placeholder` status, signable payload, and no-key-material guarantees.
- Added `HANDOFF_DIFF_SUMMARY.md` and `data/handoff_diff_summary.json` for customer-facing redelivery package-member deltas.
- Restore smoke and readiness now verify handoff diff markers, schema, current job id, and added/removed/changed/unchanged counts.
- `DESIGN.md` now includes `deliveryHandoffDiffLight` for customer-facing redelivery diff summaries.
- Added `workstation-delivery-viewer-smoke.v1` as a restored-package customer-open browser smoke.
- `scripts/verify-workstation-delivery-viewer-smoke.mjs` extracts `project_package.zip`, serves restored `viewer.html`, verifies package checksums, opens the viewer in Chromium, and records canvas screenshot metrics.
- Current workstation delivery readiness is `PASS | gates=8/8`.
- Support bundle includes 22/22 required artifacts, including workstation delivery viewer smoke.
- Restored package browser state probe confirms Material Model Lock: materialMatchPercent=100, memberAssignmentMatchPercent=100, materialMismatchCount=0, memberMaterialMismatchCount=0, comparableMemberCount=12728, sectionAssignmentChangeCount=353.
- Restored package browser state probe confirms Material-Force Interaction: schema `structure-viewer-material-force-interaction.v1`, status=ready, force rows 1056/1056 mapped, unmatched=0, sourceBacked=1056, selectedCombination=KDS_ULS_2, governingMaterialId=1, maxDcr=1.216, renderedRows=1, barCount=3, overflow=0.
- Restored package browser state probe confirms Member Section Capacity: schema `structure-viewer-member-section-capacity.v1`, status=ready, rowCount=4, sourceBacked=4, KDS sourceCapacity=4, estimatedCapacity=3, selectedCombination=KDS_ULS_2, selectedMember=27441, materialId=1, sectionId=1087, geometryReady=true, dimension=1.500 m x 1.500 m, maxDcr=1.216, renderedRows=4, barCount=8, overflow=0.
- Restored package browser state probe confirms Story Force Flow Ledger: schema `structure-viewer-story-force-flow-ledger.v1`, status=ready, selectedCombination=KDS_ULS_2, storyCount=1, rowCount=1, forceRowCount=72, sourceBacked=72, governingStory=Story 37, governingMember=27441, axialTotal=20364.63 kN, shearTotal=6516.68 kN, momentTotal=49893.34 kNm, maxDcr=1.216, renderedRows=1, barCount=3, overflow=0.
- Restored package browser state probe confirms Material Model Signature Ledger: schema `structure-viewer-material-model-signature-ledger.v1`, status=ready, rowCount=6, materialCount=6, lockedCount=6, changedCount=0, rawTokenCount=90, renderedRows=6, overflow=0, and model families include Concrete damage-plasticity, Steel bilinear, Composite steel-concrete interaction, and Rigid link constraint.
- Restored package browser state probe confirms Material Model Demand Atlas: schema `structure-viewer-material-model-demand-atlas.v1`, status=ready, selectedCombination=KDS_ULS_2, rowCount=6, materialCount=6, renderedRows=6, lockedCount=6, changedCount=0, lockStatus=locked, forceBackedMaterialCount=1, forceRowCount=72, mappedForceRowCount=72, sourceBackedCount=72, maxDcr=1.216, barCount=12, overflow=0, and all six material model rows remain visible.
- Dense source browser state confirms Material Model Force Envelope: schema `structure-viewer-material-model-force-envelope.v1`, status=ready, rowCount>=6, materialCount>=6, renderedRows=rowCount, combinationCount>=2, forceRowCount>=72, mappedForceRowCount>=72, sourceBackedCount>=72, forceBackedMaterialCount>=1, selectedCombination sync, governingMaterialId present, lockedCount=rowCount, changedCount=0, materialLockStatus=locked, materialMatchPercent=100, memberAssignmentMatchPercent=100, maxDcr>0, SVG/point/bar coverage, browser state sync, and overflow=0.
- Restored package browser state probe confirms Stage Material Model Demand Badges: schema `structure-viewer-stage-material-model-demand-badges.v1`, status=ready, selectedCombination=KDS_ULS_2, rowCount=6, renderedCount=6, materialCount=6, lockedCount=6, changedCount=0, materialLockStatus=locked, forceBackedMaterialCount=1, forceRowCount=72, mappedForceRowCount=72, sourceBackedCount=72, maxDcr=1.216, edgePinnedCount=1, dockedCount=5, overflow=0, and the force-backed concrete model is anchored to member 27441/material 1.
- Restored package browser state probe confirms Stage Material Force Ribbons: schema `structure-viewer-stage-material-force-ribbons.v1`, status=ready, selectedCombination=KDS_ULS_2, rowCount=1, renderedCount=1, materialCount=1, forceRowCount=1056, mappedForceRowCount=1056, sourceBackedCount=1056, governingMaterialId=1, maxDcr=1.216, edgePinnedCount=1, dockedCount=0, barCount=3, overflow=0, and the force-backed concrete material ribbon is anchored to member 27441.
- Dense source browser state confirms Stage Material Force Envelope: schema `structure-viewer-stage-material-force-envelope.v1`, status=ready, selectedCombination=KDS_ULS_2, cardCount>=1, renderedCount=cardCount, sourceRowCount matches the rail Material Model Force Envelope row count, material/combination/force/source counts match the rail envelope, forceBackedMaterialCount>=cardCount, governingMaterialId syncs, lockedCount matches rail locked material rows, changedCount=0, materialLockStatus=locked, materialMatchPercent=100, memberAssignmentMatchPercent=100, maxDcr syncs, SVG/point/bar coverage is present, projected or edge-pinned placement is nonzero, browser state syncs, and overflow=0.
- Restored package browser state probe confirms Stage Member Material State Badge: schema `structure-viewer-stage-member-material-state-badge.v1`, status=ready, selectedCombination=KDS_ULS_2, selectedMember=27441, materialId=1, sectionId=1087, demandRatio=1.216, maxDcr=1.216, cardCount=1, projectionCount=1, sourceBackedCount>=1, and overflow=0.
- Dense source browser state confirms Drawing Material Parity Ledger: schema `structure-viewer-drawing-material-parity-ledger.v1`, status=ready, materialMatchPercent=100, memberAssignmentMatchPercent=100, materialMismatchCount=0, memberMaterialMismatchCount=0, sectionAssignmentChangeCount=353, sheetCount=4, locked=true, drawingOnlyOptimized=true, rowCount>=4, active sheet syncs with Drawing Handoff, and overflow=0.
- Dense source browser state confirms Drawing Force Handoff Ledger: schema `structure-viewer-drawing-force-handoff-ledger.v1`, status=ready, selected combination/member syncs with Load Combination Force Matrix, active sheet syncs with Drawing Handoff, force/source row counts are nonzero, maxDcr > 0, rowCount>=1, browser state syncs, and overflow=0.
- Dense source browser state confirms Drawing Material Model Matrix: schema `structure-viewer-drawing-material-model-matrix.v1`, status=ready, rowCount=6, lockedCount=6, changedCount=0, forceBackedMaterialCount>=1, materialMatchPercent=100, memberAssignmentMatchPercent=100, active sheet and selected-combination sync, browser state sync, and overflow=0.
- Dense source browser state confirms Drawing Sheet Force Matrix: schema `structure-viewer-drawing-sheet-force-matrix.v1`, status=ready, sheetCount=4, rowCount=4, activeRowCount=1, selected combination/member sync, force/source row counts are nonzero, maxDcr > 0, materialLocked=true, browser state sync, and overflow=0.
- Dense source browser state confirms Drawing Source Detail Ledger: schema `structure-viewer-drawing-source-detail-ledger.v1`, status=ready, sheetCount=4, sourceLinkedCount>=1, detailCount=5, materialLocked=true, drawingOnlyOptimized=true, materialMatchPercent=100, memberAssignmentMatchPercent=100, sectionAssignmentChangeCount>0, active sheet syncs with Drawing Handoff, browser state syncs, and overflow=0.
- Dense source browser state confirms Drawing Sheet Detail Matrix: schema `structure-viewer-drawing-sheet-detail-matrix.v1`, status=ready, sheetCount=4, rowCount=4, activeRowCount=1, sourceLinkedCount>=1, materialLocked=true, drawingOnlyOptimized=true, materialMatchPercent=100, memberAssignmentMatchPercent=100, maxDcr>0, active sheet syncs with Drawing Handoff, browser state syncs, and overflow=0.
- Dense source browser state confirms Drawing Capacity Handoff Ledger: schema `structure-viewer-drawing-capacity-handoff-ledger.v1`, status=ready, active sheet syncs with Drawing Handoff, selectedCombination syncs with Load Combination Force Matrix, rowCount>=1, source/estimated capacity row coverage, material/force/source counts synchronize with Material Model Capacity Envelope, materialLocked=true, materialMatchPercent=100, memberAssignmentMatchPercent=100, maxDcr/minMargin sync with the rail capacity envelope, browser state syncs, and overflow=0.
- Dense source browser state confirms Drawing Material Constitutive Register: schema `structure-viewer-drawing-material-constitutive-register.v1`, status=ready, rowCount>=6, materialCount>=rowCount, renderedRows=rowCount, active sheet syncs with Drawing Handoff, selectedCombination syncs with Load Combination Force Matrix, source-backed/nonlinear/curve/capacity-backed counts match DOM state, materialLocked=true, materialMatchPercent=100, memberAssignmentMatchPercent=100, browser state rows include Concrete damage-plasticity, Steel bilinear, Composite steel-concrete interaction, and Rigid link constraint, and overflow=0.
- Dense source browser state confirms Drawing Material Curve Evidence: schema `structure-viewer-drawing-material-curve-evidence.v1`, status=ready, curveCount matches Drawing Material Constitutive Register curve rows, renderedRows/SVG/yield-marker/demand-marker counts match curveCount, active sheet syncs with Drawing Handoff, selectedCombination syncs with Load Combination Force Matrix, source-backed and capacity-backed counts match DOM state, materialLocked=true, materialMatchPercent=100, memberAssignmentMatchPercent=100, browser state rows include Concrete damage-plasticity, Steel bilinear, Composite steel-concrete interaction, and Rigid link constraint, and overflow=0.
- Dense source browser state confirms Drawing Force Vector Evidence: schema `structure-viewer-drawing-force-vector-evidence.v1`, status=ready, rowCount>=3, renderedRows=rowCount, SVG count=rowCount, active sheet syncs with Drawing Handoff, selectedCombination and selectedMember sync with Load Combination Force Matrix, source-backed count>=1, maxDcr>0, materialLocked=true, browser state syncs, vector kinds include axial/shear/moment, and overflow=0.
- Restored package browser state probe confirms Stage Story Force Flow Bands: schema `structure-viewer-stage-story-force-flow-bands.v1`, status=ready, selectedCombination=KDS_ULS_2, bandCount=1, projectedCount=1, forceRowCount=72, sourceBacked=72, governingStory=Story 37, governingMember=27441, maxDcr=1.216, renderedBands=1, barCount=3, overflow=0.
- Restored package browser state probe confirms Stage Force Demand Contour: schema `structure-viewer-stage-force-demand-contour.v1`, status=ready, selectedCombination=KDS_ULS_2, count=8, renderedCount=8, projectedCount=4, edgePinnedCount=4, forceRowCount=72, mappedForceRowCount=72, sourceBacked=72, materialLockStatus=locked, materialModelCount=6, maxDcr=1.216, markerCount=8, projectionCount=8, overflow=0.
- Selected nonlinear material state confirms member 27441 binds to material 1, section 1087, and demandRatio 1.216 in the integrated single HTML viewer.
- Full Python test suite passes: `1465 passed`.
- Structural Insight Viewer cockpit polish advanced toward the target reference image:
  - Added `src/structure-viewer/commercial-cockpit-polish.css` as the final flagship dark cockpit layer.
  - Default MIDAS33 review now enters contour-first with engineering-unit legend values and no automatic Compare geometry pass.
  - Display-only node contour scalar context supplies mm/MPa values when raw node fields lack usable range, without mutating analysis/KPI inputs.
  - MIDAS33 review/frame camera presets now use target offsets and distance scaling for a larger stage-focused view.
  - Deformed/Compare geometry now uses muted neutral ghost styling when explicitly enabled.
  - Critical member table deduplicates repeated member rows, and the utilization heatmap renders an inactive core void.
  - Contour mode now keeps instanced frame/surface meshes visible with subdued wire overlays, improving section/body readability while staying above local FPS budget.
  - Direct and instanced line members now use type/section/utilization-informed visual radius buckets, so heavier columns/beams/braces read as structural members while preserving instancing.
  - KPI provenance labels now use customer-facing `Model estimate`, `Model quantity estimate`, `Model volume estimate`, and `Model cost estimate` wording instead of internal `proxy` labels.
  - Drawing handoff is now a first-class rail panel with revision, active callout, SVG sheet links, primary sheet, and viewer deep-link copy, while preserving the KPI -> optimization -> critical-members first-viewport flow.
  - Footer instrumentation now includes a commercial analysis timeline strip with load case, active step, previous/play/next controls, scrubber, scale, solver, convergence, and runtime.
  - Drawing handoff now includes a compact sheet preview thumbnail with active sheet name, callout tag, revision, selected member, and package status, so the rail reads like a deliverable sheet handoff instead of a plain link list.
  - The viewport now includes compact stage-native result callouts for max displacement, max drift, base shear, and the leading critical member, all derived from the same analysis cockpit model as the KPI rail and critical-member table.
  - Stage critical-member callout, critical-member table row, and viewport focus now share the same member selection. The browser smoke verifies the critical callout click selects member `11744` and mirrors `is-selected`, `aria-selected`, and `aria-pressed`.
  - The active critical member now receives a 3D viewport focus halo, leader, and projected HUD badge using the same selection identity, with `nodeData` / `p0` / `p1` fallback so normalized overlay segments can still be located.
  - Drawing handoff sheet preview is now interactive: hovering or focusing a sheet updates the active thumbnail, selected `aria-current` sheet state, disabled state, and `Open Active Sheet` link while preserving the selected member/callout identity.
  - Added `scripts/verify-structure-viewer-drawing-handoff-preview.mjs` to browser-smoke the handoff rail against the live viewer.
  - Ctrl/Cmd selection from critical-member rows and search results now preserves `member_set`, keeps all selected critical rows marked, distinguishes the primary row, and publishes the selected set through the shared selection/deep-link state.
  - The viewport selection HUD now shows selected-count, edge-pinned state, and secondary selected-member markers instead of only a single active-member badge.
  - Added `scripts/verify-structure-viewer-multi-selection-hud.mjs` to browser-smoke member-set selection, row state, badge count, edge metadata, local storage, and deep-link `member_set`.
  - Stage result callouts now use collision-aware docking across right, left, bottom-right, and bottom-left positions when the active selection HUD would overlap the result callout stack.
  - Added `scripts/verify-structure-viewer-callout-docking.mjs` to browser-smoke forced HUD/callout overlap and verify the callout stack re-docks clear.
  - Updated `DESIGN.md` to document contour-first defaults and explicit Compare/deformed ghosting.
  - Updated `implementation/phase1/structure_viewer_visual_regression_baseline.json` after intentional visual change.
  - Synced generated single-file delivery viewer packaging with the current commercial cockpit source by recursively inlining local `viewer-*.js` modules plus `design-theme.css` and `commercial-cockpit-polish.css`.
  - Regenerated `implementation/phase1/release/visualization/structural_viewer_midas33_pr_singlefile.html`, `project_package.zip`, job record, retention policy, support bundle, and readiness artifacts.
  - Customer-open package viewer smoke now reports `current_cockpit_delivery` with all 7/7 required cockpit markers and a visible nonblank restored canvas.
  - Added dense cockpit short-viewport compression for 1600x900 desktop use: compact top chrome/rails/stage controls/KPIs/charts/footer so the 3D viewport, KPI rail, critical members, four lower charts, and solver timeline remain readable in one screen.
  - Added browser coverage for dense desktop layout: topbar budget, viewport/right-rail minimums, eight KPI cards, four chart panels, critical-member rows, stage callouts, zero chart/footer overlap, and zero callout/HUD overlap.
  - The material quantity chart now reads as grouped before/after result evidence with original and optimized vertical bars, unit labels, and percent deltas for steel, concrete, and rebar.
  - The story drift over height chart now reads as original/optimized comparison evidence with muted original and teal optimized curves, grid lines, drift limit marker, peak dot, and comparison legend.
  - Lower cockpit line charts now carry compact engineering axis/tick/unit labels: story drift labels drift/height and peak optimized drift, and displacement-vs-load-step uses one shared y-scale for original/optimized curves with load-step/displacement labels.
  - KPI rail cards now read as compact commercial evidence cards with label, engineering value, trend/margin badge, reference limit or before/baseline value, provenance label, filled mini-trend, and latest-point marker; dense 1600x900 browser smoke verifies KPI overflow remains zero.
  - Critical Members rows now read as commercial engineering review rows with normalized severity, D/C ratio bar, limit marker, margin label, drift-contribution microbar, status chip, and recommended-change chip while preserving stage/table focus behavior.
  - Optimization Summary cards now read as before/after evidence receipts with source/provenance, before and after values, after-vs-before bars, signed deltas, saved amount labels, metric labels, and a details link into the lower analysis strip; dense 1600x900 browser smoke verifies Optimization Summary overflow remains zero.
  - The left stage overlay now includes a compact Stage Result Receipt beside the contour legend with render mode, scalar field, engineering range, source/provenance label, load case, step, colormap, and Compare/deformation state; dense 1600x900 browser smoke verifies receipt overflow remains zero.
  - Customer-open package smoke now requires `stage_result_receipt` as part of `current_cockpit_delivery`, raising the marker contract to 8/8.
  - Load Cases in the left stage overlay now render as compact evidence rows with case kind, selected/governing/available state, source label, and step progress bars instead of plain links; dense 1600x900 browser smoke verifies at least two rows, one active row, and zero row overflow.
  - Customer-open package smoke now requires `load_case_evidence_rows` in addition to `stage_result_receipt`, raising the `current_cockpit_delivery` marker contract to 9/9.
  - The viewport tool rail now reads like a grouped commercial 3D tool strip: render/result/camera groups, icon tools, tooltip labels, active `aria-pressed` state, and synchronized render/compare/animate/view preset state.
  - The viewport now includes a compact 3D Overlay Receipt for lateral load vectors and base support markers, with load-vector count/direction, support-marker count, active load case, source label, DOM data attributes, and `window.__STRUCTURE_VIEWER_ANALYSIS_OVERLAY_STATE__` smoke state.
  - Customer-open package smoke now requires `viewport_tool_rail` and `analysis_overlay_receipt`, raising the `current_cockpit_delivery` marker contract to 11/11.
  - The left stage overlay now starts with a compact View Controls pod: view mode, camera preset, optimized model visibility, Compare Ghost state, active result field, load/step, and deformation scale. It stays synchronized with toolbar and viewport rail state.
  - Customer-open package smoke now requires `stage_review_controls`, raising the `current_cockpit_delivery` marker contract to 12/12.
  - The Utilization Heatmap panel now reads as plan-level engineering evidence with active level chip, D/C color scale, core void, hot-zone outlines, max/average D/C, hot-cell count, critical-zone share, and governing member/story receipt.
  - Customer-open package smoke now requires `utilization_heatmap_evidence`, raising the `current_cockpit_delivery` marker contract to 13/13.
  - The Contour Scale now reads as a vertical engineering result scale with five high-to-low tick labels, explicit unit/source labels, min/max summary, and dense overflow coverage.
  - Customer-open package smoke now requires `contour_scale_evidence`, raising the `current_cockpit_delivery` marker contract to 14/14.
  - The top app bar now includes a compact commercial run-control strip with load case, step, solver/runtime receipt, Compare, Export, Report, Share, New Run, Light, and Shortcuts actions. `New Run` resets local review state, Compare/deformed overlay, timeline playback/override, isolation/selection, and review camera without implying cloud solve submission.
  - Customer-open package smoke now requires `top_run_control`, raising the `current_cockpit_delivery` marker contract to 15/15.
  - The top app bar Project area is now a real manifest-backed project/drawing selector tied to the same workspace URL contract as the left Project Browser. It exposes project, drawing, variant, and review-status data attributes, stays overflow-free in the dense 1600x900 smoke, and preserves the legacy hidden `shell-project-pill` marker for shell compatibility.
  - Customer-open package smoke now requires `top_project_selector`, raising the `current_cockpit_delivery` marker contract to 16/16.
  - The 3D Overlay Receipt now includes an in-stage visual-evidence legend with yellow vector and green support swatches, visible evidence counts, browser state, dense overflow coverage, and stronger load-arrow/support-marker visual prominence so lateral-load/support evidence reads in the viewport instead of only as text.
  - Customer-open package smoke now requires `analysis_overlay_visual_evidence`, raising the `current_cockpit_delivery` marker contract to 17/17.
  - Added compact Result Evidence receipt under the KPI cards with source-backed metric count, estimate metric count, total metric count, sample base, load-step receipt, heatmap cell count, and critical-member evidence count. This makes mixed source/estimate status visible without turning model estimates into solver-source claims.
  - Customer-open package smoke now requires `analysis_result_evidence`, raising the `current_cockpit_delivery` marker contract to 18/18.
  - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support all remain PASS with latest job `20260524T130037-f8616d87d2ed53b4`.
  - Sequential artifact refresh confirmed retention and job reproducibility both point to latest job `20260524T130037-f8616d87d2ed53b4`.
  - Latest workstation service budget evidence is `ready=4745ms`, `fps=26.6`; visual regression remains `PASS | cases=11/11 | mode=baseline_update`.
  - Added Materials & Members catalog coverage to the flagship viewer:
    - Direct normalizer now promotes source material grades, inferred material families, usage counts, E/Poisson/density, material colors, thickness rows, and rebar code snippets into `material_catalog_summary`, `thickness_catalog_summary`, and `rebar_material_code_summary`.
    - MIDAS33 inline release routes now hydrate missing material/member catalog metadata from the direct roundtrip artifact without replacing the existing release compare payload.
    - Right rail now exposes a compact Materials & Members panel with 6 source materials, 183 sections, 34 thickness rows, steel/concrete grade visibility, material row focus/isolate actions, and explicit missing/inferred status labelling.
    - Selection properties now show material label, ID, family, E, Poisson ratio, density, and non-source material status when available.
    - Customer-open package smoke now requires `material_member_catalog`, raising `current_cockpit_delivery` marker coverage to 20/20.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T133322-f8616d87d2ed53b4`.
  - Added Material Schedule coverage to close missing material/member visibility:
    - Direct normalizer now adds `section_usage_summary` per material row and `material_section_schedule_count` to meta, linking each material grade to section ids, section labels, inferred family/shape, usage counts, member samples, and element samples.
    - The Materials & Members rail now includes a compact `Material schedule` block with material-section rows, usage counts, family mix labels, and click-to-isolate `material_section` filtering.
    - Browser smoke now verifies material schedule count, row count, steel/concrete schedule visibility, and zero schedule overflow in the dense 1600x900 cockpit.
    - Customer-open package smoke now requires `material_section_schedule`, raising `current_cockpit_delivery` marker coverage to 21/21.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T134607-f8616d87d2ed53b4`.
  - Added Section Schedule coverage for reverse section-to-material/member review:
    - Direct normalizer now adds `material_usage_summary` to `section_catalog_summary` and `section_material_schedule_count` to meta, linking each section id/label/family/shape to material usage, family mix, sample member ids, and sample element ids.
    - The Materials & Members rail now includes a compact `Section schedule` block with section-material rows and click-to-isolate `section_id` filtering.
    - Browser smoke now verifies section schedule count, row count, section/material visibility, and zero section schedule overflow in the dense 1600x900 cockpit.
    - Customer-open package smoke now requires `section_member_schedule`, raising `current_cockpit_delivery` marker coverage to 22/22.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T135523-f8616d87d2ed53b4`.
  - Added expanded Material/Member ontology coverage:
    - Direct normalizer now classifies concrete, structural steel, rebar, prestressing, cable, bolt/anchor, weld, FRP, timber, masonry, aluminum, stainless steel, cold-formed steel, metal deck, composite, elastomeric bearing, glass, soil/geotechnical, and rigid-link material families from source names/grades/raw tokens.
    - Section fallback inference now classifies wall, slab, column, beam, brace, cable, steel H/box/pipe/tube/angle/channel, concrete rectangular, composite, and foundation section descriptors when source `section_library` rows are sparse.
    - The Materials & Members rail now includes `Material coverage` family chips with known/unclassified counts and keeps source-status warnings visible instead of silently collapsing sparse material definitions.
    - Browser smoke now verifies material-family chip count and zero material-family overflow in the dense 1600x900 cockpit.
    - Customer-open package smoke now requires `material_family_coverage`, raising `current_cockpit_delivery` marker coverage to 23/23.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T141336-f8616d87d2ed53b4`.
  - Added Material/Member ontology breadth follow-up for previously missing delivery materials:
    - Direct normalizer and viewer fallback inference now classify rail steel, rail fasteners, grout/backfill, asphalt, seismic isolators, resilient pads, dampers, spring/nonlinear links, mass/inertia, ballast, and geosynthetic/membrane families instead of sending them to `unclassified`.
    - Section fallback inference now covers retaining/diaphragm walls, pile/mat/footing foundations, wall-boundary columns, coupling beams, transfer girders, dampers, isolators, spring links, rail sections, and tunnel segment lining.
    - The Materials & Members rail now exposes `data-material-family-ontology-count` and shows supported ontology breadth separately from used material-family chips.
    - Browser smoke verifies ontology breadth >=30, and customer-open package smoke now requires `material_ontology_breadth`, raising `current_cockpit_delivery` marker coverage to 24/24.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T142950-f8616d87d2ed53b4`.
  - Added Result Step Schedule coverage:
    - The KPI rail now includes `structure-viewer-result-step-schedule.v1` with active load case, step x/y, convergence, runtime, and five nearby load-step rows.
    - Clicking a schedule row updates the shared analysis timeline override and re-renders the footer timeline, top run-control, schedule active row, and load-step chart cursor from the same resolved step state.
    - Browser smoke verifies one active schedule row, at least five rows, nonempty load case/solver/convergence, chart label sync after a row click, and zero schedule overflow in the dense 1600x900 cockpit.
    - Customer-open package smoke now requires `result_step_schedule`, raising `current_cockpit_delivery` marker coverage to 25/25.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T144145-f8616d87d2ed53b4`.
  - Added a second Material/Member ontology breadth expansion after missing-material review:
    - Direct normalizer and viewer fallback inference now expose 47 supported material families, adding ground improvement, waterproofing/waterstop, roofing, insulation, fireproofing, coatings, sealants/joint fillers, gypsum/board, stone/tile, facade/cladding panels, rail sleepers/ties, pot/spherical bearings, expansion joints, adhesive/resin, formwork/shoring, screed/topping, and sleeve/embedded inserts.
    - Section fallback inference now covers retaining/diaphragm/parapet walls, piers/grade beams, stairs/ramps/balconies/roof slabs, mega columns, spandrel/lintel/joist/purlin/rafter/edge beams, BRBs, trusses, outriggers, diaphragm/collector/drag-strut members, tiebacks/ground anchors, and base/gusset/splice/embed plates.
    - Browser smoke now verifies ontology breadth >=45, and customer-open package smoke still passes the 25/25 current cockpit delivery marker set.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T145752-f8616d87d2ed53b4`.
  - Added Result Envelope coverage:
    - The KPI rail now includes `structure-viewer-result-envelope.v1` between Result Step Schedule and Delivery Review Receipt.
    - Envelope rows expose governing displacement, drift, base shear, utilization, and heatmap hot-zone evidence with load case, active step, source/estimate coverage, and member-scoped focus actions.
    - Browser smoke verifies the envelope is ready, contains at least four governing rows, includes member-linked rows, and remains overflow-free at dense 1600x900.
    - Customer-open package smoke now requires `result_envelope`, raising `current_cockpit_delivery` marker coverage to 26/26.
    - Regenerated the single-file delivery viewer and `project_package.zip`; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T152125-f8616d87d2ed53b4`.
  - Added compact Model Overview and source adapter matrix coverage:
    - The left rail now exposes `structure-viewer-model-overview.v1` and `structure-viewer-source-adapter-matrix.v1` for model/name/nodes/elements/stories/height/units/analysis type/last run plus MIDAS/OpenSees/Abaqus source slots.
    - Dense 1600x900 browser smoke verifies the overview is ready, MIDAS is the active source for MIDAS33, all three adapter slots render, and the overview has zero overflow.
    - Renderable JSON evidence ingest reload now persists the direct model payload through local/session storage and fixes the short-model height label fallback that previously caused demo fallback after reload.
    - Customer-open package smoke now requires `model_overview`, raising `current_cockpit_delivery` marker coverage to 27/27.
    - Regenerated the delivery package artifacts; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T161723-f8616d87d2ed53b4`.
  - Added commercial Deformation Scale control to the stage View Controls pod:
    - The stage now exposes `structure-viewer-deformation-control.v1` with a customer-facing `1.0x` display scale and machine-readable internal multiplier evidence.
    - The new stage-local range control synchronizes the legacy deformation slider, stage review controls, Compare/Animate state, and stage result receipt scale labels.
    - Dense 1600x900 browser smoke verifies the control is ready, synced to internal `100`, and overflow-free.
    - Customer-open package smoke now requires `deformation_scale_control`, raising `current_cockpit_delivery` marker coverage to 28/28.
    - Regenerated the delivery package artifacts; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T162906-f8616d87d2ed53b4`.
  - Added solver-verified Panel Zone / Joint Evidence coverage:
    - The right rail now exposes `structure-viewer-panel-zone-evidence.v1` between Result Envelope and Delivery Review Receipt.
    - The panel shows joint-geometry, rebar-anchorage, 3D clash, exact/fallback validation counts, candidate member rows, member-focus actions, source path, and `solver_verified` boundary without claiming autonomous approval.
    - Workstation delivery package now includes `evidence/panel_zone_clash_artifact.json`, `evidence/panel_zone_clash_report.json`, and `evidence/panel_zone_solver_verified_handoff_report.json`.
    - Dense 1600x900 browser smoke verifies the panel is ready, has 3/3 validated sources, 0 fallback sources, at least 45 candidates, at least 3 member rows, and zero overflow.
    - Customer-open package smoke now requires `panel_zone_evidence`, raising `current_cockpit_delivery` marker coverage to 29/29.
    - Regenerated the delivery package artifacts; workstation delivery smoke/readiness/support remain PASS with latest job `20260524T164604-81d7549969225809`.
  - Added viewport-native Panel Zone / Joint stage badge coverage:
    - The 3D viewport now exposes `structure-viewer-panel-zone-stage-badge.v1` with a compact leader, primary candidate member id, solver-verified status, source count, 3D clash count, fallback count, projection/docked state, and click-to-focus behavior tied to the same rail evidence.
    - Dense 1600x900 browser smoke verifies the badge is visible, ready, source-aligned, overflow-free, and non-overlapping with the selected-member HUD.
    - Customer-open package smoke now requires `panel_zone_stage_badge`, raising `current_cockpit_delivery` marker coverage to 30/30.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T170513-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added projected Critical Member stage hotspot coverage:
    - The 3D viewport now exposes `structure-viewer-stage-critical-hotspots.v1` as member-linked projected hotspot chips for the leading critical rows, with D/C status, recommended change, click-to-focus behavior, selected-state sync, and edge/collision metadata.
    - Hotspots avoid the selected-member HUD and panel-zone stage badge while staying inside the dense 1600x900 viewport bounds.
    - Browser smoke verifies hotspot readiness, schema, projection count, selected state, visible critical text, zero focus overlap, and zero overflow.
    - Customer-open package smoke now requires `stage_critical_hotspots`, raising `current_cockpit_delivery` marker coverage to 31/31.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T171750-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added projected Story Level Ruler coverage:
    - The 3D viewport now exposes `structure-viewer-stage-story-ruler.v1` as a compact story/height ruler derived from the same `Story Clip` bands and cockpit story-drift rows.
    - Ruler rows project to story elevations, show short story labels, height, drift context, model height/story count receipt, and click-to-clip/clear behavior tied to the existing story clip state.
    - Dense 1600x900 browser smoke verifies story ruler readiness, schema, row count, story count, height, projected rows, visible receipt text, and zero overflow.
    - Customer-open package smoke now requires `stage_story_ruler`, raising `current_cockpit_delivery` marker coverage to 32/32.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T173131-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Stage Drift Limit Bands coverage:
    - The 3D viewport now exposes `structure-viewer-stage-drift-bands.v1` as compact per-story drift utilization bands derived from cockpit story-drift evidence and the same story clip band geometry as the Story Level Ruler.
    - Drift rows show original/optimized drift, limit utilization, success/warn/danger tone, projected/edge/docked state, and click-to-clip/clear behavior synchronized with the existing story clip state.
    - Dense 1600x900 browser smoke verifies drift-band readiness, schema, at least three projected bands, positive drift limit, visible limit receipt, tone count coverage, window state, and zero overflow.
    - Customer-open package smoke now requires `stage_drift_bands`, raising `current_cockpit_delivery` marker coverage to 33/33.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T174359-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added projected Load/Support stage glyph coverage:
    - The 3D viewport now exposes `structure-viewer-stage-load-support-glyphs.v1` as pointer-transparent projected viewport glyphs that mirror the existing 3D lateral-load and base-support overlay objects.
    - Load glyphs show six bright `+X` lateral-load arrows; support glyphs preserve the source support count while sampling the visible base markers for dense-stage readability.
    - Dense 1600x900 browser smoke verifies readiness, schema, load/support counts, projection state, window state, and zero overflow.
    - Customer-open package smoke now requires `analysis_overlay_projected_glyphs`, raising `current_cockpit_delivery` marker coverage to 34/34.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T183307-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added first-viewport Critical Triage coverage:
    - The right KPI rail now exposes `structure-viewer-critical-triage.v1` directly under result evidence so top critical members are visible in the first 1600x900 cockpit viewport instead of only in the lower full Critical Members section.
    - Triage rows show the top four D/C-governed members, high-severity count, max D/C ratio, D/C limit marker, drift contribution, status/action chips, and click/Ctrl-click focus behavior tied to the same selected-member state as the full table and stage hotspots.
    - Dense 1600x900 browser smoke verifies first-viewport presence, schema, row/source/high counts, max ratio, status/action chip counts, window state, and zero overflow.
    - Customer-open package smoke now requires `critical_triage`, raising `current_cockpit_delivery` marker coverage to 35/35.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T184950-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added first-viewport Optimization Delta Strip coverage:
    - The right KPI rail now exposes `structure-viewer-optimization-delta-strip.v1` between Result Evidence and Critical Triage so before/after optimization deltas are visible in the first 1600x900 cockpit viewport.
    - Delta tiles show the first four optimization rows from the same `optimizationRows` source as the full summary, including before value, after value, saved amount, signed delta, source label, metric label, and compact before/after bars.
    - Dense 1600x900 browser smoke verifies first-viewport ordering before Critical Triage, schema/status/row counts, reduction count, max reduction percent, bar/delta counts, window state, and zero overflow.
    - Customer-open package smoke now requires `optimization_delta_strip`, raising `current_cockpit_delivery` marker coverage to 36/36.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T190435-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added non-ellipsis KPI full-value readout coverage:
    - KPI cards now expose `data-kpi-full-value` and split visible engineering readouts into `.kpi-card__value-number` plus `.kpi-card__value-unit` where units exist.
    - Dense 1600x900 browser smoke verifies eight full-value readouts, number/unit token counts, zero KPI value ellipsis style, and zero KPI value overflow, so first-viewport values no longer degrade to `13...`, `1...`, or `24...`.
    - Customer-open package smoke now requires `kpi_full_value_readout`, raising `current_cockpit_delivery` marker coverage to 37/37.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T191526-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added non-ellipsis KPI full-label readout coverage:
    - KPI cards now expose `data-kpi-full-label`, preserve the complete metric label in the DOM/title, and allow compact two-line wrapping instead of dense-card label truncation.
    - Dense 1600x900 browser smoke verifies eight full labels, includes `Max Displacement` and `Estimated Material Cost`, and reports zero KPI label ellipsis plus zero KPI label overflow.
    - Customer-open package smoke now requires `kpi_full_label_readout`, raising `current_cockpit_delivery` marker coverage to 38/38.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T192344-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added first-viewport Contour Result Scale priority:
    - The left stage overlay now places `Results` immediately after `View Controls` and before `Load Cases`, matching the reference cockpit where the active scalar legend is visible beside the model.
    - Command-shell CSS restores `#legend-container.stage-contour-scale` visibility, adds a results-priority accent, and compresses dense View Controls so the contour colorbar remains in the 1600x900 stage frame.
    - Dense browser smoke verifies `data-stage-results-priority="first-stage-viewport"`, load cases ordered after results, positive contour-scale visible area, and zero contour overflow.
    - Customer-open package smoke now requires `stage_result_scale_priority`, raising `current_cockpit_delivery` marker coverage to 39/39.
    - Regenerated the single-file delivery viewer, performance/visual evidence, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T194534-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added dense model-protagonist overlay occlusion budget:
    - The 3D viewport now declares `data-stage-overlay-occlusion-budget="dense-model-protagonist"` and dense browser smoke measures both whole-viewport and central-model overlay coverage.
    - Dense 1600x900 CSS compresses result callouts, drift bands, critical hotspots, panel-zone badge, and selected-member HUD so overlays read as edge annotations instead of covering the structure.
    - The selected-member HUD now edge-docks to the lower-overlap side when it would cover the central model clear zone.
    - Customer-open package smoke now requires `stage_overlay_occlusion_budget`, raising `current_cockpit_delivery` marker coverage to 40/40.
    - Regenerated the single-file delivery viewer, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T201023-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added dense stage-primary dominance budget:
    - The 3D viewport now declares `data-stage-dominance-budget="dense-stage-primary"` so the dense cockpit records the canvas-dominance requirement directly in the shell.
    - Dense 1600x900 CSS narrows the left stage overlay to 164px and the tool rail to 32px, increasing the central 3D canvas to 646px wide.
    - Dense browser measurement captured width ratio 0.744 and area ratio 0.725 for the central canvas inside the stage frame, keeping the structure visually dominant after the previous overlay-occlusion work.
    - Customer-open package smoke now requires `stage_dominance_budget`, raising `current_cockpit_delivery` marker coverage to 41/41.
    - Regenerated the single-file delivery viewer, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T201951-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added dense Stage Model Stack evidence:
    - The stage View Controls now expose `structure-viewer-stage-model-stack.v1` with optimized, original ghost, and deformed-shape layer rows instead of hiding model-stack evidence in the dense 1600x900 cockpit.
    - The model stack uses distinct contour, neutral ghost, and deformed-shape swatches, synchronizes Compare/deformed state with the same `showDeformed` source as the top run control and viewport tool rail, and publishes layer/status attributes for browser smoke.
    - Dense CSS compresses the model stack into a 142x18 one-line chip row, preserving the contour scale inside the stage; browser capture confirmed contour bottom 640.7px inside stage bottom 653.3px.
    - Customer-open package smoke now requires `stage_model_stack_evidence`, raising `current_cockpit_delivery` marker coverage to 42/42.
    - Regenerated the single-file delivery viewer, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T203442-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added compact KPI chip readouts:
    - KPI evidence/reference/trend chips now expose `data-kpi-chip-full-label` and `data-kpi-chip-short-label`, preserving customer-facing full label text in DOM attributes and titles while rendering short labels in dense cards.
    - Reference chips abbreviate long values such as limit and before baselines into compact engineering tokens like `L120`, `L2.0`, and `B$1.29` so the KPI rail remains readable instead of clipping.
    - Dense browser smoke initially caught 4 chip overflows, then passed after the abbreviation pass; capture `/tmp/structure-viewer-kpi-chip-readout.png` recorded 24 KPI chips and 0 chip overflows.
    - Customer-open package smoke now requires `kpi_chip_readout`, raising `current_cockpit_delivery` marker coverage to 43/43.
    - Regenerated the single-file delivery viewer, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T204745-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Material Coverage Readiness strip:
    - Materials & Members now exposes `structure-viewer-material-coverage-readiness.v1` before the detailed catalog, scoring material definitions, family classification, section breadth, material-section links, slab/wall thickness rows, and used material rows.
    - Dense browser evidence shows the MIDAS33 catalog is not blocked, but it is honest about one unclassified material: source material rows 6, missing definitions 0, unclassified 1, readiness score 83, review queue 2.
    - The readiness strip keeps missing/unclassified material evidence visible instead of collapsing it into a green catalog pass; this directly addresses the material/member coverage gap without overstating source completeness.
    - Customer-open package smoke now requires `material_coverage_readiness`, raising `current_cockpit_delivery` marker coverage to 44/44.
    - Regenerated the single-file delivery viewer, package, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T210003-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added first-viewport Critical Members compact table:
    - Critical Triage now also exposes `structure-viewer-critical-members-compact-table.v1` with a reference-style `Critical Members` heading while preserving the existing `Critical Triage` evidence label.
    - The compact table columns are ID, D/C, Drift, Status, and Recommended Change, and its rows keep the same member focus/selection behavior as the full Critical Members table and viewport callouts.
    - Browser smoke verifies compact-table schema, header count, row parity with triage rows, and zero dense overflow.
    - Customer-open package smoke now requires `critical_members_compact_table`, raising `current_cockpit_delivery` marker coverage to 45/45.
    - Regenerated the delivery package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T211110-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added commercial Analysis Timeline Footer evidence:
    - The footer now exposes `structure-viewer-analysis-timeline-footer.v1` with active load case, active/total step, solver, convergence, runtime, scale, nearby step tick controls, and a synchronized window state.
    - Timeline ticks expose at least five nearby steps with exactly one active tick; clicking the tick/scrubber/prev/play/next controls stays tied to Result Step Schedule and the load-step chart cursor.
    - Long solver labels keep the full value in the title/data attributes but render compact cockpit text such as `Nonlinear DC` to avoid footer overflow.
    - Customer-open package smoke now requires `analysis_timeline_footer`, raising `current_cockpit_delivery` marker coverage to 46/46.
    - Regenerated the single-file delivery viewer, package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T212325-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Drawing Handoff v2 receipt evidence:
    - The drawing handoff panel now exposes `structure-viewer-drawing-handoff-panel.v2` with active sheet, callout, selected member, revision, and deep-link readiness attributes.
    - The panel includes active-sheet, callout, deep-link, and selection-sync receipt rows so a customer-open package can prove which sheet/callout/member is synchronized.
    - Sheet hover/focus updates the preview, `aria-current`, open action label, panel data attributes, and receipt rows from the same active sheet source.
    - Long receipt values use compact visible tokens while preserving full values in title and `data-drawing-handoff-receipt-full-value`.
    - Customer-open package smoke now requires `drawing_handoff_receipt`, raising `current_cockpit_delivery` marker coverage to 47/47.
    - Regenerated the single-file delivery viewer, package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T213415-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Upgraded Stage Result Callouts to v2 evidence:
    - The 3D viewport callout stack now exposes `structure-viewer-stage-result-callouts.v2` with ready/missing status, callout count, source count, estimate count, active load case, and active step.
    - Each callout carries full label, full value, source label, source type, load case, step, and optional member identity while keeping compact viewport text for max displacement, drift, base shear, and the governing critical member.
    - The critical-member callout remains focus-linked to the same member selection as the Critical Members table and projected hotspot, while the KPI callouts preserve source/estimate provenance without requiring the user to inspect the rail.
    - The source HTML now includes v2 fallback markers so restored single-file delivery packages remain statically auditable even when renderer modules are base64-inlined.
    - Customer-open package smoke still reports `current_cockpit_delivery` with marker coverage 47/47, but `stage_result_callouts` now requires the v2 schema/source/full-value marker rather than only a focus button.
    - Regenerated the single-file delivery viewer, package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T214844-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Upgraded Stage Result Callouts to v3 projected anchor HUD:
    - The 3D viewport callout stack now exposes `structure-viewer-stage-result-callouts.v3` and a separate `data-stage-result-callout-anchors` layer so result callouts are not only text receipts but visibly tied to model zones.
    - Max displacement, max drift, and base shear publish semantic anchors for roof displacement, governing drift story, and base reaction; the critical-member callout projects to the member focus point when geometry is available and records edge/docked fallback state when not.
    - Each callout now carries anchor kind, anchor label, projection state, anchor x/y, edge state, and a synchronized `__STRUCTURE_VIEWER_STAGE_RESULT_CALLOUTS_STATE__` record for browser evidence.
    - Dense browser smoke verifies at least four v3 anchors, at least three semantic anchors, zero anchor overflow, and inclusion of the anchor layer in the model-protagonist occlusion budget.
    - Customer-open package smoke still reports `current_cockpit_delivery` marker coverage 47/47, but `stage_result_callouts` now requires v3 schema plus anchor-kind/projection markers.
    - Regenerated the single-file delivery viewer, package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260524T220457-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Lower Chart Axis Evidence v1:
    - All four lower cockpit chart panels now expose `structure-viewer-lower-chart-evidence.v1` through one `data-lower-chart-axis-receipt` each, with x/y axis, unit, source, metric, shared-scale mode, row count, and peak/limit/active markers.
    - Story drift, displacement load-step, material quantity, and utilization heatmap chart surfaces publish `data-lower-chart-svg` plus `data-lower-chart-shared-scale="true"` so the lower strip reads as engineering evidence rather than decorative charts.
    - Dense 1600x900 browser smoke verifies 4/4 lower-chart evidence schemas, 4/4 shared-scale chart surfaces, required chart kinds/axis labels, peak/active marker coverage, and zero lower-chart receipt overflow.
    - Customer-open package smoke now requires `lower_chart_axis_evidence`, raising `current_cockpit_delivery` marker coverage to 48/48.
    - Regenerated the single-file delivery viewer, package, smoke, retention, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T113744-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
    - Compacted `.betelgeuze/trace.jsonl` from 11 MiB to 328 KiB so the source-boundary footprint gate returns to 0 cleanup candidates.
  - Added Force Flow Lens and Material Constitutive Lens:
    - The right rail now exposes `structure-viewer-force-flow-lens.v1` with load case, active step, base reaction, and axial/shear/moment evidence rows.
    - Materials & Members now exposes `structure-viewer-material-constitutive-lens.v1` with source-backed material force envelopes, stress utilization, default constitutive law labels, nonlinear/watch state, and click-to-focus material rows.
    - Customer-open package smoke now requires `force_flow_lens` and `material_constitutive_lens`, raising `current_cockpit_delivery` marker coverage to 50/50.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T122843-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Load Combination Force Matrix:
    - The right rail now exposes `structure-viewer-load-combination-force-matrix.v1` with KDS load-combination force/check rows, D/C, demand/capacity, component, clause, governing combination/member, source-backed counts, and member focus links.
    - Direct MIDAS/KDS payloads normalize row-level force/check evidence into `load_combination_force_rows`; interactive release payloads preserve `load_combination_codecheck_table_by_name` and graph-node focus metadata so the same matrix is ready in source and package viewers.
    - Dense browser smoke verifies ready status, at least four visible rows, eight combinations, source-backed force rows, max D/C > 0.8, governing member/combination state, and zero matrix overflow.
    - Customer-open package smoke now requires `load_combination_force_matrix`, raising `current_cockpit_delivery` marker coverage to 51/51.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T124917-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Load Combination Force Stepper:
    - The load-combination matrix now also exposes `structure-viewer-load-combination-force-stepper.v1`, selected combination, selected member, selected D/C, stepper row count, and active row state.
    - Step controls switch the selected KDS combination, re-render the matrix, synchronize Force Flow Lens to the same selected combination, and focus the selected combination's governing member when available.
    - Dense browser smoke clicks a stepper control and verifies matrix selected-combination state, Force Flow selected-combination state, exactly one active step, active rows, selected member/D/C, and zero matrix/stepper overflow.
    - Customer-open package smoke now requires the stepper schema and `setLoadCombinationForceSelection` marker inside `load_combination_force_matrix`; marker coverage remains 51/51 with stricter semantics.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T130046-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Stage Load-Combination Force Glyphs and Integrated Review Map:
    - The 3D viewport now exposes `structure-viewer-stage-load-combination-force-glyphs.v1`, projecting selected KDS load-combination force/check rows onto the stage with combination, member, component, D/C, demand/capacity, clause, projection state, and click-to-focus behavior synchronized with the Load Combination Force Matrix and Force Flow Lens.
    - The viewer shell now exposes `structure-viewer-integrated-review-navigator.v1`, with visible Review Map openers, a modal project selector, active-project drawing buttons, all major review-section jump buttons, active drawing and total manifest drawing counts, close/Escape behavior, and synchronized browser state.
    - Dense browser smoke verifies modal drawing count >=2, total drawing count >= drawing count, section count >=8, modal overflow 0, stage force glyph selected-member HUD overlap 0, and glyph overflow 0.
    - Customer-open package smoke now requires `integrated_review_navigator` and `stage_load_combination_force_glyphs`, raising `current_cockpit_delivery` marker coverage to 53/53.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T133115-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Upgraded Integrated Review Map into a section-aware review preview hub:
    - The Review Map now also exposes `structure-viewer-integrated-review-preview.v1` with active-section, target, status, row-count, and synchronized browser state fields.
    - Section hover/focus previews stage, analysis, Loads/KDS, Force Flow, Critical, Materials, Drawings, Report, and Project evidence rows before the user jumps away from the modal.
    - Loads/KDS preview rows include selected combination, selected member, selected D/C, source force rows, and projected stage glyph count, so the user can see which force case is active before moving to the rail.
    - The preview `Open Section` action targets the same destination as the selected section, preserving one-click navigation while making the modal useful as a review hub.
    - Dense browser smoke verifies the Loads/KDS preview schema, selected-combination evidence, preview target, preview row parity, modal overflow 0, and synchronized window state.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T134402-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Material Stress-Strain Curves evidence:
    - Materials & Members now exposes `structure-viewer-material-stress-strain-curves.v1` with compact SVG stress-strain curves for the top constitutive materials.
    - Each curve row carries material id, constitutive model, state, D/C demand ratio, fy/fc, yield strain, yield marker, demand marker, demand provenance, and click-to-focus material identity.
    - Browser state publishes `__STRUCTURE_VIEWER_MATERIAL_STRESS_STRAIN_CURVES_STATE__` so restored packages can prove row count, nonlinear count, source-backed count, max demand ratio, and material curve metadata.
    - Dense browser smoke verifies at least four curve rows, SVG/yield/demand marker parity, source-backed material provenance, visible `Stress-Strain Curves` and `fy/fc` labels, and zero curve overflow.
    - Customer-open package smoke now requires `material_stress_strain_curves`, raising `current_cockpit_delivery` marker coverage to 54/54.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T135458-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added selected Member Force Diagram evidence:
    - The load/KDS review rail now exposes `structure-viewer-member-force-diagram.v1` directly under the Load Combination Force Matrix.
    - The panel uses the selected load combination and selected member to render compact SVG force diagram rows for governing axial, shear, moment, interaction, drift, buckling, or code-check evidence when present.
    - Each row carries kind, component, member id, combination, D/C, demand/capacity label, clause, source provenance, click-to-focus behavior, and synchronized `__STRUCTURE_VIEWER_MEMBER_FORCE_DIAGRAM_STATE__`.
    - Integrated Review Map previews now include Member Diagram counts for Loads/KDS and Force Flow sections, so the unified HTML viewer can confirm the active force diagram before jumping sections.
    - Customer-open package smoke now requires `member_force_diagram`, raising `current_cockpit_delivery` marker coverage to 55/55.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T140745-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added selected Member Force Envelope evidence:
    - The load/KDS review rail now exposes `structure-viewer-member-force-envelope.v1` below the selected Member Force Diagram.
    - The panel compares the selected member across available source-backed load combinations, renders compact SVG envelope rows, and marks the selected combination point against the governing max D/C.
    - Each row carries component kind, selected/max D/C, governing combination, clause, demand/capacity label, sample count, source-backed sample count, row-click switch-to-governing behavior, and synchronized `__STRUCTURE_VIEWER_MEMBER_FORCE_ENVELOPE_STATE__`.
    - Integrated Review Map previews now include Member Envelope sample/max D/C evidence for Loads/KDS and Force Flow sections, so the unified HTML viewer can confirm cross-combination force risk before jumping sections.
    - Customer-open package smoke now requires `member_force_envelope`, raising `current_cockpit_delivery` marker coverage to 56/56.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T141656-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added selected Member Force Playback controls:
    - The load/KDS review rail now exposes `structure-viewer-member-force-playback.v1` below the selected Member Force Envelope.
    - The panel builds frame buttons from the same selected-member source-backed load combinations, with previous/play/next controls, active frame index, active combination, selected member, max D/C, source-backed row count, component, and clause evidence.
    - Clicking next, a frame, or play advances the active combination and keeps Load Combination Force Matrix, Force Flow Lens, Member Force Diagram, Member Force Envelope, stage force glyphs, and Integrated Review Map preview aligned to the current playback frame.
    - Browser state publishes `__STRUCTURE_VIEWER_MEMBER_FORCE_PLAYBACK_STATE__` for frame count, active frame, active combination, source-backed rows, playing state, max D/C, and frame metadata.
    - Customer-open package smoke now requires `member_force_playback`, raising `current_cockpit_delivery` marker coverage to 57/57.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T142547-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Stage Member Force Playback Trail:
    - The 3D viewport now exposes `structure-viewer-stage-member-force-playback-trail.v1`, projecting the selected member's playback frames into compact model-adjacent trail buttons.
    - Trail frames show active frame, D/C bead, projection/edge/docked state, selected combination/member data attributes, and click-to-scrub behavior tied to the same Member Force Playback timeline.
    - Dense 1600x900 browser smoke verifies the trail is ready, active-frame synchronized, visible, overflow-free, selected-HUD-overlap-free, and docked outside the central model protagonist budget.
    - Customer-open package smoke now requires `stage_member_force_playback_trail`, raising `current_cockpit_delivery` marker coverage to 58/58.
    - Regenerated the single-file delivery viewer, package, smoke, and readiness artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T144129-81d7549969225809`, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Stage Member Force Vector Field:
    - The 3D viewport now exposes `structure-viewer-stage-member-force-vector-field.v1`, projecting selected-member axial/shear/moment/check vector chips from source-backed load-combination rows.
    - Vector chips show component kind, demand/capacity, D/C, active frame, active combination, selected member, projection state, and click-to-focus behavior tied to the same Member Force Playback timeline.
    - Dense 1600x900 browser smoke verifies the vector field is ready, visible, source-backed, synchronized with playback selection, overflow-free, and selected-HUD-overlap-free.
    - Customer-open package smoke now requires `stage_member_force_vector_field`, raising `current_cockpit_delivery` marker coverage to 59/59.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T145757-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added selected Member Force History evidence:
    - The load/KDS review rail now exposes `structure-viewer-member-force-history.v1` between Member Force Envelope and Member Force Playback.
    - The panel charts selected-member D/C progression across source-backed load combinations with SVG history rows, selected points, governing points, current/max/average D/C, demand/capacity labels, clause, and sample/source counts.
    - Row clicks switch to the governing combination and focus the same member identity used by Load Combination Force Matrix, Member Force Diagram, Member Force Envelope, Member Force Playback, stage force glyphs, and Critical Members.
    - Integrated Review Map previews now include Force History frame/max D/C evidence for Loads/KDS and Force Flow sections.
    - Customer-open package smoke now requires `member_force_history`, raising `current_cockpit_delivery` marker coverage to 60/60.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T150949-81d7549969225809`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Drawing Material Model Matrix and Drawing Sheet Force Matrix:
    - Drawing Handoff now exposes `structure-viewer-drawing-material-model-matrix.v1` with all six source material model rows, locked count equal to row count, changed count zero, raw signature token counts, selected-combination sync, and force-backed D/C where available.
    - Drawing Handoff now exposes `structure-viewer-drawing-sheet-force-matrix.v1` with one row per visible sheet, exactly one active sheet-force row, selected load-combination/member sync, source-backed force counts, max D/C, and material-lock state.
    - Dense 1600x900 browser smoke verifies both matrices are ready, browser state is synchronized, material parity stays 100%/100%, active sheet focus updates the matrix state, and matrix overflow is zero.
    - Customer-open package smoke now requires `drawing_material_model_matrix` and `drawing_sheet_force_matrix`, raising `current_cockpit_delivery` marker coverage to 76/76.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T191957-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Drawing Source Detail Ledger:
    - Drawing Handoff now exposes `structure-viewer-drawing-source-detail-ledger.v1` between material parity and material model matrix, with original drawing detail, source-sheet count, active sheet, material lock, force overlay, and section/drawing-only optimization scope rows.
    - The ledger reports `materialLocked=true`, `drawingOnlyOptimized=true`, `materialMatchPercent=100`, `memberAssignmentMatchPercent=100`, nonzero section/drawing edit scope, and synchronized active-sheet browser state.
    - Dense 1600x900 browser smoke verifies the ledger is ready, row/detail counts match, source sheet links exist, active sheet matches Drawing Handoff, material models remain locked, and overflow is zero.
    - Customer-open package smoke now requires `drawing_source_detail_ledger`, raising `current_cockpit_delivery` marker coverage to 77/77.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T193308-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Drawing Sheet Detail Matrix:
    - Drawing Handoff now exposes `structure-viewer-drawing-sheet-detail-matrix.v1` after the source-detail ledger, with one row per visible sheet covering source SVG linkage, revision, callout, active state, material lock, D/C force overlay, and section/drawing-only optimization scope.
    - The matrix reports `materialLocked=true`, `drawingOnlyOptimized=true`, `materialMatchPercent=100`, `memberAssignmentMatchPercent=100`, source-linked sheet counts, max D/C, and synchronized active-sheet/browser state.
    - Dense 1600x900 browser smoke verifies sheetCount=4, rowCount=4, exactly one active row, source-linked rows present, active sheet matches Drawing Handoff, material models remain locked, material/member parity stays 100%, and overflow is zero.
    - Customer-open package smoke now requires `drawing_sheet_detail_matrix`, raising `current_cockpit_delivery` marker coverage to 78/78.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T194241-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Material Model Force Envelope:
    - Materials & Members now exposes `structure-viewer-material-model-force-envelope.v1` between Material Model Demand Atlas and Material-Force Interaction, with one row per locked source material model across all available load combinations.
    - Each envelope row carries SVG D/C trace points, selected/governing combination points, governing member/component/demand, mapped/source force-row counts, force-backed material state, axial/shear/moment bars, material lock status, and click-to-focus behavior.
    - Dense 1600x900 browser smoke verifies all material models render, at least two combinations are compared, SVG/point/bar evidence is present, material-library/member-assignment parity stays 100%/100%, active load-combination state syncs, and overflow is zero.
    - Customer-open package smoke now requires `material_model_force_envelope`, raising `current_cockpit_delivery` marker coverage to 79/79.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T195828-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked at 80/100.
  - Added Stage Material Force Envelope:
    - The 3D viewport now exposes `structure-viewer-stage-material-force-envelope.v1`, projecting the all-combination rail Material Model Force Envelope into compact stage cards.
    - Stage cards carry SVG D/C traces, selected/governing points, governing combination/member/component, mapped/source force rows, axial/shear/moment bars, material/member parity receipts, click-to-governing-combination behavior, and member/material focus behavior.
    - Dense 1600x900 browser smoke verifies stage/rail material, combination, force, source, and max D/C synchronization; material-library/member-assignment parity stays 100%/100%; stage card overflow is zero.
    - Customer-open package smoke requires `stage_material_force_envelope`, raising `current_cockpit_delivery` marker coverage to 80/80.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T201404-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.
  - Added Material Model Capacity Envelope:
    - Materials & Members now exposes `structure-viewer-material-model-capacity-envelope.v1` between Material Model Force Envelope and Material-Force Interaction, grouping locked source material-model force rows across all available load combinations.
    - Each capacity row compares demand against KDS source capacity first and section-estimated capacity fallback second, then renders D/C traces, demand/capacity/margin bars, governing combination/member/section/component, source/estimated capacity basis, selected-combination sync, click-to-focus behavior, and synchronized `__STRUCTURE_VIEWER_MATERIAL_MODEL_CAPACITY_ENVELOPE_STATE__`.
    - Dense 1600x900 browser smoke verifies source and estimated capacity coverage, SVG/point/bar evidence, browser state sync, capacity-backed rows, selected-combination/governing-material synchronization with the Material Model Force Envelope, material-library/member-assignment parity at 100%/100%, and zero overflow.
    - Customer-open package smoke now requires `material_model_capacity_envelope`, raising `current_cockpit_delivery` marker coverage to 81/81.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T202702-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.
  - Added Stage Material Capacity Envelope:
    - The 3D viewport now exposes `structure-viewer-stage-material-capacity-envelope.v1`, projecting the rail Material Model Capacity Envelope's capacity-backed locked material models into compact stage cards.
    - Stage cards carry all-combination SVG D/C traces, selected/governing points, governing combination/member/section/component, source-capacity and estimated-capacity counts, demand/capacity/margin bars, capacity basis, min margin percent, material/member parity receipts, click-to-governing-combination behavior, and member/material focus behavior.
    - Dense 1600x900 browser smoke verifies stage/rail material, combination, force, source-backed, source-capacity, estimated-capacity, capacity-backed, max D/C, and min-margin synchronization; material-library/member-assignment parity stays 100%/100%; stage capacity-card overflow is zero.
    - Customer-open package smoke now requires `stage_material_capacity_envelope`, raising `current_cockpit_delivery` marker coverage to 82/82.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T203947-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.
  - Added Drawing Capacity Handoff Ledger:
    - Drawing Handoff now exposes `structure-viewer-drawing-capacity-handoff-ledger.v1` after the force handoff ledger.
    - The ledger carries active sheet, selected load combination, selected member and section, every capacity-backed material model row, source-capacity count, section-estimate count, capacity-backed material count, force/source row counts, max D/C, minimum margin, 100% material-library match, and 100% member material-assignment match.
    - Dense 1600x900 browser smoke verifies active-sheet sync, selected-combination sync, Material Model Capacity Envelope count synchronization, maxDcr/minMargin synchronization, browser state sync, click-to-focus material/member behavior, and zero overflow.
    - Customer-open package smoke now requires `drawing_capacity_handoff_ledger`, raising `current_cockpit_delivery` marker coverage to 83/83.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T205533-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.
  - Added Drawing Material Constitutive Register:
    - Drawing Handoff now exposes `structure-viewer-drawing-material-constitutive-register.v1` between Drawing Material Models and Drawing Force Handoff.
    - The register is built from all source material rows, not only beam/shell summaries, and renders model family, constitutive law/defaults, hardening, fy/fc, yield strain, usage/section links, selected-combination demand ratio, D/C, and curve/capacity evidence.
    - Dense 1600x900 browser smoke verifies row/material/source-backed/nonlinear/curve/capacity-backed count synchronization, active-sheet and selected-combination sync, 100% material-library/member-assignment parity, material lock state, browser state model-family coverage, click-to-focus behavior, and zero overflow.
    - Customer-open package smoke now requires `drawing_material_constitutive_register`, raising `current_cockpit_delivery` marker coverage to 84/84.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T211112-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.
  - Added Drawing Material Curve Evidence:
    - Drawing Handoff now exposes `structure-viewer-drawing-material-curve-evidence.v1` between the material constitutive register and force handoff ledger.
    - Curve evidence rows are built from the locked source material laws and render SVG stress-strain curves, yield markers, selected-combination demand markers, D/C receipts, source-backed and capacity-backed state, active sheet, governing combination/member/section, and click-to-focus material/member behavior.
    - Dense 1600x900 browser smoke verifies curve count matches the constitutive register curve rows, SVG/yield/demand marker counts match the row count, active-sheet and selected-combination sync, 100% material-library/member-assignment parity, browser state model-family coverage, and zero overflow.
    - Customer-open package smoke now requires `drawing_material_curve_evidence`, raising `current_cockpit_delivery` marker coverage to 85/85.
    - Regenerated the single-file delivery viewer, package, smoke, readiness, and support artifacts; workstation delivery readiness remains PASS 8/8 with latest job `20260525T212722-bd376eaf7819ec28`, support bundle remains PASS 22/22, and independent readiness remains EB/RH-blocked.

## Next Recursive Candidates

1. Add detached signature verification flow after explicit key/signature material exists.
2. Add customer-facing delivery index cross-links to QA, diff, signing, and report metadata files.
3. Continue viewer fidelity toward the reference image with deeper model/result evidence richness and high-density edge-case refinements beyond the now-covered integrated Review Map preview hub, Material Model Lock, Material Model Signature Ledger, Material Model Demand Atlas, Material Model Force Envelope, Material Model Capacity Envelope, Stage Material Model Demand Badges, Stage Material Force Ribbons, Stage Material Force Envelope, Stage Material Capacity Envelope, Stage Member Material State Badge, Drawing Material Parity Ledger, Drawing Source Detail Ledger, Drawing Sheet Detail Matrix, Drawing Material Model Matrix, Drawing Force Handoff Ledger, Drawing Capacity Handoff Ledger, Drawing Material Constitutive Register, Drawing Material Curve Evidence, Drawing Sheet Force Matrix, Material-Force Interaction, Stage Story Force Flow Bands, Stage Force Demand Contour, selected-member nonlinear material state, top project selector, top run-control strip, stage review controls, dense Stage Model Stack evidence, compact KPI chip readouts, Material Coverage Readiness, first-viewport Critical Members compact table, Analysis Timeline Footer evidence, Drawing Handoff v2 receipt, Stage Result Callouts v3 projected anchor HUD, Lower Chart Axis Evidence v1, Force Flow Lens, Material Constitutive Lens, Material Stress-Strain Curves, Load Combination Force Matrix and Stepper, Member Force History, Stage Load-Combination Force Glyphs, Stage Member Force Playback Trail, Stage Member Force Vector Field, first-viewport Contour Result Scale priority, dense model-protagonist overlay occlusion budget, dense stage-primary dominance budget, commercial deformation scale control, projected Story Level Ruler, Stage Drift Limit Bands, projected Load/Support glyphs, first-viewport Critical Triage, first-viewport Optimization Delta Strip, non-ellipsis KPI full-label/value readouts, result evidence receipt, Result Step Schedule, Result Envelope, Panel Zone / Joint Evidence rail plus stage badge, projected Critical Member stage hotspots, contour scale evidence, load-case evidence rows, utilization heatmap evidence, viewport tool rail, 3D overlay receipt with visual-evidence legend, stage result receipt, KPI evidence cards, Optimization Summary evidence receipts, Critical Members review rows, Materials & Members catalog, Material Schedule links, Section Schedule links, Material Family Coverage chips, 47-family Material Ontology Breadth receipt, and 1600x900 cockpit layout.
4. Reduce remaining operator-prompt friction by keeping verification/build paths non-interactive and using saved approval prefixes for repeated browser/sandbox gates.

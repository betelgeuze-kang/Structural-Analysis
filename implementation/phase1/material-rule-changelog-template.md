# Material Rule Changelog Template

Use this template whenever `material_rule_table.json` is changed.

## Entry
- `rule_id`:
- `source`:
- `effective_date` (YYYY-MM-DD):
- `change_reason`:
- `changed_fields`:
- `backward_compatibility` (yes/no + note):

## Validation checklist
- [ ] `rule_id` exists in `material_rule_table.json`
- [ ] `effective_date` provided
- [ ] `source` references standard/spec document
- [ ] `change_reason` explains why engineering behavior changes

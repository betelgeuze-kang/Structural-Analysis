name: Rename Science Quarantine Control Plane

on:
  push:
    branches: ["ci/product-boundary-split"]
    paths:
      - ".github/workflows/rename-science-quarantine-control-plane.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  rename:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout boundary branch
        uses: actions/checkout@v6
        with:
          ref: ci/product-boundary-split
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"
          cache: pip

      - name: Install package and tests
        run: python -m pip install -e .[dev]

      - name: Replace governance path references
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          old = ".github/workflows/molecular-quarantine-ci.yml"
          new = ".github/workflows/science-quarantine-ci.yml"
          paths = [
              Path("scripts/check_product_ci_boundaries.py"),
              Path("scripts/check_github_actions_runner_policy.py"),
              Path(".github/workflows/ci.yml"),
              Path("tests/test_product_ci_workflow_contract.py"),
          ]
          for path in paths:
              text = path.read_text(encoding="utf-8")
              if old not in text:
                  raise SystemExit(f"missing quarantine workflow reference in {path}")
              path.write_text(text.replace(old, new), encoding="utf-8")
          PY
          rm .github/workflows/molecular-quarantine-ci.yml
          rm .github/workflows/rename-science-quarantine-control-plane.yml

      - name: Verify boundary and workflow contracts
        run: >-
          python -m ruff check
          scripts/check_product_ci_boundaries.py
          scripts/check_github_actions_runner_policy.py
          tests/test_check_product_ci_boundaries.py
          tests/test_product_ci_workflow_contract.py
      - run: >-
          python scripts/check_product_ci_boundaries.py --json --fail-blocked
      - run: >-
          python scripts/check_github_actions_runner_policy.py --json --fail-blocked
      - run: >-
          python -m pytest -q
          tests/test_check_product_ci_boundaries.py
          tests/test_product_ci_workflow_contract.py
          tests/test_check_github_actions_runner_policy.py

      - name: Commit the control-plane rename
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Keep quarantine governance outside molecular path matching"
          git push origin HEAD:ci/product-boundary-split

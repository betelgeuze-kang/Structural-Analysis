# Refined column dataset metadata observation

The [publisher data-availability statement](https://ascelibrary.com/doi/10.1061/JSENDH.STENG-14725)
links the [Figshare Refined Dataset](https://figshare.com/s/341e5941ace7b2f0c9ad).
The landing-page text is now visible through web retrieval. It displays CC BY
4.0, a private-sharing label, and a 161.72 MB download listing. Its description
names circular/rectangular test data, input/output parameter tables, validation
checklists, plotted responses and MATLAB plotting scripts. This is a metadata
observation; no files were downloaded or executed.

This updates the earlier access observation in
[the PEER cohort intake](peer-spd-cohort-intake-20260909.md). It does not rewrite
that historical 403/empty-202 result, or verify file-level rights and contents.
The publisher describes revisions to existing ACI 369 parameters against original
studies. It is therefore a potential correspondence/correction source, not a new
independent experimental campaign.

For project intake, retain original specimen/campaign identities across ACI,
PEER and this curated version. Do not split copies across training and evaluation
or automatically replace existing values. Stable file/version identities,
original-study correspondence, force/displacement semantics and physical model
compatibility still require verification. Training admission remains zero.

The adjacent JSON records exactly which metadata is observed and which checks
remain false. No solver, model parameter, training partition or public capability
was changed. Previously known source restrictions remain in the earlier intake;
this observation does not authorize repeated retries of other blocked sources.

# Typed optional precision interfaces with original contract identities

Hosted checks at report head `6f534805b16d60bebc70433c0bdd48f5ce40e96a`
exposed type errors after the bounded numerical observations. The
[core-quality job](https://github.com/betelgeuze-kang/Structural-Analysis/actions/runs/34347748935)
reported one error: direct `.fget` access to the base section contract property
was typed as access to a callable. The
[frame topology job](https://github.com/betelgeuze-kang/Structural-Analysis/actions/runs/34347748967)
reported three errors in Newton: an optional compensation passed to `array_equal`
and two uses of the optional compensated-assembly method absent from the general
vector problem protocol. Earlier topology test groups passed, but that job did
not reach its later groups. These are hosted typecheck failures, not evidence of
a successful full workflow or physical qualification.

The base section hash expression is unchanged and now resides in an explicit
`_base_contract_hash` method, used by the ordinary property and retained-section
wrapper. Retained sections continue to bind the original base identity without
adding intermediate stable/direct arithmetic profile wrappers. A focused test
checks both the retained hash and its initial native state binding.

Newton declares a separate optional compensated-vector protocol and uses it only
at the existing selected compensated assembly calls. Ordinary vector problems do
not acquire a mandatory new method. The equality check narrows compensation to a
non-None vector before `array_equal`. This does not change the selected numerical
formulas, convergence gates, iteration limits, comparison tolerances or schemas.

The development and three repeated observations remain bound to frozen numerical
source `93e589f1ba121f41afa0879f96f4c1c72f5190c7`. This later type correction is
separate from that source and does not relabel the observations. Validation runs
after all numerical measurement/audit processes finish, including original native
hash/byte compatibility and both hosted typecheck scopes.

The separate [full CI run](https://github.com/betelgeuze-kang/Structural-Analysis/actions/runs/34347699815)
stopped during exact-source evidence materialization with `legal_approval=False`.
Repository-test and legacy-evidence shards also failed before their test stages
in evidence materialization. These prerequisites and full-suite acceptance remain
open; this correction does not change legal approval, evidence gates or R1/R2.


After all numerical workers and audits were terminal, both exact hosted mypy
scopes pass locally: 40 core source files and 15 frame/API/test files. Related
retained-strain, terminal-refinement and original-polishing tests pass **82 in
17.85 s**. The bounded core coverage suite passes **123 in 20.44 s**, with **86%**
branch-inclusive coverage against the unchanged 85% requirement. The first local
quality command completed mypy but could not launch coverage because that module
was absent. Coverage 7.16.0, matching the observed hosted version, was installed in
a separate temporary dependency directory; no global environment was changed.
The coverage portion was then run without repeating the already-passed typecheck.

A read-only compatibility check compiles three original models and verifies all
30 saved problem hashes/native checkpoint reopens: eight preceding short profiles
across three paths, plus both complete refined geometries across three paths.
Every native payload serializes to the original bytes. This check takes 2.539060 s,
with no Newton solve or material integration. Ruff and diff checks pass. These
local checks resolve the observed type errors; a new hosted run and full-suite
acceptance remain separate requirements.

Validation sources, patch and logs are separately preserved at `/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-rc-type-contract._wqtv9a9`: 13 files / 155938 bytes; inventory SHA-256 `4ffe398018c0ac43f8a6dee5812a2659918b07f759c1238ad2b08728f9d84bbf`. Initial missing-tool output remains included.

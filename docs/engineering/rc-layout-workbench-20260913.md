# Workbench review of original layout-search results — 2026-09-13

Source `c5a8dc978be558692dd8f748f75b5beee4c38c57` connects the explicit layout
search schema to Workbench. It preserves the original result/plan/comparison
bytes and their hashes while reusing the existing full-path engineering checks.

## Validation and display

The layout decoder checks the frozen plan, bounded pool/budget, separate policy
schema and dimensions, historical training work/costs, original price-table hash,
quantities, prediction screens/rankings, arm membership, coverage and finite-pool
cost audit. Recorded predictions remain heuristics; the browser does not refit or
claim independent calibration of the policy.

Each candidate goes through the shared original-result validator: API/result and
checkpoint hashes, authored control configuration, preload where present, stored
fresh verification binding, accepted state/history continuity, material response,
response extrema, requested screens and selection eligibility. Artifact paths use
an explicit layout profile rather than weakening the existing section-study path
rule. Existing physical comparison tolerances remain unchanged.

The original comparison stays in `review.report`. A separate `displayReport`
contains bound plan/price context and derived quantity/estimate differences for
the React panel. Original comparison downloads return the saved bytes, never the
display object. Arm time is labelled separately from shared ranking and historical
training. Layout UI text retains the lack of verified equivalent building function
and confirmed monetary savings.

Users can inspect price order, learned order and the later full-pool comparison,
review selected original model/result/checkpoint/verification files, and download
the original search metadata and price-table supplement. A malformed original
result prevents the verified selection UI from appearing.

## Tests and original graph fixture

The trusted Node 24 formal runner completed TypeScript, production build/delivery
checks and selected browser/contract tests. There were **80 distinct passing
tests** across the final focused runs: 12 new layout tests and existing search,
design and cohort regressions. The two regression selections contained 43 and 41
tests with four overlapping test identities; they are not counted twice. The
initial new test invocation matched no tests until both specs were registered in
the formal runner; that invocation is not a test pass.

The fixture stores all 85 original export files as a gzip-compressed base64 map.
Decoding preserves original JSON number spellings and byte hashes. The fixture
notes identify the original exploratory producer and price supplement. It is not
measured external data or a new analysis run.

## Actual HTTP/browser observation

A separate run used the immutable WSGI mount and the original portable export,
without route interception, at 1440px and 390px Chromium viewports. In both views:

- The price arm retained no verified feasible selection within its budget.
- Learned order selected `large`; the later complete-pool comparison selected
  cheaper `middle`, retaining the 43.47324 synthetic estimated-cost gap.
- The panel stayed within viewport bounds, with wide tables using their scroll
  regions. Original model/result/checkpoint/verification and metadata downloads
  remained byte-exact.

The observation checked **178 actual HTTP responses and 20 downloads** against
original bytes, with no page errors. Navigation, verification, selection and
downloads across both viewports took 4.581091581 s. This excludes server startup,
browser launch and teardown and is a single observation, not evidence of AI
speedup. Server-side structural analysis, fresh solver replay and policy fitting
entry points were disabled; no new numerical paths or fits ran. Browser checks
validate stored engineering records and do not replace an independent solver.

The packet contains five files / 386,978 bytes at
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-layout-workbench-SHD6Eo`,
including desktop/mobile screenshots, the observer, HTTP receipts and summary.
Inventory SHA-256:
`c59eb67ba959678ad12308b466d6cf9fb39e3d5248b396eab0efbe0e2279a184`.
See the [machine-readable observation](rc-layout-workbench-20260913.summary.json).

## Roadmap status

This supplies the layout branch's verified Workbench review and original-download
path. Historical producer flags remain unchanged; the new review evidence is
recorded separately. It does not close repeated matched search-cost benefit,
new-project/geometry/history generalization, external physical validation,
functional equivalence, licensing/owner/hardware requirements, hosted full CI, or
the broader material/3D roadmap. No release or design approval is implied.

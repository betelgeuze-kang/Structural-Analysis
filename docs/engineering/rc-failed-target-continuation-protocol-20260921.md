# Recover a failed requested target without adopting internal material states

`continuation_all_failed_targets=True` is an additional explicit option requiring `frozen_parent_continuation=True` and `continuation_on_failure=True`. It permits the same sixteen-stage frozen-parent search after a known rollback-safe native failure at any requested target, including the first one. It does not repair a failed constant-load preload, change native tolerances, or insert accepted material-history targets. Original reversal-only modes and defaults remain unchanged.

The profile has a distinct identity and a declared maximum of sixteen additional native calls per requested target. The artifact replayer recognizes the profile. Unknown trial work still stops; a known failed trial records its stage and returns no candidate. Each search holds its entry parent fixed, while only successfully accepted original requested targets advance the outer material history.

Ninety-eight focused tests pass across strategy/replay, warm starts, parent-step budgets and residual observations. Actual short L-frame tests show 20 mm completing after first-target recovery and 40 mm remaining incomplete at trial eight. The latter is retained as a known failure regression, not replaced by a larger budget or looser tolerance. The recovered 20 mm original files also reproduce through the fresh artifact replayer.

Freeze committed source before the targeted numerical study. Reuse exactly the short L-frame 20/40 mm cases, original 25 kN constant FY preload, model and native configuration from the preceding six-case study. Compare reversal-only failure continuation against all-failed-target continuation in two balanced mode/arm orders: eight comparisons / 32 paths, each report including a fresh ordinary reference. Retain all trials, complete/partial prefixes, repeated proposal checkpoints and all work. Failed-reference and incomplete-pair speed ratios remain null. Earlier broader-roster results and near-zero comparison failures are not replaced by this focused diagnostic.

# Workbench review of the new staged geometry observations

Review source `fba10d0035ad89ce3bbc8fc1acac48c84a0406a5` used a fresh pinned
Node 24.20.0 production build. TypeScript, Vite and viewer delivery checks passed.
The numerical originals remain at `0efd85afdfe33314c70fcfdc26802d81159e0bc0`,
from the [twelve-execution geometry campaign](rc-staged-geometry-20260913.md).
No solver or fit ran during browser review.

Both new geometry graphs were served through the actual authenticated Python WSGI
transport and production build, with no browser route interception. Each was
reviewed at 1440 and 390 pixels. All four views selected `middle`, retained the
verified prefix rejection of `small`, required a full path after a passing prefix,
and left cost-skipped candidates physically unknown. The missing exhaustive oracle
remained unavailable; cost pruning did not become a claim of global optimality.

The full download observation checked **256 HTTP responses and 112 downloads**
against the original byte lengths and SHA-256 values. Each view downloaded 28
original records covering selected full results, metadata, skipped pool models,
prefix records and cost decisions. No page exceptions occurred. Desktop and mobile
screenshots were inspected; the outer review panel fits the viewport. Wide inner
quantity/performance tables still require horizontal scrolling on mobile; not all
columns are visible at once. This observation does not claim a redesigned mobile
layout or full application accessibility acceptance.

A separate display observation checked the selected concrete volume, longitudinal
rebar mass and declared estimate against the original comparison row in every
view. It also checked four displayed performance fields: translation, fiber strain,
steel accumulated plastic strain and concrete tensile damage. All matched. Its
208 HTTP responses matched original bytes, with zero downloads or page exceptions.
Representative mobile/desktop quantity and performance screenshots were inspected.

The four download/review flows summed to 49.115690568 s, including explicit 250 ms
click spacing. The four final numeric-display flows summed to 5.073495861 s with
no download pacing. Both exclude server startup, browser launch, screenshots and
teardown. They are different review tasks, not comparable speed strategies, and
must not be added to the numerical campaign ratios as a demonstrated end-to-end
saving. An earlier successful display pass retained the download observer's generic
scope label; that packet remains unchanged and a fresh run corrected the label.

The [machine summary](rc-staged-geometry-workbench-20260913.summary.json) retains
separate immutable packet identities, per-view originals, checks and timing scopes.
The original numerical packet is unchanged. This extends current Workbench
transport/display verification to the two new geometry graphs. Independent physics,
learned net benefit, whole-user-flow cost comparison, hosted qualification and the
complete roadmap remain open.

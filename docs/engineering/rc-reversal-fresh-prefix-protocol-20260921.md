# Fresh-prefix reconstruction of the parent-start diagnostic

Use frozen `ca07bb1ea` and the same three models, original 600 kN preload, targets (-20, -40, +20) mm and original native acceptance tolerances. Run each model twice. First execute the public direct-control API for the original prefix (-20, -40) mm from the input model and zero initial state. Retain all prefix artifacts and work. Authenticate its final accepted checkpoint against the previously retained parent, but do not use retained parent bytes as the solve input.

For each freshly generated parent, run exactly one SciPy trust-region proposal from its original accepted-parent coordinates with the settings in the preceding trust-region protocol. Count every optimizer assembly and preserve failure. Pass the generated candidate to the original native Newton solver, recording its assembly sidecar, full step artifact, clocks and acceptance gates. Preserve all six attempts, including unsuccessful models. Compare repeat accepted checkpoint bytes only after both executions finish.

The planned work is six full prefix analyses, six optimizations and six terminal native solves. Prefix API analysis includes the constant-load preload and both accepted targets. This is a custom fresh-prefix-plus-terminal reconstruction, not the public complete-path validation API or an independently qualified reference comparison. No speed ratio, design acceptance or production promotion follows from completion alone. Prefix and native solver work, optimizer callbacks and reported clocks have different scopes and must remain separate.

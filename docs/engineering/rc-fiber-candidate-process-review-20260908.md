# Saved candidate-process review in Workbench

On 2026-09-08, clean local source
`8b63185f3cee65996f7310ff96ebc9df0ca3098b` exported a preserved candidate-process
experiment and consumed that export over actual HTTP in Chromium. All eight
online comparisons, the complete experiment accounting and 18 original-byte
downloads passed. Source HEAD and clean status were unchanged before and after
both the Python exporter check and browser observation.

This closes the local whole-process review connection. It does not create new
solver measurements, independent verification, acceleration, construction savings
or release approval. The full implementation roadmap remains open.

## Preserved inputs and portable output

The numerical producer was
`b4681eeabbaccb7cce3eedc491dc2fcd0f194444`. Its observation and limits are recorded
in `rc-fiber-candidate-process-runtime-20260908.md`. The checked-in
`tests/fixtures/fiber_frame_candidate_review/observed-suite.json.gz` retains 71
files / 16,751,824 original file bytes. Every extracted file was checked again
against the retained original observation before this review.

The Python exporter read the moved source at
`/tmp/structural-candidate-review-observation.mp8cqvod/preserved-source/suite/suite.json`.
Its output was then moved from `review-bundle-exported` to `review-bundle` and
revalidated. The original-path reader, identity helper, input freezer, public
analysis API, trainer, fitting function and worker/subprocess launch entry points
were forbidden during this check. No structural analysis, policy fitting or
candidate worker ran. Pre-analysis compilation and frozen-policy validation are
review work; their costs were not added to the saved experiment.

The review contains 72 JSON files / 16,772,413 bytes: the top manifest and suite,
54 input/request/worker artifacts, and eight ordinary M2 manifest/report pairs.
Original absolute path strings inside JSON remain unchanged; only the manifest's
safe relative mapping resolves files. The source suite and all 16 M2 files match
the original bytes exactly. The exporter reads the fixed input/slot layout and
does not traverse unrelated directories.

| Identity | Value |
| --- | --- |
| Review manifest length | 20,589 bytes |
| Review manifest SHA-256 | `a5cca06483b7b784e42281c48fbbd244dce7233beb02880859bb7c69b5a082fb` |
| Suite length | 7,754,233 bytes |
| Suite raw SHA-256 | `6539106620d6868abf8944b198c508b8eb1693302948013baefb84aa7d204dea` |
| Suite report hash | `sha256:83ff874ee1a83913850ff040d841e315dd14e25aa6f571157e001fe89f8c3491` |
| Suite identity hash | `sha256:9ca14ad3affae74444cd28cb520b0f4718e135094b707b354bb1799dbb9f5ad9` |

## Actual HTTP and browser checks

The browser loaded the genuine Python export, with no network-route fixture
replacement. It requested all 72 review files. Trusted Node was `v24.20.0`, binary
SHA-256 `89af8424dd53e560b1933f87ba650d8bf57c83ca5a04600eefb31f416aabbae7`;
Chromium was `141.0.7390.37`. The successful browser receipt is dated
`2026-09-08T09:48:17.385Z`.

The display retained one historical training artifact, four historical requests,
16 online requests and 12 later oracle requests: 28 current and 32 total. Each
online attempt displayed its own budget of two, including one baseline request.
These are the earlier measurement's values, not new runtime observations.

For both pools and both repetitions, selecting deterministic/learned strategies
displayed the corresponding verified report, physical winner, quantities,
synthetic KRW estimate, terminal response and committed-history values/status.
Learned selected `near-limit`; deterministic retained baseline. Both pools'
repeated time differences had both signs and remained visible in the paired
outcomes table. The oracle displayed three requests, no online budget credit and
no attached online comparison. A conditional reuse projection remains distinct
from an observed break-even execution.

All 18 downloads matched original bytes: top manifest and suite, plus the two
files for each of eight comparisons. Changing the viewed attempt did not create
new analysis work. The broader Workbench JSON export's parsed suite/selection and
independent standalone comparison sources were checked in focused browser tests.

At a 390-pixel mobile viewport, the panel's right edge was 376 pixels and its
client/scroll widths both 360 pixels. Desktop cost and mobile selection screenshots
were visually inspected; selectors and labels were readable and remained within
the panel. Wide comparison tables retain their own scrollable regions. There
were no page errors. The observation driver closed its browser and owned HTTP
server on exit. Nine served asset files were preserved and their bytes matched
before and after the observation.

An initial temporary observation driver used an incorrect exact-label selector;
the loaded panel was valid, but selection timed out. The corrected driver uses
the accessible combobox name and explicit repetition value. No application source
changed. The failure log and correction note are retained. A separate minimal
Chromium probe also isolated the 11th rapid-download throttle: delayed Blob URL
revocation did not change it, while spaced clicks passed 20 exact-byte downloads.
Tests and the observation use 250 ms between download clicks. The product's
download function was unchanged.

## Regression scope

- New Python review contracts: 41 passed in 36.91 s, with solver/fit/worker guards.
- Existing process/request contracts: 110 passed in 4.08 s using retained inputs;
  no repeated structural analyses or training.
- CI ownership/quality-gate contracts: 34 passed in 0.36 s.
- Complete standalone Workbench suite: 278 passed in 57.2 s, including 40 new
  consumer contracts and eight focused browser tests. TypeScript, Vite build and
  viewer-delivery checks passed. The separate 40-test/8-test runs overlap this
  total and are not added to it.
- Ruff lint and formatting checks passed for all seven changed Python files;
  whitespace checks passed.

Negative and partial contracts cover changed raw bytes, rehashed accounting and
request metadata, wrong/missing/duplicate slots, oracle predecessors, worker PIDs,
unsafe paths, missing resources, malformed failed worker bytes, unavailable
cryptography and late responses from a previous route/source. A synthesized
incomplete review passes the real provider/browser path with unlaunched warmups,
failed attempted work and unknown totals; it is not relabeled as an observed
worker failure. Same-mount source changes have a render-time URL guard and effect
cancellation; the browser test exercises the actual router's unmount/remount
boundary.

## Retention and remaining boundaries

The sealed local observation directory is
`/tmp/structural-candidate-review-observation.mp8cqvod/`. It holds the moved source,
complete exported review, raw downloads, drivers, receipts, screenshots, served
assets and verification logs. Its `artifact-manifest.json` inventories 193 files /
48,191,037 bytes, excluding itself. The manifest is 38,839 bytes with SHA-256
`004f5bb18de9994b8297ff1affee020d987852019de25227c8b719769155265e`.
Every inventory byte length and hash was reread and checked after sealing.

Hashes establish local consistency, not signatures or independent provenance.
Python recomputes canonical identities and frozen input plans; the browser checks
transported bytes, stored identity bindings and accounting arithmetic without
recreating Python canonical hashes through JavaScript number formatting. Neither
review step reruns physical J1-J5 verification. Original timings remain a local
synthetic observation with no consistent acceleration; synthetic prices remain
test inputs. Independent families/corpora, operators/hardware, licensing,
hosted exact-head checks and release approval remain outside this local closure.

# Explicit retained arithmetic for RC learning generation and evaluation

The repeated deterministic secant observation passes all six full physical
comparisons for the two declared development geometries. This implementation
connects its arithmetic recipe to the existing train-only RC learning pipeline as
explicit `arithmetic_profile="retained-twofold-refinement.v1"`. It selects exact
rational strain, native twofold increments, retained coordinate-to-fiber material
trials, rational force/tangent accumulation and two retained terminal corrections.
Every case must already enable original terminal polishing before any labels or
output are produced. The original solver gates and comparison tolerances remain.

All static physical features still come from the original supported compiled
model with the public feature extractor's exact member/material type guards.
An experimental learning context then binds the selected problem contract and
explicit arithmetic recipe while preserving physical feature values and layout.
The general public feature extractor does not gain admission for experimental
section types. Training and evaluation compile the same declared recipe; proposal
contexts must bind the actual selected problem hash.

The opt-in policy uses an exact v2 schema carrying the full arithmetic manifest,
while ordinary policies retain their v1 schema and default wire fields. A policy
cannot enter a differently declared arithmetic context even if its other metadata
matches. Modified, missing or noncanonical arithmetic fields, including a floating
refinement limit, are rejected. The model-context hash, sample hashes and policy
hash bind the recipe. These are consistency checks, not source authentication.

Only the original accepted high coordinate component trains the existing
binary64 initial-guess predictor. Each sample also preserves the original absolute
low component and step-byte hash as provenance. No low component is presented as
learned or discarded from the authoritative solver state. Proposals remain finite
initial iterates; final Newton assembly, native commit and original recovery retain
both components under the selected solver profile.

Existing project/family/physical/geometry/history split screens run before label
collection under either profile. All preprocessing and weights use only train
rows; no evaluation response enters fitting. Generation, fitting, inference,
recovery, rejected attempts and full evaluation costs remain separately charged.
The policy is frozen before evaluation and decoded in a fresh interpreter in tests.

Initial focused checks pass 20 tests in 35.09 s. Broader learning/split, retained
terminal, rational assembly and quality-contract checks pass **83 in 56.97 s**,
including all eight split-alias rejection cases under the new profile. Tests
exercise actual four-case cyclic label generation/evaluation, provenance for every
label, train-only statistics, policy mismatch rejection, schema corruption, OOD
abstention and the unchanged public feature boundary. Ruff/diff checks pass.

Before a new complete observation, fresh baseline and candidate processes must
reproduce ordinary learning samples, policy bytes, original step files and native
histories exactly. A separate pilot then reuses the four original authored
training/validation/holdout models and all 242 targets with unchanged ridge `1e-6`
and OOD margin `0.1`. Source, inputs, profile and complete costs are frozen. Full
physical comparisons and original-record verification must pass before admitting
three fresh repetitions. A pilot timing alone cannot establish repeated learned
acceleration. Earlier failed/slower learned observations remain unchanged, and
independent project/physics evidence, public admission, hosted/full-suite acceptance
and the full roadmap remain open.


## Frozen compatibility and full pilot startup

Numerical source `bdfa9a0262cf6fb74d27a7914744487178c5f150` is frozen in
433 Git-verified source/script/test files, alongside the preceding source
`08d4a320827bf41ddf222d52f988a8df8a4f682d`. Separate fresh baseline/candidate
workers reproduce **84 original step-file pairs, 14 full history/checkpoint pairs,
and all short training-sample and policy bytes exactly** in ordinary arithmetic.
Their separate total cost is 168 core calls / 428 Newton iterations and linear
solves, with parent times 11.621478 and 11.676395 s. This is compatibility evidence,
not a timing comparison between arithmetic profiles.

All four unchanged full-case inputs pass the current conservative split and
precision preflight. The preflight worker parent time is
1.856285 s; it compiles four models and makes no Newton or
material integration call. The complete 242-target four-case pilot has started at
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-rc-learning-precision.ekj7dcak` using the original authored partition labels, ridge `1e-6` and OOD margin
`0.1`. Its 14 planned paths include complete train reference/secant/fresh generation
and validation/holdout reference/secant/proposal/fresh evaluation. All work,
training samples, frozen policy and original outputs are retained as they finish.
This startup record is superseded by the terminal pilot and audit receipts below.
Earlier observations and their sealed inventories remain unchanged.


## Terminal full pilot and original-record audit

All **14 complete paths / 3,388 targets** return and commit through the original
solver gates. Every fixed full-history comparison passes with zero mismatches:
four secant comparisons and two learned-policy comparisons, plus each original
reference/fresh comparison. This includes both training geometries, validation and
holdout at all 242 declared targets. Every reference/fresh history and terminal
checkpoint is exact. The pilot worker exits zero with **1148.574603 s**
parent time. Generation costs **1,452 core / 6,879 Newton iterations and linear
solves**; evaluation costs **1,936 core / 8,436 Newton iterations and linear solves**.
Total is **3,388 core / 15,315 Newton iterations and linear solves**, with no unknown
work. The train-only fit uses 482 original pairs in 0.008856675 s.

Validation makes **241 learned proposals and one abstention**. Its path times are
reference **93.031169 s**, secant
**69.409511 s**, learned policy
**76.108521 s**, fresh reference
**93.429197 s**. The learned arm
passes physical comparisons but is slower than deterministic secant in this pilot.
These are single-run observations, not repeated acceleration evidence.

The OOD holdout **abstains at all 242 targets** and follows the reference path;
its passing policy comparison establishes fallback behavior, not learned
generalization. Its reference/secant/policy/fresh path times are
73.226652 /
59.420689 /
72.960739 /
73.230283 s.

The separate original-record audit exits zero in
**140.702966 s** parent time. It verifies
433 frozen Git source blobs, all eight original input files, all 3,388 original
step hashes, native parent chains and work/gate records, 14 exact native checkpoint
reopens, all 482 train-label high/low provenance records and all 484 frozen policy
decisions. It reconstructs train-only normalization and checks stored ridge normal
equations without refitting; relative residual is
3.18151077e-13. All fixed comparisons and
terminal identity flags are recomputed from original histories.

Independent arithmetic checks rebuild 3,388 assemblies and verify
**284,592 exact fiber strain inputs and
284,592 100-digit stress evaluations**.
Original base-material state/branch replays are exact. Every retained terminal
candidate and accepted pair is checked with rejected work retained. The audit
compiles four models and performs counted material re-evaluations; it makes no
Newton solve, fit, native commit or full reference-path replay. This is arithmetic
and record consistency evidence, not independent physical validation.

All path/core/proposal/recovery timing scopes are recorded separately in the
[machine summary](rc-learning-precision-20260909.summary.json). They are nested
scopes and must not be added together. Default compatibility and preflight costs
remain separate from pilot and audit costs.

The terminal pilot bundle is sealed and reread exactly: **22,358 files /
1,202,663,017 bytes**, inventory SHA-256
`5eba9e6f6e6921ef74f67915493bc088c53ad5b37e4c4bfa06764fc1964a608c`. Root: `/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-rc-learning-precision.ekj7dcak`.
All recorded numerical/audit processes had exited zero before inventory.

## Admitted fresh repetitions

The passing full comparisons and original-record audit admit the three arm-order
schedules declared before the pilot. They are now running sequentially in
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-rc-learning-precision.ekj7dcak-repeated`. Each repetition starts a fresh process and regenerates both training
paths, fits only its own 482 train pairs and freezes its policy before evaluating
both held-out cases. It charges all generation, fitting, inference, rejected work
and recovery again. Each complete study receives its own original-record/material
audit before the next starts. The same source, all original inputs, split labels,
ridge, OOD margin and fixed gates are retained.

The repeated bundle is active and unsealed; repetition results, cross-repeat
identity checks, paired timing dispersion and full cost accounting remain pending.
No repeated learned acceleration or OOD learned generalization is claimed.
Current published source `5e5d65c4ba0a9c5e45309d572271f10e8f8b04cb` passed hosted Core
Quality and Compatibility CI; a separate CI run failed before test execution at
exact-current-source evidence materialization. New report-head CI is separate.
Broader M1/M3, M2/M4/M5, independent corpus/physics, public admission, full-suite
acceptance, external licensing/hardware/owner and R1/R2 remain open. Full roadmap
stays active.


## Repetition aggregation and incomplete-run refusal

`scripts/verify_rc_learning_repetitions.py` consumes the explicit three-repeat
layout after every recorded numerical/audit process and the driver are terminal.
It first refuses an incomplete driver before reading the large sealed inventory.
On completion it will reread the sealed pilot, bind all repeated source/input
bytes, compare 10,164 original step pairs and 42 history/checkpoint pairs, and
require identical fresh policy/training-sample bytes. It also binds original
proposal/prefix/outcome/work fields, report/path/audit identities and recorded
costs. It does not execute saved code, rerun Newton or material integration, compile
models or refit. Existing per-run arithmetic/material audits remain required.

The summary retains all three signed secant-minus-policy timing differences,
paired reference/policy ratios, each path's samples and sample standard deviation,
complete generation/evaluation work and separate numerical/audit/driver parent
times. Its verifier-source hash identifies the summary implementation separately
from the frozen numerical source. Hash consistency is not external authentication.

Five focused pure-record tests pass in 0.26 s (initial pass 0.28 s): live-process
refusal, incomplete-driver refusal, duplicate worker receipt rejection, changed
cost/hash rejection and preservation of a losing timing sample. The actual active
bundle also refuses aggregation with no output created, in 0.064134489 s parent
time. Ruff/format/diff checks pass. The complete three-repeat positive verification
has not run yet and remains pending. Lightweight coordinator checks are recorded
in the active bundle; this is not exclusive-host timing.

The pilot's validation counts are 1,270 reference, 912 secant and 1,026 learned
Newton iterations/linear solves. Thus its learned arm adds 114 solves over secant;
its small fit time alone cannot establish low total AI cost. Full generation and
per-repeat retraining remain charged in the forthcoming repeated report.


## Completed repeated observation

The [three fresh repetitions and original-byte aggregate](rc-learning-precision-repeated-20260909.md)
now supersede the earlier running status. All fixed comparisons and original
audits pass, with complete costs and signed paired dispersion retained. The
learned policy remains slower than deterministic secant and OOD fully abstains.

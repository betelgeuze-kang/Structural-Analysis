# All 165 accepted-parent material summaries reconstructed

The [offline protocol](rc-accepted-material-summary-protocol-20260920.md)
completed at source `e2292c663` (full revision in the machine summary).
Every retained snapshot exactly reproduces the native accepted parent loaded
against its original compiled problem. All 165 original training contexts,
sample/step identities and parent hashes agree. No labels were regenerated.

Each row has 27 unweighted statistics over 72 concrete and 12 steel native
states. All five groups contain 33 rows. Group A varies in 18 statistics;
groups B–E each vary in ten. Nonconstant inputs establish available information,
not predictive usefulness, independent data coverage or net runtime benefit.
No training-only normalization, fit, reserved evaluation or runtime integration
was performed. Existing gates and secant behavior remain unchanged.

All 1,419 packet files / 126,088,366 bytes were verified against the final
inventory. Both new audit source files match their committed Git blobs.
The [machine summary](rc-accepted-material-summary-results-20260920.summary.json)
records the packet, exact inventory/content hashes and costs. The offline audit
took 5.698554 s inside a 7.500045 s process; these nested intervals are not
additive. The concurrent 2,048-layer solve prevents interpreting this observation
as isolated extraction performance. This is not the cost of an online guard.

The first invocation failed before reading parents: the shared preflight
requires evaluation declarations, but the driver passed only training cases.
Its original packet and 2.541940 s failed process are preserved separately.
The corrected driver validates all declared case definitions and summarizes
only the original training rows. Definition validation does not execute the
reserved cases. No numerical calculation or fit was repeated for this repair.

Local verification after the repair: 83 tests pass in the existing CI-selected
runtime diagnostic test module, including the new summary boundaries and a
regression for retaining split declarations. Ruff and whitespace checks pass.
These new tests are not covered by the earlier published `3eb50016d` CI run.

This establishes a reusable, parent-bound representation input. A subsequent
separate experiment must still bind summaries to the original nested labels,
exclude entire outer groups, preserve training-only normalization, and charge
all extraction and decision costs before any runtime benefit can be claimed.

# Exact-control, load-factor-eliminated reversal diagnostic

Freeze the three original failed reversal parents and `ca07bb1ea` source in large-drift packet dc9cee74b0cefb318f6b6f846b925f091af116b994a8542ed4a2522cc4e748ca. Repeat the previous six starts (accepted-parent and recorded failed-terminal coordinates per model), but optimize only unconstrained displacement coordinates. Failed-terminal starts remain post-hoc diagnostics.

Set the controlled translation to the original +20 mm target exactly. These cases must have unit physical scale on that translation and a nonzero proportional reference force there. For an observation at zero load factor, write residual F and augmented Jacobian J, with c the control coordinate and l the scaled load-factor coordinate. Eliminate l using q_l=-F_c/J_cl. Optimize R_i=F_i-(J_il/J_cl)F_c for remaining physical equations i, with analytic Jacobian J_ij-(J_il/J_cl)J_cj for unknown coordinates j. This uses the solver's affine external loading; it does not change constitutive integration or tolerances.

Use the same installed SciPy TRF/linear loss/x_scale=jac/max_nfev=100 and 1e-12 termination settings. Count every full observation, including reconstruction and a final direct augmented-residual check. Record the direct-versus-projected residual difference; optimizer termination alone cannot pass any structural gate. Feed every reconstructed vector to the original native Newton solver twice from the same parent and retain all costs, failed attempts and checkpoint comparisons. No target subdivision, production strategy change or whole-path qualification follows from this retained-parent diagnostic.

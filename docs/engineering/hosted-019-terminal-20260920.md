# Hosted terminal receipt at 01948913f

Exact source: `01948913f46f4505cc6e915433d383fd310c4c3b`.

- Python run `35502761393`: collection `106057353079` succeeds;
  development `106057353011` passes **1,870 tests in 1,311.00 s**.
- Full shards `106057353029`, `106057353043`, `106057353058` and
  `106057353270` fail at exact current-source evidence preparation. All four
  actual repository test steps are skipped. Aggregate `106058379032` fails.
  Shard 0's raw log reports `external_code_to_code_product_replay_not_passed`
  and `external_code_to_code_technical_receipt_not_ready`.
- Frontend run `35502745250`, job `106057307270`: **814 tests pass in 17.0 min**;
  aggregate `106059449966` succeeds.
- Workflow contract `35502745323` and P0 contract `35502745283` succeed;
  ordinary CI `35502745260` fails.

The logs are retained in `/tmp/structural-019-development.log`,
`/tmp/structural-019-frontend.log` and `/tmp/structural-019-shard0.log`.
These exact-source runs are terminal before the next push. They do not qualify
the later work/policy diagnostics or triple-excluded label preparation. The
later code's 114 local focused tests and exact original-audit replay remain
separate evidence until new-source hosted checks execute. No full-suite,
physical validation, independent acceptance, merge or release is claimed.

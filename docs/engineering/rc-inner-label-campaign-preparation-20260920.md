# Triple-excluded timing campaign prepared, not started

Source `d89cffa9585ba945d4b7d7a630f0712876d7a919` is frozen in a separate packet:
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-inner-label-campaign-b003_gd4`.
All 968 source files match their manifest sizes and hashes. The
[machine summary](rc-inner-label-campaign-preparation-20260920.summary.json)
pins the source manifest, launcher, protocol and observed admission refusal.

The launcher verifies source identities, the prior 2,048-layer numerical and
audit exit codes, completed prior-controller state, available RAM and disk
before starting. It refuses while either of the two observed prior PIDs is
live. A direct admission probe returned the expected refusal for live PID
347865; no execution-started receipt or numerical study directory was created.
This is an intentional not-started state, not a failed numerical experiment.

After admission, it performs a fresh-process preflight, the declared 2,970
counterbalanced comparisons, and the full original-artifact audit. Each stage
retains its own cost and exit code. Success and failures both retain terminal
state and a chunk-hashed file inventory. A prior execution-started receipt
prevents silently restarting the same packet. The source remains unchanged
when development commits advance.

The protocol has 990 unique parent/policy pairs, 11,880 planned single-target
paths and a maximum 17,820 core calls. It reuses the ten audited 66-row seeds
and preserves their historical costs. It performs zero new fits and touches
no reserved evaluation cases. The launch admission requires at least 8 GiB
available RAM and 20 GiB free disk; these are resource checks, not performance
or qualification evidence.

No new label comparison has run yet. The prior successful
[990-pair preflight](rc-inner-label-driver-preflight-20260920.md) and exact
reproduction of the old audit remain preparation evidence only. Complete
new labels, inner validation, useful policy selection and full-path runtime
benefit remain outstanding.

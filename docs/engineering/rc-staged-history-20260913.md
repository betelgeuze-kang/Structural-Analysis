# Repeated staged screening across two additional cyclic histories

Source: `5f754e338aa8d482e2765a8f6e13ab6187d911e9`. The protocol was frozen before new results were inspected. It uses the existing five-candidate L-frame pool, unchanged section/material/load/support data, a 0.003 maximum absolute fiber strain screen, two-target prefix screening and a five-candidate budget. There are no learned policies, fits or training labels. These are related generated cases, not independent experiments.

The two six-target displacement histories (metres) are mirrored `[0.01, 0.02, 0.01, -0.01, 0, 0.005]` and delayed peak `[-0.005, 0.005, 0.015, 0.02, 0.005, -0.01]`. Each has four pruned/staged pairs in P/S, S/P, P/S, S/P order. All 16 fresh processes were attempted, with no automatic retry or timeout. Existing solver tolerances and the failure/unknown-work stop rules were unchanged.

| History | Completed processes | Comparable pairs | Staged/pruned process-time sum |
| --- | --- | --- | --- |
| Mirrored | 8 / 8 | 4 / 4 | 0.955671256 |
| Delayed peak | 0 / 8 | 0 / 4 | unavailable |

Every mirrored pair selected `middle` with declared estimate 246.34835999999999. Its four ratios were 0.954397459, 0.953826307, 0.953872845 and 0.960589020. Pruned/staged arms used 6/8 API invocations, 36/32 attempted steps and 218/208 recorded Newton/linear counts. The approximately 4.43 percent process-sum reduction is a bounded deterministic scheduling observation; it is not learned benefit, a verified quote or full-user-flow acceleration.

The complete mirrored graphs admitted 360 artifacts. Twenty full rows passed fresh verification and showed nonzero steel plastic strain or concrete tensile damage; eight prefix rows were verified without full-history acceptance. Seventeen repeated full physical records and eight prefix/full-history comparisons matched exactly. This is same-solver consistency, not independent physics. Candidate-level input coordinates were also audited: the protocol's inherited 2.5 m horizontal and 2 m vertical labels describe the baseline only. Candidate member lengths differ and remain unchanged; `failure-audit.json` records that clarification and every coordinate. The frozen protocol was not rewritten.

All eight delayed-peak processes exited 1. The small candidate failed line search at the first reversal, target +0.005 m after accepting -0.005 m. Its parent remained immutable and rollback was exact. The pruned arm stopped on unknown full numerical work, while staged stopped on unknown prefix work. The returned failed result had one unknown solver-work attempt, so the original campaign retains null time ratios and no final selected candidate for this history. The successful mirrored subset does not qualify a whole-campaign speed ratio.

The failure isolated a software accounting gap: non-polished blocked vector Newton results omit work counters even though the solver has counted linear attempts and retained iteration history. Correcting that omission must preserve failure and rollback and be evaluated with new source-bound execution; it cannot repair or relabel this original experiment.

The 198.122157764 s parent campaign interval covers child launch through exit and per-slot persistence. The separate 3.076300371 s audit executes no new solve. Preparation and HTTP/UI review are outside those intervals; the host was not isolated. All original outputs, including failed attempts, are sealed in the packet named by the accompanying summary: 1,372 payload files, 165,511,444 bytes. Source/input/protocol/inventory hashes and every slot are retained in the summary. Independent physical validation, strategy admission on broader cases, complete user costs and learned net benefit remain open.

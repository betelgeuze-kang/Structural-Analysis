# Triple-excluded label driver: complete preflight, original audit unchanged

Source `c38a9cdf7` adds an explicit `--inner-validation` campaign to the existing
nested label runner. It binds the ten completed 66-row seed policies to the
declared 990 unique original parent/policy pairs. The prepared schedule has
2,970 counterbalanced comparisons, 11,880 single-target paths and a maximum
17,820 core calls including possible fallback work. These are planned counts;
no new label solve has run in this stage.

The new campaign has a distinct identity and pinned seed inventory. Its
auditor checks original sample/parent/context/seed bindings, whole-group
exclusions, exact roster, all repetition records, source revision, proposal
capture cost, arm orders, unchanged tolerances, original work counters and the
declared core-call budget. Old campaign parameters remain the default. The
new audit rows identify a label group and seed; they must not be passed to the
old outer/inner gate-table assembler as if they were old campaign rows.

Frozen-source preflight validates all **990 pairs** in 7.821964 s and creates
no numerical output directory. The updated auditor then rechecks all **1,980
original comparisons** in 10.021605 s. Its complete parsed output is exactly
equal to the earlier audit pinned by original inventory
`35fd7ed0e4552eb1a093bf1394e8bdcf5fbe694464761085c194a8f8ee88b4fb`.
No original numerical work is repeated to establish this compatibility.

The [machine summary](rc-inner-label-driver-preflight-20260920.summary.json)
records the exact full source revision, output/source inventory, byte checks
and separate process costs. All 1,199 packet files were verified. Local focused
tests: **114 passed in 7.13 s**, including wrong campaign, inventory and budget
rejection before parent reads. Ruff and whitespace checks pass.

The live 2,048-layer solve still precedes dispatch of the new timing campaign.
The preparatory runner/auditor evidence does not establish new label quality,
inner gate validation, full-path benefit or independent project generalization.
Seed preparation and its historical costs remain in the
[prior receipt](rc-gate-inner-validation-preparation-20260920.md).

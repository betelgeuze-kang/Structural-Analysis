# Actual HTTP/Workbench review of retained failed candidates

Workbench source: `84a3475faa8bab335b6ee88d15bc353cee1e4ebd`. Numerical source: `5d24fc23f974e67326fee1499a362e5cffb4c2f0`. The immutable delayed-peak pruned/staged results from the failed-work repair were served by the real local RC artifact API, without browser interception, new fits or new numerical paths. Pinned Node 24.20.0 TypeScript, production build and viewer-delivery checks passed first.

Both strategies were reviewed at 1440 px and 390 px. The four views preserved `middle` as the selected candidate with its original declared material estimate. `small` remained `verification_blocked`, its selection button disabled and its physical feasibility unknown. Opening its details exposed the full-reference failure and original per-invocation step/Newton/linear counts with no unknown work reported. The consumer did not turn failed physical verification into a limit pass or selection eligibility.

The observation matched all 234 successful RC HTTP responses to the sealed producer bytes. Fifty original downloads matched exactly: both candidates' model/result/checkpoint/verification, search result/plan/prices, and the staged small candidate's prefix result/checkpoint/verification. No exhaustive pool minimum was claimed. The report's preserved failure, declared quantities/prices and selected candidate were inspected together; this is not a new pricing or structural-validation experiment.

All outer panels fit their viewports. The mobile failed-details screenshot was inspected: original failure identity and download controls are visible, while the wide performance and execution tables require horizontal scrolling. Navigation-to-verified-panel intervals were approximately 0.919/1.402 seconds for pruned desktop/mobile and 0.985/1.444 seconds for staged desktop/mobile. The combined observed review interval was 32.452208628 seconds, including original downloads, 250 ms pacing per download and screenshots. Server startup, browser launch and teardown were excluded. This is a browser-phase observation, not complete user-flow cost or a cross-strategy speed benchmark.

There were no uncaught page exceptions. The separate default sample viewer did emit eight console errors and sixteen warnings across the four views, including missing companion resources (HTTP 404) on this limited test mount. Those logs are retained and prevent an error-free whole-Workbench claim. They do not by themselves prove the resources are missing from a deployed application.

The 9-file, 460,484-byte observation packet is sealed; its exact path, inventory hash, response/download records and per-view times are in the accompanying summary. The mounted numerical packet is unchanged. Same-source byte consistency and successful display do not establish independent physical validation, real project acceptance, a verified quote, learned benefit or release eligibility.

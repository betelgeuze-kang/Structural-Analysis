# Native history attribution for the actual direct-fiber observation

The direct coordinate-to-fiber implementation completes six full paths but
retains 79/75 original end-force/reaction failures. The earlier generalized-input
attribution is not automatically evidence about those newly accepted states.

The fiber/history diagnostic now accepts the explicit direct profile. It checks
its section-response marker and independently evaluates the exact rational direct
fiber strain from original local coordinates. Because the original numerical
profile already bypasses generalized and fiber projection rounding, those
intermediate stages alias the appropriate original direct input. Four actual
material trials per fiber evaluate the reference/candidate direct strains against
both original native parents; duplicated stage aliases do not incur redundant
material calls. Original endpoint responses must match exactly. Generalized-input
profiles retain the prior twelve-call decomposition unchanged.

Both parent-change orders preserve exact rational force closure, original
force/section arithmetic and external-load terms. Original material history stays
immutable. No element/section integration, Newton solve or commit is authorized
by this diagnostic. Under stable-stress, the observation harness counts each
selected trial and its nested original base-law integration separately.

The related neighborhood passes 96 tests in 14.59 s, including both profiles,
independent strain expressions, complete cyclic endpoint replay with actual call
counts, zero removed-stage effects for direct inputs, and rehashed tampering of
fiber strain, material state, generalized strain or local coordinates. The next
observation will use both sealed 242-target direct-fiber reference/secant pairs,
all original inputs verified against the sealed inventory, and serial fresh
source-verified processes. Counterfactual trials remain diagnostics rather than
replacement accepted outputs, new equilibrium solutions or independent physics.

## Completed full-pair observation

Diagnostic source `239f80f4af801523f26c2b8ed6711c1db6768cc6` is frozen in
421 files verified against Git. Two fresh serial workers use the declared source
paths and diagnose both original 242-target direct-fiber reference/secant pairs at
`2821165054f1107ca512fc428830d8f156b55ffd`. All original inputs match their sealed
inventory before and after execution. The numerical solver is not rerun.

All **81,312 original material endpoint responses** and 7,260 selected force
component pairs verify, with immutable native parents and exact closure in both
orders. There are 162,624 selected material trials (139,392 concrete / 23,232
steel), plus the same separately instrumented counts inside their original base
laws: **325,248 total material method entries across both layers**. Four model
compilations and 1,936 original step-force replays across initial checks and exact
endpoints are separate. Section/element integration, Newton and commit counts
remain zero. Diagnostic function times are 27.008534 s (base) and 26.897994 s (long),
including the function's material, model and original-record work; harness startup
and its preliminary inventory checks are outside these times.

| Geometry / parent order | Original failures | Finite accepted coordinates largest | Force/section arithmetic largest | Native history largest |
| --- | --- | --- | --- | --- |
| base / history last | 79 | 79 | 0 | 0 |
| base / history first | 79 | 76 | 3 | 0 |
| long / history last | 75 | 57 | 18 | 0 |
| long / history first | 75 | 57 | 18 | 0 |

All removed intermediate projection/evaluation terms are exactly zero, as required
by the original direct-input profile. External-load contributions are also zero
at these failed fields. Native history is not generally zero, but is never the
largest term here. Its largest magnitude is `5.4996083039649285e-11` SI (base) and
`3.665007919630051e-11` SI (long); finite-coordinate maxima are
`2.423006518230558e-10` and `1.8574036136303423e-10` SI. The three remaining base M2
failures have zero history contribution and coordinate effects largest in both
orders. These observations supersede earlier generalized-profile attribution for
this specific new dataset; they do not establish history irrelevance elsewhere.

A subsequent saved-force arithmetic pass makes two additional model compilations
and 968 original step-force replays / 7,260 selected force-component comparisons.
Instrumented material/section/element/Newton calls remain zero, with no commits.
Original input hashes are checked again against the sealed inventory. Its function
times are 6.004365 s and 5.916968 s, separate from material counterfactual costs.

| Geometry | Original member / reaction failures | Exact stored coefficients | Exact geometry coefficients | Exact stored-stress section sums |
| --- | --- | --- | --- | --- |
| base | 54 / 25 | 59 / 28 | 59 / 28 | 55 / 26 |
| long | 51 / 24 | 47 / 21 | 47 / 21 | 31 / 15 |

Post-processing arithmetic alone still does not eliminate the fixed-comparison
failures. The next original-solver work should therefore prioritize accepted
solution precision at fixed native parents, including finite material-input and
force-accumulation arithmetic, while retaining native-history sensitivity. These
counterfactuals do not predict a new solver trajectory, prove a unique causal
mechanism, or authorize replacing original accepted outputs. More coordinate
storage bits or a history-only rewrite are not established repairs.

The prior direct-fiber observation remains six complete paths, 1,452 core calls,
6,068 Newton-linear counts and **0/2 physical passes with 79/75 failures**. No new
nonlinear benchmark, repeated timing admission, accepted acceleration or independent
physics qualification occurs in this diagnostic.

The sealed local bundle contains **446 files / 26,852,373 bytes**, all reread and
hash-checked, at
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-rc-direct-history.kyyqh_qi`.
Inventory SHA-256:
`1fabdeb6bc5ada974a4c457415988bc3ea03e5fd587e32f27e135263c0635d31`.
All recorded workers and the separate force audit are terminal; prior evidence
is unchanged. The [machine summary](rc-control-direct-history-20260909.summary.json)
retains per-member/reaction and per-order magnitudes and counts. The full roadmap,
hosted/full-suite, independent evidence, licensing, hardware, owner and R1/R2
requirements remain open.

# Hosted receipt for 3d6fe9ec2

Exact tested revision: `3d6fe9ec2015d56c61c0f6c789b627ce9894477c`.

- [Repository Python](https://github.com/betelgeuze-kang/Structural-Analysis/actions/runs/35484416649): development job `106008033498` passes **1,691 tests in 1,016.73 seconds**. Collection passes. All four full shards fail in `Materialize exact current-source test evidence`; their actual repository tests are skipped, as confirmed in the jobs API.
- Shard 0 (`106008033678`) records `external_code_to_code_product_replay_not_passed` and `external_code_to_code_technical_receipt_not_ready`. Development success does not waive these blockers or establish full-suite success.
- [Frontend](https://github.com/betelgeuze-kang/Structural-Analysis/actions/runs/35484386909): job `106007940701` passes **814 tests in 16.9 minutes**, with seven separate setup tests passing. Required aggregate passes.
- Workflow Contract `35484386904` and P0 Canonical Verification Contract `35484386948` pass. Ordinary CI `35484386912` fails. All runs are terminal before subsequent publication.

These receipts cover this exact revision. The completed pooled numerical audit
is separate evidence, documented in its campaign report. CI elapsed times are
not controlled performance comparisons. Independent physical checks, full-suite
execution and release qualification remain open.

# Standalone layout Workbench review — 2026-09-13

Source `20af6f674e63a748f6e707c0dac5ee22a6cf61f4` connects the explicit standalone
layout schemas to the Workbench search decoder and panel. The original two-arm
layout path and existing section-study paths remain supported.

The decoder requires one declared strategy, no oracle, matching plan/result
strategy, the standalone timing scope and appropriate training accounting. Price
order reads no policy or training artifact and requires empty predictions and
null ranking. Learned order validates the existing original policy/training
bindings. Both use shared full-result engineering review, quantities/prices,
selection/work checks and finite-pool cost/coverage checks. The browser checks
recorded predictions and does not refit or independently calibrate them.

The panel shows only the strategy actually run, keeps whole-pool optimality
unavailable without a separate oracle, and preserves the layout functional-scope
qualification. Price order states that it uses no learned artifacts; learned order
shows the historical training cost separately. Original model, result, checkpoint,
verification, plan, report and price-table downloads remain byte-exact. Policy and
training downloads appear only when used. Derived display context does not replace
the original comparison report.

## Focused validation

The trusted Node 24 runner passed TypeScript, production build/delivery and
**90 distinct tests**. The layout selection passed 36 tests; the search regression
selection passed 70 with 16 overlapping identities. Twenty-four new cases cover
four original standalone graphs (budgets 2/3, price/learned), rehashed invalid
strategy/timing/coverage, changed original checkpoints, and both strategies at
1440px/390px with exact physical and metadata downloads. The initial TypeScript
nullable-policy error was fixed; interrupted intermediate work is not counted
as passing evidence. The existing large-chunk build warning remains.

The 3,668,794-byte gzip fixture stores base64 original graph bytes checked against
the previous sealed HTTP packet. It retains JSON numeric spellings and hashes.
These known geometry/history cases and synthetic prices are regression data,
not measured external data or a new independent experiment.

## Actual server/browser observation

A separate loopback WSGI and Chromium run, without route interception, reviewed
budget-3 price and learned results at both viewport widths. All four views showed
one strategy and selected `middle`, while keeping the unrun oracle unavailable.
It verified **152 actual HTTP responses and 32 original downloads**, with no page
errors, no new numerical paths and no new fits. The mobile screenshot was also
inspected: metadata controls fit the panel; wide comparison tables retain their
own horizontal scrolling.

Navigation, validation, selection and downloads took 7.310675346 s across the four
views. This excludes server startup, browser launch, screenshots and teardown.
It is a separate single review observation, not an end-to-end repeated cost
campaign or evidence of AI speedup. The earlier producer source and original
qualification flags remain unchanged.

The packet is at
`/mnt/193005ba-8531-4d0b-87c2-43c01ee2ce25/structural-layout-standalone-workbench-MCXNYp`:
8 files, 709,161 bytes, inventory SHA-256
`aa0bbf69619533e39889a0d230c5982a1b247ca4f426046f4eb2d3e91c47fd61`.
It includes the observer, four screenshots, two HTTP receipts and summary. See the
[machine-readable observation](rc-layout-standalone-workbench-20260913.summary.json).

## Remaining scope

Standalone layout result review and original downloads are now connected through
actual HTTP. This does not close full analysis-to-review repeated cost accounting,
AI net benefit, independent project/geometry/history transfer, external physical
verification, functional equivalence, hosted full CI or the broader material/3D
roadmap. The previous matched-pair training-inclusive process ratio of 1.234734
remains a measured cost disadvantage, with different timing scope from this review.

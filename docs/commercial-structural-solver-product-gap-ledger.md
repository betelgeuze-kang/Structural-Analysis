# 완전 상용 건축구조해석 솔버 전환 갭 원장

- 기준일: 2026-05-31
- 범위: 기존 워크스테이션 납품 서비스/engineer-in-loop 보조툴을 넘어, **독립 상용 건축구조해석 솔버 제품**으로 주장하기 전에 닫아야 할 개선점과 문서상 미폐쇄 항목 정리
- 비범위: 구현 순서, 담당, 일정, 커밋 단위 계획. 이 문서는 구현 계획이 아니라 **제품 갭 원장**이다.

## 결론

현재 제품은 **워크스테이션 기반 구조해석/최적화 납품 서비스**와 **engineer-in-loop 상용 보조툴**로는 상당히 닫혀 있다. `check_workstation_delivery_readiness.py --json` 기준 `PASS | gates=8/8`, delivery bundle도 `ready`다.

그러나 **완전한 독립 상용 건축구조해석 솔버**로는 아직 닫히지 않았다. `check_independent_product_readiness.py --json` 기준 readiness는 `80/100, blocked`이고, `report_commercialization_level.py --closure-mode strict --json` 기준도 `L3 engineer_in_loop_commercial_assist_ready`, `full_commercial_replacement_ready=false`다.

핵심 차이는 다음이다.

| 구분 | 현재 말할 수 있는 것 | 아직 말하면 안 되는 것 |
| --- | --- | --- |
| 제품 claim | 구조 엔지니어 검토 전제의 상용 해석/최적화 보조 및 납품 서비스 | 독립 상용 구조해석제품, 기술사 검토 대체, 인허가 자동 승인 |
| 솔버 claim | 일부 축약/부분 메쉬/상용 export proxy 기반 검증이 붙은 해석 파이프라인 | 임의 건축 구조에 대한 full 3D nonlinear global FEA solver |
| 검증 claim | 내부 evidence, commercial export sample, model-derived 비교, workstation package smoke | 외부 benchmark receipt 4/4, strict residual holdout closure 3/3, live licensed solver replay |

## 최신 Evidence 스냅샷

| Evidence | 현재값 | 해석 |
| --- | --- | --- |
| `workstation_delivery_readiness.json` | `ready`, `PASS | gates=8/8` | 로컬 납품 서비스는 green |
| `delivery_evidence_bundle.json` | `ready`, blockers `[]` | 납품 번들 조립 evidence는 green |
| `check_independent_product_readiness.py --json` | `blocked`, score `80.0/100` | 독립 상용제품은 strict EB/RH 때문에 blocked |
| `report_commercialization_level.py --closure-mode strict --json` | `L3`, score `7.5/10`, `full_commercial_replacement_ready=false` | Commercial label은 engineer-in-loop 보조툴 범위 |
| `mgt_global_fea_3d_native_solve.json` | `status=warn`, `solve_mode=mgt_npz_beam_mesh_3d_real_section_connected_component` | 대표 connected component는 Newton 수렴, full mesh nonlinear equilibrium은 아님 |
| `mgt_full_line_mesh_sparse_equilibrium.json` | `ready`, line `5572/5576`, elastic residual `3.89e-06 N`, linearized geometric residual `1.61e-06 N`, CPU sparse solve `~0.89s` | 전체 line/beam graph elastic + linearized geometric sparse equilibrium evidence; nonlinear/full shell closure는 아님 |
| `mgt_full_frame_6dof_sparse_equilibrium.json` | `ready`, line `5572/5576`, DOF `14490`, elastic residual `7.10e-06 N`, linearized geometric residual `3.72e-04 N`, deformed P-Delta `load_scale=0.5`, frame `ANGLE` nonzero rows `150`, best-residual refinement `10`, CPU sparse solve `~24.40s` | 전체 line/beam graph 6-DOF frame elastic + linearized geometric + scaled deformed-state P-Delta evidence; MGT frame local-axis roll은 소비하지만 full-load nonlinear/full shell/design adequacy closure는 아님 |
| `mgt_surface_membrane_tangent.json` | `ready`, surfaces `7152`, triangles `14120`, source thickness coverage `100%`, residual `1.38e-06 N`, CPU sparse solve `~2.42s` | MGT PLATE rows의 source-thickness membrane tangent smoke solve; shell bending/drilling/frame-shell coupling closure는 아님 |
| `mgt_surface_shell_bending_tangent.json` | `ready`, surfaces `7152`, active shell DOF `35935`, source thickness coverage `100%`, residual `4.26e-07 N`, relative residual `1.31e-08`, CPU sparse solve `~4.96s` | MGT PLATE rows의 source-thickness Mindlin-type bending/drilling/transverse pressure smoke solve; calibrated full shell closure는 아님 |
| `mgt_shell_calibration_benchmarks.json` | `ready`, cases `5/5`, clamped plate relative error `7.63%` | membrane constant-strain, rigid modes, transverse shear, zero-shear rotation, clamped square plate analytical benchmarks가 붙음. opening/non-default surface LCAXIS/higher-order/full-load nonlinear shell closure는 아님 |
| `mgt_coupled_frame_surface_sparse_equilibrium.json` | `ready`, line `5572`, surfaces `7152`, source thickness coverage `100%`, DOF `68130`, residual `2.02e-04 N`, relative residual `9.03e-09`, CPU sparse solve `~3.75s` | 6DOF node space에 frame + source-thickness surface membrane을 함께 조립한 coupled smoke solve; full-load/bending/material nonlinear closure는 아님 |
| `mgt_coupled_frame_shell_sparse_equilibrium.json` | `ready`, line `5572`, surfaces `7152`, active DOF `46027`, residual `5.23e-08 N`, relative residual `2.34e-12`, CPU sparse solve `~5.73s` | 6DOF node space에 frame + source-thickness shell bending/drilling을 함께 조립한 coupled smoke solve; calibrated/full-load/material nonlinear closure는 아님 |
| `mgt_frame_material_nonlinear_tangent.json` | `ready`, nonlinear probe elements `5487`, min tangent ratio `0.015`, smoke residual `1.60e-04 N` | MGT material E/nu와 `C40/Q235/SRC` grade proxy를 bounded frame material tangent smoke/probe로 소비함. full path-dependent material Newton/fiber closure는 아님 |
| `mgt_shell_material_nonlinear_tangent.json` | `ready`, nonlinear probe surface elements `6408`, min tangent ratio `0.948`, fixed-tangent shell JVP relative error `6.39e-17`, smoke residual `1.09e-09 N` | MGT PLATE/shell 요소가 per-surface material tangent를 소비하고 fixed-tangent global shell JVP를 통과함. full path-dependent/layered shell material Newton이나 residual gate closure는 아님 |
| `mgt_direct_residual_shell_material_tangent_base_followup379_probe.json` | `partial`, load scale `0.656`, shell material tangent residual applied `true`, base direct residual `18.97 N`, gate `5.0e-04 N` | shell material tangent를 physical direct residual path에 실제 연결했지만 residual gate는 다시 열림. state-updated consistent material Newton/globalization과 ROCm/HIP backend parity가 남음 |
| `mgt_direct_residual_shell_material_tangent_rowcorr_min_followup380_probe.json` | `partial`, base `18.97 N`, best `14.91 N`, improvement `21.4%`, relative increment `8.84e-09` | shell material tangent residual 기준에서 최소 current-tangent row correction은 하강을 찾음. residual gate와 durable checkpoint/ROCm parity는 여전히 미폐쇄 |
| `mgt_pdelta_continuation_probe.json` | `partial`, direct max load `0.5`, adaptive max load `0.505`, secant predictor max load `0.5075`, fine secant max load `0.50765625`, first direct failed step `0.55`, frontier residual-Jacobian target `0.5078125`, compact summary diagnosis `fixed_point_increment_small_but_direct_residual_gate_far_from_tolerance`, one-step line-search best residual reduction factor `>1`, extended Anderson best relative increment `2.08e-03`, coefficient-bounded Anderson best relative increment `2.07e-03`, residual-trust Anderson remap increment `1.37e-01` | best-residual refinement를 `10`회로 올려 local micro-step map residual floor는 낮췄고 accepted micro-step을 continuation path로 승격했다. 수렴 판정은 relaxed update가 아니라 unrelaxed fixed-point relative increment로 강화했다. Adaptive micro stepping은 `0.505`까지만 가지만, secant displacement predictor가 `0.5075`를 accepted path state로 승격했다. Fine secant micro-continuation은 `0.00015625` step에서 map residual `9.48e-04 N`과 fixed-point relative increment `2.93e-06`으로 accepted frontier를 `0.50765625`까지 소폭 확장했다. Ledger frontier diagnostic은 accepted frontier 바로 위 bracket `0.5078125`가 residual gate만 실패한다고 분리해 기록한다. 새 finite-difference residual-Jacobian probe는 이 nearest failed bracket을 직접 `K(u)u-f`로 replay해 네 번의 accepted correction으로 direct residual을 `10310.5165 -> 5166.5141 -> 2578.8257 -> 2404.6470 -> 2092.1725 N`까지 낮춘다. 즉 map residual과 직접 equilibrium residual이 분리되어 있으며, compact summary는 best direct residual이 `0.001 N` gate 대비 약 `2.09e6x` 남아 있다고 노출한다. full-load claim에는 consistent residual/Jacobian이 필요하다. `0.50828125`는 다시 residual/increment gate를 놓치며, 48-iteration Anderson과 L1-bounded Anderson은 새 frontier seed에서 각각 `2.08e-03`, `2.07e-03`으로 `1e-4` gate에는 아직 못 미친다. Residual-trust Anderson은 후보를 다시 fixed-point map에 통과시키는 정직한 remap 검사를 추가했고 residual은 `7.98e-06 N`까지 낮지만 remap increment가 `0.1373`이라 closure가 아니다. 다음 단위는 consistent Newton/Jacobian 또는 더 강한 globalization이다 |
| `mgt_coarsened_authored_support_pdelta_probe.json` | `partial`, authored support nodes `2133`, mapped to coarsened line mesh `756`, restrained DOF `2544`, elastic-link rows with both endpoints mapped `0`, first failed load `0.5`, residual `5.55e-03 N`, fixed-point relative increment `0.752`, max translation `25.79m` | component-base restraint P-Delta path를 authored support mask로 조용히 교체하면 첫 단계부터 미수렴한다는 counter-evidence다. Coarsened line mesh에서는 finite elastic link를 조립할 양끝 endpoint가 남지 않으므로, boundary condition migration의 다음 단위는 uncoarsened boundary global tangent를 nonlinear continuation/Newton path에 연결하는 것이다 |
| `mgt_uncoarsened_boundary_pdelta_probe.json` + checkpoint continuation receipts | `partial`, uncoarsened nodes `13047`, authored restrained DOF `7707`, finite spring components `10152`, main relaxation-1.0 frontier `0.45`, alpha-scan checkpoint continuation frontier `0.656`, first failed `null`; at `0.656` best residual `1.10e-04 N`, fixed-point relative increment `5.18e-06`, max translation `0.900m`; seed alpha scan best alpha `0.02`, checkpoint schema `mgt-uncoarsened-boundary-pdelta-checkpoint.v1`, segment count `63`, accepted steps `146` | rigid-link coarsening을 끈 frame-shell + finite spring + authored support tangent를 deformed-state P-Delta fixed-point loop에 실제 연결했다. Relaxation `1.0` path는 `0.45`에서 멈추지만, durable `0.45` checkpoint를 seed로 한 relaxation `0.5` segmented continuation은 `0.5406`까지 accepted frontier를 확장했고, seed alpha-scan globalization과 짧은 relaxed remap 반복은 `0.540601`에서 `0.656`까지 accepted state를 추가 승격했다. 아직 다음 failed bracket은 공식 aggregate에 없지만 full-load `1.0`에는 미달한다. 다음 단위는 더 넓은 globalization 또는 consistent Newton/Jacobian/material tangent다 |
| `mgt_direct_residual_newton_probe.json` | `partial`, direct residual contract `R(u, lambda)=F_int(u)-lambda*F_ext`, reference-configuration `F_ext`, checkpoint `0.656`, direct residual `4.28e4 N`, fixed-point receipt residual `1.10e-04 N`, 6 accepted micro trust-region steps, directional residual-Jacobian alpha candidate included, final direct residual `1.551637e4 N`, improvement factor `2.755x`; accepted-history secant subspace residual-only candidate `1.550832e4 N` fails increment gate, gate-eligible secant candidates are promoted 6 times, and matrix-free consistent-Jacobian subspace promotes eight gate-limited JVP passes with per-pass basis `1/2` candidate selection plus `1e-5` minimum relative-improvement governance. `mgt_direct_residual_probe_scale_sweep_smoke.json` is a new opt-in CPU diagnostic smoke for JVP probe-scale selection, not closure evidence. | frame service material tangent + frame geometric delta + shell/surface tangent + authored support/finite springs와 displacement-independent reference load vector를 같은 residual 체계에 넣고, solver regularization 없이 물리 residual을 평가했다. 이 결과는 기존 fixed-point/remap frontier가 physical direct residual closure가 아님을 분리한다. 방향성 finite-difference residual-Jacobian alpha 후보는 첫 step에서 configured alpha보다 나은 하강을 만들고, 3회차 이후 increment gate는 통과한다. Accepted-history secant subspace는 residual-only best `1.550832e4 N`을 찾지만 relative increment `1.80e-03`으로 gate를 놓친다. 같은 subspace의 gate-limited/finer alpha 후보는 6차례 relative increment gate를 통과하며 residual을 `1.552967e4 N`까지 낮춘다. 이어 matrix-free consistent-Jacobian subspace는 최근 basis `1`과 `2` 후보를 pass마다 함께 평가하고 full residual/increment gate를 통과한 최선 후보를 승격한다. Selected basis sequence는 `[2,1,2,2,2,2,2,1]`이며, 8회 모두 relative-improvement floor를 넘긴 뒤 `max_promotions_exhausted`로 멈추고, final residual을 `1.551637e4 N`까지 낮추며 relative increment `9.499e-05`를 유지한다. 2026-06-05 opt-in probe-scale sweep smoke는 scales `[0.125,0.25]` 중 `0.25`를 선택했지만 `promotion_count=0`, `stop_reason=no_residual_descent`, final residual `15543.349715249427 N`로 official default에 못 올랐다. residual gate는 여전히 실패하므로 G1의 다음 단위는 full consistent residual Jacobian, arc-length/trust-region globalization, regularization-free residual gate 동시 폐쇄다 |
| `mgt_g1_direct_residual_terminal_gate_report.json` | `ready`, direct replay/terminal equilibrium/summary receipts cross-check, strict residual tolerance `5.0e-04 N`, final direct residual `1.7877e-06 N`, terminal relative increment `0.0` | attached-policy `0.656` checkpoint의 terminal direct-residual residual+increment gate는 닫혔다. 이 report는 `mgt_direct_residual_attached_policy_followup365_gate_replay_probe.json`, `mgt_equilibrium_newton_focused_followup375_structural_terminal_increment_gate_probe.json`, `mgt_g1_followup362_365_attached_equilibrium_newton_gate_summary.json`을 대조해 residual gate와 terminal no-descent increment gate를 동시에 검증한다. 단 full-load `1.0`, full-mesh/full-building nonlinear path, material Newton breadth, production ROCm/HIP residency는 여전히 G1 claim boundary 밖이다. |
| `g1_full_load_hip_newton_lane_report.json` | `blocked`, `checkpoint_resolution_gate.full_load_candidate_count=0`, scanned candidates `50`, loadable candidates `42`, highest observed load scale `0.656`, gap to required load scale `0.344`, blockers include `checkpoint_resolution_no_full_load_candidate` and HIP runtime/proof blockers | strict full-load HIP Newton lane은 현재 evidence sources에서 loadable full-load checkpoint를 찾지 못한다. 이 gate는 후보 스캔 전체의 full-load 입력 부재를 명시하는 차단 증거이며, residual/increment/material Newton/production ROCm-HIP closure를 합성하지 않는다. 따라서 다음 G1 단위는 sub-full row-polish 재실행이 아니라 full-load checkpoint generation plus production HIP residual/Jacobian proof, or a coupled Newton/load-continuation path that reaches load scale `1.0` with closure gates intact이다. |
| `mgt_global_fea_condensed_solve.json` | `ready`, `condensed_global_fea_wired` | 축약 story 모델은 수렴하지만 full 3D FEA 대체 아님 |
| `mgt_native_modal_buckling_solver.json` | `ready`, modes `4`, critical buckling factor `7053.141` | 대표 MGT beam component의 native K/M/Kg eigen solve가 붙어 G2 local closure |
| `load_stage_runtime_flow_receipt.json` | `ready`, solve/viewer/export/audit flow `true` | typed load/stage entity flow와 unsupported hazard queue가 붙어 G3 local closure |
| `material_element_tangent_support_matrix.json` | `ready`, line tangent `true`, surface source thickness `100%`, frame/shell material tangent smoke `true`, unsupported queue `true` | line/beam elastic real-section tangent와 source-thickness surface smoke solve, bounded frame/shell material tangent smoke, contact/path-dependent material unsupported queue가 분리되어 G4 local closure |
| `mgt_beam_offset_support_receipt.json` | `partial`, MGT `*OFFSET` rows `708`, unmatched element refs `0`, max offset `0.523m` | 실제 MGT beam end offset typed ingest/provenance와 6DOF frame/coupled rigid-end-offset tangent 적용은 닫혔지만, ELEMENT local-yz/roundtrip/non-frame eccentricity 소비는 남음 |
| `mgt_element_local_axis_opening_semantics_receipt.json` | `ready`, `*ELEMENT` local-axis rows `12728`, frame `ANGLE` rows `5576`, nonzero frame angle `150`, PLATE LCAXIS rows `7152` all default, opening markers `0` | frame local-axis `ANGLE`은 roundtrip NPZ와 6DOF frame/coupled tangent에서 소비됨. benchmark bridge MGT에 opening/hole/void source row가 없으므로 current-source opening semantics는 checked no-op ready이며, generic cutout meshing은 claim하지 않음 |
| `mgt_boundary_entity_support_receipt.json` | `partial`, `*CONSTRAINT` rows `8`, support nodes `2133`, `*ELASTICLINK` rows `1692`, story eccentricity present | 실제 MGT support/link/story-eccentricity typed ingest와 roundtrip rigid-link coarsening evidence는 붙었지만, authored support mask/finite link/story eccentricity solver consumption은 남음 |
| `mgt_boundary_spring_tangent_receipt.json` | `ready`, finite spring components `10152`, restrained DOF `7707`, relative residual `0.0` | 실제 MGT support masks와 elastic-link stiffness를 boundary subsystem sparse tangent로 수치 소비했지만, full frame/shell global tangent 통합과 story eccentricity load generation은 남음 |
| `mgt_uncoarsened_boundary_global_equilibrium.json` | `ready`, uncoarsened nodes `13047`, finite spring components `10152`, authored support restrained DOF `7707`, relative residual `1.32e-08` | rigid-link coarsening을 끈 MGT roundtrip에서 실제 `*CONSTRAINT` mask와 `*ELASTICLINK` finite spring을 full coupled frame-shell global tangent에 조립해 solve함. 여전히 linear smoke solve이며 full-load nonlinear/material nonlinear/shell calibration closure는 아님 |
| `mgt_story_eccentricity_load_receipt.json` | `ready`, story bands `31`, generated seismic eccentricity cases `4`, max torsion `3.44e6 Nm` | 실제 `*STORY-ECCEN`을 equivalent accidental torsion load generation으로 소비했지만, full global solve injection과 design-code response spectrum/wind code는 남음 |
| `mgt_coupled_frame_shell_story_eccentricity_equilibrium.json` | `ready`, cases `4/4`, max relative residual `3.99e-7`, max translation `1.13m` | generated story eccentricity loads를 full coupled frame-shell sparse solve에 주입했지만, smoke-scale linear equilibrium이며 full-load nonlinear/design-code closure는 아님 |
| `kds_detailing_support_matrix.json` | `ready`, clauses `23`, guarded rows `20/20` | KDS RC clause/detailing matrix와 자동 claim guard가 붙어 G5 local closure |
| `korean_medium_large_ingest_receipt.json` | `sources=19 attached=10 metadata_only=9 mgt_header_ok=4` | 중대형 한국 corpus는 bridge 중심, 실제 공모 MGT는 아직 부족 |
| `commercial_solver_cross_validation.json` | `pass_with_marginal_metrics`, 12/12 cases | 상용 export sample 비교는 통과하나 live solver replay는 아님 |
| `phase4_analytic_physical_fallback_scorecard.json` | `ready`, generated seed fallback cases `30/30`, expected-output comparisons `88/88`, physical equilibrium `30/30`, commercial reference cases `0` | 상용툴 결과가 없는 generated seed case를 analytic expected-output과 physical residual/equilibrium으로 평가할 수 있음을 보강한다. operator output 첨부, 두 독립 reference solver 비교, GUI story/member/mode 추적성, Phase 4/G6/commercial closure는 아님 |
| `commercial_gap_ledger_status.json` | `closed=17/20`, `partial=2`, `open=0`, `external_blocked=1`, `locally_closable_nonclosed_count=1` | G3 load/stage flow와 G10 governance/support closure, G9 ROCm/HIP 6900XT receipt 복구를 반영한다. G1은 `full_load_gate_not_closed`, `full_mesh_nonlinear_equilibrium_not_closed`, `material_newton_breadth_not_closed`, `production_rocm_hip_residency_not_closed`가 남아 partial이고, `locally_closable_nonclosed_row_ids=["G1"]`로 노출된다. G7은 operator-attached corpus 증거 부족으로 partial이며, G6은 외부 receipt가 없어 external-blocked다 |
| `optimization_productization_audit.json` | `status=ready`, accepted rows `20/20` guarded | 최적화 변경은 solver/cost/Pareto/review/code-guard evidence로 재현 가능 |
| `ml_surrogate_checkpoint_manifest.json` | `status=ready`, validation/OOD/fallback pass | ML은 shadow+solver-fallback 모드로만 제품화 증거에 연결 |

## 문서상 덜 닫힌 부분

| 문서 | 현재 유용한 점 | 덜 닫히거나 갱신 필요한 점 |
| --- | --- | --- |
| [independent-commercial-product-gap-reassessment.md](independent-commercial-product-gap-reassessment.md) | 독립제품 readiness `80/100 blocked`와 EB/RH 경계를 명확히 잡음 | strict RH는 여전히 `0/3`으로 보는 것이 최신 gate와 일치한다. local signed/HMAC packet과 외부 법적/독립 RH closure를 더 명확히 분리해야 함 |
| [commercialization-gap-current-state.md](commercialization-gap-current-state.md) | P0/P1/P2와 P1 evidence intake 절차를 잘 정리 | 일부 표현은 P0/P1 publication 중심이고, “완전한 솔버 제품” 관점의 full 3D nonlinear/eigen/buckling/contact/code-check gap은 별도 원장이 필요 |
| [continued-improvement-backlog.md](continued-improvement-backlog.md) | I1~I8 후속 개선 축이 실무적으로 좋음 | I3는 최신 상태로 갱신 필요: legacy fallback에서 발전해 대표 connected component Newton은 수렴하지만 `partial_connected_component_mesh=true`라 full mesh closure 아님. I5도 현재 receipt `19/10/9/4`와 bridge provenance를 반영해야 함 |
| [engine-ai-and-comparison-commercialization-gaps.md](engine-ai-and-comparison-commercialization-gaps.md) | 도면 비교, 해석엔진, AI/최적화 gap을 넓게 포착 | “E-P3 완료(조건부)”와 strict readiness의 EB/RH pending이 혼재되어 있다. local engineer-in-loop closure와 independent/legal closure를 분리해야 함 |
| [workstation-service-productization-roadmap.md](workstation-service-productization-roadmap.md) | 로컬 납품 서비스 경계를 잘 잡음 | 완전 독립 솔버 gap 문서가 아니므로, full solver completeness 기준은 본 문서 같은 별도 원장이 필요 |
| `commercialization_level.md` | L3/Commercial engineer-in-loop claim을 노출 | checked-in markdown은 score `7.0`, blocker에 `p1_benchmark_execution_blocked...`가 남아 있으나 현재 strict script 출력은 score `7.5`, P1 execution unblocked, EB/RH pending이다. 재생성/동기화 필요 |

## 상용 독립 솔버로 가기 위한 개선 작업 원장

아래 항목은 구현 순서가 아니라 **닫아야 할 제품 능력**이다. 구현 계획은 별도 문서에서 순서/테스트/산출물 단위로 다시 쪼갠다.

### G1. Full 3D Global FEA Core

현재 상태:

- MGT roundtrip/mesh contract는 strong하다.
- `commercial_gap_ledger_status.json`의 `G1.evidence.source_receipts`는 `mgt_pdelta_continuation_probe.json`, `mgt_uncoarsened_boundary_pdelta_probe.json`, `mgt_uncoarsened_boundary_pdelta_checkpoint_continuation.json`, `mgt_uncoarsened_boundary_pdelta_frontier_0p85_receipt.json`, `mgt_direct_residual_newton_probe.json`, `mgt_g1_direct_residual_terminal_gate_report.json`, `mgt_g1_followup387_shell_material_budgeted_continuation_status.json`, `g1_full_load_hip_newton_lane_report.json`, `mgt_residual_jacobian_consistency_probe.json`, `mgt_rocm_sparse_solver_probe.json`를 G1 partial/blocker 판정 출처로 기록한다.
- 같은 G1 evidence는 `blocker_terminal_requirements`로 남은 네 blocker를 row-level로 노출한다: `full_load_gate_not_closed`, `full_mesh_nonlinear_equilibrium_not_closed`, `material_newton_breadth_not_closed`, `production_rocm_hip_residency_not_closed`. 현재 네 row 모두 `terminal_requirement_satisfied=false`이며, 각 row는 terminal target, observed values, source receipt paths, non-closing reasons를 기록한다. 이 crosswalk는 다음 실행 단위를 찾기 위한 routing evidence일 뿐이고 G1 closure를 승격하지 않는다.
- `solve_mgt_beam_mesh_3d_global.py`는 real section/material을 일부 주입하고 대표 connected component Newton 수렴 evidence를 낸다.
- 최신 3D native evidence는 `raw_beam_elements_available=358`, `beam_elements_solved=4`, `nodes_in_submesh=5`, `partial_connected_component_mesh=true`, `nonlinear_equilibrium=false`, `representative_component_nonlinear_equilibrium=true`다.
- `mgt_full_line_mesh_sparse_equilibrium.json`은 full line/beam graph에 대해 `raw_line_element_count=5576`, `line_elements_solved=5572`, `skipped_short_or_degenerate_count=4`, `dof_count=7245`, `free_dof_count=4908`, elastic `residual_inf_n=3.89e-06`, linearized geometric `residual_inf_n=1.61e-06`인 sparse equilibrium을 생성한다.
- linearized geometric tangent는 positive axial element `2711`개와 max axial force `93,522,660 N`을 기록한다. 이는 대표 component보다 훨씬 넓은 full line mesh evidence지만, `full_line_mesh_nonlinear_equilibrium=false`이고 collapsed horizontal DOF라서 full 3D nonlinear solver closure로 보지 않는다.
- `mgt_full_frame_6dof_sparse_equilibrium.json`은 같은 full line/beam graph를 node당 6DOF 3D frame stiffness로 조립해 `dof_count=14490`, `free_dof_count=9816`, elastic `residual_inf_n=7.10e-06`, linearized geometric `residual_inf_n=3.72e-04`, real section/material coverage `98.58%`인 equilibrium을 생성한다.
- 6DOF linearized geometric tangent도 positive axial element `2711`개와 max axial force `93,522,660 N`을 기록한다. 다만 `full_frame_6dof_nonlinear_equilibrium=false`이고 auto base-restraint evidence harness를 사용하므로, deformed-state nonlinear/full-shell/설계 적합성 closure로 보지 않는다.
- 같은 artifact는 deformed-coordinate 6DOF frame P-Delta fixed-point path를 `target_load_scale=0.5`, `iteration_count=18`, `residual_inf_n=7.53e-05`, `relative_increment=7.78e-05`로 수렴시킨다. 이는 geometric nonlinear frame evidence지만 full gravity 1.0, material nonlinear Newton, shell/plate tangent closure는 아니다.
- 이 artifact는 정규화된 free-DOF 선형 부분문제에 `best_residual_iterative_refinement`, 최대 10회 refinement를 적용한다. 이 refinement는 linear residual floor를 낮추는 수치 안정화 증거이지, consistent nonlinear Newton/Jacobian을 대체하지 않는다.
- `mgt_pdelta_continuation_probe.json`은 이 P-Delta path를 load-step continuation으로 재실행해 direct step 기준 `0.5`는 ready, 이전 변위를 seed로 넣은 `0.55`는 `iteration_count=28`, `residual_inf_n=1.46e-05`까지 낮아져도 `relative_increment=0.209`, `max_translation=1.95m`에서 미수렴임을 기록한다. 따라서 G1의 다음 구현 단위는 local micro-step refinement를 넘어선 consistent full-load Newton/Jacobian과 material Newton closure다.
- Adaptive micro continuation은 accepted path state를 `0.505`까지 승격했다. 수렴 판정은 damping으로 축소된 relaxed update가 아니라 unrelaxed fixed-point relative increment를 사용한다. `0.5 -> 0.505`는 refinement `10`, `iteration_count=38`, residual `9.17e-04 N`, fixed-point relative increment `6.55e-05`로 ready지만, 다음 `0.51` step은 residual `1.44e-03 N`, fixed-point relative increment `0.889`로 실패하고 half-step fallback `0.5075`도 adaptive-only path에서는 residual `1.27e-03 N`, fixed-point relative increment `0.423`으로 실패한다.
- Post-failed relaxation sensitivity는 accepted `0.505` state에서 `0.5075`를 conservative relaxation `0.25`로 재시도한다. 이 경로는 fixed-point relative increment를 약 `1.85e-04`까지 낮추지만 residual floor가 약 `3.03e-03 N`으로 남아, 단순 damping 조정만으로는 closure가 아니며 consistent residual/Jacobian 개선이 필요함을 분리해서 보여준다.
- Secant displacement predictor는 accepted `0.5`와 `0.505` state로 `0.5075` 초기 변위를 예측한다. Latest evidence에서는 relaxation `0.25` row가 residual `7.38e-04 N`, fixed-point relative increment `1.66e-05`로 두 gate를 동시에 통과해 `0.5075`를 accepted path state로 승격했다. 일반 secant micro-continuation은 `0.51`에서 residual `3.29e-04 N`으로 residual gate는 통과하지만 fixed-point relative increment `0.434`가 남고, half-step `0.50875`도 residual/increment gate를 모두 놓친다. Fine secant micro-continuation은 `0.508125`와 `0.5078125`에서 increment gate만 통과하고 residual floor가 남지만, `0.50765625`는 residual `9.48e-04 N`과 fixed-point relative increment `2.93e-06`으로 accepted되어 frontier를 소폭 확장했다. Ledger의 `pdelta_frontier_diagnostic`은 이 bracket을 구조화해 `frontier_load_scale=0.50765625`, `next_failed_load_scale_after_frontier=0.5078125`, `next_failed_gate_mode=residual_gate_failed`, `frontier_to_full_load_increment=0.49234375`로 노출한다. 다음 `0.50828125`는 다시 residual/increment gate를 놓치므로 full-load closure는 아니다.
- Frontier residual-Jacobian probe는 nearest failed bracket `0.5078125`에서 finite-difference residual-Jacobian correction을 시도한다. Regularized tangent Newton, fixed-point delta, load-path secant delta에 이전 residual-Jacobian correction을 더한 4-pass least-squares correction을 만들고 직접 `K(u)u-f` residual과 fixed-point remap increment를 동시에 검사한다. 결과는 accepted correction `4`회, direct residual `10310.5165 -> 5166.5141 -> 2578.8257 -> 2404.6470 -> 2092.1725 N`, best alpha `1.0`, fixed-point relative increment `3.33e-06`이다. `commercial_gap_ledger_status.json`의 compact `pdelta_frontier_residual_jacobian_summary`는 increment gate는 통과하지만 direct residual gate가 tolerance 대비 약 `2.09e6x` 남았다고 노출한다. 기존 fixed-point map residual만으로는 full nonlinear equilibrium을 주장할 수 없고 consistent residual/Jacobian이 필요하다.
- Extended constrained Anderson acceleration은 first direct failed `0.55`에서 `48`회/8-depth 진단을 수행한다. Fine secant frontier를 seed로 한 latest evidence에서는 best relative increment가 `2.08e-03`으로 `1e-4`보다 커서 closure는 아니며, coefficient L1이 `138`까지 커져 high-coefficient instability가 남는다.
- Coefficient-bounded Anderson probe는 L1 상한 `8`로 high-coefficient extrapolation을 latest mapped state 쪽으로 blend한다. 이 경로는 max coefficient L1을 `8`로 제한하고 20개 iteration에서 bounding을 적용하지만 best relative increment가 `2.07e-03`으로 tolerance보다 커 closure가 아니다. 따라서 단순 coefficient clipping이 아니라 residual-trust-region acceptance, line-search globalization, 또는 consistent Newton/Jacobian이 필요하다.
- Residual-trust-region Anderson probe는 first direct failed `0.55`에서 fixed-point map, full Anderson, `0.5`, `0.25` blend 후보를 모두 다시 P-Delta fixed-point operator에 remap해 true fixed-point increment를 측정한다. Best candidate는 `anderson_blend_1`이고 residual은 `7.98e-06 N`으로 residual gate를 통과하지만 remapped fixed-point relative increment가 `0.1373`으로 tolerance `1e-4`를 크게 넘는다. 따라서 residual-only acceptance도 closure가 아니며, 다음 구현 단위는 deformed-state consistent tangent/Newton residual Jacobian이다.
- 같은 probe는 첫 실패 load `0.55`에서 one-step approximate Newton correction과 alpha scan도 수행한다. Seed residual은 약 `3.26e5 N`이고, best alpha `0.125`도 residual을 약 `2.06e8 N`으로 키워 residual reduction factor가 `>1`이다. 즉 현재 tangent로는 line search damping만 추가해도 full-load closure가 되지 않는다.
- 같은 실패 load에서 Anderson 계열 probe는 residual을 낮추는 후보와 fixed-point increment를 낮추는 후보가 분리됨을 기록한다. 이는 현재 반복식이 수치적으로 “잔차만 작아 보이는” 상태를 만들 수 있음을 보여주며, full-load nonlinear claim에는 remapped fixed-point increment와 residual을 동시에 통과하는 consistent tangent가 필요하다.
- `mgt_coarsened_authored_support_pdelta_probe.json`은 coarsened line-only P-Delta path에 실제 MGT authored support mask를 매핑해 component-base auto-restraint를 대체하는 진단이다. Source support node `2133`개 중 coarsened line mesh에 남은 node는 `756`개이고 restrained DOF는 `2544`개다. `*ELASTICLINK`는 `1692` rows 중 line mesh에 양끝 endpoint가 모두 남은 row가 `0`개라 이 probe에서는 finite spring을 조립하지 않는다. 결과는 첫 load `0.5`부터 `ready=false`, residual `5.55e-03 N`, fixed-point relative increment `0.752`, max translation `25.79m`로 실패한다. 따라서 기존 component-base P-Delta path를 authored support로 단순 교체하는 것은 closure가 아니며, 다음 경계조건 단위는 `mgt_uncoarsened_boundary_global_equilibrium.json`의 authored support/finite spring global tangent를 nonlinear continuation/Newton 경로에 연결하는 것이다.
- `mgt_uncoarsened_boundary_pdelta_probe.json`은 그 다음 경계조건 단위를 실제로 태워본다. Rigid-link coarsening을 끈 uncoarsened model에서 authored support `7707` DOF와 finite elastic-link spring component `10152`개를 유지하고, source-thickness shell + frame geometric stiffness를 deformed nodal coordinates에서 재조립한다. Relaxation `1.0` 경로는 `0.05`, `0.1`, `0.2`, `0.25`, `0.3`, `0.35`, `0.36`, `0.37`, `0.38`, `0.39`, `0.4`, `0.41`, `0.42`, `0.43`, `0.44`, `0.45`를 accepted path state까지 승격했다. `0.45`의 best residual은 `3.08e-05 N`, best fixed-point relative increment는 `6.20e-05`, max translation은 `0.642m`다. 기존 main probe의 `0.455`는 residual `2.47e-05 N`으로 residual gate를 통과해도 best fixed-point relative increment가 `2.18e-04`로 `1e-4` gate를 넘어서 실패한다. 이어서 checkpoint/resume tooling을 추가해 accepted displacement vector와 node id를 `mgt-uncoarsened-boundary-pdelta-checkpoint.v1` NPZ로 저장/검증하고 exact seed로 재개할 수 있게 했다. 공식 `mgt_uncoarsened_boundary_pdelta_checkpoint_continuation.json`은 durable `0.45` checkpoint에서 relaxation `0.5` segmented continuation을 이어 `0.5406`까지 accepted frontier를 확장한다. 그 위에서는 seed alpha-scan globalization이 첫 fixed-point correction 방향의 alpha 후보를 remap하고, 필요한 경우 짧은 relaxed fixed-point 반복을 이어서 gate를 재검사한다. 이 방식으로 `0.540601`에서 `0.656`까지 추가 accepted됐고, `0.656`의 residual은 `1.10e-04 N`, fixed-point increment는 `5.18e-06`, max translation은 `0.900m`다. 공식 aggregate의 first failed load는 이제 `null`이지만 full-load `1.0`에는 미달한다. 이는 boundary tangent wiring과 globalization 인프라가 전진했지만, G1 closure에는 여전히 full-load consistent Newton/Jacobian, material tangent가 더 필요하다는 증거다.
- `mgt_direct_residual_newton_probe.json`은 이 claim boundary를 더 엄격하게 만든다. 새 probe는 `R(u, λ)=F_int(u)-λF_ext`를 직접 정의하고, service material tangent frame stiffness, frame geometric stiffness delta, shell/surface tangent, authored support restraints, finite elastic-link springs, reference-configuration external load vector를 같은 residual matrix에 넣는다. `0.656` checkpoint에서 fixed-point receipt residual은 `1.10e-04 N`이지만 solver regularization을 제거한 direct residual은 `4.28e4 N`이다. current tangent로 푼 Newton correction에 방향성 finite-difference residual-Jacobian alpha 후보를 섞으면 첫 step에서 `directional_residual_jacobian_l2` alpha 약 `0.0006626`이 configured alpha보다 나은 하강을 만들고, 6회 accepted step 후 residual은 `1.553959e4 N`까지 낮아진다. 3회차 이후 increment gate는 통과하지만 residual gate는 여전히 통과하지 못한다. Accepted-history secant subspace globalization은 alpha `0.5`에서 residual-only 후보 `1.550832e4 N`을 찾지만 relative increment가 `1.80e-03`이라 closure 후보가 아니다. 대신 gate-limited/finer alpha 후보를 6차례 승격해 residual을 `1.552967e4 N`까지 낮춘다. 이어 matrix-free consistent-Jacobian subspace는 현재 final state에서 최근 basis `1`과 `2` 후보를 pass마다 함께 평가하고 full residual/increment gate를 통과한 최선 후보를 8회 승격해 final residual을 `1.551637e4 N`까지 낮춘다. 이 loop는 `1e-5` minimum relative-improvement floor와 `stop_reason=max_promotions_exhausted`를 receipt에 기록하므로, 다음 확장은 단순 반복 횟수 증가가 아니라 residual Jacobian/arc-length/trust-region 모델 변경이어야 한다. 따라서 남은 G1 closure는 더 이상 checkpoint micro-step 문제가 아니라 regularization-free physical residual, consistent Jacobian, arc-length/trust-region globalization, material Newton을 동시에 닫는 문제다.
- 이후 attached-policy terminal gate는 별도 cross-check로 닫혔다. `mgt_g1_direct_residual_terminal_gate_report.json`은 `mgt_direct_residual_attached_policy_followup365_gate_replay_probe.json`의 final direct residual `1.7877e-06 N`, `mgt_equilibrium_newton_focused_followup375_structural_terminal_increment_gate_probe.json`의 terminal `relative_increment=0.0`, 그리고 `mgt_g1_followup362_365_attached_equilibrium_newton_gate_summary.json`의 summary를 대조해 residual+increment gate를 동시에 검증한다. 이로써 `direct_residual_newton_not_closed` blocker는 제거됐지만, G1은 full-load `1.0`/full-mesh nonlinear equilibrium, state-updated material Newton breadth, production ROCm/HIP residency가 남아 partial이다. `commercial_gap_ledger_status.json`은 이 terminal gate를 `direct_residual_terminal_gate_scope`로, 아직 partial인 shell-material budgeted CPU-diagnostic lane을 `shell_material_budgeted_lane_scope`로 따로 노출해 두 scope가 서로를 덮어쓰지 못하게 한다.
- A new opt-in matrix-free JVP probe-scale sweep can be enabled with `--matrix-free-jacobian-probe-scales`. The reduced smoke receipt `mgt_direct_residual_probe_scale_sweep_smoke.json` uses `[0.125,0.25]`, selects `0.25` for the available basis, but records `promotion_count=0` and `stop_reason=no_residual_descent`; it stays diagnostic-only and does not replace the official direct residual receipt or the ROCm/HIP closure lane.
- The matrix-free JVP path now also supports `--matrix-free-jacobian-difference-scheme central` in addition to the default forward difference. `mgt_direct_residual_matrix_free_central_jvp_smoke.json` records a stable plus/minus free-DOF set, plus residual `15542.064591564755 N`, minus residual `15544.63483889865 N`, and central JVP infinity norm `42792.44427210307 N`; the matching forward comparison records JVP infinity norm `42794.61590178555 N`. Both one-pass smokes stop with `promotion_count=0`, `stop_reason=no_residual_descent`, and final direct residual `15543.349715249427 N`. This is useful JVP consistency coverage and counter-evidence against "finite-difference accuracy alone" as the G1 closure route; it is not closure evidence.
- The matrix-free basis selector now has an opt-in `--matrix-free-jacobian-basis-sources` control and per-direction `--matrix-free-jacobian-probe-max-step` cap. This lets the probe add the current regularized Newton correction as a measured JVP basis without applying the history-tuned scale to a much larger vector. Uncapped current-Newton evidence in `mgt_direct_residual_matrix_free_current_newton_basis_smoke.json` shows the problem: a `0.125` scale on a `0.8122932295227102 m` basis probes a `~0.1015 m` displacement and spikes the probe residual to `729713.957247828 N`. Capped evidence in `mgt_direct_residual_matrix_free_current_newton_capped_jvp_smoke.json` limits the probe step to `6.75e-05 m` and records `effective_probe_scale=8.309807043407448e-05`, but still stops with `promotion_count=0`, `stop_reason=no_residual_descent`, and final direct residual `15543.349715249427 N`. This narrows G1 again: current Newton direction can be included safely in JVP diagnostics, but it is not the missing consistent residual/Jacobian closure by itself.
- The same matrix-free subspace now supports `--matrix-free-jacobian-ridge-factors`, solving ridge-augmented basis least-squares with `lambda=factor*max(singular(J_basis),1)`. The focused current-Newton ridge smoke `mgt_direct_residual_matrix_free_current_newton_ridge_smoke.json` verifies the receipt path with factors `[0,1e-8,1e-6]`, but it is counter-evidence for this single-basis case: all factors keep coefficient `~4.052e-07`, best candidate residual `15543.349727522387 N`, `promotion_count=0`, and `stop_reason=no_residual_descent`. Ridge conditioning is now available for broader matrix-free bases, but it does not close G1 on the current Newton basis alone.
- The matrix-free subspace now also has opt-in signed-alpha globalization via `--matrix-free-jacobian-allow-negative-alphas`. Focused evidence in `mgt_direct_residual_matrix_free_current_newton_signed_alpha_smoke.json` shows that the capped current-Newton JVP direction is partly a line-search/sign issue: the best positive candidates still worsen, but negative alpha `-1.0` lowers direct residual from `15543.349715249427 N` to `15543.343432173222 N` while passing the increment gate (`3.6565e-07`). This is real but tiny progress (`0.006283076205363614 N`) and was run with `--matrix-free-jacobian-min-relative-improvement 0`; it remains far below the residual gate and should not be mistaken for G1 closure.
- A three-pass signed-alpha continuation smoke, `mgt_direct_residual_matrix_free_current_newton_signed_alpha_3pass_smoke.json`, confirms the negative-alpha signal is repeatable but still too small for closure. With ridge fixed to `0`, capped current-Newton JVP, signed alphas, and zero relative-improvement floor, negative alpha `-1.0` is selected three times and lowers direct residual `15543.349715249427 -> 15543.343432173222 -> 15543.330866082199 -> 15543.305733582718 N`. Relative improvements are only `4.04e-7`, `8.08e-7`, and `1.62e-6`, below the default `1e-5` governance floor and still many orders above the residual tolerance. This is a globalization clue for a stronger basis, not G1 closure.
- Matrix-free signed-alpha now has an opt-in `--matrix-free-jacobian-max-alpha` clamp so gate-limited candidates are not hard-coded to `1.0`. This is productive evidence: `mgt_direct_residual_matrix_free_current_newton_signed_alpha16_smoke.json` selects negative alpha `-16` and lowers residual by `0.10052923294642824 N`; `mgt_direct_residual_matrix_free_current_newton_signed_alpha32_floor_smoke.json` keeps the default `1e-5` relative-improvement floor and still promotes negative alpha `-32`, lowering residual to `15543.148656784975 N` with relative improvement `1.293533685695564e-05` and relative increment `1.1700705073541012e-05`. G1 remains open by orders of magnitude, but the alpha cap was a real matrix-free globalization bottleneck.
- A near gate-limited alpha smoke confirms the same pattern at larger step. `mgt_direct_residual_matrix_free_current_newton_signed_alpha256_floor_smoke.json` promotes negative alpha `-256`, lowers residual to `15541.741247508204 N`, and still passes the increment gate with relative increment `9.359797445238958e-05`; its gate-limited alpha is `259.8103627725543`. A checkpoint follow-up from that state, `mgt_direct_residual_matrix_free_current_newton_signed_alpha256_checkpoint_followup_smoke.json`, then promotes a new gate-limited negative alpha `-1.0075474192347846` and lowers residual again to `15540.108614387922 N`. This is repeatable current-Newton signed-alpha descent, but still CPU diagnostic and still far from the `5e-4 N` residual gate.
- A second checkpoint follow-up, `mgt_direct_residual_matrix_free_current_newton_signed_alpha256_checkpoint_followup_2_smoke.json`, shows the same single-basis path is not a complete continuation strategy. It finds a best negative candidate at alpha `-0.00390625`, lowering the candidate residual to `15540.095861749318 N`, but relative improvement is only `8.206273791589089e-07`, below the default `1e-5` floor, so no promotion is accepted and the durable frontier remains `15540.108614387922 N`. The next G1 unit should therefore combine signed alpha with a stronger measured basis/Jacobian rather than simply replay current-Newton single-basis continuation.
- A configurable active-set current-tangent residual-row correction was added behind opt-in CLI controls: `--current-tangent-residual-row-target-counts`, `--current-tangent-residual-row-support-column-counts`, and `--current-tangent-residual-row-support-expansion-depth`. Focused evidence in `mgt_direct_residual_row_active_set_smoke.json` now uses a zero-trust path (`max_trust_iterations=0`) and applies row-active correction directly to the base direct residual. With `[2]` target rows, `[8]` support columns, depth `0`, and two promotions, it reduces direct residual `42754.66805918372 -> 17540.09324314082 -> 15553.630703419272 N` while passing the increment gate. The same receipt now writes history-bearing durable checkpoint `mgt_direct_residual_row_active_set_zero_trust_final_checkpoint.npz`, and `mgt_direct_residual_row_active_set_checkpoint_replay.json` verifies that replaying the checkpoint with trust/secant/matrix-free/row corrections disabled starts at the same direct residual `15553.630703419272 N` with `accepted_state_history_count=3`. A checkpointed row-continuation receipt, `mgt_direct_residual_checkpoint_row_continuation.json`, resumes from that state and applies four more row-active promotions, lowering direct residual further to `13698.533336101626 N`; `mgt_direct_residual_checkpoint_row_continuation_replay.json` verifies replay from the `accepted_history_count=7` continuation checkpoint. This does not close G1 because the residual gate remains orders of magnitude above tolerance, but it is useful frontier evidence: row-active correction is a strong early residual descent mechanism and should be composed from the checkpointed state with a stronger consistent Jacobian/globalization model. A longer secant/matrix-free history-composition attempt timed out at `180s`, so accepted-history replay alone is not promoted as closure evidence.
- A directional row-alpha extension was added behind `--current-tangent-residual-row-directional-probe-alpha`. The focused `mgt_direct_residual_row_directional_alpha_probe.json` receipt resumes from the `13698.533336101626 N` checkpoint, probes actual residual change along the row-active correction direction at alpha `0.03125`, and lowers the durable frontier to `12855.687023192413 N` after two more row-active promotions; `mgt_direct_residual_row_directional_alpha_probe_replay.json` verifies replay from the `accepted_history_count=9` checkpoint. This still does not close G1. The new directional alpha candidates proposed near-`1.0` values, but the accepted best rows remained fixed-alpha `0.03125`, so this receipt is both progress and counter-evidence: scalar row-direction alpha fitting is not yet the missing consistent residual/Jacobian closure.
- Row-active fixed alpha candidates are now configurable through `--current-tangent-residual-row-alpha-values`. This exposed useful but state-dependent globalization evidence: `[0.0625,0.03125,0.015625]` lowers the frontier `12855.687023192413 -> 10426.970626342814 N`, `[0.125,0.0625,0.03125,0.015625]` lowers it further to `8424.675514897524 N`, and an aggressive `[0.25,0.125,0.0625,0.03125,0.015625]` two-pass probe accepts only one smaller-alpha promotion to `8330.920342893796 N`. Replays verify each durable checkpoint, with the latest `mgt_direct_residual_row_aggressive_alpha_probe_replay.json` starting from `8330.920342893796 N` and `accepted_history_count=18`. This is progress, not closure: the residual gate remains orders of magnitude above tolerance, and the alpha sweep shows row-active globalization needs state-aware control plus a real consistent Jacobian/model rather than ever-larger scalar alpha guesses.
- Fine row-alpha continuation from `8330.920342893796 N` was also tested. The full four-pass `[0.03125,0.015625,0.0078125,0.00390625]` run exceeded the `180s` diagnostic timeout, but bounded `mgt_direct_residual_row_fine_alpha_smoke.json` completes in two passes and lowers the replay-verified frontier to `8261.85717314353 N` with `accepted_history_count=20`. The accepted alphas are `0.00390625` and `0.0078125`, so the frontier still moves, but the improvement is much smaller. This keeps G1 partial and supports the current diagnosis: scalar row-active continuation is now a diminishing-return diagnostic, not a product-grade nonlinear closure.
- Expanded row-active support improves the G1 frontier more than further fine-alpha polishing. `mgt_direct_residual_row_expanded_support_smoke.json` uses `target_row_count=4` and `support_column_count=16` from the `8261.85717314353 N` checkpoint and lowers residual to `8003.652871020366 N` in one promotion. `mgt_direct_residual_row_expanded_support_continuation.json` accepts two more expanded-support promotions and lowers the replay-verified frontier to `7511.203920880909 N` with `accepted_history_count=23`. This remains partial and CPU diagnostic only, but it sharpens the next implementation direction: richer active subspaces are useful; product closure still requires consistent physical Jacobian/globalization rather than claiming residual-row least-squares as nonlinear convergence.
- Follow-up expanded-support evidence shows the current row-active route is narrowing. Repeating `[4]/[16]` from `7511.203920880909 N` produces no residual descent (`mgt_direct_residual_row_expanded_support_continuation_2.json`), while widening to `[8]` target rows with `[16]` support columns accepts one small-alpha promotion to `7452.444439515273 N` (`mgt_direct_residual_row_wider_support_smoke.json`, replay verified with `accepted_history_count=24`). This is the latest G1 frontier and still far from the residual gate; it is evidence for richer active subspaces, not a closure claim.
- Active-subspace sweep evidence further narrows the useful row-correction route. `mgt_direct_residual_row_active_subspace_sweep.json` tests `[8,12]` target rows against `[16,24]` support columns from `7452.444439515273 N`; `[8]/[16]` wins and lowers the frontier to `7219.400801402895 N`, while larger support or more target rows do not improve. One more `[8]/[16]` continuation lowers the replay-verified frontier to `7106.522708708845 N` with `accepted_history_count=26`. G1 remains partial and CPU diagnostic only; the evidence now points to adaptive active-subspace/Jacobian construction rather than simply expanding every row/support knob.
- Residual-weighted support selection was added as an opt-in row-active support strategy (`--current-tangent-residual-row-support-selection residual_weighted`). From the `7106.522708708845 N` frontier, both `[8]/[16]` residual-weighted support (`support_size=128`) and compact `[8]/[8]` residual-weighted support (`support_size=64`) fail with `no_residual_descent`; a same-state `row_strongest` follow-up also fails. This keeps G1 open and shows the current row-active least-squares family is locally exhausted at the latest frontier. Support scoring heuristics are not enough for product closure.
- Node-block support expansion was added behind `--current-tangent-residual-row-node-block-support` to make support columns same-node/6DOF aware. From the same `7106.522708708845 N` frontier, `[8]/[16]` node-block support expands from `52` to `120` support columns and fails with `no_residual_descent`; `[4]/[16]` expands from `37` to `90` and also fails. This keeps G1 partial and shows that same-node block widening alone is not a consistent Jacobian or block-element solve.
- Residual-node target selection was added behind `--current-tangent-residual-row-target-mode residual_node_blocks`. Unlike node-block support expansion, this changes the target equations: high-residual structural nodes are selected and all free 6DOF residual rows on those nodes are solved as a block. This directly re-tests the older distributed-node/orthogonal-projection correction idea under the same direct residual gate. The result is counter-evidence: `mgt_direct_residual_row_node_target_smoke.json` from the `7106.522708708845 N` frontier targets one/two high-residual nodes and worsens the best candidate to `7119.692693978816 N`; `mgt_direct_residual_row_node_target_fine_alpha_smoke.json` lowers alpha to `0.0009765625` and still worsens to `7108.168956867663 N`. G1 remains partial; node-distributed projection may remain a preconditioner/coarse-basis ingredient, but it is not the nonlinear residual-closure engine.
- Finite-difference residual-row Jacobian mode was added behind `--current-tangent-residual-row-jacobian-mode finite_difference`. This replaces the row/support tangent slice with measured physical residual JVP columns before solving the local least-squares correction. A wider `[4]/[8]` smoke is counter-evidence (`8884.487342922255 N`, worse than the `7106.522708708845 N` start), but compact `[2]/[4]` evidence lowers the durable frontier to `7092.642781544098 N` and `mgt_direct_residual_row_fd_jacobian_compact_smoke_replay.json` verifies replay. This is still CPU diagnostic only and far above tolerance, but it is meaningful G1 direction evidence: actual residual Jacobian action can move the late frontier after current-tangent row/support heuristics stall.
- A global directional alpha follow-up was added for row correction. The probe now records a full-residual L2 alpha and, if that alpha is negative, evaluates the reverse signed direction as a trust candidate. Repeating compact `[2]/[4]` FD-Jacobian correction from `7092.642781544098 N` is exhausted: the plain continuation worsens best candidate to `7133.944505406227 N`; the global-alpha run records raw alpha `-0.055637562526651044`, evaluates a reverse candidate, and that reverse candidate worsens to `8004.657412096502 N`. The latest G1 frontier remains `7092.642781544098 N`; continuation needs broader measured-Jacobian subspace selection rather than repeating the same tiny active block.
- Finite-difference residual JVP columns are now cached within each row-correction pass. This makes broader active measured-Jacobian sweeps feasible because repeated support columns are reused across target/support candidates. `mgt_direct_residual_row_fd_jacobian_cached_sweep.json` sweeps `[1,2,4]` target rows and `[4,8]` support counts from `7092.642781544098 N`, accepts `[4]/[4]`, and lowers the replay-verified frontier to `7078.789963612223 N`. `mgt_direct_residual_row_fd_jacobian_cached_sweep_continuation.json` repeats the cached active-set refresh from that checkpoint and lowers the replay-verified frontier to `7071.877082788062 N`. G1 remains partial and CPU diagnostic only, but measured residual-Jacobian action is now repeatable when the active set is refreshed rather than fixed.
- Fixed-pattern continuation is not sufficient. `mgt_direct_residual_row_fd_jacobian_winning_pattern_continuation.json` repeats only the latest `[4]/[4]` FD-Jacobian pattern from `7071.877082788062 N` and stops with `no_residual_descent`; best candidate worsens to `7216.035042010209 N`. The latest frontier remains `7071.877082788062 N`, and the next G1 work should refresh/broaden the measured-Jacobian active set instead of replaying one winning block.
- A refreshed broader target sweep moves the frontier again. `mgt_direct_residual_row_fd_jacobian_refreshed_target_sweep.json` tests `[4,8]` target rows with `[4,8]` support counts and FD cap `12` from `7071.877082788062 N`; `[8]/[8]` lowers the replay-verified frontier to `7064.970952826998 N`. Repeating the same refreshed sweep from that checkpoint is exhausted (`no_residual_descent`, best worsens to `7094.058778498981 N`). Latest G1 frontier is `7064.970952826998 N`; this remains CPU diagnostic, but it confirms active-set refresh/broader measured-Jacobian subspaces are necessary for further progress.
- A still broader measured-Jacobian sweep continues the descent. `mgt_direct_residual_row_fd_jacobian_broader_target_sweep.json` expands to `[8,12]` target rows, `[8,12]` support counts, and FD cap `16` from `7064.970952826998 N`; `[12]/[8]` lowers the replay-verified frontier to `7051.172181441225 N`. Latest G1 frontier is `7051.172181441225 N`. G1 remains partial and CPU diagnostic only, but the measured residual-Jacobian active-subspace route remains productive as the target/support family broadens.
- `mgt_direct_residual_row_fd_jacobian_wider_target_sweep.json` expands again to `[12,16]` target rows, `[8,12]` support counts, and FD cap `20` from `7051.172181441225 N`; `[12]/[8]` lowers the replay-verified frontier to `7037.400360781316 N`. Latest G1 frontier is `7037.400360781316 N`. This is still not closure, but it confirms the measured residual-Jacobian active-subspace route is not exhausted at the current breadth.
- `mgt_direct_residual_row_fd_jacobian_very_wide_target_sweep.json` expands to `[16,24]` target rows, `[8,12]` support counts, and FD cap `28` from `7037.400360781316 N`; `[16]/[8]` lowers the replay-verified frontier to `7023.655438207834 N`. The `[24]` target candidates are unstable and worsen to `1748394.335228357 N`, so target widening needs conditioning/governance. Latest G1 frontier is `7023.655438207834 N`.
- Ridge-damped local row least-squares was added behind `--current-tangent-residual-row-ridge-factor`. With ridge factor `1e-6`, `mgt_direct_residual_row_fd_jacobian_very_wide_ridge_sweep.json` reduces the unstable `[24]` candidate from `1748394.335228357 N` to `195251.9798241862 N`, but still does not make it useful; the best `[16]/[8]` ridge candidate essentially matches but does not beat the unregularized frontier. Latest G1 frontier remains `7023.655438207834 N`.
- `mgt_direct_residual_row_fd_jacobian_stable_target_broader_support_sweep.json` keeps the target count at `16` but widens the support search to `[16,20]` with FD cap `36` from the `7023.655438207834 N` checkpoint. The winning `[16]/[16]` candidate lowers the replay-verified frontier to `7009.937361185119 N`; `[16]/[20]` reuses the same `36` FD columns and matches that best value. G1 remains partial and CPU diagnostic only; this is another measured-JVP descent step, not commercial nonlinear closure.
- `mgt_direct_residual_row_fd_jacobian_stable_target_widest_support_sweep.json` widens the same stable target family again to support counts `[24,32]` with FD cap `48` from `7009.937361185119 N`. The winning `[16]/[24]` candidate lowers the replay-verified frontier to `6996.246077280766 N`, while `[16]/[32]` is pure cache reuse of the same `48` FD columns. This took `364.8887116219994s` of FD column assembly for `13.691283904352531 N` improvement, so G1 remains partial and the next product path should move beyond serial row polishing.
- `mgt_direct_residual_secant_family_frontier_probe.json` adds an opt-in windowed/ridge secant-family globalization layer to reuse accepted residual-correction history as a Newton candidate family. From the `6996.246077280766 N` checkpoint, recent secant windows `[4,8,16,32]` and ridge factors `[0,1e-8]` find a `window=4`, `ridge=0`, `alpha=1.0` candidate that lowers the replay-verified frontier to `6949.9113895643495 N` while passing the increment gate (`3.02e-07`). The follow-up `mgt_direct_residual_secant_family_window4_continuation.json` records only a `2.75e-4 N` best improvement and blocks promotion under the `1e-5` relative-improvement floor. This is useful evidence that older projection/history correction can be a gated subspace proposal, but it is still CPU diagnostic and does not close G1.
- `mgt_direct_residual_row_fd_jacobian_svd_truncation_smoke.json` adds truncated-SVD governance to the measured residual-row finite-difference Jacobian solve. From the replay-verified secant-family frontier `6949.9113895643495 N`, a one-pass `[16,24]` target / `[8,12]` support smoke with FD cap `28` and condition cap `1e6` lowers the durable frontier to `6936.337343886427 N`; replay verifies the checkpoint. The `[24]` target candidates drop one singular direction and avoid the earlier million-Newton blow-up, but their best residual is only `6947.0741907554575 N`, while `[16]/[8]` still wins. This is better numerical governance for large active measured-JVP blocks, not G1 closure: the run takes `436.33s`, remains CPU diagnostic, and is still far above the `5e-4 N` residual gate.
- `mgt_direct_residual_row_element_block_target_smoke.json` adds an element-connected target mode to the same measured residual-row path. `--current-tangent-residual-row-target-mode residual_element_blocks` selects high-residual mesh elements and targets every free DOF row on their connected nodes, so the correction is no longer only scalar-row or distributed-node projection. From the replay-verified `6936.337343886427 N` checkpoint, the two-element candidate (`21535`, `20846`) targets `36` rows, uses `16` measured FD residual-JVP columns plus truncated-SVD conditioning, and lowers the replay-verified frontier to `6931.641138710459 N`; the one-element candidate is counter-evidence and worsens badly. This is useful progress toward element/block residual equations, but it is still CPU diagnostic and still far above the residual gate, so G1 remains partial.
- `mgt_direct_residual_row_element_patch_target_smoke.json` adds an opt-in shared-node element neighbor expansion for that element target mode through `--current-tangent-residual-row-element-neighbor-depth`. From the `6931.641138710459 N` checkpoint, a one-seed/one-neighbor-layer patch expands to `21` elements and `114` target rows, but with the same `16` measured FD columns the best candidate worsens to `6931.882795083492 N`; no promotion is accepted and replay keeps the checkpoint at `6931.641138710459 N`. This rules out naive broad element patches under a fixed small measured-column budget; compact element blocks are useful, but closure needs better sparse/consistent Jacobian coverage or GPU preconditioned Newton/Krylov.
- Follow-up FD32 probes now rule out the simplest "just add more measured columns" explanation. `mgt_direct_residual_row_element_patch_fd32_smoke.json` reruns the `21`-element / `114`-row patch with `32` finite-difference support columns and still worsens to `6931.900944921984 N`. `mgt_direct_residual_row_element_block_fd32_followup_smoke.json` keeps the compact two-element / `36`-row target but widens the measured support to `32` columns and worsens much more to `7225.620217042061 N`. G1 remains partial; the next useful unit is no longer local FD column widening, but a sparse/consistent residual Jacobian or a physically coupled Newton/Krylov operator.
- A first global matrix-free residual-JVP Krylov bridge was added and measured as CPU-diagnostic evidence. `--enable-matrix-free-global-krylov` builds an opt-in GMRES `LinearOperator` from full direct-residual finite-difference JVP matvecs and then line-searches the resulting correction through the same physical residual/increment gates. From the replay-verified `6931.641138710459 N` element-block frontier, `mgt_direct_residual_global_matrix_free_krylov_smoke.json` uses `2` Krylov iterations (`3` JVP matvecs, stable free DOF set) but finds no descent: best candidate is `6931.7024565551865 N`, final remains `6931.641138710459 N`, and `matrix_free_global_krylov.stop_reason=no_residual_descent`. The JVP probes are badly scaled (`7.0e6` to `3.1e8 N` probe residuals for `1e-6 m` steps), so naive global FD-GMRES is not yet the missing consistent Jacobian. G1 still needs a better scaled/sparse residual Jacobian, trust/arc-length globalization, or GPU-preconditioned Newton/Krylov operator.
- The global Krylov bridge now also supports diagonal residual/displacement scaling and signed alpha replay. `mgt_direct_residual_global_matrix_free_scaled_krylov_smoke.json` solves `D_r J D_u y=-D_r R` with `D_u=1e-4 m`; this removes most of the unscaled overshoot but still gives no positive-alpha descent (`6931.641139346858 N`, only `6.36e-7 N` worse than base). `mgt_direct_residual_global_matrix_free_scaled_signed_krylov_smoke.json` then allows negative alpha and finds a tiny replayed descent to `6931.641118344325 N` at alpha `-1.0`, with relative improvement `2.94e-9`. This is a useful direction/sign/scaling signal, not closure: it required a zero improvement floor, moves only `2.04e-5 N`, and remains far above the `5e-4 N` residual gate. The next G1 unit should retain nondimensionalized global residual-JVP Krylov but add real preconditioning or stronger globalization.
- The global Krylov bridge now has a current-tangent right-preconditioned mode. `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_smoke.json` solves each Krylov direction through the current regularized tangent before full residual-JVP replay, using `preconditioner_mode=current_tangent`, `3` JVP matvecs, and `4` tangent solves. From the same `6931.641138710459 N` frontier it accepts alpha `1.0` and lowers the replayed residual to `6931.640219069869 N`, with relative increment `2.91e-06` and improvement `9.20e-04 N`. This is about `45x` larger than the scaled signed micro-descent, so the useful route is whole-system residual-JVP Krylov with tangent/preconditioned coordinates. It is still not closure: the smoke used a zero relative-improvement floor and the observed relative improvement `1.33e-7` is below the default `1e-6` governance floor, residual gate remains far above `5e-4 N`, the receipt is CPU diagnostic only, and the product path needs the same residual/Jacobian/preconditioned Krylov idea on the official ROCm/HIP lane or an equivalent consistent Newton/globalization operator.
- The global Krylov alpha clamp was then exposed as `--matrix-free-global-krylov-max-alpha`, preserving legacy `1.0` by default. With the same current-tangent right preconditioner, `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_floor_smoke.json` uses max alpha `8` and the default `1e-6` relative-improvement floor; alpha `8.0` is gate eligible and lowers `6931.641138710459 -> 6931.632714519975 N` with relative improvement `1.22e-6` and relative increment `5.62e-5`. Three checkpoint follow-ups continue the same route: `followup` lowers to `6931.610704092173 N` with alpha `-8.0`, `followup_2` lowers to `6931.566973287116 N` with alpha `-2.0`, and `followup_3` lowers to `6931.557151848129 N` with alpha `-0.25` and relative improvement `1.42e-6`. `followup_4` then finds the boundary: from `6931.557151848129 N`, the best candidate worsens to `6931.562985874174 N` and the receipt records `matrix_free_global_krylov_stop_reason=no_residual_descent`. This is real G1 progress under the CPU-diagnostic governance floor, but same-operator alpha8 continuation has saturated and still is not product closure. Residual gate remains orders of magnitude open and official commercial closure still requires AMD ROCm/HIP or equivalent production-lane nonlinear residual/Jacobian evidence.
- A broader Krylov basis retry also rules out the simplest adaptive fix. `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_broader_basis_smoke.json` resumes from the same `6931.557151848129 N` checkpoint, raises the global Krylov restart/basis width from `3` to `6`, and performs `7` JVP matvecs plus `8` current-tangent preconditioner solves. The best candidate still worsens, now to `6931.573327681425 N`, and the receipt remains `no_residual_descent`. Therefore G1 should not just widen this one-pass preconditioned GMRES basis; the next unit needs a changed/adaptive basis source, different preconditioner/globalization, or production-lane Newton/Jacobian assembly.
- Current-tangent preconditioner regularization sensitivity is now measured at the same checkpoint. Lowering the tangent regularization factor from `1e-8` to `1e-9` worsens (`mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_reg1e9_smoke.json`: best `6931.578307076875 N`, no descent). Raising it to `1e-7` reopens descent: `mgt_direct_residual_global_matrix_free_tangent_preconditioned_krylov_alpha8_reg1e7_smoke.json` finds a raw best residual `6931.538073710218 N` at alpha `4.0`, but that row misses the increment gate; the gate-limited alpha `3.294677571546404` is promoted and lowers the durable frontier to `6931.541434355825 N`. A direct follow-up at the same `1e-7` setting finds only sub-floor descent (`6931.539418181269 N`, relative improvement `2.91e-7`) and is not promoted. Increasing regularization from that same frontier shows the active axis: `3e-7` improves the best candidate to `6931.536916621216 N` but still misses the `1e-6` floor (`6.52e-7`), while `1e-6` passes the floor (`4.74e-6`) and promotes the durable frontier to `6931.50857647534 N`. Three same-factor checkpoint follow-ups continue the descent to `6931.46276643071 N`, `6931.432267962403 N`, and `6931.422474165393 N`, all above the governance floor and increment-gate eligible. The next same-factor follow-up then hits `no_residual_descent` (`best=6931.433329472165 N`), and stronger `3e-6` regularization is also counter-evidence (`best=6931.477813298451 N`). This confirms preconditioner quality/globalization is the live G1 axis after same-operator alpha8 repetition and plain basis widening saturated, but the current factor ladder has a measured boundary. It is still CPU diagnostic and far above the residual gate.
- The manual regularization continuation is now represented by an adaptive controller receipt. `mgt_direct_residual_adaptive_preconditioned_global_newton_smoke.json` resumes from the `6931.422474165393 N` checkpoint, tries factors `[1e-6, 3e-6]`, records `promotion_count=0`, and stops with `no_regularization_factor_promoted`. This is not closure, but it is the correct productionization direction: factor rows and boundary reasons are captured in one controller receipt instead of relying on ad hoc single-factor smokes.
- The controller can now use secant-family history as a seed generator before the global Krylov replay. `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_smoke.json` starts from the same `6931.422474165393 N` frontier, enables secant-family seed windows `[4,8,16,32]`, and then runs the same preconditioned global Krylov gate. It promotes one controller step and lowers the frontier to `6931.387293436975 N`; both secant-family and matrix-free global Krylov components are accepted. This supports the improvement-plan rule that residual-correction history is useful as a seed, not as standalone closure.
- A checkpoint follow-up confirms repeatability: `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_followup_smoke.json` resumes from `6931.387293436975 N`, again accepts both secant-family seed and global Krylov replay, and lowers the frontier to `6931.346282307703 N`. This remains CPU diagnostic and takes about `650 s` for one controller step, so the next implementation unit should add runtime budget controls or move the same controller concept toward the production backend.
- Runtime budget governance is now part of the controller. `mgt_direct_residual_adaptive_preconditioned_global_newton_runtime_budget_smoke.json` proves that `--max-controller-runtime-seconds` blocks new child launches once the controller budget is exhausted, records `runtime_budget_exceeded=true`, and leaves `promotion_count=0` with no child rows. The current child probe is still in-process, so this is a launch-boundary and receipt-control feature rather than mid-child cancellation; broad continuation sweeps should use this budgeted controller receipt before any further long CPU-diagnostic exploration.
- A budgeted follow-up continues the same G1 route: `mgt_direct_residual_adaptive_preconditioned_global_newton_secant_seed_followup_2_smoke.json` resumes from `6931.346282307703 N` with a `720 s` controller budget and finishes in about `670 s` without exceeding the budget. It promotes one more step to `6931.331952798518 N`; this time secant-family seed is enabled but not accepted, while matrix-free global Krylov is accepted. This is valid controller progress but also shows tapering, not closure.
- The shell-material row-correction continuation moved one more bounded checkpoint. `mgt_shell_material_rowcorr_budget_controller_followup395_multistep_target4_support4.json` resumes from the followup394 compact checkpoint, completes within a `300 s` controller budget, and promotes the frontier from `6.908082594771521 N` to `6.486346878826488 N` with `target_row_count=4`, `support_column_count=4`, and four child row promotions. The residual remains about `1.30e4x` above the `5e-4 N` direct-residual gate, so this is CPU-diagnostic descent evidence only; G1 still needs a stronger consistent residual/Jacobian/Newton path and production ROCm/HIP closure before commercial promotion.
- On the official ROCm/HIP G9 Schur lane, signed projected Schur-basis modes were added and tested. `mgt_dof_block_schur_signed_basis_shell_smoke.json` shows the current `rhs_weighted` shell basis remains best at `12.466549536358821 N`; signed variants are worse (`13.18924047920264 N` and `16.248662930527985 N`). `mgt_dof_block_schur_signed_basis_coupled_smoke.json` likewise keeps `rhs_weighted` best at `4.37671406620138 N`; signed variants reach `6.847400559043649 N` and `4.8103591251878015 N`. This rules out residual-sign aggregate weights as the missing G9 preconditioner. G9 still needs stronger Schur/AMG/domain-decomposition backend quality.
- The same G9 Schur lane now has an aggregate-count sweep for the current `rhs_weighted` basis. `mgt_dof_block_schur_rhs_weighted_schur_aggregate_sweep_shell.json` and `mgt_dof_block_schur_rhs_weighted_schur_aggregate_sweep_coupled.json` both keep aggregate count `8` best. Shell residuals for `[4,8,16,32]` are `12.639024094175015`, `12.466549536358821`, `12.772640011564716`, and `13.350879149999306 N`; coupled residuals are `6.7092752223624075`, `4.37671406620138`, `6.204393286835398`, and `6.36603252400505 N`. This rules out simple low-rank Schur basis width tuning as G9 closure.
- G9 coarse-operator diagnostics now explicitly record that node-block coarse correction uses a Galerkin projected operator, `P^T A P`, not rediscretized coarse stiffness. `mgt_dof_block_schur_residual_region_diagnostic_shell_smoke.json` records `node_block_coarse_operator=galerkin_ptap` for the restricted enriched interface basis and decomposes the replay residual by support region. The best row is still partial (`23.223669181750267 N` in this bounded smoke), but the residual concentration is diagnostic: primary interface/coarse support contains `65.6%` of the top-64 absolute residual rows and reaches `94.2%` of the global residual infinity norm, while translation DOFs contain `100%` of the top-64 residual rows and rotation DOFs reach only `27.2%` of the global infinity norm. This confirms the next G9 lever is not another load-partition ratio inside the same basis; it should change the interface/translation operator or basis space itself, such as energy-minimizing/GENEO-style local modes or interface p-refinement.
- The node-block coarse domain partitioner now has an RHS-hotspot connected BFS option, `rhs_graph_bfs`, but focused ROCm receipts are counter-evidence. Shell worsens to `15.392137365307763 N` versus the high-degree graph-BFS frontier `12.466549536358821 N`; coupled worsens to `4.65869423252434 N` versus `4.37671406620138 N`. This rules out RHS-only aggregate seeding as the missing domain-decomposition step.
- The same node-block coarse operator now has overlap-depth support for connected graph-BFS aggregates, but `mgt_dof_block_schur_overlap_graph_bfs_shell_probe.json` and `mgt_dof_block_schur_overlap_graph_bfs_coupled_probe.json` are also counter-evidence. A one-hop halo worsens shell to `15.278792855657246 N` and coupled to `5.055470397196352 N`, despite adding `3365` and `3015` overlap nodes. Simple halo-overlapped rigid-body coarse columns are not the missing G9 interface treatment.
- The focused ROCm/HIP block-Schur route now exposes and sweeps the internal coupling-hotspot dense correction under the current best Schur/coarse settings. This moves the focused preconditioner frontier, while not replacing the full official sparse-probe frontier: shell improves from `12.466549536358821 N` to `12.099760245908454 N` with size `64` / `rhs_residual`, and coupled improves from `4.37671406620138 N` to `3.699645345812989 N` with size `64` / `coupling_strength`. Size sweeps over `[32,64,96,128]` keep `64` best for both.
- The same focused Schur route now has an opt-in post-interface hotspot polish after the projected Schur basis, node coarse correction, and node smoother. This is family-specific evidence: `mgt_dof_block_schur_coupling_hotspot_postpass_shell_probe.json` improves shell from `12.099760245908454 N` to `11.09282697775784 N` with one post pass, but `mgt_dof_block_schur_coupling_hotspot_postpass_coupled_probe.json` shows coupled post-pass rows worsen to about `5.27 N` and the best remains the no-post-pass `3.699645345812989 N` row. G9 still misses the shell `1e-3 N` and coupled `5e-2 N` gates.
- The post-interface hotspot subspace can now be selected independently from the pre-hotspot subspace, but coupled evidence remains counter-evidence. With pre-hotspot fixed at size `64` / `coupling_strength`, `mgt_dof_block_schur_coupling_hotspot_split_post_selection_coupled_probe.json` reaches `5.273263330318059 N` for post `coupling_strength`, `8.041777475205777 N` for post `rhs_residual`, and `5.285900152662634 N` for post `mixed`, all worse than the no-post baseline `3.699645345812989 N`. This closes the simple post-local interface-polish branch for coupled; useful coupled work needs a different Schur/coarse operator rather than another final hotspot solve.
- A new coupling-pair smoother changes the main Schur preconditioner action by applying batched ROCm `2x2` translation-rotation pair solves selected from strong `K_tr` coupling entries. It gives only a small shell improvement and is coupled counter-evidence. `mgt_dof_block_schur_coupling_pair_smoother_shell_probe.json` lowers shell from `11.09282697775784 N` to `11.018219447125434 N` at `128` pairs / weight `0.1`. `mgt_dof_block_schur_coupling_pair_smoother_coupled_probe.json` keeps no-pair best at `3.699645345812989 N`; tested pair rows worsen to `4.027417777564491 N` through `5.09044963551105 N` for `64` pairs and `4.961935669314357 N` / `4.778060260239954 N` for `128` pairs. G9 remains open.
- `mgt_surface_membrane_tangent.json`은 MGT `PLATE`/surface code2 요소 `7152`개를 constant-strain membrane triangle `14120`개로 triangulate하여 `active_membrane_dof_count=19897`, `stiffness_nnz=475416`, source plate thickness coverage `100%`, smoke solve residual `1.38e-06`을 기록한다.
- Surface membrane evidence는 plate/shell rows를 전부 unsupported로 남겨두던 상태와 deterministic fallback thickness 경계를 줄였지만, `surface_shell_full_bending_tangent_ready=false`라서 bending/drilling/pressure/coupled frame-shell/full shell closure로 보지 않는다.
- `mgt_surface_shell_bending_tangent.json`은 같은 PLATE rows에 MGT `*THICKNESS` row 기반 두께를 `100%` 적용하고, Mindlin-type CST bending/shear와 drilling regularization을 조립해 `active_shell_dof_count=35935`, `stiffness_nnz=1901664`, transverse pressure smoke residual `4.26e-07`, relative residual `1.31e-08`을 기록한다.
- `mgt_shell_calibration_benchmarks.json`은 membrane constant-strain, membrane rigid-body, shell transverse shear, shell zero-shear rotation, clamped square plate uniform pressure benchmark를 `5/5` ready로 기록한다. Clamped square plate 중앙 처짐은 고전 해석식 대비 relative error `0.0763`이다.
- Shell bending/drilling smoke solve와 calibration benchmark는 source thickness promotion과 bending/drilling unsupported 상태를 줄였고 current-source opening no-op semantics도 명시됐지만, drilling은 regularization이므로 generic opening cutout, non-default surface LCAXIS breadth, higher-order shell breadth, coupled full-load shell claim은 아직 아니다.
- `mgt_coupled_frame_surface_sparse_equilibrium.json`은 line/frame과 PLATE membrane stiffness를 같은 6DOF node space에 조립해 `dof_count=68130`, `active_dof_count=32131`, `free_dof_count=26623`, `coupled_stiffness_nnz=437000`인 coupled sparse smoke solve를 생성한다.
- Coupled solve는 frame gravity를 `0.01` scale로 둔 tangent smoke이고 residual `2.02e-04`, relative residual `9.03e-09`를 기록한다. 이는 frame-shell same-node membrane coupling evidence지만 full-load Newton, shell bending/drilling, pressure load, material nonlinear closure는 아니다.
- `mgt_coupled_frame_shell_sparse_equilibrium.json`은 line/frame과 source-thickness PLATE shell bending/shear/drilling stiffness를 같은 6DOF node space에 조립해 `dof_count=68130`, `active_dof_count=46027`, `free_dof_count=39859`, residual `5.23e-08`, relative residual `2.34e-12`인 coupled sparse smoke solve를 생성한다.
- Coupled frame-shell solve는 same-node tangent coupling evidence를 membrane에서 bending/drilling까지 넓혔고 frame `ANGLE` roll도 frame assembly를 통해 소비하지만, frame gravity는 여전히 `0.01` smoke scale이고 generic opening cutout, non-default surface LCAXIS, full-load Newton, material nonlinear closure는 아니다.
- `mgt_beam_offset_support_receipt.json`은 실제 MGT `*OFFSET` block의 beam end offset row `708`개를 typed runtime metadata로 파싱하고, offset element reference가 `*ELEMENT` id와 모두 매칭됨을 기록한다. 출처는 bridge MGT SHA256으로 남기며 max absolute offset은 `0.523m`다.
- `mgt_full_frame_6dof_sparse_equilibrium.json`은 이 GLOBAL beam end offset `708`개를 6DOF frame stiffness와 gravity eccentricity load에 rigid-end transformation으로 적용한다. offset 적용 후에도 elastic residual `7.10e-06 N`, linearized geometric residual `3.72e-04 N`, deformed P-Delta `load_scale=0.5`, iteration `18`로 ready 상태다.
- Coupled frame-surface/frame-shell artifacts도 같은 offset-aware frame assembly를 통해 GLOBAL beam offsets를 소비한다. 이 offset evidence는 frame/coupled elastic-geometric tangent gap을 줄였지만, ELEMENT local-yz offset, roundtrip export, non-frame shell/surface eccentricity semantics는 아직 닫히지 않았다.
- `mgt_boundary_entity_support_receipt.json`은 실제 MGT `*CONSTRAINT` row `8`개를 typed support restraint entity로 파싱하고 distinct support node `2133`개가 모두 `*NODE`와 매칭됨을 기록한다. restraint code는 `111000` 1 row, `111111` 7 rows다.
- 같은 receipt는 `*ELASTICLINK` `1692`개 GEN link를 node-pair/stiffness typed entity로 파싱하고, link node ref `3384`개가 모두 `*NODE`와 매칭됨을 기록한다. 기존 roundtrip parser의 rigid-like elastic-link coarsening도 `1692/1692` 적용 상태로 확인된다.
- `*STORY-ECCEN`도 typed setting으로 파싱되어 seismic eccentricity include `YES`, wind include `NO`, eccentricity percent `5/15`가 기록된다. 단 current sparse solver harness는 authored support mask 대신 component base-node auto-restraint를 쓰고, finite elastic link spring과 story eccentricity load generation은 아직 tangent/load path에 조립하지 않는다.
- `mgt_boundary_spring_tangent_receipt.json`은 같은 실제 MGT의 `*CONSTRAINT` mask와 `*ELASTICLINK` stiffness를 별도 6DOF boundary subsystem sparse tangent로 조립한다. active boundary node `5517`, finite spring component `10152`, restrained DOF `7707`, tangent nnz `40608`, self-equilibrating probe solve relative residual `0.0`이다.
- 이 boundary spring evidence는 typed boundary entity가 실제 수치 tangent로 소비됨을 보여준다. support node와 elastic-link node의 direct overlap은 `0`이므로, 물리적 연결성은 전체 element graph를 통해 해석되어야 한다.
- `mgt_uncoarsened_boundary_global_equilibrium.json`은 rigid-like elastic-link coarsening을 끈 MGT roundtrip을 생성해 node `13047`, element `12728` 상태에서 line/frame + source-thickness shell + finite `*ELASTICLINK` spring tangent를 같은 6DOF global matrix에 조립한다. 실제 `*CONSTRAINT` authored support mask `7707` DOF를 적용하고 spring component `10152`, spring nnz `40608`, relative residual `1.32e-08`, max translation `1.27m`로 ready다.
- 이로써 boundary support/link subsystem의 full coupled frame-shell global tangent 소비는 linear smoke-solve 범위에서 닫혔다. 이어진 uncoarsened boundary P-Delta probe는 이 tangent를 deformed-state loop에 연결해 load-scale frontier를 `0.45`까지 확장했고, checkpointed relaxed/alpha-scan continuation은 frontier를 `0.656`까지 더 밀었다. 하지만 아직 full-load nonlinear Newton/material closure는 아니며, 다음 failed bracket도 공식 aggregate에 없다. diaphragm/generic opening cutout/non-default surface LCAXIS 처리도 여전히 G1 미폐쇄다.
- `mgt_frame_material_nonlinear_tangent.json`은 MGT line/frame element `5572`개에 대해 source material E/nu와 material-name grade proxy(`C40`, `Q235`, `C40+Q235`)를 소비한다. service-load material state와 controlled yield/damage probe를 분리하고, probe tangent를 per-element frame stiffness에 조립해 residual `1.60e-04 N`, nonlinear probe element `5487`개, min tangent ratio `0.015`를 기록한다.
- `mgt_shell_material_nonlinear_tangent.json`은 MGT PLATE/shell surface element `7152`개에 대해 같은 source material E/nu와 grade proxy를 per-surface tangent로 소비한다. controlled probe에서 nonlinear tangent reduction surface element `6408`개, min tangent ratio `0.948`, fixed-tangent global shell JVP relative error `6.39e-17`, material-tangent shell smoke residual `1.09e-09 N`을 기록한다.
- `mgt_direct_residual_shell_material_tangent_base_followup379_probe.json`은 이 shell material tangent를 physical direct residual path와 Newton tangent assembly에 연결한 base replay다. `0.656` checkpoint에서 direct residual은 `18.97 N`으로 `5.0e-04 N` gate를 통과하지 못한다. 따라서 bounded tangent smoke와 direct residual closure 사이에는 아직 state-updated material Newton/globalization 갭이 남는다.
- `mgt_direct_residual_shell_material_tangent_rowcorr_min_followup380_probe.json`은 같은 residual 기준에서 target row `1`, support `4`의 최소 current-tangent row correction을 재생해 residual을 `18.97 -> 14.91 N`으로 낮춘다. relative increment는 `8.84e-09`로 gate를 통과하지만 residual gate는 아직 실패한다.
- 이 material tangent evidence는 bounded frame/shell tangent smoke/probe다. controlled probe는 production load combination이 아니며 fiber-section, layered shell integration, path-dependent internal force history, state-updated consistent material Newton closure를 뜻하지 않는다.
- `mgt_story_eccentricity_load_receipt.json`은 실제 `*STORY-ECCEN` row를 equivalent accidental torsion load generation으로 소비한다. source row가 seismic eccentricity `YES`, wind eccentricity `NO`, eccentricity percent `5/15`를 지정하므로, story gravity proxy와 plan span으로 seismic X/Y ± eccentricity case `4`개를 만들고 max absolute torsion `3.44e6 Nm`를 기록한다.
- `mgt_coupled_frame_shell_story_eccentricity_equilibrium.json`은 이 generated story eccentricity load cases `4`개를 full coupled frame-shell sparse stiffness에 주입해 모두 solve한다. max residual은 `0.0089 N`, max relative residual은 `3.99e-7`, max translation은 `1.13m`다.
- 이 story eccentricity evidence는 load-generation과 coupled linear smoke solve injection까지 닫았지만, full-load nonlinear Newton이나 KDS/IBC response-spectrum/wind-code workflow를 구현했다는 claim은 아니다.
- `mgt_global_fea_3d_native_solve.json`의 상위 status는 licensed fingerprint bridge 때문에 productization path에 붙어 있지만, mesh solve 자체는 `warn`이다.

상용제품까지 남은 개선점:

- 임의 3D 골조/벽/슬래브/셸/솔리드 요소의 전역 stiffness/mass/load assembly.
- 연결 컴포넌트 축약이 아닌 전체 structural graph boundary condition 중 authored support mask와 finite elastic link의 linear global tangent 통합은 uncoarsened boundary receipt에서 닫혔다. Frame `ANGLE` local-axis roll, current-source opening no-op policy, shell calibration benchmark suite도 붙었다. 남은 것은 diaphragm, generic opening cutout, non-default surface LCAXIS, member release/eccentricity의 더 넓은 runtime semantics다.
- consistent tangent 기반 기하+재료 nonlinear Newton. 현재 bounded frame material tangent smoke와 P-Delta continuation probe는 붙었지만 path-dependent material Newton/full-load Newton closure는 아니다.
- P-Delta/P-delta 축력 업데이트, deformed-state consistent tangent, load/path increment control.
- support/release/spring, rigid diaphragm, ELEMENT local-yz/roundtrip/non-frame offset/eccentricity 적용, member end release의 typed runtime entity.
- solver failure가 “linear tangent fallback ready”로 보이지 않도록 nonlinear/full-mesh/full-building status 분리 유지.

완료 판정 예:

- full mesh nonlinear solve에서 `nonlinear_equilibrium=true`, fallback 없이 representative benchmark suite 100% pass.
- response sanity envelope가 drift/base shear/displacement에 대해 engineering bounds를 검증.
- partial component solve는 보조 evidence로만 남고 product claim gate에 쓰이지 않음.

### G2. Modal, Buckling, Stability

현재 상태:

- commercial cross-validation에는 `mode_shape_mac`, `buckling_factor` export sample summary가 있다.
- `mgt_native_modal_buckling_solver.json`이 대표 MGT beam component에 대해 in-repo elastic stiffness/mass/geometric-stiffness eigen solve를 생성한다.
- 현재 evidence는 `status=ready`, `native_solver_ready=true`, `benchmark_contract_pass=true`, `mode_count=4`, `critical_load_factor=7053.141480946014`, `real_section_property_coverage_pct=100.0`이다.
- `commercial_gap_ledger_status.json`의 `G2.evidence.source_receipts`는 `mgt_native_modal_buckling_solver.json`와 `commercial_solver_cross_validation.json`를 G2 closure 출처로 기록한다. 현재 status evidence는 solve scope `representative_native_beam_component`, raw beam `358`개 중 solved component beam `4`개, DOF `15`, free DOF `12`, stiffness/mass/geometric stiffness matrix ready, restrained DOF `3`, modal mode `4`, critical buckling factor `7053.141480946014`, geometric stiffness positive rank `6`, Euler member factor min `241.56518577501603`를 노출한다.
- Commercial cross-validation은 `pass_with_marginal_metrics`, case `12`, metric comparison `84`, hard metric failure `0`, marginal accepted `1`을 기록한다. 이 cross-validation은 paired commercial HF/LF export agreement check이며 independent solver certification이 아니다.
- 이로써 G2의 **native modal/buckling artifact attached** 기준은 local closure다.
- 단, claim boundary는 대표 component solve다. full-building all-member modal model과 nonlinear stability breadth는 G1의 full mesh 확장 뒤 별도 확장이 필요하다.
- 따라서 G2 closed는 representative component modal/buckling artifact closure이지, full-building dynamics/stability, response spectrum/time-history workflow, nonlinear buckling/path-following, imperfection sensitivity, external certification, 또는 G1 full 3D nonlinear solver readiness를 닫지 않는다.

남은 개선점:

- full-building global mass matrix와 all-member modal extraction.
- response spectrum / modal combination workflow.
- broader linear eigen buckling `K + lambda Kg` across full structural graph.
- nonlinear buckling/path-following, arc-length, snap-through/snap-back detection.
- imperfection sensitivity envelope.
- stability report가 design-code check와 연결되는 trace.

완료 판정 예:

- 현재 local closure 기준: modal frequency, mode shape head, buckling factor가 `commercial_solver_cross_validation`의 tolerance 계약과 함께 in-repo artifact로 생성된다.
- full commercial dynamics/stability 기준: response spectrum/time-history/path-following까지 full-building benchmark suite와 tolerance 계약을 가진다.

### G3. Loads, Combinations, Staged Construction Semantics

현재 상태:

- MIDAS parser는 geometry/material/topology 보존이 강하다.
- `load_stage_semantics_contract.json`은 6개 case entity, 8개 combination entity, 24개 staged construction stage를 typed contract로 만든다.
- `load_stage_runtime_flow_receipt.json`은 typed load/stage entity가 solve, viewer, export, audit surface로 흐르는 evidence를 묶는다.
- 현재 receipt는 `status=ready`, `solve_flow_ready=true`, `viewer_flow_ready=true`, `export_flow_ready=true`, `audit_flow_ready=true`, `unsupported_hazard_queue_ready=true`다.
- `commercial_gap_ledger_status.json`의 `G3.evidence.source_receipts`는 `load_stage_semantics_contract.json`, `load_stage_runtime_flow_receipt.json`, `delivery_evidence_bundle.json`를 G3 closure 출처로 기록한다. 현재 status evidence는 case entity `6`, combination entity `8`, max nested depth `2`, unresolved reference `0`, required load pattern coverage `1.0`, construction stage `24`, construction case `4`를 노출한다.
- Solve-flow readiness는 legacy native/condensed solve와 현재 full sparse line, 6-DOF frame, coupled frame-shell receipts를 함께 검사한다. Latest bundle에서는 legacy native/condensed basis가 `ready`이고, full sparse receipts도 `ready`로 동반 기록된다. 이는 load/stage flow closure이며, G1의 full-load nonlinear solver closure를 대신하지 않는다.
- 현재 source lane은 D/L/W case inventory, D/L load-combination graph, selfweight/nodal/pressure load rows를 가진다.
- seismic response spectrum, temperature, settlement, prestress는 현재 MGT lane에 source row가 없으므로 `unsupported_hazard_queue`와 `unsupported_hazard_families`에 명시된다. 따라서 G3 closed는 typed D/L/W + staged-analysis runtime flow closure이지, source-row 없는 hazard-specific solver claim, arbitrary construction-stage editor breadth, 또는 G1 full-load/full-mesh nonlinear solver closure가 아니다.

남은 개선점:

- unsupported queue에 있는 hazard family는 source row를 붙인 뒤 typed entity, solve replay, export roundtrip, audit evidence를 다시 통과해야 한다.
- response spectrum input normalization과 hazard-specific authoring UX는 source evidence가 붙는 즉시 별도 validation lane이 필요하다.
- current solve flow는 product receipt에 연결되었지만 full 3D nonlinear full-mesh closure는 G1에 남아 있다.

완료 판정 예:

- 현재 local closure 기준: normalized load/load-combo/stage entity가 solve, viewer, export, audit에 같은 id/provenance로 흐르고, 미지원 hazard가 명시적 queue로 올라간다.
- hazard-specific commercial claim 기준: seismic/temperature/settlement/prestress row가 실제 source evidence와 함께 들어와 같은 flow receipt를 통과한다.

### G4. Material, Section, Element Breadth

현재 상태:

- real section/material parser가 일부 SB/P 계열과 material modulus를 사용한다.
- solver breadth gate는 surface/material/contact breadth evidence를 많이 갖고 있다.
- viewer의 Material Model Lock/Force Envelope 등 evidence surfacing은 강하다.
- `commercial_gap_ledger_status.json`의 `G4.evidence.source_receipts`는 `material_element_tangent_support_matrix.json`, `mgt_surface_membrane_tangent.json`, `mgt_surface_shell_bending_tangent.json`, `mgt_shell_calibration_benchmarks.json`, `mgt_coupled_frame_surface_sparse_equilibrium.json`, `mgt_coupled_frame_shell_sparse_equilibrium.json`, `mgt_frame_material_nonlinear_tangent.json`, `mgt_shell_material_nonlinear_tangent.json`를 G4 closure 출처로 기록한다.
- `material_element_tangent_support_matrix.json`은 line/beam-family code1을 `tangent_coupled`로, plate/shell surface code2를 source-thickness membrane+bending/drilling smoke solved but full benchmark unsupported로, nonlinear material family를 bounded frame/shell material tangent smoke ready/full Newton unsupported로, contact family를 explicit unsupported queue로 분리한다.
- 현재 matrix는 `line_beam_tangent_ready=true`, line family section/material coverage `98.58%`, native representative component real-section coverage `100.0%`, fallback 없이 Newton 수렴이다.
- `mgt_surface_membrane_tangent.json`은 plate/shell surface code2의 membrane tangent smoke solve를 붙였고, `mgt_surface_shell_bending_tangent.json`은 bending/drilling/transverse pressure smoke solve를 붙였다.
- `mgt_shell_calibration_benchmarks.json`은 shell benchmark suite `5/5` ready를 기록하고, clamped square plate analytic deflection relative error `7.63%`를 기록한다.
- Matrix는 surface row를 `membrane_bending_drilling_calibrated_benchmark_ready_opening_local_axis_unsupported`로 노출한다.
- Matrix의 line row는 frame local-axis roll transform ready와 nonzero `ANGLE` `150`개 소비를 노출한다. surface row의 local-axis unsupported 표기는 benchmark MGT의 PLATE LCAXIS가 모두 default zero라 non-default surface LCAXIS breadth가 아직 없다는 의미다.
- `mgt_coupled_frame_surface_sparse_equilibrium.json`은 line/frame과 surface membrane을 하나의 sparse global tangent에 조립해 G4 breadth evidence를 보강한다.
- `mgt_coupled_frame_shell_sparse_equilibrium.json`은 line/frame과 source-thickness shell bending/drilling을 하나의 sparse global tangent에 조립해 same-node frame-shell tangent coupling evidence를 보강한다.
- `mgt_frame_material_nonlinear_tangent.json`은 frame material law를 bounded scope에서 steel bilinear, concrete cracking/crushing proxy, SRC composite proxy로 확장하고, service state/probe state/global tangent smoke solve를 evidence로 남긴다.
- `mgt_shell_material_nonlinear_tangent.json`은 shell material law를 bounded scope에서 per-surface tangent override와 fixed-tangent global shell JVP consistency까지 확장하지만, layered/path-dependent shell Newton closure는 claim하지 않는다.
- 현재 G4는 real-section line tangent, source-thickness surface membrane/bending/drilling smoke solves, shell calibration benchmarks, coupled frame-surface/frame-shell tangent smoke solves, bounded frame/shell material tangent smoke에 대해 locally closed only다. Generic opening cutouts, non-default surface local axes, higher-order shell breadth, contact/interface/foundation tangent, path-dependent/fiber/layered material Newton, G1 full-load nonlinear full-mesh closure는 닫지 않는다.
- `mgt_direct_residual_shell_material_tangent_base_followup379_probe.json`은 이 shell material tangent가 physical residual path에서 소비됨을 보이지만, direct residual gate 미통과를 함께 기록한다.
- `mgt_direct_residual_shell_material_tangent_rowcorr_min_followup380_probe.json`은 최소 row-correction descent를 기록하지만, broad sweep은 비용이 커서 다음 단계는 runtime-budgeted continuation과 ROCm/HIP parity가 필요하다.
- `mgt_shell_material_rowcorr_budget_controller_followup381.json`은 followup380 seed frontier `14.90754179546407 N`을 보존하면서 zero-budget launch boundary를 기록한다. `runtime_budget_exceeded=true`, child rows `0`, promotion `0`이므로 잔차 하강 claim은 추가하지 않고, 긴 shell-material row-correction sweep을 budgeted child receipt 단위로 쪼개기 위한 실행 경계만 닫는다.
- `mgt_g1_followup387_shell_material_budgeted_continuation_status.json` v3는 shell-material budgeted continuation chain을 재생성 가능한 status receipt로 묶는다. `mgt_shell_material_rowcorr_budget_controller_followup383_target2_support4.json`부터 `followup395_multistep_target4_support4.json`까지 completed controller receipts는 compact checkpoint를 사용해 direct residual frontier를 `14.90754179546407 N`에서 `6.486346878826488 N`까지 낮춘다. compact checkpoint는 `displacement_u` 중심으로 full-history checkpoint 팽창을 피하고, followup388/followup389/followup391/followup392/followup393/followup394/followup395는 한 child 안에서 multi-promotion row correction을 수행해 restart overhead를 줄인다. followup390 child launch receipt는 `in_progress`/launch-only라 frontier에 세지 않으며, residual gate `5e-4 N`에는 아직 도달하지 못했다.
- `mgt_direct_residual_shell_material_adaptive_global_krylov_followup396_smoke.json`은 followup395 compact checkpoint에서 adaptive preconditioned global Krylov 1-step controller를 실행해 `6.486346878826488 -> 6.117776861921165 N`까지 한 단계 더 내렸다. `apply_shell_material_tangent=True`, `shell_pressure_load_path_policy=structural_components_only`, regularized current-tangent right preconditioner 1회, matrix-free JVP 기반 6 alpha 후보 스캔을 거친 CPU-diagnostic 결과이며 residual gate `5e-4 N`과는 여전히 약 `1.22e4x` 떨어져 있다.
- `mgt_direct_residual_shell_material_adaptive_global_krylov_followup397_compact_smoke.json`은 followup396 final checkpoint 위에서 같은 adaptive preconditioned global Krylov 1-step controller를 compact-output 모드로 재실행해 `6.117776861921165 -> 6.117752205061414 N`을 기록한다. `relative_increment_gate_passed=True`지만 `residual_gate_passed=False`이며, G1 latest frontier는 이 값으로 옮겨졌고 status receipt는 chain 끝 `mgt_direct_residual_shell_material_adaptive_global_krylov_followup397_compact_smoke.json`를 가리킨다. 새 followup들은 `cpu_diagnostic_only` / `official_rocm_hip_closure_required` claim boundary를 유지하고 G1 closure로 promote 하지 않는다.
- `mgt_shell_material_rowcorr_budget_controller_followup398_after_global_krylov_target4_support4.json`은 followup397 compact checkpoint에서 target `4` / support `4` row-correction controller를 재개해 `6.117752205061414 -> 5.74426714604332 N`을 기록한다. 한 child 안에서 four row-correction promotions를 모두 사용했고, compact checkpoint `mgt_shell_material_rowcorr_budget_controller_followup398_after_global_krylov_target4_support4_compact_checkpoint.npz`를 남겼다. 이 값이 G1 latest frontier지만 residual gate `5e-4 N`보다 여전히 약 `1.15e4x` 크므로, status receipt는 G1 `partial`과 `direct_residual_gate_not_closed`, `full_mesh_nonlinear_equilibrium_not_closed`, `production_rocm_hip_residual_row_backend_not_closed`, `consistent_residual_jacobian_newton_not_closed` blockers를 유지한다.
- `mgt_shell_material_rowcorr_budget_controller_followup399_target_rows_strict_hip_smoke.json`와 `mgt_g1_followup400_strict_hip_target_rows_alternating_smoke.json`은 followup398 checkpoint 위에서 strict HIP residual replay 경계를 시도했지만, HIP runtime/preflight 미가용 또는 promotion `0`으로 residual frontier를 움직이지 못했다. `mgt_g1_followup387_shell_material_budgeted_continuation_status.json`은 이 둘을 `non_promoting_launch_receipts`로 노출하고 `counted_in_frontier=false`로 유지한다. 이 둘은 HIP/production closure로 승격되지 않으며, latest counted frontier는 아래 followup401 CPU diagnostic receipt가 별도로 낮춘다.
- `mgt_shell_material_rowcorr_budget_controller_followup401_target4_support4_cpu_continuation.json`은 같은 followup398 compact checkpoint에서 CPU diagnostic row-correction continuation을 한 번 더 완료해 `5.74426714604332 -> 5.393578678157873 N`으로 frontier를 낮췄다. 이 child도 load scale `0.656`, `residual_gate_passed=false`, full-load closure gate false, HIP residual engine closure false를 기록하므로, status receipt는 followup401을 latest frontier로 세더라도 G1 closure를 promote 하지 않는다.
- `mgt_shell_material_rowcorr_budget_controller_followup402_target4_support4_cpu_continuation.json`은 followup401 compact checkpoint에서 같은 CPU diagnostic continuation을 반복해 `5.393578678157873 -> 5.064299859094071 N`까지 낮췄다. direct residual gate `5e-4 N` 대비 여전히 약 `1.01e4x` 크고 full-load/material Newton/production HIP gate가 모두 닫히지 않았으므로, 이 receipt도 G1 closure가 아니라 partial frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup403_target4_support4_cpu_continuation.json`은 followup402 compact checkpoint에서 같은 bounded controller를 한 번 더 실행해 `5.064299859094071 -> 4.985171267567502 N`으로 낮췄다. child는 `row_correction_promotion_count=1`, `row_correction_stop_reason=no_residual_descent`, `residual_gate_passed=false`를 기록한다. residual gate 대비 여전히 약 `9.97e3x` 크므로, status receipt는 followup403을 latest frontier로 세더라도 G1 closure를 promote 하지 않는다.
- `mgt_shell_material_rowcorr_budget_controller_followup404_target4_support4_cpu_continuation.json`은 followup403 compact checkpoint에서 같은 bounded controller를 반복했지만 `promotion_count=0`, `stop_reason=max_candidates_reached`, child `row_correction_stop_reason=no_residual_descent`를 기록했다. Latest counted frontier는 followup403의 `4.985171267567502 N`에 머물며, status receipt는 followup404를 `non_promoting_launch_receipts`에만 노출한다.
- `mgt_shell_material_rowcorr_budget_controller_followup405_target8_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup405_target8_support4_cpu_continuation_candidate1_target8_support4.json`은 followup403 compact checkpoint에서 target rows를 `8`로 넓히고 support `4`를 유지한 changed operator를 실행해 `4.985171267567502 -> 4.7551275786384295 N`으로 frontier를 다시 낮췄다. child는 `row_correction_promotion_count=3`, `row_correction_stop_reason=no_residual_descent`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `9.51e3x` 크고 full-load gate도 닫히지 않았으므로, followup405는 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup406_target8_support8_cpu_continuation.json`은 followup405 compact checkpoint에서 support를 `8`로 넓혔지만 `promotion_count=0`, child `row_correction_stop_reason=no_residual_descent`로 끝났다. Status receipt는 이를 `non_promoting_launch_receipts`에만 노출하며, latest counted frontier는 followup405의 `4.7551275786384295 N`으로 유지한다.
- `mgt_shell_material_rowcorr_budget_controller_followup407_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup407_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup405 compact checkpoint에서 target rows를 `16`으로 넓히고 support `4`를 유지해 `4.7551275786384295 -> 4.464830283599703 N`으로 frontier를 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `8.93e3x` 크므로, followup407도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup408_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup408_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup407 compact checkpoint에서 bounded target16/support4 continuation을 이어 `4.464830283599703 -> 4.192254050995224 N`으로 counted frontier를 낮췄다. 같은 source checkpoint에서 실행한 target16/support8 branch `mgt_shell_material_rowcorr_budget_controller_followup408_target16_support8_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup408_target16_support8_cpu_continuation_candidate1_target16_support8.json`도 `4.192255248745177 N`까지 낮췄지만 support4보다 약간 높으므로 counter-evidence로만 남긴다. 두 child 모두 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록하므로, wider support가 G1 closure나 better frontier라는 claim을 만들지 않는다.
- `mgt_shell_material_rowcorr_budget_controller_followup409_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup409_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup408 target16/support4 compact checkpoint에서 같은 bounded operator를 이어 `4.192254050995224 -> 3.9363193038418913 N`으로 counted frontier를 다시 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `7.87e3x` 크므로, followup409도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup410_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup410_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup409 compact checkpoint에서 같은 bounded operator를 이어 `3.9363193038418913 -> 3.696008025377786 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `7.39e3x` 크므로, followup410도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup411_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup411_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup410 compact checkpoint에서 같은 bounded operator를 이어 `3.696008025377786 -> 3.4703679688375715 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `6.94e3x` 크므로, followup411도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup412_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup412_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup411 compact checkpoint에서 같은 bounded operator를 이어 `3.4703679688375715 -> 3.258503495183767 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `6.52e3x` 크므로, followup412도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup413_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup413_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup412 compact checkpoint에서 같은 bounded operator를 이어 `3.258503495183767 -> 3.059571710813806 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `6.12e3x` 크므로, followup413도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup414_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup414_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup413 compact checkpoint에서 같은 bounded operator를 이어 `3.059571710813806 -> 2.8727899829039316 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `5.75e3x` 크므로, followup414도 G1 closure가 아니라 next frontier CPU diagnostic evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup415_target16_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup415_target16_support4_cpu_continuation_candidate1_target16_support4.json`은 followup414 compact checkpoint에서 같은 bounded operator를 이어 `2.8727899829039316 -> 2.8279050801832355 N`으로 counted frontier를 조금 더 낮췄다. child는 `row_correction_promotion_count=1`, `row_correction_stop_reason=no_residual_descent`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `5.66e3x` 크고 same target16/support4 operator가 no-descent boundary에 닿았으므로, followup415도 G1 closure가 아니라 tapering frontier CPU diagnostic evidence이며 다음 locally useful route는 changed target/support/operator, consistent residual/Jacobian Newton path, 또는 production ROCm/HIP lane이다.
- `mgt_shell_material_rowcorr_budget_controller_followup416_target32_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup416_target32_support4_cpu_continuation_candidate1_target32_support4.json`은 followup415 final checkpoint에서 target rows를 `32`로 바꾸고 support `4`를 유지해 `2.8279050801832355 -> 2.655279334091837 N`으로 counted frontier를 다시 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `5.31e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup416은 G1 closure가 아니라 changed-target CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup417_target32_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup417_target32_support4_cpu_continuation_candidate1_target32_support4.json`은 followup416 compact checkpoint에서 같은 target32/support4 operator를 이어 `2.655279334091837 -> 2.4931796692692743 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `4.99e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup417도 G1 closure가 아니라 continued target32/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup418_target32_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup418_target32_support4_cpu_continuation_candidate1_target32_support4.json`은 followup417 compact checkpoint에서 target32/support4를 한 번 더 이어 `2.4931796692692743 -> 2.4542242481459553 N`으로 counted frontier를 조금 낮췄다. child는 `row_correction_promotion_count=1`, `row_correction_stop_reason=no_residual_descent`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `4.91e3x` 크고 same target32/support4 operator도 no-descent boundary에 닿았으므로, followup418은 G1 closure가 아니라 tapering CPU diagnostic frontier evidence이며 다음 route는 changed target/support/operator, consistent residual/Jacobian Newton path, 또는 production ROCm/HIP lane이어야 한다.
- `mgt_shell_material_rowcorr_budget_controller_followup419_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup419_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup418 compact checkpoint에서 target rows를 `64`로 바꾸고 support `4`를 유지해 `2.4542242481459553 -> 2.3043625138009602 N`으로 counted frontier를 다시 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `4.61e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup419는 G1 closure가 아니라 changed-target CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup420_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup420_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup419 compact checkpoint에서 같은 target64/support4 operator를 이어 `2.3043625138009602 -> 2.163647319258679 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `4.33e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup420도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup421_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup421_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup420 compact checkpoint에서 같은 target64/support4 operator를 이어 `2.163647319258679 -> 2.0315225271909085 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `4.06e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup421도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup422_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup422_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup421 compact checkpoint에서 같은 target64/support4 operator를 이어 `2.0315225271909085 -> 1.9074658845723107 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `3.81e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup422도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup423_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup423_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup422 compact checkpoint에서 같은 target64/support4 operator를 이어 `1.9074658845723107 -> 1.7909880486798002 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `3.58e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup423도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup424_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup424_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup423 compact checkpoint에서 같은 target64/support4 operator를 이어 `1.7909880486798002 -> 1.6816207062823096 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `3.36e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup424도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup425_target64_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup425_target64_support4_cpu_continuation_candidate1_target64_support4.json`은 followup424 compact checkpoint에서 같은 target64/support4 operator를 이어 `1.6816207062823096 -> 1.5789301142824925 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `3.16e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup425도 G1 closure가 아니라 continued target64/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup426_target64_support4_cpu_continuation.json`은 followup425 compact checkpoint에서 같은 target64/support4 operator를 재시도했지만 child가 `1.5789301142824925 -> 1.5789301142824925 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 counted frontier가 아니라 latest checkpoint에서 같은 target64/support4 operator 반복이 소진됐다는 non-promoting boundary evidence이며, 다음 G1 진행은 target/support/operator 변경, consistent residual/Jacobian Newton, 또는 production ROCm/HIP lane으로 전환해야 한다.
- `mgt_shell_material_rowcorr_budget_controller_followup427_target128_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup427_target128_support4_cpu_continuation_candidate1_target128_support4.json`은 followup426의 no-descent boundary 뒤 target count를 target128/support4로 바꿔 descent를 다시 열었고 `1.5789301142824925 -> 1.482511519898516 N`으로 counted frontier를 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `2.97e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup427도 G1 closure가 아니라 changed target128/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup428_target128_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup428_target128_support4_cpu_continuation_candidate1_target128_support4.json`은 followup427 compact checkpoint에서 같은 target128/support4 operator를 이어 `1.482511519898516 -> 1.391983647766473 N`으로 counted frontier를 더 낮췄다. child는 `row_correction_promotion_count=4`, `row_correction_stop_reason=max_promotions_exhausted`, `residual_gate_passed=false`, full-load observed load scale `0.656`을 기록한다. residual gate 대비 여전히 약 `2.78e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, followup428도 G1 closure가 아니라 continued target128/support4 CPU diagnostic frontier evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup429_target128_support4_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup429_target128_support4_cpu_continuation_candidate1_target128_support4.json`은 followup428 compact checkpoint에서 target128/support4를 이어 `1.391983647766473 -> 1.3277400507770736 N`으로 counted frontier를 더 낮췄지만, 세 번째 promotion 뒤 `row_correction_stop_reason=no_residual_descent`로 멈췄다. 이 receipt는 latest frontier evidence인 동시에 latest checkpoint에서 같은 target128/support4 operator 반복이 소진됐다는 boundary evidence다. residual gate 대비 여전히 약 `2.66e3x` 크고 full-load/full-mesh/material Newton/production ROCm-HIP gates가 닫히지 않았으므로, 다음 G1 진행은 target/support/operator 변경, consistent residual/Jacobian Newton, 또는 production ROCm/HIP lane으로 전환해야 한다.
- `mgt_shell_material_rowcorr_budget_controller_followup430_target256_support4_cpu_continuation.json`은 followup429 compact checkpoint에서 target256/support4를 시도했지만 child가 `1.3277400507770736 -> 1.3277400507770736 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 counted frontier가 아니라 target256/support4도 현 checkpoint에서 non-promoting임을 보여주는 boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup431_target128_support8_cpu_continuation_children/mgt_shell_material_rowcorr_budget_controller_followup431_target128_support8_cpu_continuation_candidate1_target128_support8.json`은 followup429 compact checkpoint에서 support를 `8`로 넓혀 `1.3277400507770736 -> 1.3092276661494922 N`으로 counted frontier를 낮췄지만, 첫 promotion 뒤 `row_correction_stop_reason=no_residual_descent`로 멈췄다. 이 receipt도 residual gate 대비 약 `2.62e3x` 크고 `residual_gate_passed=false`, load scale `0.656`이므로 G1 closure가 아니라 latest target128/support8 CPU diagnostic frontier 및 same-operator boundary evidence다.
- `mgt_shell_material_rowcorr_budget_controller_followup432_target128_support16_cpu_continuation.json`은 followup431 compact checkpoint에서 support를 `16`으로 넓혔지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 counted frontier가 아니라 target128/support16도 최신 checkpoint에서 non-promoting임을 보여주는 boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup433_target192_support8_cpu_continuation.json`은 followup431 compact checkpoint에서 target을 `192`로 넓히고 support `8`을 유지했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 counted frontier가 아니라 target192/support8도 최신 checkpoint에서 non-promoting임을 보여주는 boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup434_target96_support8_cpu_continuation.json`은 followup431 compact checkpoint에서 target을 `96`으로 줄이고 support `8`을 유지했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 counted frontier가 아니라 target96/support8도 최신 checkpoint에서 non-promoting임을 보여주는 boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup435_bending_drilling_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `largest_rows`가 아닌 `residual_shell_bending_drilling_rows`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 shell bending/drilling residual-row family도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup436_normal_rows_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_shell_normal_rows`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 shell normal residual-row family도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup436_shell_normal_support8_cpu_continuation.json`은 같은 shell-normal no-descent run의 duplicate alias로 status receipt의 `duplicate_alias_receipts`에만 노출된다. `mgt_shell_material_rowcorr_budget_controller_followup436_normal_rows_support8_cpu_continuation.json`이 canonical non-promoting receipt이며, alias는 counted frontier, 추가 row-target exhaustion, 또는 G1 closure evidence로 세지 않는다. `commercial_gap_ledger_status.json`의 G1 evidence도 이 alias list를 `g1_shell_material_budgeted_duplicate_alias_receipts`로 직접 전파해 commercial row에서 같은 non-count boundary를 확인할 수 있게 한다.
- `mgt_shell_material_rowcorr_budget_controller_followup437_geometry_normal_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_shell_geometry_normal_rows`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. 이 receipt는 shell geometry-normal residual-row family도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup438_geometry_normal_bending_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_shell_geometry_normal_bending_rows`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `1749.3042279599304 N`으로 크게 악화되어 rejected 상태로 남았다. 이 receipt는 shell geometry-normal+bending residual-row union도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup439_shell_element_blocks_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_shell_element_blocks`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `31.421682913668302 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 shell element-block residual target도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup440_element_blocks_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_element_blocks`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `28.245090827968422 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 broader element-block residual target도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup441_frame_element_blocks_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_frame_element_blocks`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `3.125238018978351 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 frame element-block residual target도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup442_node_blocks_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `residual_node_blocks`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `12.670106321519167 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 node-block residual target도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup443_current_component_rows_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode를 `current_component_rows`로 바꾸고 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `3.837920324197677 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 current-component residual rows도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup444_frontier_component_rows_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 `mgt_residual_jacobian_ridge_alpha_polished_frontier_component_probe.json`의 component rows를 target mode `frontier_component_rows`로 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate도 `1.3092276661494922 N`에 머물러 accepted descent가 없었다. 이 receipt는 frontier-component row target 반복도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup445_fd_largest_rows_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode `largest_rows`를 유지하되 row Jacobian을 `finite_difference`로 바꿔 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `5.651760306857457 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 finite-difference row-Jacobian replay도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup446_residual_weighted_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode `largest_rows`와 current-tangent row Jacobian을 유지하되 support selection을 `residual_weighted`로 바꿔 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `3.6018975608505404 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 residual-weighted support selection도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup447_fd_target_rows_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode `largest_rows`와 finite-difference row Jacobian을 유지하되 support selection을 `target_rows`로 바꿔 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `1.310562903565632 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 target-rows support selection도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_shell_material_rowcorr_budget_controller_followup448_fd_residual_weighted_support8_cpu_continuation.json`은 같은 followup431 compact checkpoint에서 target mode `largest_rows`, finite-difference row Jacobian, support selection `residual_weighted`로 target128/support8을 재검사했지만 child가 `1.3092276661494922 -> 1.3092276661494922 N`으로 개선하지 못했고 `row_correction_promotion_count=0`, `row_correction_stop_reason=no_residual_descent`, controller `stop_reason=max_candidates_reached`를 기록했다. Best trial candidate는 `5.651760306857457 N`으로 악화되어 rejected 상태로 남았다. 이 receipt는 finite-difference plus residual-weighted support selection도 최신 checkpoint에서 non-promoting임을 보여주는 CPU-diagnostic boundary evidence이며, G1 closure가 아니다.
- `mgt_g1_followup387_shell_material_budgeted_continuation_status.json` now exposes `top_level_child_frontier_mismatch=true` for non-promoting controller receipts whose parent top-level `final_direct_residual_inf_n` still reflects the seed/controller summary rather than the child frontier. For followup440, followup441, followup442, followup443, followup444, followup445, followup446, followup447, and followup448, the status counts the child receipt residual `1.3092276661494922 N`, keeps `top_level_direct_residual_counted=false`, and records `top_level_controller_residual_differs_from_child_frontier`; parent top-level residuals are therefore boundary metadata, not counted frontier or closure evidence.
- The same status receipt now exposes `row_target_mode_exhaustion.all_configured_modes_exhausted_at_latest_checkpoint=true` for the latest followup431 checkpoint. The exhausted plain target modes are `largest_rows`, `residual_node_blocks`, `residual_element_blocks`, `residual_frame_element_blocks`, `residual_shell_element_blocks`, `residual_shell_bending_drilling_rows`, `residual_shell_normal_rows`, `residual_shell_geometry_normal_rows`, `residual_shell_geometry_normal_bending_rows`, `frontier_component_rows`, and `current_component_rows`; this is a row-retuning exhaustion boundary only, not G1 closure. `commercial_gap_ledger_status.json` mirrors this object as `g1_shell_material_budgeted_row_target_mode_exhaustion` so the commercial row can prove the retuning boundary without treating it as closure. The next credible G1 work should move to a consistent residual/Jacobian Newton path, a production ROCm/HIP residual/JVP worker, or a different coupled operator/preconditioner.
- The same status receipt now exposes `largest_rows_operator_strategy_exhaustion.all_configured_strategies_exhausted_at_latest_checkpoint=true` for the latest followup431 checkpoint. The exhausted target128/support8 largest-rows strategy matrix covers current-tangent row-strongest, current-tangent residual-weighted, finite-difference row-strongest, finite-difference target-rows, and finite-difference residual-weighted support selection. This is a support/Jacobian retuning boundary only, not G1 closure; `commercial_gap_ledger_status.json` mirrors it as `g1_shell_material_budgeted_largest_rows_operator_strategy_exhaustion` to redirect future G1 work away from another largest-rows support heuristic replay and toward a consistent residual/Jacobian Newton path, production ROCm/HIP residual/JVP worker, or genuinely different coupled operator/preconditioner.
- The commercial G1 evidence also mirrors `same_operator_no_descent_receipts` and `counter_evidence` as `g1_shell_material_budgeted_same_operator_no_descent_receipts` and `g1_shell_material_budgeted_counter_evidence`. These lists expose repeated no-descent/counter-evidence receipts directly at the commercial status layer; they are route-pruning evidence, not residual descent or G1 closure.
- `mgt_g1_followup387_shell_material_budgeted_continuation_status.json` now also summarizes the attached-policy HIP residual-row backend receipts `mgt_rocm_backend_newton_row_integration_followup67_summary.json`, `mgt_direct_residual_hip_row_backend_followup68_69_summary.json`, `mgt_direct_residual_hip_largest_rows_followup70_72_summary.json`, and `mgt_direct_residual_hip_largest_rows_followup73_74_saturation_summary.json`. That HIP-backed lane is live descent/saturation evidence, but latest residual-row backend frontier is only `0.03346553206631597 N` against its `0.001 N` gate and the receipts explicitly do not claim production in-process ROCm/HIP residency. `commercial_gap_ledger_status.json` mirrors this object as `g1_shell_material_budgeted_hip_residual_row_backend`, so commercial G1 can expose the live backend evidence while keeping `production_rocm_hip_residual_row_backend_not_closed` as a blocker rather than a hidden PASS.
- The same status receipt now summarizes the consistent residual/Jacobian/Newton evidence receipts `mgt_residual_jacobian_consistency_hip_required_probe.json`, `mgt_residual_jacobian_attached_policy_global_krylov_followup43_frontier_probe.json`, `mgt_residual_jacobian_attached_policy_global_krylov_post_shell_followup62_frontier_probe.json`, `mgt_residual_jacobian_attached_policy_hip_largest_rows_followup69_frontier_probe.json`, `mgt_residual_jacobian_attached_policy_hip_largest_rows_followup71_frontier_probe.json`, `mgt_residual_jacobian_attached_policy_hip_largest_rows_followup73_frontier_probe.json`, `mgt_residual_jacobian_physical_audit_followup85_86_summary.json`, and `mgt_cached_residual_jvp_top96_multi_ridge_followup354_361_after_total_scaled_component_fd_hip_latest_only_controller_summary.json`. The physical audit shows checked tangent/JVP directions are consistent but disclaims closure; the latest cached residual/JVP continuation lowers the diagnostic lane to `0.033136466982157 N` with `0.032136466982157 N` remaining to its `0.001 N` gate, still `partial`, not consistency-ready, and not production ROCm/HIP resident. Its advertised restart checkpoint `mgt_cached_residual_jvp_top96_multi_ridge_followup361_final_checkpoint.npz` is not present in the current worktree even though the controller summary marks that step as retained/promoted, and the controller's advertised start checkpoint `mgt_direct_residual_attached_policy_followup353_frontier_component_total_scaled_fd_hip_final_checkpoint.npz` is also absent. The status receipt exposes `latest_checkpoint_exists=false`, `controller_start_checkpoint_exists=false`, `latest_checkpoint_retained_by_summary=true`, `advertised_retained_checkpoint_missing=true`, `missing_controller_replay_artifact_count=10`, `restart_ready=false`, and `restart_blockers=["latest_checkpoint_missing","advertised_retained_checkpoint_missing","controller_start_checkpoint_missing","controller_replay_artifacts_missing"]`. It also exposes `regeneration_feasibility.can_regenerate_advertised_chain=false` and `can_replay_advertised_chain=false` because the start checkpoint, advertised latest checkpoint, and eight step basis NPZs are absent; any future regeneration command must remain `--allow-cpu-diagnostic` and non-promoting until those artifacts are restored or regenerated and replay-verified. `commercial_gap_ledger_status.json` mirrors the full object as `g1_shell_material_budgeted_consistent_residual_jacobian_newton`, so this summary is retained as diagnostic value evidence, not a restart-ready closure path. `consistent_residual_jacobian_newton_not_closed` remains a G1 blocker rather than being hidden behind diagnostic Jacobian/JVP evidence.
- 모델 inventory는 `BEAM=2192`, `COMPTR=3384`, `PLATE=7152`이며, current tangent-coupled claim은 line/beam elastic real-section 범위로 제한된다.

남은 개선점:

- generic opening cutout semantics, non-default surface LCAXIS treatment, higher-order shell formulation, coupled full-load pressure/load-combo solve는 unsupported queue에 남아 있다.
- RC/steel/composite frame material tangent smoke는 붙었지만, fiber section, plastic hinge, distributed plasticity, path-dependent history variables, consistent Newton Jacobian은 별도 constitutive benchmark가 필요하다.
- contact/interface/foundation tangent는 source row와 benchmark가 붙기 전까지 product claim 금지다.

완료 판정 예:

- 현재 local closure 기준: element family별 supported scope와 unsupported rejection reason이 자동 생성되고, supported line family와 surface membrane+bending/drilling smoke solves가 coverage/residual threshold를 통과한다.
- broader commercial closure 기준: plate/shell bending/contact/nonlinear material family가 unsupported queue에서 빠질 때마다 tangent benchmark와 report exposure를 새로 통과한다.

### G5. Design Code And Detailing Automation

현재 상태:

- KDS RC rule engine과 row provenance가 있다.
- `commercial_gap_ledger_status.json`의 `G5.evidence.source_receipts`는 `kds_detailing_support_matrix.json`, `ai_code_reasoning_guard.json`, `design_optimization_cost_reduction_changes.json`, `implementation/phase1/kds_rc_rule_engine.py`를 G5 closure 출처로 기록한다.
- `kds_detailing_support_matrix.json`은 KDS RC rule engine의 clause/detail family와 optimization row guard를 제품 evidence로 묶는다.
- rule engine clause inventory는 23개이며 beam/column/wall/slab/foundation/connection family를 모두 포함한다.
- 현재 optimization lane은 20개 row 중 19개가 explicit governing clause를 갖고, 1개는 engineer-review-required unsupported queue로 들어가 자동 코드 claim을 막는다.
- `commercial_gap_ledger_status.json`의 `G5.evidence`는 이 수치를 `jurisdiction_profile=KDS-2022 bounded lane`, `distinct_governing_clause_count=2`, `change_row_count=20`, `explicit_clause_row_count=19`, `missing_governing_clause_count=1`, `review_guarded_row_count=20`, `all_rows_have_clause_or_review_guard=true`, `unsupported_clause_queue_count=1`, `broader_unsupported_claim_queue_count=3`으로 노출한다.
- engineer-in-loop boundary와 hallucination guard가 유지된다.
- 현재 G5는 deterministic KDS RC rule/detailing support와 bounded optimization row citation/review guard에 대해 locally closed only다. 이 closure는 autonomous legal/code approval, steel/composite/seismic/project-specific detailing, broader jurisdiction editions, signed engineer approval을 닫지 않는다.

남은 개선점:

- steel/composite, special seismic detailing, project-specific connection/foundation detailing은 unsupported claim queue에 남아 있다.
- broader jurisdiction/edition profile은 source clause map과 signed review disposition이 붙은 뒤 승격해야 한다.
- waiver/exception workflow는 현재 engineer review queue와 governance contract에 묶여 있지만, 고객별 legal approval workflow는 외부 evidence가 필요하다.

완료 판정 예:

- 현재 local closure 기준: code/detailing row가 governing clause 또는 engineer-review-required queue를 가지며, unsupported clauses are explicitly blocked or queued, not silently omitted.
- broader commercial claim 기준: steel/composite/seismic/project-specific clauses가 source evidence와 solver/load-combo/member trace를 갖고 support matrix에 추가된다.

### G6. V&V, External Benchmarks, Residual Holdouts

현재 상태:

- strict independent gate는 EB receipt `0/4`, RH closure `3/3`로 blocked. RH signed closure evidence는 붙어 있지만 EB external receipt가 없으므로 G6 closure가 아니다.
- `commercial_gap_ledger_status.json`의 `G6.evidence.source_receipts`는 `p1_benchmark_breadth_status.json`, `external_benchmark_submission_updates.json`, `external_benchmark_submission_readiness.json`, `residual_holdout_closure_updates.json`를 G6 external-blocked 판정 출처로 기록한다.
- commercial export sample cross-validation은 12/12 pass, marginal accepted 1.
- `phase4_analytic_physical_fallback_scorecard.json`은 commercial/operator output이 없는 generated seed case도 analytic expected-output comparison과 physical residual/equilibrium으로 평가 가능함을 기록한다(`30/30` pass, expected-output comparison `88/88`, physical equilibrium `30/30`). 이는 fallback evidence일 뿐이며 live MIDAS/SAP/ETABS/OpenSees replay, operator permission, 두 독립 reference solver 비교, GUI story/member/mode traceability를 대체하지 않는다.
- `commercial_gap_ledger_status.json.external_submission_closure_gate` now records the external submission queue as non-closing boundary evidence: queue `ready`, queue count `4`, ready-to-submit count `4`, receipt attached count `0`, receipt pending count `4`, pending ids `hardest_external_10case`, `tpu_hffb`, `peer_spd_hinge`, and `korean_public_structures`. It also records row-level `terminal_receipt_requirements` for all four lanes with `terminal_receipt_attached=false`, `terminal_closure_evidence_attached=false`, `ready_to_submit_is_terminal=false`, and `terminal_requirements_missing_count=4`. `ready_to_submit` lifecycle and local dry-run evidence do not replace attached external submission/closure receipts.

남은 개선점:

- EB receipt attached for hardest external 10-case, TPU HFFB, PEER/SPD hinge, Korean public structures.
- Keep RH-001/RH-002/RH-003 signed closure evidence visible, but do not use it to cover the missing EB receipts.
- live MIDAS Gen/SAP/ETABS/OpenSees replay results, not only model-derived or paired CSV proxy.
- public benchmark breadth for static, dynamic, modal, buckling, nonlinear, contact, foundation/SSI.
- reason-code taxonomy for numerical failure, model unsupported, input invalid, external evidence pending.

완료 판정 예:

- `check_independent_product_readiness.py --fail-blocked` passes.
- EB receipt `4/4`, RH closure `3/3`, strict evidence preflight passes.

### G7. Korean Medium/Large Real-Project Corpus

현재 상태:

- Current receipt: `sources=19 attached=10 metadata_only=9 mgt_header_ok=4`.
- `commercial_gap_ledger_status.json`의 `G7.evidence.source_receipts`는 `korean_medium_large_ingest_receipt.json`, `operator_attachment_manifest.queue.json`, `operator_attachment_manifest.queue.validation_report.json`, `operator_attachment_direct_download_review.json`를 G7 partial 판정 출처로 기록한다.
- The 4 valid MGT rows are currently `repo_benchmark_bridge` copies of `midas_generator_33.optimized.mgt`.
- The refreshed operator attachment queue remains non-closing: `operator_attachment_manifest.queue.json` has `attachment_count=14`, `auto_promotable_repo_candidate_count=0`, `source_mapping_blocked_action_count=14`, and `rights_blocked_private_candidate_action_count=5`. `operator_attachment_manifest.queue.validation_report.json` rejects all `14` fill-in rows because no operator-confirmed source-native/right-cleared overlay exists yet, and `operator_attachment_direct_download_review.json` records `5` specific remote download actions plus `9` portal landing actions but keeps `status=pending_rights_review`. This is action-queue evidence only; it does not replace operator-attached real corpus evidence.
- `commercial_gap_ledger_status.json` now carries these G7 inputs as a single `operator_attachment_closure_gate` evidence object. That gate records `contract_pass=false`, `accepted_operator_overlay_source_count=0`, `rejected_operator_overlay_source_count=14`, rejection counts `artifact_missing=9`, `rights_not_confirmed=14`, and `source_native_artifact_not_confirmed=14`, plus direct-download split `2` expected-file-type hits and `3` derivation/replacement actions. The gate is authoritative boundary evidence for why G7 remains partial; it is not a substitute for attaching source-native, rights-confirmed operator artifacts and replaying the ingest pipeline.
- The same closure gate now exposes `operator_terminal_attachment_requirements` for all `14` queued operator attachment rows. Current terminal requirement counts are `operator_terminal_attachment_requirement_count=14`, `operator_terminal_attachment_satisfied_count=0`, `operator_terminal_attachment_missing_count=14`, `operator_terminal_attachment_artifact_missing_count=9`, `operator_terminal_attachment_rights_missing_count=14`, and `operator_terminal_attachment_source_native_missing_count=14`. Each row preserves source ID, action/file/provenance metadata, acceptance checks, artifact existence, rights/source-native flags, overlay acceptance, and non-closing reasons, so queued downloads or benchmark-bridge placeholders cannot be mistaken for terminal G7 evidence.
- The same gate now records source receipt paths for the ingest receipt, operator attachment queue, validation report, and direct-download review so the non-closing G7 decision is traceable to concrete queue artifacts rather than a summary-only claim.
- The gate also exposes the minimum operator real-MGT replacement batch `replace_benchmark_bridge_mgt`: source IDs `koneps_goyang_changneung_powerplant_design_service`, `lh_bucheon_yeokgok_a1_housing_native_baseline`, `lh_happy_city_5_1_native_baseline`, and `lh_newtown_highrise_block_competition`, with acceptance checks `file_size_gt_500_bytes`, `mgt_header_contains_VERSION_or_UNIT`, `roundtrip_parse_contract_pass_when_enabled`, and `sha256_differs_from_repo_benchmark_bridge`. This is collection guidance only and remains non-closing until source-native/right-cleared overlay artifacts are accepted, ingest replay passes, and benchmark bridge MGTs are removed from the valid corpus count.
- One attached MGT placeholder remains too small: `kci_concrete_building_design_examples_2e_native_baseline`, `mgt_file_too_small:283`.
- 9 medium/large sources remain metadata-only.

남은 개선점:

- Replace benchmark bridge copies with real operator-attached public/competition MGT files where permitted.
- Attach real IFC/PDF/ZIP-derived structural artifacts with provenance/license checks.
- Roundtrip parse, mesh contract, load/material/section coverage, and solve-readiness per source.
- Separate “bridge validation” from “real corpus validation” in summary dashboards.

완료 판정 예:

- `attach_provenance=operator_attached` for target real MGT lanes.
- Metadata-only count is explained by policy/legal constraints, not missing ingestion.

### G8. Optimization And AI Productization

현재 상태:

- Production optimization is mostly deterministic/rule-constrained search with solver/proxy checks.
- ML/Pareto track is research/archive or opt-in, not default production.
- Cost model provenance exists, but 실측 단가/시공성 calibration은 full product claim 수준으로 닫히지 않았다.
- `optimization_productization_audit.json` 기준 현재 accepted optimization delta 20건은 solver/proxy replay, cost evidence, decision trace, review queue, Pareto context, ML bypass guard를 갖는다.
- `ai_code_reasoning_guard.json` 기준 19건은 explicit `governing_clause`, 1건은 automatic code claim 금지와 engineer review queue로 추적된다.
- `ml_surrogate_checkpoint_manifest.json` 기준 validated checkpoint가 존재하지만 final design promotion은 solver/code/human gate가 필요하다.
- `commercial_gap_ledger_status.json`의 `G8.evidence.source_receipts`는 `optimization_productization_audit.json`, `ml_multi_objective_status.json`, `design_optimization_cost_reduction_changes.json`, `post_optimization_reanalysis_gate.json`, `proxy_solver_divergence_gate.json`, `kds_detailing_support_matrix.json`를 G8 closure 출처로 기록한다. 현재 G8 local closure는 accepted delta `20`개, cost/safety/code evidence, solver replay, proxy replay, decision trace, review queue, Pareto context, ML bypass prevention, and final-report promotion requirements `solver_replay_passed`, `code_check_replay_passed`, `cost_provenance_attached`, `human_review_recorded`에 근거한다.
- 같은 evidence는 `accepted_rows_have_explicit_clause=false`, `missing_governing_clause_count=1`, `review_guarded_row_count=20`, `multi_objective_pareto_wired=false`, `research_pareto_front_count=3`, `ml_surrogate_gate_status=production_ready_shadow_solver_gated`를 함께 기록한다. 따라서 G8 closed는 deterministic/replayable optimization productization closure이지, autonomous AI design, production NSGA/RL/Pareto decisioning, full 3D nonlinear solver closure, measured cost/labor calibration, legal/code approval automation을 닫지 않는다.
- AI 엔진 상세 갭은 별도 원장 [structural-analysis-ai-engine-gap-ledger.md](structural-analysis-ai-engine-gap-ledger.md)에서 추적한다.

남은 개선점:

- Current lane의 solver-in-the-loop/replay hash/review evidence는 닫혔지만, 더 넓은 모델군과 full 3D nonlinear solver closure로 확장해야 한다.
- 실측 cost, labor, constructability, rebar congestion, formwork, schedule provenance.
- Multi-objective Pareto front는 audit evidence와 research archive에 연결되었지만, production NSGA/RL optimizer claim은 아직 금지.
- ML surrogate checkpoint governance는 현재 bounded shadow lane에서 닫혔지만, 임의 구조물/응답 종류로 generalization breadth를 확장해야 한다.
- “AI” marketing wording tied to actual deployed algorithm.

완료 판정 예:

- Every accepted optimization delta has before/after solver evidence, explicit code-check evidence or engineer-review code guard, cost provenance, and replayable audit log.
- ML suggestions cannot bypass hard solver/code gates.

### G9. Runtime, GPU, Performance, And Scale

현재 상태:

- `commercial_gap_ledger_status.json`의 `G9.evidence.source_receipts`는 `independent_product_readiness.json`, `gpu_solver_claim_receipt.json`, `gpu_rocm_workstation_receipt.json`, `solver_runtime_backend_policy.json`, `mgt_rocm_sparse_solver_probe.json`, `mgt_surface_shell_bending_tangent.json`, `mgt_coupled_frame_shell_sparse_equilibrium.json`를 G9 closure 출처로 기록한다.
- AMD ROCm/HIP workstation receipt confirms the target GPU path on this machine: `rocminfo` reports `gfx1030` / `AMD Radeon RX 6900 XT`, PyTorch is `2.6.0+rocm6.1`, `torch.version.hip=6.1.40091-a8dbc0c19`, and a device tensor smoke runs on `cuda:0`. In PyTorch ROCm this `cuda:0` label is the HIP device abstraction, not an NVIDIA requirement.
- 2026-06-01 local recheck: `rocm-smi` is `/usr/local/bin/rocm-smi`, `rocminfo` is `/usr/bin/rocminfo`, `hipcc`/`hipconfig` resolve through `/opt/rocm/bin`, `/dev/kfd` is present, and `nvidia-smi` is intentionally absent from the AMD 6900XT gate.
- `solver_runtime_backend_policy.json` now records the official compute lane as `amd_rocm_hip`: GPU is required for commercial solver closure, `nvidia-smi` is not required, CPU direct-residual receipts are diagnostic only, and CPU fallback is not allowed to promote G1/G9 closure. CPU reference vectors inside ROCm receipts are validation replay references, not accepted production solver backends.
- The same receipt now carries both `official_solver_compute_backend` and operator-facing `official_solver_backend` aliases, both fixed to `amd_rocm_hip`, plus `official_solver_backend_family=rocm_hip`. It also records that PyTorch's `cuda:0` device label is a ROCm compatibility alias when `torch.version.hip` is present.
- `validate_productization_delivery_evidence.py` now treats that runtime policy as a hard productization invariant: changing the official backend away from `amd_rocm_hip`, changing the backend family away from `rocm_hip`, allowing CPU diagnostic/fallback promotion, setting `cpu_solver_fallback_detected=true`, or making `nvidia-smi` required on the AMD 6900XT lane fails validation.
- GPU Newton terminal certification exists for optimization story fingerprint using `rocm_torch_hip_mainloop`, no CPU fallback, and device residency ratio `1.0`.
- 현재 G9는 workstation ROCm/HIP availability, official `amd_rocm_hip` backend policy, terminal GPU mainloop/story-fingerprint evidence, runtime packaging, and attached MGT sparse ROCm probe evidence에 대해 locally closed only다. `mgt_rocm_sparse_solver_probe.json` 자체도 `full_3d_rocm_nonlinear_equilibrium_not_closed` blocker를 계속 기록하므로, G9 closure는 G1 full 3D nonlinear/full-load GPU solver readiness, CPU fallback promotion, customer hardware throughput/SLA, external certification을 닫지 않는다.
- MGT ROCm sparse probe status is now `ready` for the bounded sparse-probe contract, while still carrying `full_3d_rocm_nonlinear_equilibrium_not_closed` as a blocker. This means line/frame/shell/coupled ROCm sparse evidence is attached for the current probe contract, but full 3D nonlinear/full-load GPU solver closure is still out of scope. It solves the full 3-DOF line elastic sparse matrix on `rocm_torch_sparse_cg` with device residency ratio `1.0` and relative residual below `1e-8`.
- The same probe solves the full 6-DOF frame elastic matrix as component-wise ROCm dense direct solves (`rocm_torch_component_dense_direct`) with relative residual below `1e-8`; simple sparse CG remains recorded as not converged, so production sparse preconditioning is still open.
- The ROCm probe now also replays full surface-shell and coupled frame-shell sparse residuals on ROCm CSR tensors using CPU reference solutions. Surface shell replay records relative residual about `1.31e-08`; coupled frame-shell replay records relative residual about `3.13e-12`. This proves device-resident sparse residual parity for those full matrices, but not GPU sparse solve closure.
- The same ROCm probe now records actual sparse solve attempts for the full shell/coupled matrices: PyTorch `torch.sparse.spsolve` is present as an API symbol but fails on this ROCm build because it requires CUDA cuDSS, while ROCm sparse CG, diagonal BiCGSTAB, symmetric diagonal-scaled BiCGSTAB, and block-Jacobi BiCGSTAB probes do not converge to the required residual. The latest block-Jacobi sweep now focuses around the observed coupled optimum (`48,56,64,72,88`) and uses up to `3000` iterations. BiCGSTAB/block-BiCGSTAB rows now report final true residuals recomputed by ROCm sparse matvec, plus recursive residuals for drift diagnostics. A coupled-frame-shell restarted block-BiCGSTAB refinement keeps the best true-residual iterate over `20` restarts and lowers the residual to `4.8291641718622635 N`; defect-correction now tries `3` passes, `20` correction restarts, and alpha scan `[1, 0.5, 0.25, 0.125, 0.0625, 0.03125]`. Only pass 1 is accepted (`alpha=1`) and improves the residual to `1.5131802298142958 N`; passes 2 and 3 are rejected because every alpha worsens the residual. Restarted contiguous block-Jacobi GMRES uses the defect-corrected vector as its initial state, runs `4` cycles with restart dimension `24` on ROCm sparse CSR matvec/device block preconditioner, and lowers the coupled residual further to `1.377426491587741 N`. Node-wise block-Jacobi GMRES uses the structural global DOF map so each node's free translational/rotational DOF are inverted as one `<=6x6` block; it lowers the coupled residual again to `1.0588801058470378 N`. Solution-fusion carries the selected block-BiCGSTAB, restarted, defect-corrected, contiguous-GMRES, and node-GMRES candidate solutions through ROCm CSR matvec residual checks and a tiny candidate-space least-squares solve; for coupled frame-shell it improves the frontier to `0.6769019326804031 N`. Hotspot subspace correction starts from that fused state, selects only high-residual structural nodes/DOF, solves the small selected dense subsystem on ROCm when supported, and accepts a correction only after full ROCm CSR residual replay. It lowers coupled frame-shell first to `0.2560322041370924 N` with node hotspots and then to `0.19175341268585733 N` with individual-DOF hotspots after `24` requested passes, where pass `11` stops because no hotspot candidate improves the residual. A wider individual-DOF hotspot solve over `[512,768,1024]` selected DOF and selected-column least-squares correction over `[32,64,128,256,384]` do not improve. Residual-row-neighborhood correction now targets the largest residual rows and their sparse row-neighbor columns, then also evaluates support-connected equation rows with stronger ROCm ridge regularization. Compressed overdetermined/equal subproblems run on ROCm dense least-squares (`device_residency_ratio=1.0`, no host fallback) but do not improve coupled frame-shell, while the rectangular row-neighborhood path uses ROCm ridge normal-equation solves instead of host `numpy` fallback and lowers coupled frame-shell to `0.188768968245979 N`. A new small-component direct correction then identifies disconnected components no larger than `32` DOF, applies vectorized singleton updates and small ROCm dense solves, and replays the full ROCm CSR residual. Shell has no nonzero-residual small components left, so the global `3.6595890543492713 N` residual is already in the `30164`-DOF largest component. Coupled applies `3` singleton updates plus one `6`-DOF component solve with no host fallback and lowers residual L2, but the inf residual remains `0.188768968245979 N` in the `36943`-DOF largest component. Post-hotspot contiguous block-GMRES then starts from the small-component/post-hotspot state and runs `4` cycles / restart dimension `24`; it does not improve either shell or coupled by itself, but adding that state to a post-small-component candidate fusion lowers the coupled frontier slightly to `0.18874947496692732 N` through an unconstrained candidate-space combination. A residual-row Kaczmarz coordinate correction then targets the largest residual rows, builds minimum-norm updates from each row's strongest sparse coefficients, and accepts only after full ROCm CSR residual replay. Shell accepts three very small damped passes and lowers the frontier to `3.598281468473605 N`; coupled accepts pass 1 with target rows `8`, pivot depth `1`, alpha `0.0078125`, lowering the frontier to `0.18866597120746142 N` before pass 2 rejects all candidates. Preconditioned residual-polishing and large-component coarse correction do not improve. A new post-coarse micro residual-row Kaczmarz then retries smaller target rows `[1,2,4,8,16,32]`, deeper pivots `[1,2,4,8,16]`, and micro alphas down to `3.814697265625e-06`; shell accepts 8 passes and lowers the residual to `3.594178194392615 N`, while coupled accepts 10 passes and lowers the residual to `0.18751791475959756 N`. The latest residual-row block least-squares correction then groups the largest residual rows, forms a small support from their strongest sparse coefficients, optionally expands equations to support-connected rows, solves the ridge-normal subproblem on the HIP device, and accepts only full ROCm CSR residual replay improvements. With the `72` requested-pass contract and a `1e-7` minimum relative-improvement floor, shell lowers to `3.100590677534983 N`; coupled lowers to `0.17985507354228858 N`, with no host dense-solve fallback, but both gates still fail. A post-block-LS residual-row Kaczmarz polish then starts from the block-LS state and replays every accepted candidate through full ROCm CSR residual evaluation: shell lowers further to `3.0976034733638036 N` before no candidate improves, and coupled lowers to `0.17961805492277472 N` after the full 16-pass polish. A new post-Kaczmarz support-expanded residual-row block-LS refinement then restarts support-connected ridge-normal subproblems from that Kaczmarz state and accepts `12/12` replayed improvements for both matrices, lowering shell to `3.0892099421746106 N` and coupled to `0.17923921541721022 N`. A post-refinement residual-row Kaczmarz polish then accepts `8/8` replayed ROCm CSR improvements, lowering shell to `3.087244829042059 N` and coupled to `0.17919523103181234 N`; post-block-LS candidate fusion now selects `post_refinement_residual_row_kaczmarz_polish`. This is stronger device-resident frontier evidence, not GPU sparse solve closure. It also reinforces that the remaining gap is true large-component preconditioner/solver quality rather than an `nvidia-smi` or device-availability issue.
- The ROCm solve attempt now records matrix topology and preconditioner diagnostics. Surface shell has one large connected component of `30164` free DOF plus `3768` singleton components; coupled frame-shell has one large component of `36943` free DOF plus `2848` singleton components. This proves the frame-style component dense direct GPU strategy cannot be promoted to shell/coupled closure. Symmetric diagonal scaling improves shell residual to `6.57 N` but still misses `1e-3 N`; shell node-block GMRES is non-closing (`24.43 N`), shell solution fusion selects the existing block-BiCGSTAB candidate at `7.52128831916159 N`, node-hotspot correction lowers shell to `3.9844827864214007 N`, compressed row-neighborhood lowers it slightly to `3.9807747077897506 N`, and device-resident rectangular/support-connected row-neighborhood lowers it further to `3.6595890543492713 N`, post-fusion row-neighborhood lowers it to `3.6029222561076146 N`, residual-row Kaczmarz correction lowers it again to `3.598281468473605 N`, micro residual-row Kaczmarz lowers it to `3.594178194392615 N`, residual-row block least-squares lowers it to `3.557611507949124 N`, the 12-pass block-LS frontier lowers it to `3.5495141763737585 N`, adaptive 24-request block-LS lowers it to `3.548139843221999 N`, support-connected 24-pass expansion lowers it to `3.2552306567282017 N`, support-connected 36-pass expansion lowers it to `3.2074072545603096 N`, support-connected 48-pass expansion lowers it to `3.1671974947612984 N`, support-connected 60-pass plus `1e-6` floor lowers it to `3.124106762855189 N`, support-connected 72-pass plus `1e-7` floor lowers it to `3.100590677534983 N`, post-block-LS residual-row Kaczmarz lowers it to `3.0976034733638036 N`, post-Kaczmarz support-expanded residual-row block-LS refinement lowers it to `3.0892099421746106 N`, and post-refinement residual-row Kaczmarz polish lowers it to `3.087244829042059 N`. For coupled frame-shell, contiguous block-Jacobi size `64` improved to `6.96 N` after the focused sweep/iteration increase, restarted best-iterate refinement improves it to `4.8291641718622635 N`, accepted defect-correction improves it to `1.5131802298142958 N`, restarted contiguous GMRES improves it again to `1.377426491587741 N`, node-wise GMRES improves it to `1.0588801058470378 N`, solution fusion improves it to `0.6769019326804031 N`, node-hotspot correction improves it to `0.2560322041370924 N`, individual-DOF hotspot correction improves it to `0.19175341268585733 N`, device-resident row-neighborhood correction improves it to `0.188768968245979 N`, post-small-component fusion lowers it to `0.18874947496692732 N`, residual-row Kaczmarz correction lowers it to `0.18866597120746142 N`, micro residual-row Kaczmarz lowers it to `0.18751791475959756 N`, residual-row block least-squares lowers it to `0.1855009656064901 N`, the 12-pass block-LS frontier lowers it to `0.18519121694350238 N`, adaptive 24-request block-LS lowers it to `0.18516644574001617 N`, support-connected 24-pass expansion lowers it to `0.18187573023073395 N`, support-connected 36-pass expansion lowers it to `0.18088345647535284 N`, support-connected 48-pass expansion lowers it to `0.18055366672156836 N`, support-connected 60-pass plus `1e-6` floor lowers it to `0.18040251129581186 N`, support-connected 72-pass plus `1e-7` floor lowers it to `0.17985507354228858 N`, post-block-LS residual-row Kaczmarz lowers it to `0.17961805492277472 N`, post-Kaczmarz support-expanded residual-row block-LS refinement lowers it to `0.17923921541721022 N`, and post-refinement residual-row Kaczmarz polish lowers it to `0.17919523103181234 N`; the path still misses `5e-2 N`.
- Superseding G9 ROCm/HIP frontier update: after the post-polish support-expanded block-LS state wins post-block-LS candidate fusion, a post-fusion support-expanded residual-row block-LS refinement now starts from that fusion best state, adds secondary support-connected pivots `[1,2,4,8]` with a `160` DOF cap, and accepts `6/6` full ROCm CSR replayed improvements for both matrices. The official `mgt_rocm_sparse_solver_probe.json` receipt lowers surface shell from `3.0824783275534497 N` to `3.0793146005754326 N` and coupled frame-shell from `0.17912703911878047 N` to `0.1790266766257389 N`, with host dense-solve fallback count `0`. This remains device-resident frontier progress, not G9 closure: shell still misses `1e-3 N`, coupled still misses `5e-2 N`, and CPU diagnostics/fallbacks cannot promote this gap.
- Large-component coarse counter-evidence is now stronger: the ROCm coarse correction no longer tests only free-DOF/global-position aggregate ordering. It also evaluates residual-hotspot aggregate ordering inside the dominant connected component, so shell/coupled each run `8` device coarse solves per first pass across `free_global_dof_order` and `residual_hotspot_order`. Both orderings still fail to improve the full ROCm CSR residual (`shell 3.598281468473605 N`, coupled `0.18866597120746142 N` at that stage), so the remaining G9 gap is not solved by simple aggregate reordering. The next credible closure path is a real large-component sparse preconditioner/AMG/Schur or domain-decomposition solver, not more scalar CPU fallback or `nvidia-smi` availability checks.
- Superseding G9 ROCm/HIP frontier/counter-evidence update: a device-resident overlapping Schwarz patch correction now starts from the post-fusion block-LS state, builds residual-hotspot patches with overlap depths `[0,1]`, solves all local ridge-normal patch systems on HIP, and accepts only full ROCm CSR residual replay improvements. The official receipt records `host_dense_solve_fallback_count=0`, `112` shell device patch solves, and `56` coupled device patch solves. Surface shell moves only from `3.0793146005754326 N` to `3.0792002574716264 N`; coupled frame-shell remains `0.1790266766257389 N`. This is useful counter-evidence for naive overlapping patch correction, not G9 closure.
- Post-Schur residual-row block-LS was tested as a follow-on hook after bounded Schur-interface correction, but it is now disabled by default. An official bounded integration attempt triggered a ROCm memory access fault (`Page not present or supervisor privilege`), recorded in `mgt_rocm_post_schur_block_lstsq_refinement_safety_receipt.json`. The hook remains opt-in via `RUN_POST_SCHUR_BLOCK_LSTSQ_REFINEMENT=1`; the default commercial path records it as non-promoting safety evidence and keeps the G9 frontier at shell `3.0792002451710445 N` / coupled `0.17902634022354746 N`.
- Superseding G9 ROCm/HIP frontier/counter-evidence update: additive Schwarz-Krylov correction now uses the overlapping patch solves as a device-resident preconditioner, builds a small flexible Krylov correction basis, solves the candidate-space normal equation on HIP, and accepts only after full ROCm CSR residual replay. The official receipt records `host_dense_solve_fallback_count=0`, shell `device_patch_solve_count=90` / `device_krylov_lstsq_solve_count=4`, and coupled `device_patch_solve_count=180` / `device_krylov_lstsq_solve_count=8`. Surface shell remains `3.0792002574716264 N`; coupled frame-shell moves only `0.1790266766257389 -> 0.17902634131095496 N`. G9 remains partial because shell still misses `1e-3 N` and coupled still misses `5e-2 N`.
- Superseding G9 ROCm/HIP counter-evidence update: a deflated Jacobi-Krylov correction now starts from the additive Schwarz-Krylov state, builds global Jacobi, raw residual, residual-hotspot, and Jacobi-preconditioned Krylov directions, solves the small candidate-space normal equation on HIP, and accepts only after full ROCm CSR replay. The official receipt records `host_dense_solve_fallback_count=0` and `device_candidate_solve_count=4` for both shell and coupled. It does not improve either matrix: shell remains `3.0792002574716264 N`, coupled remains `0.17902634131095496 N`, and both stop after one pass with `no_deflated_jacobi_krylov_candidate_improved_residual`. This rules out another cheap global candidate-space correction family and keeps the next credible closure path at AMG/Schur/domain-decomposition or a stronger production sparse backend.
- Superseding G9 ROCm/HIP frontier update: a structural-node coarse correction now uses `free_global_dof` and `dof_per_node` metadata to build piecewise-constant node/DOF aggregate modes over the dominant connected component, evaluates structural node-id and residual-hotspot aggregate orderings, solves the reduced Galerkin normal equation on HIP, and accepts only full ROCm CSR replay improvements. The official receipt records `host_dense_solve_fallback_count=0`, `device_coarse_solve_count=24`, shell largest-component node count `7490`, and coupled largest-component node count `8161`. It improves shell only `3.0792002574716264 -> 3.0792002451710445 N` and coupled only `0.17902634131095496 -> 0.17902634022354746 N`, then stops at the `1e-8` minimum-improvement floor. G9 remains partial; this is a real but tiny physics-aware coarse frontier step, not sparse solver closure.
- Latest G9 ROCm/HIP counter-evidence: an enriched structural-node coarse stage now adds node-order linear ramp and residual-signed weight modes to the piecewise-constant aggregate basis, evaluates aggregate counts `[8,16]`, solves all reduced Galerkin systems on HIP, and accepts only full ROCm CSR replay improvements. The official receipt records `device_coarse_solve_count=16`, `host_dense_solve_fallback_count=0`, and `device_residency_ratio=1.0` for both shell and coupled, but no candidate improves the replayed residual. Shell remains `3.0792002451710445 N` and coupled remains `0.17902634022354746 N` with `no_structural_node_coarse_candidate_improved_residual`. This rules out a wider cheap aggregate-basis family; the remaining credible G9 path is a true AMG/Schur/domain-decomposition preconditioner or production ROCm sparse backend.
- Runtime packaging, support bundle, on-prem skeleton, viewer performance budget are locally ready.
- The ROCm sparse solver test path is now split for CI/runtime scale: default pytest validates the existing productization receipt in about `1.12s`, while full ROCm regeneration remains available with `RUN_MGT_ROCM_HEAVY_GPU_TEST=1` or the official `scripts/run_mgt_rocm_sparse_solver_probe.py` command. This avoids treating a 9-minute GPU evidence-generation job as a routine CI smoke while preserving the heavy receipt as official solver evidence.
- Full line sparse CPU solve now records backend `scipy_sparse_spsolve_cpu` and total runtime about `0.89s` for 7,245 DOF across elastic and linearized geometric passes.
- Full 6-DOF frame sparse CPU solve records backend `scipy_sparse_spsolve_cpu_6dof_frame`, linear subproblem best-residual refinement `10`, and total runtime about `24.40s` for 14,490 DOF across elastic, linearized geometric, and scaled deformed-state P-Delta passes.
- Surface membrane sparse CPU solve records backend `scipy_sparse_spsolve_cpu_surface_membrane` and total runtime about `2.42s` for 34,065 translational DOF / 19,897 active membrane DOF.
- Surface shell bending/drilling sparse CPU solve records backend `scipy_sparse_spsolve_cpu_surface_shell_bending` and total runtime about `4.96s` for 68,130 DOF / 35,935 active shell DOF.
- Coupled frame/surface sparse CPU solve records backend `scipy_sparse_spsolve_cpu_coupled_frame_surface` and total runtime about `3.75s` for 68,130 DOF / 32,131 active DOF.
- Coupled frame/shell sparse CPU solve records backend `scipy_sparse_spsolve_cpu_coupled_frame_shell` and total runtime about `5.73s` for 68,130 DOF / 46,027 active DOF.
- `nvidia-smi` is not a valid availability gate for the AMD 6900XT target; the receipt uses `rocm-smi`, `rocminfo`, `/dev/kfd`, and `torch.version.hip`.
- Some CLI behavior still depends on environment/runtime path; CPU fallback may produce different convergence behavior than GPU path for condensed solve.

남은 개선점:

- Promote the new full MGT sparse shell/coupled ROCm residual replay, block-GMRES, node-block-GMRES, solution-fusion, hotspot-correction, row-neighborhood correction, post-hotspot-fusion, small-component direct correction, post-hotspot block-GMRES, post-small-component fusion, residual-row Kaczmarz correction, residual-polishing, large-component coarse-correction, micro residual-row Kaczmarz, support-connected residual-row block-LS, post-block-LS residual-row Kaczmarz, post-Kaczmarz support-expanded residual-row block-LS refinement, post-refinement residual-row Kaczmarz polish, post-polish support-expanded residual-row block-LS refinement, post-fusion support-expanded residual-row block-LS refinement, and post-block-LS candidate-fusion attempts to actual ROCm sparse solve/preconditioned iterative closure. Current production surface membrane, shell bending, frame/surface, and frame/shell MGT artifacts are still SciPy CPU solve backends, with ROCm residual replay and non-closing iterative evidence only for shell/coupled matrices.
- Replace the failed PyTorch ROCm sparse-direct/CG/BiCGSTAB/symmetric-scaled-BiCGSTAB/contiguous-block/node-block/fusion/hotspot/row-neighborhood/post-hotspot-fusion/small-component/post-hotspot-block-GMRES/post-small-component-fusion/residual-row-Kaczmarz/residual-polishing/large-component-coarse/micro-Kaczmarz attempts with a production sparse backend that works on gfx1030, likely custom HIP/rocSPARSE kernels, AMG/domain decomposition, or a stronger physics-aware Schur/block preconditioner.
- GPU evidence for full 3D global solver, not only story fingerprint/static path.
- CPU/GPU parity and deterministic tolerances across all solver families.
- Memory and time budgets for medium/large models with graceful degradation.
- Customer-device performance evidence separated from local workstation smoke.
- Long-run reproducibility, numerical drift, and floating-point tolerance policy.

완료 판정 예:

- Full solver benchmarks record backend, device residency, runtime, memory, and residual histories.
- CPU fallback is either product-supported with its own thresholds or explicitly blocked for workloads requiring GPU.

### G10. Product UX, Reports, Governance, Support

현재 상태:

- The viewer/product cockpit, delivery package, manifest/checksum, acceptance packet, QA summary, signing skeleton, and customer-open smoke are strong.
- Claim governance already forbids full autonomous replacement.
- `solver_governance_support_contract.json` is now `ready`; unsupported/proxy/fallback states are first-report visible, and delivery evidence bundle validation passes with `delivery_evidence_bundle.status=ready`.
- `commercial_gap_ledger_status.json`의 `G10.evidence.source_receipts`는 `workstation_delivery_readiness.json`, `solver_governance_support_contract.json`, `delivery_evidence_bundle.json`를 G10 closure 출처로 기록한다. 현재 workstation delivery는 `contract_pass=true`, gate `8/8`, delivery package manifest, customer-open viewer smoke, visual/performance evidence, client input validation, job reproducibility, retention policy를 포함한다. Governance contract는 `full_gap_ledger_ready=false`, `full_gap_ledger_status=open`, first-report unsupported-state policy `true`, report state separation, headline trace chain `customer_report_metric -> viewer_surface_or_delivery_index_link -> evidence_json_path -> solver_or_proxy_run_id -> input_model_hash -> claim_boundary_label`, engineer-review workflow states, support policy를 기록한다.
- 따라서 G10 closed는 workstation delivery packaging과 governance/support contract가 evidence-backed라는 뜻이지, full commercial readiness가 아니다. 같은 evidence 안에 unsupported/external rows `G1`, `G6`, `G7`가 남아 있고, workstation claim boundary도 independent commercial structural analysis product, structural engineer replacement, full autonomous replacement, customer-device FPS claim, multi-tenant SaaS throughput claim을 금지한다.

제품 확장 시 남는 운영정책:

- Engineer-review workflow는 상태와 final-report promotion 요구사항은 있으나, 실제 issue triage, acceptance/rejection, waiver, signed review, revision loop의 signed 운영 receipt가 필요하다.
- Customer-facing reports는 solver facts, proxy facts, external evidence, unsupported/fallback states를 분리해야 하며, 현재 G10은 그 contract를 닫지만 G1/G6/G7의 missing evidence를 대체하지 않는다.
- Product license/support policy, update channel, crash diagnostics, audit export, retention/delete workflow는 policy shape와 일부 workstation receipt가 있으나, 독립 상용/SaaS 운영 claim에는 별도 evidence가 필요하다.
- Security model for project data, tenant isolation if SaaS is ever claimed, secrets and audit retention.
- Release note policy for solver numerical changes.

완료 판정 예:

- A customer can trace every headline metric from report -> viewer -> evidence JSON -> solver run -> input model hash.
- Any unsupported or proxy/fallback result is visible in the first customer-facing report, not buried in internal JSON.

## Cross-Document Consistency Fixes Needed

These are documentation fixes, not solver implementation.

1. Refresh `commercialization_level.md` from the strict report so score/blockers match current script output.
2. Update `continued-improvement-backlog.md` I3 to reflect representative connected-component Newton convergence and full-mesh non-closure.
3. Update I5 wording to current `19/10/9/4` receipt and `repo_benchmark_bridge` provenance.
4. Split “RH local signed closure packet” from “strict external/legal RH closure evidence” everywhere it appears.
5. Add explicit warning wherever `mgt_global_fea_3d_native_solve.native_solve_status` is bridged by licensed fingerprint while `mesh_3d_global_solve.nonlinear_equilibrium=false`.
6. Add engineering sanity-envelope language for condensed story solve outputs so numerical convergence is not confused with physically validated full-building response.

## 2026-06-05 G9 ROCm/HIP Schur Interface Counter-Evidence

- Latest G9 ROCm/HIP counter-evidence: a bounded Schur-like interface correction now runs after enriched structural-node coarse correction on the official AMD ROCm/HIP path. It partitions the largest structural component, selects partition-boundary interface DOF, forms a capped interface subproblem (`partition_counts=[8]`, `max_interface_dof_count=96`, `max_equation_row_count=256`), solves ridge-normal interface systems on HIP, and accepts only full ROCm CSR residual replay improvements.
- Official receipt remains `partial line=True frame=True`. `rocm-smi` observed live Navi 21 activity around `61-67%` GPU during the heavy probe, confirming this was not a CPU fallback path. The new stage itself ran quickly (`~0.51s` shell, `~0.56s` coupled), with `device_schur_solve_count=4`, `host_dense_solve_fallback_count=0`, and `device_residency_ratio=1.0` for both shell and coupled.
- The stage did not improve the replayed residual: shell remains `3.0792002451710445 N` against the `0.001 N` gate and coupled remains `0.17902634022354746 N` against the `0.05 N` gate, both with `no_schur_interface_candidate_improved_residual`. G9 remains partial; this rules out another cheap bounded interface correction and keeps the credible closure path at production-grade AMG/Schur/domain-decomposition or stronger ROCm sparse backend work.

## 2026-06-05 G9 GPU Preconditioner Diagnosis

- A focused diagnostic using the same assembled shell free system confirmed that the shell matrix is mathematically solvable: SciPy sparse direct CPU reference remains `4.256728516338626e-07 N` residual in about `0.34s`, well below the `1e-3 N` shell gate. This does not close G9 because the official closure lane requires AMD ROCm/HIP solve evidence and forbids CPU sparse-direct fallback promotion.
- New bridge work added `implementation/phase1/rocalution_sparse_solve.cpp` and a Python rocALUTION invocation path. The bridge compiles against `/opt/rocm` and replays every returned solution with the full assembled CSR residual in Python before any gate can be marked closed.
- Focused rocALUTION multi-colored ILU counter-evidence is now stored in `mgt_rocalution_shell_ilu_focused_probe.json`. GMRES with multi-colored ILU `(p,q)=(0,1)` leaves shell residual `4936.275753520634 N`; `(1,2)` improves to `383.9171239394624 N`; `(2,3)` gives `504.62667877072585 N`. BiCGStab variants stay at the initial `32.47377433333335 N`. None reaches the `1e-3 N` gate.
- Focused rocALUTION SAAMG evidence is stored in `mgt_rocalution_shell_saamg_focused_probe.json`. The default SAAMG and GMRES+SAAMG bridge paths currently report iteration `0` and replay to the initial residual, so the simple rocALUTION AMG wrapper is not yet a valid production preconditioner.
- Conclusion: G9 is no longer primarily a residual-polishing problem. The remaining local closure item is a production-quality GPU preconditioner/backend: correctly configured AMG hierarchy with smoothing/coarse solver, AMGCL/Ginkgo HIP, or a custom HIP/rocSPARSE ILU/AMG implementation. More Jacobi/Ruiz/coarse-basis/local-patch residual correction should be treated as counter-evidence, not the main closure path.

## 2026-06-05 G9 ROCm/HIP Backend Reality Check

- The CPU sparse-direct path was used only as a non-promoting mathematical reference. It must not be interpreted as the active commercial solve lane. The active closure lane remains AMD ROCm/HIP; `rocm-smi` confirms the workstation GPU as Navi 21 / Radeon RX 6900 XT class hardware, and a long coupled rocALUTION SAAMG+GMRES attempt drove GPU utilization to `99%`, proving the tested path was not a silent CPU solver fallback.
- The bridge now fixes a rocALUTION GMRES configuration bug: GMRES supports L2 residual norm internally, while product closure still uses independent full CSR infinity-norm replay in Python. After the fix, GMRES rows run instead of aborting on the rocALUTION assertion.
- Expanded focused shell rows still do not close the gate. `multi_colored_ilu(1,2)+GMRES` remains the best tested rocALUTION family at `383.9171239394624 N`; standard `ILU(1)+GMRES` replays to `532.8228836243367 N`; `ILU(0)+GMRES` worsens to `18838.212800963247 N`; `ILUT+GMRES` and manual/default `SAAMG` replay to the initial `32.47377433333335 N` residual or perform no useful iteration. The shell gate remains `1e-3 N`.
- Ginkgo HIP was checked as the next credible backend route, but this workstation cannot currently build external HIP kernels: `hip-dev` is installed, yet `rocm-device-libs` / `AMDDeviceLibsConfig.cmake` and ROCm device bitcode are absent. CMake reaches HIP compiler detection only after forcing `/opt/rocm/bin/amdclang++`, then fails on missing device libraries. `sudo` requires a password, so the missing package cannot be installed by the agent.
- Updated next route: install/repair ROCm development libraries (`rocm-device-libs` matching the active ROCm stack) before Ginkgo/AMGCL HIP integration, or stay inside prebuilt ROCm libraries and implement a rocSPARSE/hipSPARSE preconditioned Krylov path that does not require compiling new HIP kernels. G9 remains open.

## 2026-06-06 G9 Dependency-Free ROCm Preconditioner Attempt

- The Ginkgo blocker was narrowed. A local extraction of `rocm-device-libs_1.0.0.50701-98~22.04` supplied `AMDDeviceLibsConfig.cmake` and bitcode paths, and Ginkgo HIP configure passed for `gfx1030`. The build still failed in HIP CSR kernel instantiation because Ginkgo's bfloat16 helper path references `__hip_bfloat16_raw` / conversions not available in the installed ROCm 6.0.2 HIP headers, even with `GINKGO_ENABLE_BFLOAT16=OFF`. This keeps Ginkgo as a non-closure dependency route for now.
- Following the no-new-dependency direction, `implementation/phase1/hipsparse_ilu_bicgstab_solve.cpp` adds a standalone bridge using only prebuilt HIP runtime + hipSPARSE: CSR SpMV, `csrilu02` ILU(0), `csrsv2` triangular solves, and host-orchestrated BiCGStab. A tiny CSR smoke closes to machine precision, proving the bridge and GPU library calls work.
- Focused shell evidence in `mgt_hipsparse_ilu0_focused_probe.json` shows hipSPARSE ILU(0)-BiCGStab does not close G9: the shell run reaches 3000 iterations and diverges to `1.502116910094704e+73 N` replay residual against the `1e-3 N` gate. This is counter-evidence against plain ILU(0) as the production preconditioner.
- rocALUTION incomplete Cholesky was also enabled as a no-new-dependency prebuilt ROCm route. Focused shell evidence in `mgt_rocalution_shell_ic_focused_probe.json` shows `CG+IC` reaches only `55.310924450017765 N`, while `GMRES+IC` returns to the initial `32.47377433333335 N` residual. IC is not sufficient for the shell gate.
- Updated next route: stop trying one-level ILU/IC/Jacobi/local-polish families as closure candidates. The remaining implementation path is a real multilevel method or stronger decomposition: custom smoothed-aggregation/aggregation AMG V-cycle, physics-aware Schur/domain decomposition, or a vendor/library backend that successfully builds and supplies those multilevel components on ROCm/HIP.

## 2026-06-06 G9 Two-Level Smoothed Aggregation Counter-Evidence

- A first no-new-dependency two-level smoothed aggregation PCG helper was added to `run_mgt_rocm_sparse_solver_probe.py`. It builds RCM-ordered algebraic aggregates, forms a tentative prolongation, optionally smooths it with damped Jacobi, forms `A_c = P^T A P`, and runs the sparse operator, prolongation, and coarse dense solve on the ROCm torch device. Returned candidates are still judged only by full assembled CSR residual replay.
- Focused shell evidence is stored in `mgt_smoothed_aggregation_pcg_focused_probe.json` and `mgt_smoothed_aggregation_pcg_sensitivity_probe.json`. The basic aggregate counts `[128,256,512]` do not close; the best sensitivity row is `aggregate_count=256`, `smoothing_weight=0.0`, `jacobi_weight=0.0`, `40` iterations, replay residual `31.997625165812096 N` against the `1e-3 N` shell gate.
- Interpretation: a naive two-level aggregation coarse space is not enough. It barely moves the initial shell residual and often worsens when diagonal/Jacobi mixing or prolongation smoothing is added. G9 should move to recursive multilevel hierarchy, better structural near-nullspace modes, or a stronger Schur/domain-decomposition operator rather than another single coarse correction.

## 2026-06-06 G9 Projected Multilevel Counter-Evidence

- The older orthogonal-projection/distributed-node residual-correction idea was reintroduced in a solver-consistent form, not as a learned post-hoc correction. `run_mgt_rocm_sparse_solver_probe.py` now includes a recursive projected V-cycle path whose first-level prolongation uses distributed structural-node DOF modes from `free_global_dof`; coarser levels use RCM algebraic aggregation. Sparse matvec, restriction, prolongation, Jacobi smoothing, Arnoldi/least-squares, and coarse dense solves run on ROCm torch.
- Focused shell evidence is stored in `mgt_multilevel_projected_pcg_focused_probe.json` and `mgt_multilevel_projected_fgmres_focused_probe.json`. PCG is unstable for this preconditioner family and worsens the residual. Flexible GMRES makes real progress from the initial shell residual `32.47377433333335 N` to a best replay residual `22.769081918072025 N`, but still misses the `1e-3 N` shell gate by about `2.28e4x`.
- Interpretation: distributed-node orthogonal projection is useful as a near-nullspace ingredient, but it is not enough by itself. G9 should not return to AI residual polishing as the main route. The next credible local implementation needs a stronger smoother/coarse operator, energy-minimized prolongation, or a block/Schur decomposition that captures shell bending and 6-DOF coupling modes more completely.
- A follow-up energy-smoothed prolongation option applies damped `P <- P - omega D^-1 A P` smoothing during hierarchy setup and records projector nonzero counts per level. Focused shell evidence in `mgt_energy_smoothed_projected_fgmres_focused_probe.json` does not improve the frontier: the best smoothed row is `24.324310976328043 N`, worse than the unsmoothed projected FGMRES best `22.769081918072025 N`. This narrows the next route away from simple prolongation smoothing and toward block/Schur physics or a materially stronger smoother/coarse solve.
- A coordinate-aware rigid-body near-nullspace variant was also tested. It keeps the first-level column count comparable to the node/DOF projector but adds aggregate rigid translations/rotations and rotation-induced translation terms from node coordinates. Focused shell evidence in `mgt_rigid_body_projected_fgmres_focused_probe.json` reaches only `24.25603853096381 N`. This is more compact than the constant node/DOF projector but does not improve the residual frontier, so G9 still needs a stronger block/Schur or production multilevel preconditioner rather than another small near-nullspace tweak.
- A first physics-aware translation/rotation DOF block Schur FGMRES preconditioner is now implemented as focused evidence. It splits the shell free system into translational and rotational DOF subblocks, applies approximate block triangular Schur solves with ROCm sparse subblock matvecs and Jacobi inner solves, and keeps full residual replay on the assembled CSR. Focused shell evidence in `mgt_dof_block_schur_fgmres_focused_probe.json` lowers the residual frontier to `15.396808816819568 N`, better than projected FGMRES (`22.769081918072025 N`) but still far from the `1e-3 N` shell gate. This is real frontier progress and confirms the block/Schur direction, but it needs stronger subblock solvers or a nested multilevel Schur preconditioner before G9 can close.
- A dynamic recycled-Krylov correction was added to the same DOF block-Schur FGMRES path. It stores FGMRES residual/preconditioned vectors on the ROCm torch device, builds a small orthogonal Galerkin correction basis, and accepts only damped candidates that improve full CSR residual replay. Undamped residual/preconditioned recycling is counter-evidence (`mgt_dof_block_schur_recycled_krylov_sensitivity_probe.json` worsens candidate residuals), and solution-update directions are also counter-evidence (`mgt_dof_block_schur_recycled_krylov_solution_update_sensitivity_probe.json` improves no row). Damped residual-only recycling is productive: `mgt_dof_block_schur_recycled_krylov_alpha_sensitivity_probe.json` reaches `15.031766501031372 N`, `mgt_dof_block_schur_recycled_krylov_residual_basis_followup_probe.json` reaches `14.728535946405538 N`, multi-pass correction in `mgt_dof_block_schur_recycled_krylov_multipass8_probe.json` reaches `14.030266570199556 N`, and basis-width sensitivity in `mgt_dof_block_schur_recycled_krylov_multipass_basis_sensitivity_probe.json` reaches `13.984934900412316 N` with residual-only basis size `16`. This is device-resident G9 progress, not closure; it still misses the `1e-3 N` shell gate by orders of magnitude and points to residual-recycled Schur/FGMRES as a useful ingredient rather than the final preconditioner.
- The focused recycled-Krylov runner now also supports the full coupled frame-shell 6-DOF matrix family, using the same coupled assembly as the official ROCm sparse probe (`frame_stiffness + shell_stiffness`, authored combined restraints, and frame gravity load scale `0.01`). Coupled evidence in `mgt_dof_block_schur_recycled_krylov_coupled_probe.json`, `mgt_dof_block_schur_recycled_krylov_coupled_restart_probe.json`, `mgt_dof_block_schur_recycled_krylov_coupled_restart_deep_probe.json`, `mgt_dof_block_schur_recycled_krylov_coupled_deep_inner_probe.json`, and `mgt_dof_block_schur_recycled_krylov_coupled_deep_inner_long_probe.json` narrows this route: restart budget and stronger inner Jacobi lower the focused coupled best from `189.40518585720895 N` to `49.81599342638738 N`, but the long run plateaus (`49.85823142913492 N`) and the `5e-2 N` coupled gate remains missed by about `996x`. This improves on the previous TT/RR subblock multilevel coupled counter-evidence (`703.9110398140688 N`) but is far worse than the official residual-polished coupled frontier (`0.17902634022354746 N`), so it is implementation coverage plus counter-evidence, not a commercial closure path.
- A node-local 6DOF block smoother was then added inside the same Schur/FGMRES preconditioner. It groups free DOF by source node, precomputes small `<=6x6` local block inverses as ROCm torch batches, and applies damped additive node-block residual correction after the translation/rotation Schur step. Focused shell evidence improves the recycled-Schur frontier from `13.984934900412316 N` to `13.670481762504783 N` with one sweep at weight `0.1`; weights `0.05`, `0.15`, `0.25`, and two sweeps are counter-evidence. Focused coupled evidence improves from `49.81599342638738 N` to `36.848427883622634 N` with one sweep at weight `0.15`; weights `0.05`, `0.1`, and `0.25` are weaker. This is real coupled 6DOF block progress and confirms that same-node translation/rotation coupling matters, but G9 remains open: shell still misses `1e-3 N` by about `1.37e4x`, and coupled still misses `5e-2 N` by about `737x`.
- A follow-up node-aggregate 6DOF coarse correction was implemented to test whether the node-local smoother can be lifted into a minimal hierarchy. It builds aggregate constant modes per local DOF, forms `B^T A B` on ROCm torch, and supports `coarse_then_smooth`, `smooth_then_coarse`, and `smooth_coarse_smooth` ordering. Focused evidence is counter-evidence for the current basis: shell aggregate counts `32` and `64` reach only `14.792773605311131 N` and `14.57075984773655 N`, the shell V-cycle ordering reaches `14.393511104295039 N`, and coupled aggregate `64` reaches `39.253358162057566 N`. All are worse than the smoother-only frontiers (`13.670481762504783 N` shell, `36.848427883622634 N` coupled). The next hierarchy must therefore use a better prolongator/domain partition, energy minimization, or true AMG/domain-decomposition operator rather than constant node-aggregate modes alone.
- Energy-smoothed node-aggregate prolongation was also tested by applying damped `P <- P - omega D^-1 A P` with column normalization before forming `B^T A B`. This does not rescue the constant aggregate basis: shell `omega=0.1` reaches `14.645940256098811 N`, shell `omega=0.33` worsens to `17.3820734477982 N`, and coupled `omega=0.1` reaches `39.44310265127285 N`. All remain worse than smoother-only, so the blocker is not just rough prolongation; the coarse space/domain partition itself is inadequate.
- A graph-aware RCM node partition option was added to replace sorted node-id aggregate chunks with matrix-adjacency RCM ordering before forming node-aggregate 6DOF coarse modes. This also underperforms: shell RCM aggregate `64` reaches `20.71995361193298 N`, and coupled RCM aggregate `64` reaches `46.83877198477329 N`. This rules out simple bandwidth ordering plus contiguous chunking as the missing domain partition; useful progress needs real graph clustering/domain decomposition, interface treatment, or an external-quality AMG partitioner.
- A connected graph-BFS node partition option was then added. Unlike RCM chunking, it grows connected node subdomains from high-degree seeds before forming the same 6DOF aggregate coarse modes. This is mixed but important evidence: shell BFS aggregate `64` worsens to `26.030906950015893 N`, but coupled BFS aggregate `64` improves the coupled focused frontier from smoother-only `36.848427883622634 N` to `7.566263063217367 N`. Coupled aggregate `128` worsens to `17.778351028061582 N`. G9 remains open because `7.566 N` is still about `151x` above the `5e-2 N` coupled gate, and shell remains best with smoother-only `13.670481762504783 N`; however, connected subdomains are now confirmed as a productive coupled direction while plain constants remain inadequate for shell.
- A damped graph-BFS coarse correction weight was then added so the connected-subdomain coarse update can be under-relaxed instead of applied at full strength. This moves both focused frontiers: shell graph-BFS aggregate `64` at weight `0.25` improves from the node-local smoother frontier `13.670481762504783 N` to `12.74233293760405 N`, and coupled graph-BFS aggregate `64` at weight `0.25` improves the connected-subdomain frontier from `7.566263063217367 N` to `5.815998578901954 N`. Coupled weights `0.5` and `0.125` are weaker (`8.463946951817654 N` and `8.17749304305611 N`). G9 remains open: shell still misses the `1e-3 N` gate by about `1.27e4x`, and coupled still misses the `5e-2 N` gate by about `116x`; damping is a useful hierarchy control, not closure.
- The focused runner now supports `--node-block-coarse-weights` to sweep multiple under-relaxation candidates in one receipt and record `node_block_coarse_weight_candidates` in the probe contract. Smoke evidence in `mgt_dof_block_schur_bfs_node_coarse_weight_sweep_smoke.json` records two rows (`0.25` and `1.0`) and selects the lower residual row, so future graph-BFS/coarse damping searches are reproducible instead of hand-orchestrated one-off probes.
- Interface-aware graph-BFS coarse modes were then tested. `node_block_coarse_mode=interface_split` separates each aggregate into interior and boundary node/DOF modes; `interface_boundary` keeps only boundary node/DOF modes. This is useful implementation coverage but not a frontier improvement: shell split evidence reaches only `17.088544552184835 N`, boundary-only weight sweep reaches `14.697228607242236 N` at weight `0.0625`, and coupled boundary smoke is far worse (`11339.570007567216 N`). The existing damped constant graph-BFS frontier remains stronger (`12.74233293760405 N` shell, `5.815998578901954 N` coupled). G9 remains open, and the next useful interface route needs an actual Schur complement/domain-decomposition operator rather than simply splitting aggregate constants at boundaries.
- Coordinate-aware rigid-body graph-BFS coarse modes were then wired into the same Schur/FGMRES path. The focused assembler passes source `node_xyz` into `_torch_sparse_dof_block_schur_fgmres`, and `node_block_coarse_mode=rigid_body` builds per-aggregate translational/rotational rigid-body columns from aggregate-centered coordinates before the ROCm `B^T A B` solve. This is a modest but real frontier move: shell improves from damped constant graph-BFS `12.74233293760405 N` to `12.530892150341918 N` at weight `0.125`, and coupled improves from `5.815998578901954 N` to `5.405688506206863 N` at weight `0.25`. G9 remains open: shell still misses `1e-3 N` by about `1.25e4x`, and coupled still misses `5e-2 N` by about `108x`; rigid-body coarse modes are a useful basis ingredient, not a production sparse solver closure.
- A broader coordinate affine-DOF coarse mode was tested next. `node_block_coarse_mode=affine_dof` adds constant plus local coordinate ramp columns per aggregate and local DOF, increasing the shell graph-BFS coarse basis to `1821` columns. This does not improve the Schur route: focused shell weight sweep reaches only `23.489876634475685 N` at weight `0.03125`, with larger weights worse. G9 remains open, and the result narrows the geometry route: aggregate-centered rigid-body modes help, but naive per-DOF affine ramps over-enrich or miscondition the coarse operator.
- A hybrid `rigid_body_plus_constant` mode was also tested to combine the best compact geometry basis with the previous local-DOF constants. This doubles the shell graph-BFS coarse basis to `1072` columns, but the shell sweep reaches only `24.457691968098256 N` at weight `0.0625`. This rules out simple basis union as the next step; compact rigid-body alone remains the strongest geometry-aware coarse mode.
- The focused runner now also supports `--node-block-coarse-smoothing-weights`, so prolongation smoothing weights can be compared in one receipt. Applying one damped smoothing step to the winning rigid-body graph-BFS basis is counter-evidence: shell reaches `12.883446086856926 N` at smoothing weight `0.1`, worse than unsmoothed rigid-body `12.530892150341918 N`. This narrows the current geometry path further: the compact rigid modes are useful, but naive Jacobi-smoothed prolongation is not the missing hierarchy quality.
- The focused runner now supports `--node-block-coarse-aggregate-counts`, recording `node_block_coarse_aggregate_count_candidates` so graph-BFS rigid-body aggregate counts can be swept in one receipt. Shell aggregate requests `[32,64,128]` confirm the existing `64` request remains best (`12.530892150341918 N`, actual aggregate count `111`), while coupled aggregate requests `[32,64,128]` improve the focused coupled frontier from `5.405688506206863 N` to `4.681515148086191 N` at request `32` (actual aggregate count `196`). This is useful frontier movement, but G9 remains open: shell still misses `1e-3 N`, and coupled still misses `5e-2 N` by about `94x`.
- A residual/RHS-weighted projected Schur-basis selection option was added and measured. The runner now supports `--schur-basis-selection`, `--schur-basis-selections`, and `--schur-basis-aggregate-counts`; `rhs_weighted` multiplies the algebraic `K_tr D_r^-1` / `K_rt D_t^-1` coupling scores by the normalized RHS magnitude before forming the projected `B^T A B` correction. This gives small but real frontier movement when combined with rigid-body graph-BFS coarse correction: shell improves `12.530892150341918 -> 12.466549536358821 N` at Schur basis aggregate `8`, and coupled improves `4.681515148086191 -> 4.37671406620138 N` at Schur basis aggregate `8`. G9 remains open by orders of magnitude, but this confirms residual-weighted algebraic Schur basis selection is more useful than static coupling-only projected Schur basis.
- The same projected Schur-basis correction now supports `--schur-basis-weight` / `--schur-basis-weights` to test under-relaxation. Focused weight sweeps show damping is counter-evidence rather than another frontier improvement: shell weights `[0.125,0.25,0.5,1.0]` keep full weight `1.0` best at `12.466549536358821 N`, and coupled weights `[0.5,1.0]` keep full weight `1.0` best at `4.37671406620138 N`. The current blocker is not over-application of this projected correction; it remains Schur complement/hierarchy quality.
- A nested diagonal-preconditioned CG option for the translational/rotational subblock solves was added and measured in `mgt_dof_block_schur_nested_pcg_focused_probe.json`. It does not improve the frontier: best nested-PCG row is `23.90068961216808 N`, while the Jacobi-Schur frontier remains `15.396808816819568 N`. This suggests the next blocker is not just subblock solve accuracy; the Schur complement approximation and block coupling model need to become stronger, likely by approximating the true complement or nesting multilevel/block preconditioners inside the Schur operator.
- A diagonal Schur-complement correction was also tested in `mgt_dof_block_schur_diagonal_focused_probe.json`, using `diag(K_tt) - diag(K_tr D_r^-1 K_rt)` and `diag(K_rr) - diag(K_rt D_t^-1 K_tr)` style scaling for the eliminated block. It does not improve: best diagonal-corrected row is `30.26888443952602 N`, and the frontier remains `15.396808816819568 N`. This rules out a purely diagonal Schur complement approximation; useful progress likely requires an actual sparse/low-rank complement action, block scaling, or nested multilevel preconditioner rather than diagonal-only coupling.
- Repeated coupled block-Schur sweeps were tested in `mgt_dof_block_schur_coupling_sweep_focused_probe.json`. They alternate translational and rotational block solves to approximate the coupled Schur action more directly, but still do not improve the frontier: best sweep row is `16.17976916961354 N` versus the one-pass Jacobi-Schur best `15.396808816819568 N`. This rules out simple block Gauss-Seidel repetition as a closure mechanism and points next to a better sparse/low-rank complement basis or a true multilevel Schur operator.
- A coupling-hotspot dense subspace correction was tested in `mgt_dof_block_schur_coupling_hotspot_focused_probe.json`. It selects translational/rotational DOF with the largest `K_tr`/`K_rt` coupling strength and applies a small ROCm dense correction to the preconditioner residual. It still does not improve the frontier: best hotspot row is `16.02124789197754 N`, while one-pass Jacobi-Schur remains `15.396808816819568 N`. This narrows the low-rank route: the basis cannot be chosen only by static coupling magnitude; it likely needs residual-adaptive, algebraic Schur, or multilevel coarse selection.
- An initial RHS/residual-hotspot selection mode was tested in `mgt_dof_block_schur_residual_hotspot_focused_probe.json`. It chooses the low-rank correction subspace from large initial RHS/residual DOF, and a mixed RHS+coupling score was also evaluated. This also does not improve the frontier: best row is `19.10982584203132 N`. The result narrows the basis-selection problem further: one-shot initial residual magnitude is not enough; a useful low-rank Schur basis likely needs Krylov-state adaptive selection, algebraic coarse modes, or a true multilevel complement.
- A first Krylov-state residual hotspot correction was tested in `mgt_dof_block_schur_krylov_adaptive_hotspot_focused_probe.json`. It evaluates the current FGMRES candidate residual, selects top residual DOF, solves a small dense correction, and only accepts if full CSR replay improves. It accepts `0` corrections in the focused sweep and does not improve the frontier; best row is `17.38041804427162 N`. This is exploratory host-selected evidence, not closure-grade, and it rules out a naive top-residual submatrix correction. The remaining low-rank route needs a better algebraic basis or projected Schur operator, not just largest residual coordinates.
- A projected algebraic Schur-basis correction was tested in `mgt_dof_block_schur_projected_basis_focused_probe.json`. It builds translation/rotation aggregate basis columns from approximate `K_tr D_r^-1 K_rt` / `K_rt D_t^-1 K_tr` Schur energy scores and applies a `B^T A B` projected correction inside the ROCm FGMRES preconditioner. This still does not improve the frontier: best row is `20.329296601244092 N` at `4` aggregates (`8` basis columns), versus the one-pass Jacobi-Schur frontier `15.396808816819568 N`. This rules out a simple aggregate projected Schur basis; useful G9 work now needs a true multilevel Schur operator, stronger smoother, or production AMG backend rather than another small projected low-rank space.
- A TT/RR subblock multilevel V-cycle was tested in `mgt_dof_block_schur_multilevel_subblock_focused_probe.json`. It builds RCM Galerkin hierarchies for the translation and rotation diagonal blocks and applies those V-cycles inside the Schur FGMRES preconditioner. It does not improve the shell frontier: best shell row is `22.975876831045937 N`, and the coupled rows are much worse (`703.9110398140688 N` best versus the `5e-2 N` gate). This narrows the blocker again: solving TT/RR subblocks with a local hierarchy is not enough; the preconditioner must approximate the coupled Schur complement itself, or use a production AMG/domain-decomposition backend that sees the full 6-DOF operator.
- A translation-rotation coupling-pair projected basis was added as a stricter re-test of the earlier orthogonal-projection/distributed residual-correction idea. The new `coupling_pair_basis_*` controls select strong `K_tr` pairs, build orthogonalized pair basis columns, and apply a device-resident `B^T A B` correction after the Schur/coarse/smoother stages. Focused receipts `mgt_dof_block_schur_coupling_pair_basis_shell_probe.json` and `mgt_dof_block_schur_coupling_pair_basis_coupled_probe.json` are counter-evidence: shell keeps the no-basis baseline `11.018219447125434 N`, while pair-basis rows worsen to `11.44438580837591`-`11.463268758123816 N`; coupled keeps `3.699645345812989 N`, while pair-basis rows worsen to `4.853945462017236`-`5.061124080994205 N`. G9 remains open; pair projection is useful implementation coverage but not the missing coupled Schur operator.
- A bounded graph-BFS subdomain Schwarz smoother was added as the next no-new-dependency domain-decomposition attempt. It reuses the connected graph-BFS node aggregate partition, builds bounded local subdomain matrices, truncates large aggregate supports by RHS magnitude to the configured dense width, inverts the local blocks in ROCm torch batches, and applies additive local residual correction after the existing Schur/coarse/smoother stages. Focused receipts `mgt_dof_block_schur_subdomain_schwarz_shell_probe.json` and `mgt_dof_block_schur_subdomain_schwarz_coupled_probe.json` do not improve: shell weight `0` remains best at `11.018219447125434 N`, while `0.025` and `0.05` worsen to `12.505130230810465` and `12.724763989404131 N`; coupled weight `0` remains best at `3.699645345812989 N`, while `0.025` and `0.05` worsen to `4.126192494233578` and `7.435020056198596 N`. This rules out a simple bounded additive Schwarz local solve on the current partition; useful G9 domain decomposition now needs a better interface/coarse coupling operator, overlap treatment, or production AMG/DD backend.
- ROCm bridge runtime safety was tightened after a full official probe rerun reached the ROCm path but timed out at `420s` and left a `rocalution_sparse_solve` child process alive. `run_mgt_rocm_sparse_solver_probe.py` now runs external rocALUTION/hipSPARSE solver bridges in their own process group and kills the group on timeout, recording `killed_process_group` and `kill_signal` in timeout rows. SAAMG rocALUTION candidates are no longer part of the default sweep after a prior GPU memory fault; they require explicit `MGT_ROCALUTION_ENABLE_SAAMG_SWEEP=1`. The standalone `mgt_rocm_sparse_solver_probe.json` was restored from the embedded G9 ledger evidence after a sandbox-blocked probe overwrote it, and the restored receipt explicitly preserves `status=partial`. This is execution-safety and evidence-boundary hardening, not G9 closure.
- The previously unused rocALUTION preconditioner family was then promoted into a focused shell sweep before any full official promotion. `mgt_rocalution_shell_preconditioner_sweep.json` tests `GMRES+MultiColoredILU(p=1,q=1)`, `GMRES+ILUT(q=15)`, and `GMRES+IC` in addition to the existing multi-colored ILU rows. None closes the `1e-3 N` shell gate: `p=1,q=1` replays to `32.47378268212585 N`, `ILUT(q=15)` exits at `0` iterations and replays to the initial `32.47377433333335 N`, `IC` replays to the same initial residual after `3` iterations, `p=1,q=2` remains `383.9171239394624 N`, and some multi-colored ILU/BiCGStab rows time out or diverge. `mgt_rocalution_shell_saamg_debug_sweep.json` then runs opt-in SAAMG debug candidates: `GMRES+SAAMG` hits a ROCm memory access fault, standalone default-smoother SAAMG builds `3` levels but replays to `30298939059.84492 N`, and coarse-size `30000` builds `2` levels but replays to `50448142818.25446 N`. This closes the cheap rocALUTION-candidate question as counter-evidence; the next G9 route should be a real RAS/AMG/DD operator or a production backend configuration, not another one-level ILU/IC/ILUT/SAAMG wrapper.
- The "larger local RAS patch" hypothesis was also tested without padding the entire batch to the largest block. The ROCm/HIP Schur path now switches large subdomain local inverses to `streamed_dense_inverse` storage, so each connected graph-BFS local matrix is inverted on the GPU separately instead of allocating a wasteful padded batch. `mgt_dof_block_schur_streamed_large_ras_shell_probe.json` uses `57` subdomains with max local width `4096` and `host_dense_solve_fallback_count=0`. The weight `0` baseline is still best at `14.744813152434435 N`; nonzero RAS weights worsen to `15.60196509888516 N` and `19.80249934320678 N`, far above the `1e-3 N` shell gate. `mgt_dof_block_schur_large_ras_current_shell_recycled_order_probe.json` also reproduces the current shell frontier at weight `0` (`11.018219447125434 N`) while `512/1024`-width RAS weights worsen. This rules out "the old patch was merely too small" as G9 closure. The blocker is now specifically interface/coarse coupling quality or true multilevel/domain-decomposition operator design for the dominant shell component.
- A multiplicative/swept RAS update mode was added to test whether the failure was caused by additive Schwarz applying every local correction against the same stale residual. In `node_block_subdomain_smoother_update_mode=multiplicative`, each local GPU dense correction immediately updates the global candidate and recomputes the residual before the next subdomain. `mgt_dof_block_schur_multiplicative_ras_shell_smoke.json` compares additive and multiplicative update modes on the same focused shell setup with `57` graph-BFS subdomains and max width `1024`, keeping device residency at `1.0` and host fallback at `0`. The best row remains the weight `0` baseline at `16.622547625913917 N`; additive RAS at weight `0.001` reaches `17.229920506541845 N`, and multiplicative RAS is slightly worse at `17.229922490627388 N`. This means the current local subdomain inverse is not failing only because of stale additive residual application; the missing G9 piece is still a better coarse/interface operator or true multilevel/DD preconditioner.
- An interface-edge coarse basis was added to move beyond whole-boundary splitting. `node_block_coarse_mode=interface_edge` identifies graph cut edges between aggregate pairs and creates pair-boundary DOF coarse columns before the device-resident `B^T A B` correction. The smoke receipt `mgt_dof_block_schur_interface_edge_coarse_shell_smoke.json` proves the mode is active (`81` aggregate-interface pairs, `1708` boundary nodes, `411` coarse columns, `device_residency_ratio=1.0`, host fallback `0`) and gives a small directional signal: weight `0` is `16.622547625913917 N`, while weight `0.01` reaches `16.594877302281674 N`. A stronger current-style receipt `mgt_dof_block_schur_interface_edge_coarse_shell_probe.json` does not promote that signal: weight `0` remains best at `14.744813152434435 N`, while weights `0.01` and `0.025` worsen to `14.745128722655913 N` and `15.321965032587467 N`. G9 therefore remains open; pair-boundary coarse modes are useful instrumentation and a possible ingredient, but not closure without a stronger multilevel/interface operator.
- The interface-edge basis was then combined with one damped smoothing step. `mgt_dof_block_schur_interface_edge_smoothed_shell_smoke.json` shows smoothing helps the reduced smoke: unsmoothed interface-edge weight `0.01` is `16.594877302281674 N`, smoothing weight `0.05` reaches `16.584897457260737 N`, and smoothing weight `0.1` reaches `16.571199149536795 N`. The stronger current-style receipt `mgt_dof_block_schur_interface_edge_smoothed_shell_probe.json` is counter-evidence: the weight `0` baseline stays `14.744813152434435 N`, unsmoothed `0.01` is `14.745128722655913 N`, and smoothed `0.01/0.1` worsens to `15.09784789888118 N`. This narrows the route again: smoothed pair-boundary modes may be useful inside a real hierarchy, but single-level smoothing does not close G9.
- The pair-boundary coarse correction can now be reapplied with residual recomputation, a minimal V-cycle-like repeated coarse pass. `mgt_dof_block_schur_interface_edge_repeated_coarse_shell_smoke.json` shows repeated passes can strengthen the reduced smoke: the best row is weight `0.01`, smoothing `0.1`, `3` coarse passes at `16.525447145065655 N`, improving over both zero coarse (`16.622547625913917 N`) and the one-pass smoothed row (`16.571199149536795 N`). The current-style receipt `mgt_dof_block_schur_interface_edge_repeated_coarse_shell_probe.json` is still counter-evidence: zero coarse stays best at `14.744813152434435 N`, while repeated nonzero coarse rows range from `14.860352857620295 N` to `15.359900620053148 N`. G9 remains open; repeated pair-boundary coarse passes are a route signal, not a closure mechanism without a better hierarchy/coarse operator.
- RHS-weighted pair-boundary interface columns now give a small but real current-style G9 improvement when damped. `node_block_coarse_mode=interface_edge_rhs_weighted` weights each aggregate-interface/DOF column by source RHS magnitude instead of a constant value, then applies the same device-resident `B^T A B` projected correction. `mgt_dof_block_schur_interface_edge_rhs_weighted_shell_weight_sweep_probe.json` lowers the current-style shell frontier from zero coarse `14.727081739528142 N` to `14.717569494056175 N` at coarse weight `0.005`, one correction pass, and smoothing `0.1`; weight `0.01` worsens to `14.804693730008754 N`. This is progress in the interface/coarse operator itself, but still far above the `1e-3 N` shell gate, so G9 remains open and the next step needs a stronger multilevel/interface Schur operator rather than larger scalar damping.
- Signed RHS pair-boundary weighting was tested under the same current-style shell sweep. `mgt_dof_block_schur_interface_edge_rhs_signed_shell_weight_sweep_probe.json` improves over zero coarse (`14.727081739528142 N`) to `14.720757625786018 N` at weight `0.005` and smoothing `0.1`, but it is weaker than magnitude-only RHS weighting (`14.717569494056175 N`) and full weight `0.01` worsens to `14.892200906801715 N`. The evidence keeps RHS-aware interface prolongation alive, but points to magnitude weighting rather than signed weighting as the better local basis.
- A simple constant+RHS enriched pair-boundary basis is counter-evidence. `node_block_coarse_mode=interface_edge_rhs_enriched` keeps the constant pair-boundary column and adds an orthogonalized RHS-magnitude column, raising the shell coarse column count from `411` to `564`, but `mgt_dof_block_schur_interface_edge_rhs_enriched_shell_weight_sweep_probe.json` keeps zero coarse best at `14.727081739528142 N`; nonzero weights worsen from `14.731079949057982 N` to `15.005981651201672 N`. This narrows the route: keep the damped RHS-weighted single basis as a useful signal, but do not assume wider pair-boundary enrichment is the missing hierarchy.
- Load-restricted enriched pair-boundary correction validates the coarse-load contamination diagnosis and gives the strongest current-style shell movement in this family. `node_block_coarse_mode=interface_edge_rhs_enriched_restricted` keeps the enriched stiffness/projected basis (`564` columns) but projects the preconditioner load/residual vector away from the `411` constant pair-boundary coarse columns before forming `B^T r`. `mgt_dof_block_schur_interface_edge_rhs_enriched_restricted_shell_weight_sweep_probe.json` lowers the shell residual to `13.824047006173632 N` at weight `0.0025`, three correction passes, and no smoothing, beating both magnitude-only RHS weighting (`14.717569494056175 N`) and unrestricted enriched (`14.727081739528142 N`). A fine weight follow-up confirms the same best row. G9 remains open by a wide margin against the `1e-3 N` shell gate, but the next Schur/DD route should preserve load-restricted enriched interface prolongation as the active frontier.
- The same load-restricted enriched route has a coupled-family signal but not a coupled frontier replacement. A full multi-row current-best coupled sweep with the new mode exceeded the bounded interactive timeout before writing a receipt, so it was not promoted. A smaller coupled smoke, `mgt_dof_block_schur_interface_edge_rhs_enriched_restricted_coupled_smoke.json`, keeps device residency `1.0` and no host dense fallback, and improves its local zero-coarse row from `62.44090595153894 N` to `61.45142142272349 N` at weight `0.0025`. A later single-row full-strength coupled probe, `mgt_dof_block_schur_interface_edge_rhs_enriched_restricted_coupled_current_single_probe.json`, completes and reaches `27.963756977175404 N`, better than the reduced smoke but still weaker than the existing coupled frontier (`3.699645345812989 N`) and far above the `5e-2 N` coupled gate. Coupled G9 therefore still needs a coupled-specific hierarchy/Schur combination rather than directly replacing rigid-body coarse with the shell-winning interface basis.
- A follow-up hybrid test now preserves the coupled `rigid_body` graph-BFS primary coarse route and adds `interface_edge_rhs_enriched_restricted` as a secondary projected correction. This validates the implementation path but is counter-evidence for simple stage composition: the bounded full-strength single row overran the interactive window before writing a receipt, the reduced smoke baseline without secondary correction is `719.4718284713658 N`, the shell-derived secondary setting worsens to `727.2150871516874 N`, and a tiny secondary setting (`weight=0.00025`, one pass) still worsens to `719.8017291511305 N`. The next coupled G9 unit should therefore change the coupled interface/coarse operator itself, not just append the shell-winning interface correction after rigid-body coarse.
- A direct test of the proposed full enriched-load orthogonalization route is now recorded as counter-evidence. `node_block_coarse_mode=interface_edge_rhs_enriched_orthogonalized` keeps the enriched pair-boundary basis but projects the preconditioner load/residual away from every enriched coarse column, not just the constant columns. The smoke receipts prove the projection is active (`restriction_column_count == coarse_column_count`: `564` for shell, `2188` for coupled) and stay device-resident with host dense fallback `0`, but they worsen the frontier: shell best is `15.396808816819568 N` versus restricted shell `13.824047006173632 N`, and coupled best is `189.36077165845572 N` versus restricted coupled smoke `61.45142142272349 N`. This means full RHS orthogonalization removes useful forcing as well as contamination; production G9 routing should keep selective constant-load restriction/RHS-weighted ingredients and move to a stronger coupled interface/coarse or AMG/DD operator.
- A first local energy-minimizing interface basis has now been added as `node_block_coarse_mode=interface_edge_energy_restricted`. For each aggregate-interface/local-DOF support, it forms a small symmetric local stiffness block, takes the lowest-energy eigenmodes as coarse basis columns, keeps constant interface columns only as load-restriction columns, and still applies the correction through the same device-resident Galerkin `B^T A B` projected solve. The focused shell smoke `mgt_dof_block_schur_interface_edge_energy_restricted_shell_smoke.json` records `244` energy modes, `122` load-restriction columns, device residency `1.0`, and no host dense-solve fallback. It gives a small basis-quality signal: zero coarse is `23.23643604628073 N`, while weight `0.0025` with three correction passes reaches `23.130067918481974 N`. This is larger than the ppm-scale load-partition tuning signal, but still far above the `1e-3 N` shell gate, so G9 remains open; the next energy route needs a real GENEO/multilevel interface operator rather than another single-level local eigenvector injection.
- A GENEO-style generalized version of that interface basis now gives a stronger route signal. `node_block_coarse_mode=interface_edge_geneo_restricted` scales each local interface stiffness block by its diagonal mass proxy before extracting the lowest generalized modes, then maps those modes back into the same Galerkin projected correction with the same selective constant-load restriction. The focused shell smoke `mgt_dof_block_schur_interface_edge_geneo_restricted_shell_smoke.json` records `22` interface pairs, `244` generalized energy modes, `122` load-restriction columns, device residency `1.0`, and no host dense fallback. It improves the same zero-coarse row `23.23643604628073 N` to `22.826183134567387 N`, beating the plain local `eigh(K)` energy basis (`23.130067918481974 N`) but still missing the `1e-3 N` shell gate by orders of magnitude. G9 therefore remains open, but the next no-new-dependency implementation should extend this into a true multilevel GENEO/interface Schur or domain-decomposition operator instead of tuning load splits.
- GENEO local mode-count sensitivity is now exposed by `--node-block-coarse-energy-modes-per-dof` and measured in `mgt_dof_block_schur_interface_edge_geneo_mode_count_sweep_shell_smoke.json`. The focused shell sweep over modes-per-DOF `[1,2,3,4]` keeps the same `22` interface pairs and selective `122` load-restriction columns, with device residency `1.0` and no host fallback. It improves the GENEO shell frontier from `22.826183134567387 N` at `2` modes/DOF to `22.673608427637085 N` at `3` modes/DOF, while `4` modes/DOF worsens to `22.83171465230876 N`. This confirms the blocker is not simply "more coarse columns"; the next route needs basis selection, multilevel transfer, and coupled interface-Schur quality.
- Local GENEO mode ordering is also governed by `--node-block-coarse-energy-mode-selection` and tested in `mgt_dof_block_schur_interface_edge_geneo_selection_sweep_shell_smoke.json`. At the current best `3` modes/DOF, `low_eigen` remains best at `22.673608427637085 N`; `rhs_projection` worsens to `23.197418644884337 N`, and `rhs_energy_score` worsens to `23.182806652657437 N`. This is counter-evidence against a simple RHS-weighted local mode selector. G9 still needs multilevel/coarse-transfer quality rather than only reordering single-level local modes by load projection.
- GENEO damping/pass sensitivity is now bounded in `mgt_dof_block_schur_interface_edge_geneo_weight_pass_sweep_shell_smoke.json`. With modes/DOF fixed at `3` and low-eigen ordering, weights `[0,0.0015,0.0025,0.0035,0.005]` and passes `[1,2,3,4,5]` improve the best only from `22.673608427637085 N` to `22.6733256606884 N` at weight `0.0015` and five passes. Larger repeated corrections are counterproductive: weight `0.005` reaches `26.122705369264658 N` at five passes. This closes the cheap single-level tuning question; G9 needs a structural multilevel/interface Schur/DD change, not more scalar damping/pass search.
- A first Schur-after-GENEO cycle was implemented to test a minimal multilevel-like alternation: after the GENEO coarse correction, the preconditioner recomputes residual, reapplies the translation/rotation Schur approximation, then reapplies the coarse correction. `mgt_dof_block_schur_interface_edge_geneo_schur_cycle_shell_smoke.json` is counter-evidence. The zero-cycle row keeps the current `22.6733256606884 N` frontier, while nonzero cycles worsen (`23.32046819597725 N` best one-cycle row; `23.20940148067457 N` best two-cycle row). This rules out a simple residual Schur-after-coarse alternation; the missing G9 piece is a materially different Schur complement/DD operator, not just cycling the same approximate blocks.
- A one-hop harmonic transfer extension was then added as `node_block_coarse_mode=interface_edge_geneo_harmonic_restricted` to test whether the GENEO basis was too tightly confined to interface DOFs. `mgt_dof_block_schur_interface_edge_geneo_harmonic_shell_smoke.json` extends selected generalized interface modes through adjacent CSR rows with diagonal Jacobi scaling, keeps the Galerkin `B^T A B` correction device-resident, and records `26189` harmonic-extension DOFs at the best row. This gives a small but real operator/transfer signal: extension weight `0.5` lowers the shell residual from the zero-extension `22.6733256606884 N` to `22.593705557868677 N`, while weight `1.0` worsens to `23.170971964048427 N`. G9 remains far from the `1e-3 N` shell gate; the next dependency-free route needs true multilevel transfer or interface/DD coupling, not more load-partition tuning.
- Harmonic transfer depth is now governed instead of hard-coded to one hop. `mgt_dof_block_schur_interface_edge_geneo_harmonic_depth_shell_smoke.json` fixes the best extension weight at `0.5` and sweeps depths `[1,2,3]`. The one-hop row remains best at `22.593705557868677 N`; two-hop expands the support to `28652` DOFs but worsens to `22.899531912544845 N`, and three-hop expands to `29657` DOFs and worsens to `22.993533780768566 N`. This is counter-evidence against simply pushing the same Jacobi harmonic extension deeper. The next G9 implementation should stabilize transfer through a real multilevel hierarchy, coarse solve, or interface/DD coupling rather than unbounded one-level support growth.
- Coarse-basis QR stabilization is now available through `--node-block-coarse-basis-orthogonalization=qr` and measured in `mgt_dof_block_schur_interface_edge_geneo_harmonic_qr_shell_smoke.json`. Under the same harmonic GENEO setting, QR keeps all `362` coarse columns but slightly improves the one-hop row from `22.593705557868677 N` to `22.5936722466134 N`; the two-hop QR row also only slightly improves its local row (`22.899531912544845 -> 22.899515893609532 N`) and remains worse than one-hop. This is a transfer-conditioning signal, not closure. G9 still needs a stable multilevel/interface Schur/DD operator rather than only Euclidean QR on a single-level basis.
- The load-restriction operator now exposes `--node-block-coarse-load-restriction-target` so selective coarse-space removal can act on the original preconditioner load vector or on the current residual vector. `mgt_dof_block_schur_interface_edge_geneo_harmonic_residual_restriction_shell_smoke.json` is counter-evidence for residual-space restriction in the harmonic GENEO route: the existing `load` target keeps the QR frontier at `22.5936722466134 N`, while `residual` target worsens to `23.26506328336595 N`. The route should therefore preserve selective load-target restriction and move to stronger multilevel/interface coupling rather than projecting out the evolving residual in the same single-level basis.
- A bounded interface-pair domain-decomposition smoother was then added as the smallest local interface/DD solve around the harmonic GENEO frontier. `mgt_dof_block_schur_interface_pair_dd_smoother_shell_smoke.json` builds `22` interface-pair local blocks on the ROCm/HIP path, keeps max block width at `128`, sweeps halo depths `[0,1]`, uses `padded_batched_dense_inverse`, and records `device_residency_ratio=1.0` with host dense fallback `0`. The best row is still the disabled smoother (`weight=0`) at `22.5936722466134 N`; nonzero weights worsen, with the best nonzero row only `22.81499423648211 N` (`halo_depth=1`, `weight=0.001`) and other rows reaching `23.160496091639935 N` to `23.90251435107608 N`. G9 therefore remains open, and this is counter-evidence for bounded additive interface-pair local solves; the next route needs stronger overlapped/stable DD, multilevel interface coupling, or AMG-grade preconditioning.
- Swept/multiplicative interface-pair DD application is now also bounded. `mgt_dof_block_schur_interface_pair_dd_swept_shell_smoke.json` keeps the same harmonic GENEO frontier and `22` local interface blocks, then compares additive versus residual-recomputed multiplicative update at `halo_depth=1` and weight `0.001`. Multiplicative slightly improves the nonzero local-solve row (`22.814994235978013 N` additive to `22.814749673790256 N` multiplicative), but the disabled smoother baseline remains best at `22.5936722466134 N`. This rules out stale additive residual application as the main blocker; useful DD work must change the interface/coarse coupling or overlap/operator quality itself.
- A direct post-interface coarse rebalance was tested next. `mgt_dof_block_schur_interface_pair_dd_coarse_rebalance_shell_smoke.json` applies the same multiplicative interface-pair local solve and then optionally reapplies the harmonic GENEO Galerkin coarse correction with rebalance weight `0.5`. This is counter-evidence: no rebalance keeps the frontier at `22.5936722466134 N`, while one rebalance pass worsens sharply to `32.01348863445636 N` without interface smoothing and `32.00781560811737 N` with interface smoothing. The missing G9 piece is therefore not a naive "local DD then same coarse again" cycle; it needs a materially different stable interface/coarse operator, overlap treatment, or AMG-grade hierarchy.
- The post-interface rebalance direction was then swept at small signed weights to rule out a simple scale/sign issue. `mgt_dof_block_schur_interface_pair_dd_coarse_rebalance_weight_sweep_shell_smoke.json` fixes multiplicative interface smoothing at weight `0.001` and sweeps rebalance weights `[-0.01,-0.001,0.001,0.01]`. The no-rebalance row remains best at `22.814749673790256 N`; the best signed rebalance row is worse (`23.015726093014894 N` at `-0.001`), and positive weights worsen more (`26.49219433187056 N` to `27.49464278727498 N`). This closes the cheap post-DD same-coarse tuning route; the next G9 operator has to alter the coarse/interface space itself rather than just changing rebalance damping.
- A residual-region-guided local DOF filter was then added to the harmonic GENEO interface basis. `mgt_dof_block_schur_interface_edge_geneo_harmonic_dof_filter_shell_smoke.json` compares `all`, `translations`, and `rotations` local DOF filters while keeping the Galerkin operator, QR basis, one-hop harmonic transfer, load-target restriction, and device-resident ROCm/HIP replay. The filter is active: coarse columns drop from `362` for `all` to `167` for `translations` and `195` for `rotations`. However, the all-DOF row remains best at `22.5936722466134 N`; translation-only worsens to `23.128330475949674 N`, and rotation-only worsens to `23.246764238877844 N`, even though the all-DOF residual summary places `100%` of top-64 residual rows on translation DOFs. This means the residual location is real, but local DOF filtering is not the missing operator. G9 still needs a stronger transfer/coarse hierarchy, interface Schur complement, or AMG/DD-grade preconditioner.
- Galerkin-energy basis orthogonalization was then tested as a direct conditioning check on the same harmonic GENEO coarse space. `mgt_dof_block_schur_interface_edge_geneo_harmonic_energy_orthogonalization_shell_smoke.json` compares Euclidean QR with `energy`, which diagonalizes `B^T A B` and rescales the basis by the inverse square root of the Galerkin energy magnitudes before replay. The energy path is active and keeps all `362` columns with no drop, but it slightly worsens the residual from the QR frontier `22.5936722466134 N` to `22.59379546634976 N`. This rules out simple single-level Galerkin-coordinate reconditioning as the missing G9 lever; the next operator must change hierarchy/interface coupling quality, not just rescale the same subspace.

## Completion Definition For “Complete Commercial Solver”

The following must all be true before product wording can move beyond engineer-in-loop assist.

| Area | Required closure |
| --- | --- |
| Full solver core | Full 3D global FEA assembly and nonlinear equilibrium on benchmark/full models, no hidden linear fallback |
| Dynamics/stability | 현재 대표 native modal/buckling은 닫혔고, response spectrum/time history/path-following 및 full-building stability는 benchmarked solver로 확장되어야 함 |
| Inputs | MGT/IFC/load/code entities are normalized, typed, round-trippable, and provenance-linked |
| Design codes | KDS breadth covers main building member/detail families with traceable governing clauses |
| V&V | External benchmark receipt `4/4`, strict RH closure `3/3`, commercial/live replay evidence attached |
| Corpus | Real medium/large building MGT/IFC corpus replaces bridge-only evidence for product validation |
| Optimization | Accepted optimization changes are replayable, solver-verified, code-checked, and cost-calibrated |
| Runtime | CPU/GPU parity, full-solver GPU evidence, scale/performance budgets, reproducibility policy |
| Product governance | Claim boundary, report provenance, support/security/release policies, engineer review workflow |

## Next Document

After this gap 원장과 AI 엔진 갭 원장을 accept하면 다음 문서는 **구현 계획**이어야 한다. 그 문서에서는 위 G1~G10이나 AI-G1~AI-G10을 그대로 구현 순서로 쓰지 말고, dependency, local closability, risk, evidence artifact, test command, rollback 기준으로 재정렬한다.

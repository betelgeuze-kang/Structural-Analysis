# Engine v2 Capability Matrix

- Status: Phase 0 planning and implementation tracker
- Authority: [Engine v2 master roadmap](structural-solver-engine-v2-master-roadmap.md)
- Machine-readable source: [`validation/capabilities/engine_v2_capability_matrix.json`](../validation/capabilities/engine_v2_capability_matrix.json)
- Claim boundary: 이 matrix는 미래 목표와 구현상태를 추적하며 현재 release readiness를 승격하지 않는다.

## 상태 축

- `implementation_state`: 코드 또는 계약이 실제로 존재하는 정도
- `promotion_state`: 검증 계층에서 사용할 수 있는 권한

prototype 또는 legacy asset이 존재해도 `promotion_state=non_promoting`이면 Engine v2 결과를 확정할 수 없다.

## 현재 요약

| Capability | Phase | Implementation | Promotion | 현재 경계 |
| --- | --- | --- | --- | --- |
| ModelIR v2 contract | Phase 0 | implemented | contract_only | strict schema/semantic validator와 golden fixture 완료; solver parity는 별도 미완료 |
| MGT → ModelIR v2 Phase 0 adapter | Phase 0 | implemented | contract_only | MGT 9.3.0 explicit linear-frame subset의 lossless audit/SI/semantic roundtrip; MIDAS 전체 호환 아님 |
| SolverModelBuffers v1 | Phase 0 | implemented | contract_only | 공통 dtype/shape/code/unit/entity/hash ABI; device allocation은 미구현 |
| ExecutionPlan v1 | Phase 0 | implemented | contract_only | node-major 6DOF, canonical full/reduced CSR, exact 7-stage graph, K/F/recovery hash; HIP 실행 아님 |
| Sparse-only ExecutionPlan v2 + CPU direct CSR | Phase 0 | implemented | contract_only | global dense K 없이 sorted full/reduced CSR을 직접 결정론 조립하고 sparse residual/JVP·direct solve·reaction/recovery/energy와 exact same-runtime replay를 검증; retained-array memory slope `1.0219`만 관찰했으며 peak memory·direct/end-to-end O(N)·HIP·ResultIR 연결은 미완료 |
| CPU fixed-restart FGMRES reference v1 | Phase 0 | implemented | cpu_reference | `r0=b-Ax0`, positive unshifted Jacobi right preconditioning, DGKS MGS, incremental Givens, true-residual dual gate와 scale-relative breakdown을 독립 구현하고 strict immutable receipt·mandatory deterministic replay를 검증; SciPy iterative/fallback은 없으며 HIP recurrence·iteration host-copy 0·AMG/DD·end-to-end O(N)·속도·ResultIR·상용 증거는 없음 |
| StateIR v1 | Phase 0 | implemented | contract_only | immutable initial/trial/commit 및 exact rollback; stateless linear elastic 한정 |
| ResultIR v1 CPU receipt | Phase 0 | implemented | cpu_reference | residual/reaction/recovery/energy 재검증과 전체 receipt chain; HIP/상용 증거 아님 |
| CPU reference operator | Phase 0 | implemented | cpu_reference | zero-offset/release 6DOF EB frame 및 linear truss narrow scope; buffer 무결성 검증 포함, shell/nonlinear/HIP parity 미구현 |
| HIP DeviceExecutionContext v1 | Phase 0 | implemented | contract_only | native runtime probe, 16개 ModelBuffer 지속 적재·실측 전송계수와 failed-open cleanup-only owner; operator/state/solver는 미결합 |
| HIP owner-minted allocation lineage foundation v1 | Phase 0 | implemented | contract_only | process-local typed extent·range·generation capability, atomic multi-owner borrow, immutable free/orphan target와 terminal quarantine, fresh exact-token owner control·publication history·controlled borrow admission을 제공; solver/device-content/signed promotion 증거는 없음 |
| HIP FreeSpace/Krylov allocation-lineage integration v1 | Phase 0 | implemented | contract_only | FreeSpace 12개·Krylov 9개 owned 실제 malloc/free와 parent 5-capability atomic borrow, context-v2 byte/stage conservation 및 중단 안전 RTC owner handoff를 검증; FGMRES predecessor·host-copy-zero·O(N)·속도·상용 증거는 없음 |
| HIP frame/truss device assembly v1 | Phase 0 | implemented | contract_only | host axis code와 symbolic reverse map만 H2D하고 HIPRTC 요소 기여도·결정론적 CSR gather로 수치 CSR을 장치에서 생성; `gfx1030` 컴파일·symbol 확인만 완료했으며 fresh native launch/parity·resident consumer·Krylov·속도·O(N)·상용 증거는 없음 |
| HIP assembly-resident CSR consumer v1 | Phase 0 | implemented | contract_only | live assembly owner의 CSR row/column/value와 foundation load를 exclusive lease로 같은 runtime/device/stream에서 빌리고 state/direction/residual/JVP 네 벡터만 소유; test-double full/free/constrained parity, zero-transfer enqueue, pre-lease kernel 회수성, atomic lifetime/cleanup, concurrent receipt snapshot과 strict telemetry/stage/receipt 상태기계를 검증했다. Resident 단독 API에는 device producer가 없고 아래 free-space child가 별도 제공하며, Krylov·iteration host-copy 0·native hardware parity·O(N)·speedup·상용 증거는 없음 |
| HIP free-space device-direction operator apply v1 | Phase 0 | implemented | contract_only | 5개 symbolic 배열만 H2D하고 borrowed full CSR/state/load에서 장치 내 `K_ff`·축약 state/load를 물질화한 뒤 `F-Ku` direction→resident residual/JVP→reduced JVP gather를 단일 세대 token으로 연결; test-double FP64 parity, `gfx1030` HIPRTC 3-symbol compile/확인, native hardware gate만 있으며 Krylov/CG/FGMRES/PCG·선조건기·linear solver·Newton·iteration host-copy 0·end-to-end O(N)·speedup·signed promotion·상용 준비 증거는 없음 |
| HIP Krylov vector/reduction + positive Jacobi primitives v1 | Phase 0 | implemented | contract_only | exact latest free-space apply 재검사와 child lease를 원자화한 same-stream context가 reduced CSR/direction/JVP 5개를 빌리고 9개 workspace를 소유해 positive unshifted Jacobi, affine, deterministic fixed-tree dot, scale-first LASSQ norm의 고정 diagnostic batch를 enqueue한다. Raw batch H2D/D2H/allocation/sync/fallback 0, strict schema/live witness, parity-failure shared poison, ack-failure retry owner, test-double FP64 parity, `gfx1030` HIPRTC 9-symbol compile과 조건부 native gate까지이며 recurrence·CG/FGMRES/PCG·SPD 증명·solver-integrated preconditioner·iteration host-copy 0·end-to-end O(N)·speedup·상용 증거는 없음 |
| HIP fixed-restart FGMRES allocation/policy plan v1 | Phase 0 | implemented | contract_only | exact ExecutionPlan/free-space overlay/FGMRES policy와 finite-positive Jacobi inverse를 결박하고 7 borrowed + 9 owned buffer, `M<=16`, global `I<=4096`, `P=ceil(F/512)`, dense `M²+5M+1`, little-endian field/code/flag가 명시된 solve record `192+72R` 및 DGKS/Givens/right-Jacobi ABI를 strict schema+mandatory semantic replay로 고정; compile-time HIP plan뿐이며 allocation·live apply/primitive lineage·HIPRTC recurrence·iteration host-copy 0·parity·O(N)·속도·상용 증거는 없음 |
| HIPRTC FGMRES 7-symbol recurrence substrate v1 | Phase 0 | implemented | contract_only | plan과 공유하는 solve-record field/status/reason/hint/flag ABI, fixed-source active-mask CSR SpMV·residual·host-scalar copy/scale·Jacobi·terminal control·restart record, gfx/code-object identity와 pending-stream unload fence를 구현하고 실제 `gfx1030` HIPRTC compile/7-symbol을 확인했다. 후속 resource-only live context는 별도 구현됐지만 이 v1 substrate 자체의 L2/L∞ producer·device-scalar recurrence·MGS/DGKS·Givens·backsolve·solution update·live solver receipt·native numerical parity·iteration host-copy 0·O(N)·속도·상용 증거는 없음 |
| HIP FGMRES recurrence allocation/control plan v2 | Phase 0 | implemented | contract_only | 7 borrowed+10 owned extent/256-byte control에 valid-predecessor column-0 `DECIDE -> non-advancing PREFLIGHT -> gated COMMIT -> FINALIZE` schedule을 결박했다. Current checkpoint contract `sha256:0583f66e5faa848da734ff8fbcc430d8bb71ef9fc854fab49121be3f61691e5d`는 exact full-final-cycle의 mandatory `FINAL_GUARD` handoff와 partial-final-cycle checkpoint terminalization을 분리하고, malformed mandatory prestate는 row publish 전 code 47로 fail-closed한다. Runtime allocation·authoritative transaction·solver·host-copy-zero·성능·상용 증거는 없음 |
| HIPRTC FGMRES initial + first-column checkpoint v0.2.15 | Phase 0 | implemented | contract_only | `E/Q=26+14S/14S`의 valid predecessor에서 raw `x_scale=trial+committed`와 `DECIDE -> gated COMMIT -> FINALIZE`를 실행해 `29+14S/14S`로 끝내고 mask 0/1792/7936을 finalizer까지 보존한다. Plan 58, RTC 57, oracle 95, focused 222, hardware 12, FGMRES 289, broad 1019와 actual HIPRTC를 통과했다. Raw launch owner만 true이며 invalid-source all-or-nothing, shifted/range overlap, same-buffer atomic 3-launch owner와 duplicate native policy는 미증명이다 |
| HIP FGMRES caller-attested checkpoint transaction context v0.2.16 | Phase 0 | implemented | contract_only | exact 11-role f64/u8 extent·alignment·uintptr/range registry, process/device native runtime domain, loader-minted runtime와 sealed native callable, actual `hipGetDevice`, compiler-issued handle/callable witness, atomic raw lease, single-use nonconstructible predecessor와 ambiguity poison, exact-runtime fence·atomic pending consume·retry cleanup을 구현했다. Historical three-row context/raw/HIP-context/FGMRES/broad/native `246/60/258/538/1268/12`; current binding은 fixed four-row지만 계속 caller-attested다. Canonical capability 소비·authoritative transaction, later recurrence·host-copy-zero·상용 증거는 없음 |
| HIP FGMRES live checkpoint resource context v0.2.19 | Phase 0 | implemented | contract_only | exact Krylov parent3와 fresh/exclusive allocator-owned8을 exact11 lease, internal RTC v2 module/checkpoint token, same runtime/device/stream과 semantic-last cleanup에 결속했다. Publication count exactly 8, controlled borrow, failed-open/interrupt recovery, actual `gfx1030` resource gate를 검증했다. Owned content, authoritative predecessor/mask validator, checkpoint transaction, solver/solution, host-copy-zero, O(N), speedup과 상용 증거는 없음 |
| HIP FGMRES canonical predecessor producer v0.2.20 | Phase 0 | implemented | contract_only | live exact11과 delegated CSR3/scratch2를 same runtime/device/stream의 allocation 추가 없는 exact16 projection으로 결속하고, owned8을 sealed `hipMemsetAsync`로 zero-initialize한 다음 exact `27+14S` canonical first-column kernel schedule과 device mask-domain validator gate를 단일 exact-runtime fence로 종료한다. Source-apply/Jacobi completion·owned initialization·device gate는 bound되지만 mask/verdict를 host에서 관찰하지 않으며 authoritative predecessor, checkpoint transaction, invalid-source atomicity, solver/solution, iteration host-copy 0, O(N), speedup, commercial readiness와 promotion은 모두 false인 non-promoting 계약이다 |
| HIP FGMRES checkpoint invalid-source atomicity v0.2.21 | Phase 0 | implemented | contract_only | source-only mode 9 preflight와 state 3 ticket을 추가한 fixed four-row raw transaction으로 late invalid lane에서도 `solution_x`/`true_residual` 전체 bytes를 보존한다. Plan/RTC/context-focused/native `63/100/77/13`, full context `261 passed in 523.33s`, race stress `5/5`와 final High/Medium 0을 확인했다. True 범위는 exact registered nonoverlap allocation, same stream, exclusive source ownership과 fixed owner sequence뿐이다. Canonical capability 소비·authoritative predecessor/transaction, arbitrary duplicate/external writer, later recurrence, host-copy-zero, O(N), speedup과 상용 증거는 없음 |
| HIP FGMRES canonical-capability-consuming sealed checkpoint transaction v0.2.22 | Phase 0 | implemented | contract_only | still-open canonical context의 single non-owning child가 conditional predecessor capability를 reserve 후 enqueue에서 single-use consume하고, 기존 live kernel/checkpoint token/stream/direct11과 physical16 projection에 fixed four-row transaction을 제출해 transaction-owned final fence 1회로 닫는다. Canonical parent 포함 total fence 2, 추가 allocation/device bytes/borrow/checkpoint owner/module/H2D/D2H/intermediate sync/fallback 0이다. Unit/legacy `23/56`, actual `gfx1030` valid/late-invalid scoped cases `2 passed`를 확인했다. Consume-return interruption reconciliation과 closed current-binding claim release를 포함하고, standalone receipt는 semantic consistency만 검증하므로 provenance authenticity에는 `expected_context` 또는 서명이 필요하다. Invalid predecessor state `{2,3}`와 snapshot 보존을 허용하며 terminal gate clear는 future action 차단일 뿐 과거 no-commit/rollback 단독 증거가 아니다. Product receipt는 actual mask/verdict/commit/device outcome을 관찰하지 않아 conditional continuation만 발행하며 authoritative predecessor/numerical transaction/solver/solution·later recurrence·host-copy-zero·O(N)·speedup·commercial/promotion은 false다 |
| HIP FGMRES sealed-continuation global recurrence owner v0.2.26 | Phase 0 | implemented | contract_only | v0.2.24의 flat exact-type/atomic pending/immutable dispatch `O(L)` host-control gate와 v0.2.25 process-local recovery를 유지한다. v0.2.26 actual `gfx1030` `F=24,nnz=360,M=2,I=4,R=2`에서 full/prefix/suffix `156/45/111`, checkpoint `E/Q=147/48`, active `FINAL_GUARD` `E/Q=148/48`, CPU iteration/restart `4/2` 및 operator/preconditioner `7/4`, solution/residual allclose를 확인했다. `I=5` partial-final-cycle은 기존 checkpoint-terminal `E/Q=179/58`과 inactive guard를 보존한다. Mandatory handoff required/validity를 분리해 malformed `next_expected_restart`를 error/status/code `1/6/47`, restart-row 미발행으로 fail-closed한 actual native 회귀도 통과했다. Global product path allocation/H2D/D2H/intermediate sync/fallback/live-read/host-branch 0, fence 1이지만 receipt는 outcome/parity를 계속 관찰하지 않는다. Kernel/solver speedup·일반 N-DOF O(N)·terminal observation·completion export·full parity·host-copy-zero·상용 증거는 없음 |
| HIP FGMRES exact stream query + abandoned global-owner recovery v0.2.25 | Phase 0 | implemented | contract_only | Checkpoint lease에 loader-sealed exact `hipStreamQuery`를 결속하고 status `0=complete`, `600=not ready`, 그 외·예외는 fail-closed로 처리한다. Parent-owned weak-liveness recovery cell은 child/context/lease를 strong-reference하지 않고 GC callback에서 HIP을 호출하지 않으며, abandoned consumed/pending child를 `query -> 필요 시 exact sync 1회 -> query -> pending ack -> terminal release`로 process-local 회수한다. Exact bool, frozen authority, stale-pending/partial-close, 중단 안전 단조 release를 강제한다. Actual `gfx1030` lifecycle gate는 query `not-ready -> complete`, sync `1`, pending `39 -> 0`, product malloc/H2D/D2H/runtime-facade sync·verification D2H `0`, `1 passed, 2 deselected in 37.42s`; RTC `111 passed in 34.77s`, checkpoint context `261 passed in 248.58s`, global owner full `54 passed in 1387.12s`, sealed transaction full `30 passed in 507.23s`, independent audit `BLOCKER/HIGH/MEDIUM/LOW=0`이다. Wheel/sdist는 `875235`/`823734` bytes, SHA-256 `e6522f810af2a4a0f6d62c770f510bcab57278e64cec4e0070b8fbec2eb2b8e2`/`8094a8bcaf30d3aaf954d5c5f0183baaf03881ff96ae62b33b6832276b2b3d3c`이며 wheel 격리 설치에서 global public API/schema/HIP kernel resource import를 확인했다. 이는 process crash/GPU reset/cross-process 회수, 수치·completion·parity·solution·O(N)·속도·상용 promotion 증거가 아닌 lifecycle 계약이다 |
| HIP FGMRES full recurrence ABI v2 | Phase 0 | in_progress | unavailable | later columns/restarts·terminal padding·final guard kernel과 conditional suffix owner를 구현했고, integrated owner에서 active later column, restart `1->2->3`, exact full-final-cycle checkpoint handoff 및 active final-guard epoch claim까지 actual hardware로 검증했다. Partial-final-cycle checkpoint terminalization과 mandatory-handoff malformed-prestate fail-closed도 별도 회귀로 고정했다. Product terminal observation, completion export, model-family full parity, iteration host-copy 0·일반 N-DOF O(N)·kernel/solver speedup·상용 증거는 없음 |
| HIP canonical-CSR AOT artifact v1 | Phase 0 | implemented | contract_only | 단일 source·versioned C ABI·동일 ROCm-root toolchain·hash/target 검증형 prebuilt loader; 실행/parity/speed 증거 아님 |
| HIP canonical-CSR residual/JVP replay v1 | Phase 0 | implemented | contract_only | plan/committed-state 상주, fused `Ku-F`/`Kv`, exact attempt/success telemetry와 test-double CPU oracle; 현재 native 실행 아님 |
| HIPRTC canonical-CSR residual/JVP v1 | Phase 0 | implemented | contract_only | RX 6900 XT `gfx1030`에서 fixed-source HIPRTC fused `Ku-F`/`Kv` 실행과 full/free/constrained FP64 parity·fallback 0 관찰; unsigned v1은 항상 non-promoting, 솔버/성능/O(N) 증거 아님 |
| HIPRTC fixed-degree-3 kernel scaling v1 | Phase 0 | implemented | non_promoting | 사전 고정한 off-cache 5개/4x family에서 HIP-event OLS `1.0089` (R² `0.99999`), Theil-Sen `1.0090`, bootstrap 95% CI `[1.0084, 1.0098]`과 정합성·timed transfer/allocation·fallback 0을 관찰; fixed synthetic fused kernel 전용이며 솔버/end-to-end O(N)·speedup·상용 증거 아님 |
| HIP AOT-native CSR residual/JVP parity | Phase 0 | in_progress | unavailable | AOT lane은 matching ROCm device-library build/run receipt가 없어 native parity 미증명; 별도 HIPRTC lane 관찰을 AOT release 증거로 승격하지 않음 |
| HIP resident residual | Phase 0 | research_asset | non_promoting | batch residual/FFI 자산은 있으나 full device solver 아님 |
| Device-resident Newton/Krylov | Phase 1 | planned | unavailable | iteration당 host copy 0 증거 없음 |
| E(3) spatial runtime | Phase 0/1 | planned | unavailable | 문서 요구사항만 존재 |
| Fixed-rank projection v1 | Phase 0 | implemented | contract_only | Jacobi square-root-energy scaling·2-pass MGS·implicit `Q(Qᵀv)`와 bounded complexity receipt; 솔버 해 아님 |
| AI correction·physics gate·shadow v1 | Phase 0 | implemented | shadow | immutable `DQy`, CPU full `Ku-F`/`||DRf||₂`/energy/BC/linear-constitutive replay, exact rollback, AI-off/observed bit parity; OOD 미평가로 reject, proposal 미소비 |
| Solver-approved fixed-rank QR memory v1 | Phase 0 | implemented | contract_only | validated ready CPU run만 teacher, rank≤16 FIFO/provenance/projection rebuild; RLS/Kalman/학습 update 아님 |
| Temporal no-attention runtime | Phase 0/1 | research_asset | non_promoting | 기존 T-GNN은 backprop/attention 구성 포함 |
| No-backprop local learning | Phase 0/1 | in_progress | unavailable | solver-approved QR memory substrate는 있으나 RLS/Kalman/readout parameter update는 없음 |
| Solver-gated AI proposal | Phase 0/1 | in_progress | shadow | Phase 0 safety contract는 구현; calibrated OOD/UQ, eligible proposal, iterative solver warm-start 소비와 가속 검증은 미구현 |
| Solver-verified optimization | Phase 1/2 | legacy_asset | non_promoting | 축약/proxy loop를 full FE 검증으로 교체 필요 |
| Independent commercial V&V | Phase 2/3 | blocked_external | unavailable | 외부/실프로젝트 증거 필요 |

v0.2.24 global owner의 immutable-dispatch TOCTOU 수정 후 required F12 later-column/F24 active-restart hardware 두 case는 combined `2 passed in 96.11s`로 재통과했다. Final independent linear re-audit는 transient drift/alias/registry forgery/signed-zero를 포함한 요청 범위에서 remaining defect 0으로 종료했다. 이는 numerical/performance/commercial promotion이 아니다.

v0.2.25는 v0.2.24의 C++/HIP/public schema/ABI identity를 변경하지 않고 exact stream-completion query와 process-local abandoned-owner lifecycle recovery만 추가한다. Recovery의 stream completion은 device numerical outcome이나 solver completion을 의미하지 않으며, abandoned path는 completion capability을 발행하지 않는다.

v0.2.26 current checkpoint/global/combined/source identity는 `0583f66e…` / `7c18ba91…` / `6a361ccf…` / `a5b39fb9…`다. Recurrence semantic payload/schema와 HIP source는 변경됐지만 public global receipt/completion schema는 변경되지 않았다. Active guard 실행 증거는 receipt에 actual outcome/parity 관찰을 추가하지 않는다.

## 갱신 규칙

- `implementation_state` 변경에는 코드 또는 schema path가 필요하다.
- `promotion_state` 변경에는 focused test와 authoritative evidence path가 모두 필요하다.
- 빈 `evidence_paths`로 promotion할 수 없다.
- `partial`, `proxy`, `fallback`, `external_blocked` 표시는 제거하지 않는다.
- release-facing capabilities surface와 연결하기 전 별도 claim-boundary review를 수행한다.

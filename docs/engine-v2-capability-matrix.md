# Engine v2 Capability Matrix

- Status: Phase 0 planning and implementation tracker
- Authority: [Engine v2 master roadmap](structural-solver-engine-v2-master-roadmap.md)
- Machine-readable source: [`validation/capabilities/engine_v2_capability_matrix.json`](../validation/capabilities/engine_v2_capability_matrix.json)
- Current local candidate milestone: v0.2.65 HIP FGMRES fixed-rank coarse live-route strict public receipt
- `origin/main` inclusion은 주장하지 않음
- Claim boundary: 이 matrix는 미래 목표와 구현상태를 추적하며 현재 release readiness를 승격하지 않는다.

## 상태 축

- `implementation_state`: 코드 또는 계약이 실제로 존재하는 정도
- `promotion_state`: 검증 계층에서 사용할 수 있는 권한

prototype 또는 legacy asset이 존재해도 `promotion_state=non_promoting`이면 Engine v2 결과를 확정할 수 없다.

## 현재 요약

v0.2.65는 private live typed-slot-plus-terminal-guard 실행 context를 공개하지
않으면서 strict pointer-free receipt/schema를 추가한다. Receipt는 exact live/coarse
opening receipt, FGMRES/coarse plan, recurrence/coarse/slot/guard identity와 source,
full/sealed/continuation schedule, coordinate와 schedule epoch를 결박한다. 각 성공
application은 logical recurrence/legacy Jacobi/slot/guard/total launch를
`1/0/4/1/5`로 고정하고 두 physical owner의 accept와 parent-fence acknowledgement를
분리해 기록한다. Full fixed program의 coordinate 수는 `R*M`, restart 수는
`ceil(I/M)`, retained rank는 free DOF 이하로 고정한다. Detached validation은
schema와 semantic self-consistency만,
`expected_context` validation은 process-local provenance와 freshness까지 검증한다.
Stale opening receipt, coherently rehashed status/context/telemetry/reason, shortened
schedule, oversized rank, non-tuple application container, unknown field, unredacted
pointer-like hex, direct physical-owner fence bypass는 fail-closed한다. Sealed
full route는 logical `4`, Jacobi `0`, slot `16/16`, guard `4/4`, parent fence `2`이고
poisoned partial prefix는 slot `5`, guard `1`을 모두 parent-fence ack한 상태로만
게시한다. Focused live/receipt `7 passed in 130.89s`, receipt public API `2 passed in
1.80s`, public/capability `25 passed in 1.96s`이며 공개 표면은
Engine/Assembly/Solvers `1263/1071/66`이다. Actual integrated typed-slot full recurrence
device execution/parity, full-iteration host-copy 0, external actual `gfx1100`, O(N),
speedup, promotion/commercial은 계속 미증명이다.

v0.2.64 당시에는 v0.2.63의 live typed gate/dot/solve/apply 뒤에 exact `1x1`
same-stream device terminal guard를 붙인다. Exact inactive 값만 no-op이고 mixed
status는 fail-closed하며, uncertain launch는 matching parent fence 전 재제출할 수
없다. Slot `4`+guard `1`은 recurrence ledger의 logical row `1`로 유지되고 두
physical owner가 모두 기존 canonical/global parent fence를 승인받아야 한다. Native
unload가 성공을 반환한 뒤 Python finalization이 중단돼도 known-success 상태를
보존해 double unload를 막는다. Actual local `gfx1030` device-visible 관찰에서는
nonfinite coarse status가 terminal status/code/error/origin `6/43/4/2`로 게시됐고
guard launch-window H2D/D2H copy delta는 `0`이었다. 현재 device-visible 재검증은
local `gfx1030` hardware 2건을 포함해 `21 passed in 8.16s`였다. 이 관찰은
process-local·unsigned·non-promoting이다. Live recurrence `7 passed in 131.91s`,
adjacent canonical/overlay/selected-global `26 passed in 461.50s`,
public/capability `25 passed in 1.95s`, full RTC `141 passed in 46.18s`를
확인했으며 독립 재실행한 guard/live/full-RTC 합계는 `169 passed`다. 공개 표면은
Engine/Assembly/Solvers `1249/1057/66`이고 live recurrence context는 private이다.
이 milestone 자체에서는 strict public receipt/schema, integrated typed-slot full-solve parity,
full-iteration host-copy 0, external actual `gfx1100`, O(N), speedup,
promotion/commercial이 미증명이었다. Strict public receipt/schema는 v0.2.65에서
후속 구현됐지만 나머지 actual 실행 경계는 유지된다.

v0.2.63은 exact live checkpoint와 fixed-rank coarse child를 독점 예약해 canonical
prefix와 global suffix의 immutable `APPLY_JACOBI_INDEXED` 좌표를 typed slot으로
치환한다. Sealed full-route test double은 logical application `4`, legacy Jacobi
launch `0`, slot accept/ack `16/16`, parent fence `2`를 확인했다. v0.2.64 guard
결합 뒤 focused recurrence는 `7 passed in 131.91s`다. 이는 private process-local
test-double integration이었으며 actual integrated device solve가 아니다. Public strict
receipt/schema는 v0.2.65에서 추가됐지만 live context는 계속 private이다.

v0.2.62는 frozen recurrence-v2와 coarse-v1 source bytes를 변경하지 않고 coarse
source를 전용 namespace에 넣은 뒤 typed slot supplement와 하나의 HIPRTC translation
unit으로 합성한다. 선택된 fixed-rank 경로는 logical `APPLY_JACOBI_INDEXED` 1개를
legacy Jacobi launch `0`, schedule claim `1`, gate/dot/solve/apply physical launch `4`로
고정한다. Full pointer extent/alignment/non-overlap, partial·ambiguous·BaseException
launch ownership, exact-stream fence, task-local compile handoff, unload uncertainty와
post-unload finalization을 fail-closed로 소유한다. Source compile은 `gfx1030/gfx1100`
모두 통과했고 raw recurrence RTC owner는 four physical launches를 checkpoint
pending/audit ledger의 logical row 1개로 예약한다. Selected
plan/RTC/safety/public/capability/adjacent aggregate는
`69 passed, 3 hardware skipped in 5.76s`, full recurrence RTC v2는
`137 passed in 44.63s`다. 공개 표면은 Engine/Assembly/Solvers
`1232/1040/66`이다. 현재 namespace에는 `/dev/kfd`가 없어 실제 typed-slot module
load/실행은 관찰하지 않았다. Live canonical/global route가 아직 이 owner를 선택하지
않으므로 실제 Jacobi 행 치환, coarse status terminal 결박, integrated full-solve parity,
full-iteration host-copy-zero, O(N), speedup, promotion/commercial은 미증명이다.

v0.2.61은 immutable global schedule의 모든 fixed Jacobi 좌표를 canonical prefix와
global suffix exact order로 결박하고, 기존 `APPLY_JACOBI_INDEXED` 직후 same stream에
fixed-rank coarse four-kernel application을 제출한다. Canonical/global parent fence가
coarse pending을 별도 coarse runtime sync 없이 승인하며 application 추가
H2D/D2H/allocation/sync/CSR apply는 `0`이다. Exact live/coarse exclusive route, final
global receipt와 optional terminal-observation receipt 결박, fence-ack STORE 중단 복구,
partial accepted launch의 fence·poison·lease cleanup, pointer-free strict schema를
구현했다. 공개 표면은 Engine/Assembly/Solvers `1211/1019/66`이다. Focused overlay
`8 passed in 239.62s`, public/capability `22 passed in 1.99s`, adjacent coarse/live
`24 passed in 190.08s`/`42 passed in 204.33s`, adjacent canonical/global
`14 passed in 113.18s`/`54 passed in 1212.23s`, adjacent sealed checkpoint
`30 passed in 449.24s`, public-count compatibility
`9 passed in 42.47s`를 통과했다. 이는 기존 Jacobi launch를 유지하는 test-double 기반
additive integration이다. Direct coarse device-status terminal binding, current-source
actual-device integrated full-solve parity, full-iteration host-copy-zero, external
`gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다.

v0.2.60은 live checkpoint parent가 이미 독점 borrow 중인 `jacobi_inverse`,
`basis_v`, `preconditioned_basis_z` 세 capability를 중복 registry borrow 없이 exact
semantic child로 위임하고, 별도 peer allocation-lineage owner가 coarse `Z/AZ/L`,
`rhs/coefficients/status` 여섯 capability를 소유하는 실행 context를 추가한다. Static
H2D `3`회와 setup fence `1`회 뒤 ready가 되며 application 경계는 launch `4`,
H2D/D2H/allocation/sync/additional CSR apply `0`이다. Partial launch, fence 실패,
known-not-freed, uncertain unload, second-child/parent-close 경쟁과 internal compiler
return/STORE 중단을 fail-closed로 회수한다. Context/application receipt는 pointer-free
strict schema이며 공개 표면은 Engine/Assembly/Solvers `1196/1004/66`이다. 현재 증거는
injected test double 수명주기와 required local `gfx1030` live-context 실행이다. Context
`24 passed in 194.48s`, plan/loader/public `37 passed, 2 hardware skipped in 12.33s`,
capability `19 passed in 0.24s`, adjacent live-checkpoint context
`42 passed in 208.55s`, adjacent RTC/public API `45 passed in 51.44s`, hardware
`1 passed in 23.55s`를 통과했다. 실제
`gfx1030` 관찰은 `F=12`, `k=2`, owned `452` bytes, static H2D `3/416` bytes,
setup+application fence `2`, launch accepted/acknowledged `4/4`, status `0`, application
copy delta `0/0/0`, CPU exact FP64 equality다. Canonical recurrence row 교체, device
status terminal 결속, full solve/iteration host-copy-zero, independent external `gfx1100`,
AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다.

v0.2.59는 fixed-rank coarse RTC의 full-extent pointer range·alignment·interior-overlap
검증, native launch 전 uncertain pre-arm, uncertain unload 재호출 금지, dot barrier 전
block-uniform status gate, i32 상한에서도 정의된 apply grid를 추가했다. Current source
`sha256:d7a20e80…f5912`와 ABI `sha256:d79dbc97…1191`은 `gfx1030`/`gfx1100`
HIPRTC source compile을 통과했다. Plan/loader/public focused set은
restricted namespace에서 `35 passed, 2 hardware skipped in 12.39s`였고,
device-visible root namespace의 current 37-case suite는 두 hardware case와 additive
two-thread serialization·reentrant fail-closed case를 포함해 `37 passed in 13.58s`였다. Hardware 두
항목의 별도 current-source 실행은 `2 passed in 2.95s`였다. Additive reentrant
callback 회귀도 deadlock 없이 fail-closed했다. Public symbols는
당시 Engine/Assembly/Solvers `1176/984/66`이었다.
Current-source local `gfx1030` identity는 `sha256:4646cffb…ea3d`이며 `4/4`
launch, status `0`, application copy delta `0/0/0`, CPU exact FP64 parity를 관찰했다.
v0.2.58의 local `gfx1030` 수치 관찰은 superseded source `sha256:feaad0d5…e50d`에
결박되므로 current-source 실행/parity 증거로 재사용하지 않는다. v0.2.60이
allocation-lineage context와 exact live parent delegation을 추가했지만 FGMRES recurrence
통합, external `gfx1100`, O(N)/speedup/promotion/commercial은 계속 미증명이다.

v0.2.57은 CPU fixed-rank coarse artifact의 `Z/AZ/L`과 exact `HipFgmresPlanV1`을
결속하고 parent 할당 `3`개를 borrow하며 additive buffer `6`개를 소유하는
application plan/source ABI를 고정한다. Static H2D는 `3`회,
적용당 launch는 prepare/dot/solve/apply `4`회이고 application 경계의
H2D/D2H/allocation/sync/additional CSR apply와 dense `N×N` projector는 `0`으로
계획됐다. Strict plan/schema/source 검증 `16 passed in 12.05s`와 package source의
`gfx1030`/`gfx1100` HIPRTC compile을 통과했지만 이 milestone 자체는 실행 영수증이
아닌 contract-only 계획이다.

v0.2.56은 v0.2.54의 detached reviewer-root registry-v3 genesis와 v1 runner PoP 사이에
epoch-2 enrollment와 epoch-3 activation 전이를 추가한다. 두 event는 전용 review domain
아래 서로 다른 purpose/statement를 사용하고 ordered unique `2..3-of-3` reviewer signature를
검증한다. Detached receipt는 source genesis, PoP, 두 event와 rolling registry hash를
재생하고 attached result는 bootstrap source까지 deterministic하게 다시 결박한다. Focused
unit/public/registry-public `26 passed in 34.97s`, adjacent trust chain `144 passed in 58.66s`,
capability `18 passed`, public symbols Engine/Assembly/Solvers `1152/960/66`이다. Constructed detached state의 reviewer/enrolled/active
count `3/1/1`은 synthetic contract일 뿐 package count는 계속 `0/0/0`이다. 실제 reviewer,
isolated runner/HSM, package activation, external `gfx1100`, signed TraceIR, durable ledger,
promotion/commercial은 미검증이다.

v0.2.55는 기존 sparse `ExecutionPlanV2`와 CPU FGMRES recurrence 사이에 fixed-rank
coarse right-preconditioner를 실제로 연결한다. Energy-scaled two-pass MGS basis `Z`,
retained `AZ`, small SPD `E=Z^TAZ`를 사용해 dense `N×N` projector와 apply-time 추가 CSR
matvec 없이 multiplicative coarse correction 뒤 Jacobi smoothing을 수행한다. `k<=16`의
build/apply receipt는 각각 `O(nnz*k+N*k^2+k^3)`/`O(N*k+k^2)`이고 focused/public/schema
`21 passed in 7.53s`, 기존 CPU FGMRES 인접 회귀 포함 `52 passed in 120.60s`, wall
`121.19s`, peak RSS `132,432 KiB`다. 이
결과는 CPU 진단 seam이며 HIP/AMG/DD, mesh-independent iteration, 전체 O(N), speedup,
ResultIR/promotion/commercial 증거는 아니다.

v0.2.53은 검증된 general-history parity v2 receipt를 final solution·ResultIR·StateIR
commit 권한과 분리된 `solver_restart_diagnostic_trace`로 투영한다. Restart 순서와
source-row canonical hash, CPU/HIP vector hash reference, 다섯 scalar roundoff envelope를
보존하면서 raw vector와 result array는 embed하지 않는다. Projection 추가
device/D2H/solve/export/state-commit/fallback은 모두 0이고 wire work는 `O(R)`이지만,
source history의 `O(RF)` 비용이나 전체 해석 `O(N)`·speedup을 주장하지 않는다.
Exact-resource wheel을 포함한 final focused set은 `34 passed in 38.76s`, 기존
ResultIR v3/DiagnosticIR/family DiagnosticIR 인접 권한 회귀는
`34 passed in 362.33s`였다. Public symbols는 Engine/Assembly/Solvers `1085/912/47`이다.
Current-source actual local RX 6900 XT `gfx1030` required gate는 trace rows `3`, end
`(2,4,5)`, completion D2H `5/5`, trace 추가 device/D2H/solve/export/state-commit/fallback
0으로 `1 passed in 103.32s`, wall `103.90s`, peak RSS `358,760 KiB`를 기록했다. Trace
ID는 `sha256:567e7df6…6cfe`, receipt는 `sha256:ccecb0e3…eb48`이고 실행 전후
source/schema/harness aggregate `sha256:2661f374…deda1`이 동일했다. External `gfx1100`,
signed/persistent provenance, recovery/ResultIR 권한, promotion/commercial readiness는
여전히 미검증이다.

v0.2.49는 v0.2.48의 componentwise `B_i` receipt를 full replay한 뒤 reverse triangle
inequality로 `L2/Linf/scaled-Linf` 예산을 유도한다. Residual exact-real norm은
outward interval로 감싸고, 검증된 CPU stable-L2와 HIP GPU-tree-L2 record의 interval
evaluation error를 더해 caller tolerance 없이 terminal 세 scalar record 차이를 검증한다.
기존 model-case parity v1 schema/receipt와 fixed solution/residual/terminal/history gate는
불변이며 v2는 additive detached receipt다. Focused `14 passed in 9.53s`,
v0.2.48+v0.2.49 `34 passed in 14.57s`, legacy/capability 포함 `71 passed in 15.06s`,
all-converged 인접 `140 passed in 395.82s (0:06:35)`, capability `13 passed`를
통과했다. Required actual local `gfx1030` 원래
고하중 3-case gate는 completion D2H 3회·fallback 0을 유지하며
`1 passed in 226.25s (0:03:46)`, peak RSS `429,808 KiB`였다. 최대 terminal
record/bound ratio는 four-span `0.002952072072072049`; 실행 전후 source aggregate
`sha256:f88ab168…66dc3`가 동일했다. Core receipt actual backend/provenance,
restart-history v2, 고하중 ResultIR/aggregate, external `gfx1100`, signed/persistent evidence,
speedup·commercial은 여전히 false 또는 미완료다.
Single dirty non-release wheel은 `1,427,672` bytes/`270` members,
`sha256:da8eeef8160cdd4dfdd0f83250162b603ae3a4e37453ecd5c65dadcd28b355ae`,
격리 public symbols는 Engine/Assembly/Contracts/Elements `932/746/93/10`이었다.

v0.2.48은 caller absolute tolerance 없이 `|A||delta_x|` solution transport와 두 계산
경로의 `gamma_k*(|b|+|A||x|)+k*eta`를 합친 행별 FP64 residual bound를
`ExecutionPlanV2`에 결속한다. 모든 bound 연산을 `nextafter(+inf)`로 outward rounding하고
componentwise backward error를 기록하며 좁은 contract work/storage는 `O(nnz+n)`이다.
Focused/adapter `20 passed`, legacy v1 `24 passed`, capability `12 passed`를 통과했다.
All-converged registry/family/ResultIR/public API를 포함한 7-file adjacent cross-check는
`116 passed in 264.39s (0:04:24)`였다.
Required actual local `gfx1030` 원래 고하중 3-case gate도 strict solution, completion D2H
3회, fallback 0을 유지한 채 `1 passed in 218.17s (0:03:38)`, peak RSS `428,820 KiB`로
통과했다. 최대 error/bound는 `0.03547771831820348`, HIP componentwise backward error는
`1.2238707852511078e-15`였다. Core receipt는 backend/provenance를 주장하지 않고 기존 v1
terminal/history metric 및 ResultIR authority도 아직 변경하지 않는다.
Current-source single dirty non-release wheel은 `1,411,419` bytes/`266` members,
`sha256:1e61eac1fddf52329d1daa3b789e1f59b838054aff87b908d7adfed19ce56e28`이고
격리 public symbols는 Engine/Assembly/Contracts/Elements `903/732/78/10`이었다.

v0.2.47은 종료 의미론 registry를 변경하지 않는 별도 all-converged 10-slot registry와
exact 10/10 ResultIR vertical-slice contract/harness다. Current-source required local
`gfx1030` gate는 CPU fallback 없이 `1 passed in 1087.52s (0:18:07)`로 exact case/bridge
`10/10`, family, aggregate와 post-close validation을 통과했다. Process peak RSS는
`450,868 KiB`, post-close current RSS는 `433,188 KiB`였다. 첫 두 actual run은 각각
rotated-axis와 four-span의 near-zero absolute residual parity에서 fail-closed했고,
tolerance를 변경하지 않은 채 cancellation-sensitive 세 fixture를 unit load로 정규화해
strict parity/ResultIR-ready를 확인한 뒤 최종 gate를 다시 실행했다. 일반 load scale의
residual vector bound는 downstream v0.2.48에서 별도로 관찰했지만 v0.2.47 ResultIR
authority, persistent/signed hardware evidence 또는 promotion 증거로 소급하지 않는다.
과거 v0.2.45 termination-registry의 `5757.94s`와 current-source `7820.35s` 실행도 신규
all-converged hardware evidence로 재사용하지 않는다. v0.2.46 backend
dependency refactor와 historical wire operator identity/frozen CPU·sparse hash도 그대로 유지한다.

Current-source 검증은 combined contract `57 cases within the 68-case cross-check`, capability
`11 passed in 0.31s`, 5-file cross-check `68 passed in 259.34s (0:04:19)`다. 별도
restricted namespace의 hardware non-required는 `1 passed, 1 skipped in 1.74s`,
required는 `1 passed, 1 failed in 1.76s`였고 실패 이유는
`No real gfx agent was detected.`다.

| Capability | Phase | Implementation | Promotion | 현재 경계 |
| --- | --- | --- | --- | --- |
| HIP FGMRES fixed-rank coarse slot recurrence strict receipt v1 v0.2.65 | Phase 0 | implemented | contract_only | Private live typed-slot-plus-guard route의 strict pointer-free public receipt/schema를 추가한다. Exact live/coarse opening receipt, FGMRES/coarse plan, recurrence/coarse/slot/guard identity와 source, full/sealed/continuation schedule, coordinate/epoch를 결박하고 full fixed-program `R*M`, restart `ceil(I/M)`, retained-rank bound와 application logical/legacy/slot/guard/total `1/0/4/1/5`를 고정한다. Slot과 guard accept/parent-fence ack를 분리하며 full route `4`, Jacobi `0`, slot `16/16`, guard `4/4`, parent fence `2`; poisoned partial prefix slot `5`, guard `1`을 보존한다. Detached semantic validation과 `expected_context` process-local provenance/freshness 권한을 분리하고 stale opening receipt, coherent rehash, shortened schedule, oversized rank, reason/container, unknown field, unredacted pointer-like hex, direct physical-owner fence bypass를 거부한다. Focused `7 passed in 130.89s`, receipt public API `2 passed in 1.80s`, public/capability `25 passed in 1.96s`, public `1263/1071/66`이다. Live context export, actual integrated typed-slot full recurrence device execution/parity, full-iteration host-copy-zero, external actual `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES fixed-rank coarse device terminal guard v1 v0.2.64 | Phase 0 | implemented | contract_only | Frozen recurrence source 뒤의 exact `1x1` guard가 typed slot의 coarse status를 기존 first-error-wins terminal state machine에 device-side로 게시한다. Live recurrence ledger는 logical row `1`당 slot `4`+guard `1`을 같은 stream과 parent fence로 소유한다. Test-double full route는 logical `4`, Jacobi `0`, slot `16/16`, guard `4/4`, parent fence `2`; guard rejection/exception은 slot prefix 때문에 logical ambiguous `1`로 보존한다. Actual local `gfx1030` guard는 nonfinite status를 terminal status/code/error/origin `6/43/4/2`로 게시했고 launch-window H2D/D2H delta `0`이었다. `gfx1030/gfx1100` HIPRTC compile과 public `1249/1057/66`을 확인했다. Strict live receipt/schema, actual integrated typed-slot full solve/parity, full-iteration host-copy-zero, external actual `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES typed fixed-rank coarse slot live recurrence v1 v0.2.63 | Phase 0 | implemented | contract_only | Exact live/coarse exclusive route가 immutable Jacobi 좌표를 canonical prefix와 global suffix에서 dispatch 전에 typed gate/dot/solve/apply로 치환한다. v0.2.64 current route는 각 logical row에 device terminal guard를 추가해 slot `4`+guard `1`을 같은 parent fence로 회수한다. Test-double full route는 logical `4`, legacy Jacobi `0`, slot accepted/fenced `16/16`, guard `4/4`, parent fence `2`, generic coarse launch `0`을 확인했고 partial global slot prefix는 slot total `5`와 guard `1`을 exact fence했다. Open lease rollback, incomplete/parent-fence-bypass close 거부와 lock-order 계약을 포함한다. Private context에는 strict public receipt/schema가 없으며 actual integrated typed-slot full-solve parity, full-iteration host-copy-zero, external `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES typed fixed-rank coarse slot source/RTC owner v1 v0.2.62 | Phase 0 | implemented | contract_only | Frozen recurrence/coarse source를 byte-identical하게 합성하고 logical Jacobi row 1개를 selected coarse route의 legacy Jacobi launch `0`, epoch claim `1`, gate/dot/solve/apply launch `4`로 고정한다. Inactive padding은 bit 31로 분리하며 full pointer range·launch uncertainty·external fence·compile handoff·unload/finalization 수명을 소유한다. Raw recurrence RTC checkpoint/audit ledger가 four physical launches를 logical pending 1개로 예약한다. Source `gfx1030/gfx1100` compile, selected aggregate `69 passed, 3 hardware skipped in 5.76s`, full recurrence RTC `137 passed in 44.63s`, public `1232/1040/66`을 확인했다. Live canonical/global selector의 이 seam 소비, post-gate coarse error terminal binding, actual module execution/full solve parity, full-iteration host-copy-zero, external `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES fixed-rank coarse recurrence overlay v1 v0.2.61 | Phase 0 | implemented | non_promoting | Immutable schedule의 모든 fixed Jacobi 좌표에서 retained `APPLY_JACOBI_INDEXED` 직후 same stream에 exact four coarse kernels를 제출하고 canonical/global parent fence로 pending을 승인한다. Exact live/coarse route, ordered application/global/optional terminal-observation receipt, zero additional application H2D/D2H/allocation/sync/CSR apply, partial accepted-launch poison cleanup, parent-fence 전 healthy close 거부, coherent semantic forgery 거부와 strict pointer-free schema를 구현했다. Focused `8 passed in 239.62s`, public/capability `22 passed in 1.99s`, no-overlay coarse/live/canonical/global `24/42/14/54 passed`, sealed checkpoint `30 passed in 449.24s`, public compatibility `9 passed in 42.47s`다. Legacy Jacobi 행 치환, direct coarse-status terminal binding, actual integrated device full-solve parity, full-iteration host-copy-zero, external `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES fixed-rank coarse execution context v1 v0.2.60 | Phase 0 | implemented | non_promoting | Live checkpoint parent의 exact 3 capability를 중복 registry borrow 없이 semantic delegation하고 peer allocation-lineage owner가 coarse 6개 allocation을 소유한다. `Z/AZ/L` static H2D `3`과 setup fence `1` 뒤 ready가 되며 application은 exact launch `4`, H2D/D2H/allocation/sync/additional CSR apply `0`이다. Fence/known-not-freed 재시도, uncertain unload quarantine, second-child/parent-close exclusion, internal compiler return/STORE handoff, pointer-free strict context/application schema를 구현했다. Context `24 passed in 194.48s`, plan/loader/public `37 passed, 2 hardware skipped in 12.33s`, capability `19 passed in 0.24s`, adjacent live-checkpoint context `42 passed in 208.55s`, adjacent RTC/public API `45 passed in 51.44s`, required local `gfx1030` hardware `1 passed in 23.55s`다. Actual context 관찰은 `F=12`, `k=2`, owned `452` bytes, static H2D `3/416` bytes, fence `2`, launch `4/4`, status `0`, copy delta `0/0/0`, CPU exact FP64 equality다. Recurrence row 교체, terminal status 결속, full solve/iteration host-copy-zero, independent external `gfx1100`, AMG/DD, O(N), speedup, signed evidence, promotion/commercial은 미증명이다. |
| HIP FGMRES fixed-rank coarse HIPRTC loader v1 v0.2.59/v0.2.60 handoff | Phase 0 | implemented | non_promoting | Package-owned fixed source의 exact 4-symbol/identity와 same-stream launch/fence 수명을 유지하면서 full pointer-range·alignment·overlap, launch uncertainty pre-arm, nonretryable uncertain unload, serialized/nonreentrant module operations, block-uniform dot status gate, overflow-safe apply grid를 추가했다. v0.2.60은 internal compile owner의 one-shot task-local return/STORE handoff를 더했다. Current source/ABI `d7a20e80…f5912`/`d79dbc97…1191`은 `gfx1030`/`gfx1100` compile을 통과했다. v0.2.59 restricted/current hardware 검증은 `35 passed, 2 hardware skipped`, device-visible `37 passed`, 별도 hardware `2 passed`였고 local `gfx1030` identity `4646cffb…ea3d`, `4/4`, status `0`, copy delta `0/0/0`, CPU exact FP64 parity를 기록했다. v0.2.58 관찰은 superseded source에만 유효하다. Loader 자체는 recurrence/terminal-state 통합, full-iteration host-copy-zero, external `gfx1100`, O(N), speedup, signed evidence, promotion/commercial 권한을 발행하지 않는다. |
| HIP FGMRES fixed-rank coarse application plan/source v1 v0.2.57 | Phase 0 | implemented | contract_only | CPU coarse artifact과 exact FGMRES plan을 replay하고 parent borrow `3`, owned extent `6`, static H2D `3`, same-stream launch `4`를 고정한다. Application 경계의 H2D/D2H/allocation/sync/additional CSR apply와 dense `N×N` projector는 `0`이다. Strict plan/schema/source `16 passed in 12.05s`, package source `gfx1030`/`gfx1100` HIPRTC compile을 통과했다. Live context, actual execution at this milestone, recurrence status binding, AMG/DD, mesh-independent iteration, end-to-end O(N), speedup, promotion/commercial은 미증명이다. |
| HIP FGMRES diagnostic restart TraceIR v1 v0.2.53 | Phase 0 | implemented | contract_only | General-history parity v2의 ordered restart rows를 diagnostic-only TraceIR로 deterministic projection한다. Source receipt/bindings/row hash, CPU/HIP vector hash reference, solution fixed gate와 residual diagnostic gate, 다섯 outward scalar envelope를 보존하고 embedded vector/result array 및 projection 추가 device/D2H/solve/export/state commit/fallback은 0이다. Detached receipt는 standalone provenance가 아니며 attached wrapper도 process-local exact source replay에 한정된다. Exact-resource wheel 포함 final focused `34 passed in 38.76s`, 인접 ResultIR/DiagnosticIR 권한 회귀 `34 passed in 362.33s`, public symbols `1085/912/47`을 확인했다. Current-source actual local `gfx1030` required gate는 3 rows/end `(2,4,5)`, completion D2H `5/5`, trace 추가 device/D2H/solve/export/state commit/fallback 0으로 `1 passed in 103.32s`, peak RSS `358,760 KiB`였다. Trace ID `sha256:567e7df6…6cfe`, receipt `sha256:ccecb0e3…eb48`, source aggregate `sha256:2661f374…deda1`은 전후 동일하다. Final solution/ResultIR/recovery, external `gfx1100`, signed/persistent provenance, process-wide host-copy-zero, end-to-end O(N), speedup, promotion/commercial은 false 또는 미검증이다. |
| FP64 CSR normwise / FGMRES terminal metric parity v2 v0.2.49 | Phase 0 | implemented | contract_only | Retained v0.2.48 componentwise result를 full replay하고 outward exact-real norm interval과 reverse triangle inequality로 `L2/Linf/scaled-Linf` vector budget을 발행한다. CPU stable-L2와 candidate GPU-tree LASSQ-L2/max-Linf/scaled division을 각 residual에서 재생하고 record-to-interval evaluation error를 더한 total budget으로 terminal record 차이를 caller tolerance 없이 검증한다. Nonzero multiply/divide underflow의 upper bound는 최소 subnormal로 올리도록 v0.2.48 primitive도 보강했다. Legacy v1 schema hash `4da38578…6f050`, fixed `1e-12/1e-8` gate와 terminal/history semantics는 불변이고 migration action은 additive v2다. Focused `14 passed in 9.53s`, componentwise+normwise `34 passed in 14.57s`, legacy/capability cross-check `71 passed in 15.06s`, all-converged adjacent `140 passed in 395.82s (0:06:35)`, capability `13 passed`; actual local RX 6900 XT `gfx1030` 고하중 rotated `10 kN`, 4·5경간 `100 kN` gate는 exact completion D2H 3회, fallback 0으로 `1 passed in 226.25s (0:03:46)`, peak RSS `429,808 KiB`를 기록했다. 최대 ratio는 four-span `0.002952072072072049`, rotated L2 `0.0023069006688603657`; source aggregate `sha256:f88ab168…66dc3`는 전후 동일했다. Single dirty non-release wheel은 `1,427,672` bytes/`270` members, `sha256:da8eeef8…b355ae`, 격리 public symbols `932/746/93/10`이었다. Core actual backend/provenance, restart-history v2, high-load ResultIR/aggregate, formal proof, actual subnormal hardware, external `gfx1100`, signed/persistent evidence, end-to-end O(N), speedup, promotion/commercial은 false 또는 미완료다. |
| FP64 CSR residual roundoff/backward-error v1 v0.2.48 | Phase 0 | implemented | contract_only | Backend-neutral public contract가 reduced CSR 각 행에서 `k=2*nnz+1`, `gamma_k`, `|b|+|A||x|`, `|A||delta_x|`, subnormal guard를 계산하고 모든 nonnegative bound 연산을 `nextafter(+inf)`로 outward rounding한다. Caller tolerance는 없고 네 vector immutable snapshot·strict schema·canonical receipt/result replay, componentwise backward error, sparse `O(nnz+n)` narrow work bound를 제공한다. Detached FGMRES adapter는 solution에 legacy fixed gate를 유지하면서 CPU-vs-candidate 및 candidate-vs-independent-`fsum` residual 계약을 발행하며 기존 v1 fixed residual/terminal/history gate는 완화하지 않는다. Focused/adapter `20 passed in 6.85s`, legacy v1 `24 passed in 1.83s`, current 3-file cross-check `56 passed in 7.06s`, 7-file adjacent cross-check `116 passed in 264.39s`, capability `12 passed in 0.31s`다. Required actual local RX 6900 XT `gfx1030` 원래 고하중 rotated `10 kN`, 4·5경간 `100 kN` gate는 strict solution, exact completion D2H 3회, fallback 0으로 `1 passed in 218.17s (0:03:38)`, peak RSS `428,820 KiB`를 기록했다. CPU-HIP/HIP-replay 최대 error/bound는 rotated `0.0210129/0.0354777`, 4·5경간 `0.00295208`이고 최대 HIP backward error는 `1.2238707852511078e-15`; source aggregate `sha256:d30dd5e…c176f6`는 전후 동일했다. Single dirty non-release wheel은 `1,411,419` bytes/`266` members, `sha256:1e61eac1…56e28`, 격리 public symbols `903/732/78/10`이며 reproducible/authoritative artifact 증거가 아니다. Core receipt actual backend/provenance, v1 terminal metric replacement, high-load ResultIR/aggregate, formal machine proof, subnormal hardware acceptance, external `gfx1100`, signed/persistent evidence, end-to-end O(N), speedup, promotion/commercial은 false 또는 미완료다. |
| HIP FGMRES all-converged registry + ResultIR v1 v0.2.47 | Phase 0 | implemented | contract_only | 별도 package registry가 raw `sha256:e3414a…f31f1`, canonical `sha256:85611e…3a19`로 10개 고유 ModelIR/ExecutionPlan을 고정하고 실제 해 `9`건과 zero-free-RHS `1`건의 CPU convergence·dual tolerance `10/10`을 재생한다. Strict schema는 전체 manifest와 registry/case/registration/model raw identity, expected CPU·dense-oracle·package hash `13`개를 exact `const`로 pin한다. 기존 termination registry와 역사적 `7 ResultIRV2 + 3 DiagnosticIRV1`은 보존한다. Process-local family/aggregate 계약은 canonical `10 ready/10 solution/0 not-issued/0 diagnostic/10 committed`와 exact totals `G=168,E=18,F=103,nnz=2,304`, arrays `60/6,728` bytes, detached raw `1,648` bytes, upstream D2H `30/30/0`·allocated `4,288` bytes를 고정한다. Aggregate direct additional device/D2H/solve/export/fallback은 `0`, registry CPU replay zero는 false다. Registry `16 functions/27 collected`, family `9 functions/10 collected`, aggregate `10 functions/15 collected`, public/actual-wheel `5`의 combined contract는 `57 cases within the 68-case cross-check`이고 capability `11 passed in 0.31s`, 5-file cross-check `68 passed in 259.34s (0:04:19)`다. 실제 wheel test는 fixture JSON `11`개와 all-converged schema `3`개의 exact path/source byte를 검증한다. current-source final non-release wheel은 `1,401,602` bytes, `sha256:d9deea5512465f5fea353fffa4cab036eb03dff91fbbedf78c8a98bc560efc00`이며 public symbols Engine/Assembly/Contracts/Elements `886/730/63/10`, 관련 schema `5`, registry `10`-slot fresh CPU replay를 소스 경로를 제거하고 현재 런타임 의존성을 사용한 설치에서 통과했다. 단일 dirty build smoke이며 재현 빌드 증거가 아니다. 같은 source의 연속 wheel 2개는 `1,401,602` bytes/`264` members와 member content diff `0`이지만 `dist-info` timestamp `5`개 및 archive hash `sha256:f704a687e0ca13233c2fb379a9b71714888c6e0c192b120f3fa2cae82fa08838`/`sha256:18bcb7f7f405ceb5be75474e0926bcf906bc35ad274d9946faa7d09ba4da0ba0`가 달라 reproducible wheel은 false다. Family factory는 full replay `1` + fast raw refresh `1`, aggregate factory는 entry full replay `1` + live recapture `3` + fast raw refresh `3`이며 detached self-validator full replay는 없다. Required hardware flow의 full replay는 `4`회(`40` CPU solves + `40` dense oracles), fast raw refresh는 `4`회다. Hardware file은 static-bound와 actual gate `2`개다. Current-source required local `gfx1030` gate는 CPU fallback 없이 `1 passed in 1087.52s (0:18:07)`로 exact case/bridge `10/10`, family, aggregate와 post-close validation을 통과했고 process peak RSS `450,868 KiB`, post-close current RSS `433,188 KiB`를 계측했다. 첫 두 run의 near-zero absolute residual fail-closed 뒤 cancellation-sensitive rotated/four/five-span을 tolerance 변경 없이 unit load로 정규화했으며 일반 load scale roundoff-aware parity는 여전히 미증명이다. 별도 restricted namespace에는 `/dev/kfd`가 없어 actual gate가 non-required에서 skip, required에서 `No real gfx agent was detected.`로 fail-closed했으며 이 실행은 root-host probe를 반박하거나 durable observation을 생성하지 않는다. External `gfx1100`, multiarchitecture, broad/process-wide host-copy-zero, GPU recovery/result recovery, standalone/persistent/signed provenance, O(N), speedup, nonlinear/dynamic/shell/solid/contact, commercial/promotion은 false 또는 미검증이다. |
| Shared linear frame/truss element semantics v1 v0.2.46 | Phase 0 | implemented | contract_only | Backend-neutral public module이 linear-isotropic material/frame-truss section validation, strict `abs(local_x_z)>0.9` reference-axis policy, roll-aware right-handed transform, EB-frame/axial-truss FP64 12x12 stiffness를 소유한다. CPU reference와 sparse ExecutionPlanV2가 직접 소비하고 HIP symbolic plan은 shared axis/transform을 소비하며 CPU-private import를 제거했다. Source semantics version과 historical wire identity를 분리해 frozen CPU operator/recovery/numeric/plan hash를 보존했다. Sealed HIP C++ v1 계수는 current-source actual native RTC에서 기존 global-Z와 추가 global-Y frame/truss shared oracle을 통과했지만 unsigned·비영속·non-promoting이다. Code-generated single source, broad/signed native parity, shell/offset/release/nonlinear, ill-conditioned refinement, peak RSS, O(N), speedup, commercial readiness는 미증명이다. |
| HIP FGMRES nonconverged DiagnosticIR v1 v0.2.45 | Phase 0 | implemented | contract_only | Exact fixed registry의 intentional `max_iterations` 3건을 solution-ready ResultIRV2로 승격하지 않고 descriptor-only DiagnosticIRV1로 보존한다. 이미 export된 raw `solution_x`/`true_residual`/`solve_record`를 exact sparse plan 및 case/CPU/terminal/export/device lineage에 결속하고 full partial displacement, full `K*u-F`, exported free `F-K*u`, policy·counter·metric·restart history를 재생한다. Canonical initial accepted StateIR에서 trial만 평가하고 exact rollback하며 state commit/restart checkpoint는 없다. Raw source payload hash와 signed-zero canonical array hash를 분리한다. Public generic builder는 `diagnostic_ready=false`와 raw-source preservation claim false만 발행한다. 두 번째 live capture 후 private process-local exact-object issuer를 통과한 bridge receipt만 scoped true이며 replace/copy/direct/coherent-rehash/standalone manifest는 권한을 잃는다. 이는 hostile same-process 보안 주장이 아니다. Post-close validator는 detached three-payload decode·sparse physics·state·process-local issuance를 재생한다. Additive family companion은 기존 v0.2.44의 `7 ready ResultIRV2 + 3 not-issued nonconverged` 진실표를 유지하고 세 nonconverged slot에만 DiagnosticIR을 결속한다. 합계는 `G=72,E=9,F=54,nnz=1,080`, slot당 3 categories·3 slots = aggregate 9 arrays와 `1,584` bytes, detached raw `1,872` bytes, upstream D2H `9/9/0`·`1,872` bytes이며 projection 추가 device/D2H/solve/export/fallback/commit은 `0`이다. `diagnostic_ready`는 진단 권한일 뿐 solution/result/reaction/member-force/energy/code-check/design/optimization/commercial/promotion 권한이 아니다. Current-source focused/public 4개 파일은 `42 passed in 380.25s (0:06:20)`, capability matrix는 `9 passed`, v0.2.43~v0.2.45 인접 통합 suite는 `138 passed in 497.62s (0:08:17)`, hardware harness는 `1 test collected`를 확인했다. Token-hardened current-source actual local `gfx1030` integrated 10-slot required gate는 CPU fallback 없이 `1 passed in 7820.35s (2:10:20)`로 nonconverged 3건 각각의 DiagnosticIRV1 bridge, family exact totals와 context close 뒤 validator를 통과했고 같은 harness에서 upstream v0.2.42 audited totals, v0.2.43 converged 7 ResultIRV2 bridge/post-close, v0.2.44 7-ready/3-not-issued disposition/post-close도 재생했다. v0.2.45 단계 당시 별도 all-converged 10/10 ResultIRV2는 미완료였으나 downstream v0.2.47의 unsigned local gfx1030 관찰에서 후속 검증됐다. External `gfx1100`, broad host-copy-zero, standalone/persistent/signed provenance, O(N), speedup, commercial/promotion은 계속 pending 또는 false다. |
| HIP FGMRES model-family ResultIR disposition v1 v0.2.44 | Phase 0 | implemented | contract_only | Exact package `gfx1030` registry 10-slot을 v0.2.42 audited family receipt 및 이미 발행된 v0.2.43 bridge와 결속해 canonical `7 ready_result_ir_v2 + 3 not_issued_nonconverged` disposition으로 만든다. Intentional `max_iterations` 3건은 ResultIRV2/solution-ready로 승격하지 않는다. Factory는 live audited authority를 세 번 재생하고, non-recycled weak-registry token으로 각 bridge를 exact live model-case에 결속해 직렬값이 같은 cross-run splice와 CPython `id()` 재사용을 거부한다. 결과에는 live result/transfer/ordinal context를 보유하지 않고 detached audited receipt와 exact bridge 7개만 남긴다. Post-close validator는 fresh registry, audited receipt, seven bridge physics/issuance와 aggregate process-local issuance를 재생한다. Ready 합계는 `G=90,E=8,F=43,nnz=1,116`, six-array `3,336` bytes, detached raw payload `688` bytes이고 nonconverged 3건의 ResultIR array는 materialize하지 않는다. 전체 package는 `G=162,E=17,F=97,nnz=2,196`; upstream completion D2H `30/30/0`, `4,408` bytes는 유지하고 aggregate projection 추가 device/D2H/native-device-solve/export/fallback은 `0`이다. Fixed registry validation은 deterministic CPU reference를 재생할 수 있으므로 registry-validation CPU replay zero는 false다. Focused adversarial `23 passed`, identity-token model-case/ResultIR·disposition·capability·public API 최종 통합 `91 passed in 116.59s`, capability/FGMRES-public/ResultIRV2-public 부분 집합 `19 passed`를 확인했고 hardware harness는 `1 test collected`다. Token-hardened current-source actual local `gfx1030` integrated 10-slot required gate는 CPU fallback 없이 `1 passed in 7820.35s (2:10:20)`로 v0.2.42 audited source와 v0.2.43 ResultIRV2 bridge 7건을 재사용해 exact 7-ready/3-not-issued disposition을 발행하고 context close 뒤 family disposition validator까지 통과했다. 이 v0.2.44 receipt 자체에는 DiagnosticIR이 없지만 additive v0.2.45 companion을 같은 harness의 별도 downstream 단계에서 검증했다. all-converged 10/10 ResultIRV2는 이 단계 당시 미완료였으나 downstream v0.2.47의 unsigned local gfx1030 관찰에서 후속 검증됐다. External `gfx1100`, broad host-copy-zero, GPU recovery, standalone/persistent/signed provenance, O(N), speedup, commercial/promotion은 계속 pending 또는 false다. |
| HIP FGMRES ResultIR v2 v0.2.43 | Phase 0 | implemented | contract_only | Retained converged HIP single-model-case completion의 이미 export된 `solution_x`/`true_residual` bytes를 exact sparse `ExecutionPlanV2`와 결속해 full displacement, full `K*u-F`, constrained reaction/free exact `+0.0`, local member force, element/global/external/residual-work energy 및 trial→committed StateIR을 복원한다. Public generic builder는 `result_ir_ready=false`만 발행하고, 두 번째 live capture를 통과한 bridge의 비공개 process-local exact-object mint만 scoped true를 발행하며 upstream false claim은 불변이다. Replace/direct construction/standalone manifest는 true 권한을 잃는다. 이는 hostile same-process 보안 주장이 아니다. Global dense matrix나 추가 solve를 사용하지 않고 descriptor-only ResultIRV2 manifest를 발행한다. Live process-local source authority는 factory에서만 검증하고, factory-issued model-case의 weak-registry bare token을 bridge seal/issuance에 결속해 source context를 보유하지 않으면서 exact clone과 `id()` 재사용을 거부한다. Retained-source factory direct-call surface의 device operation/D2H/solve/export/fallback 추가량 `0`은 contract이며 transitive/process-wide 계측값이 아니다; upstream completion export의 기존 blocking D2H `3`회는 유지된다. Materialization은 `O(G+nnz+144E)`, ResultIR 여섯 raw array는 `24G+104E+8F` bytes이고 detached source seal raw payload `16F`는 별도다. Generic `16 passed`, bridge `13 passed`, public API `3 passed`, capability `9 passed`, model-case authority `24 passed`, combined `65 passed in 29.31s`를 통과했다. Exact clone/coherent rehash/registry transplant의 final issuance 거부도 회귀로 고정했다. Identity-token 패치 전 source의 required local `gfx1030` 단일 모델 gate `1 passed in 169.10s (0:02:49)`, CPU fallback 없음은 unsigned 비영속 역사 관찰로 보존한다. Token-hardened current-source actual local `gfx1030` integrated 10-slot required gate도 CPU fallback 없이 `1 passed in 7820.35s (2:10:20)`로 converged 7건의 ResultIRV2 bridge를 발행하고 context close 뒤 각 detached validator를 통과했다. Fixed package 10/10 solution-ready는 이 단계 당시 미완료였으나 downstream v0.2.47의 unsigned local gfx1030 관찰에서 후속 검증됐다. External `gfx1100`, broad host-copy-zero, GPU recovery, standalone/persistent/signed provenance, promotion/commercial, end-to-end O(N)/speedup, nonlinear/dynamic/shell/solid/contact claim은 계속 pending 또는 false다. |
| HIP FGMRES model-family audited parity v0.2.42 | Phase 0 | implemented | contract_only | Exact package `gfx1030` 10-slot fixed suite의 retained model-case parity/family-v2, host-transfer audit, RTC launch/fence ordinal audit authority를 additive receipt 하나로 결속한다. Source composition과 각 ordinal `expected_context`를 재생하고 동일 completion-export 객체 identity, canonical/sealed/global/completion, registry-derived ABI/kernel source/canonical-checkpoint-global schedule/program descriptor, architecture/device lineage를 교차검증한다. Caller 입력은 registry slot 순서로 canonicalize하며 cross-run retained-object splice를 거부한다. Exact 합계는 `10/10`(matrix `10/20`), recurrence copy attempt `0`, completion D2H `30/30/0`·`4,408` bytes, owned memset `80/80/0/0/0`, fence `30/30/0/0/0`, launch `1,230/1,230/0/0/0`이다. Factory exact direct-call surface는 retained validator만 호출하고 runtime 회귀는 export/seal 호출을 차단한다. Token-hardened current-source focused `16 passed in 137.54s`, public/capability `16 passed`를 통과했다. Identity-token 패치 전 source의 required local `gfx1030` gate `1 passed in 3171.31s (0:52:51)`, CPU fallback 없음은 unsigned 비영속 역사 관찰로 보존한다. Token-hardened current-source actual local `gfx1030` integrated 10-slot required gate도 CPU fallback 없이 `1 passed in 7820.35s (2:10:20)`로 같은 exact audited totals와 downstream v0.2.43~v0.2.45 lineage를 확인했다. Process-wide visibility, hostile same-process mutation/interposition 저항, pre-window/setup/teardown/case-between transfer zero, raw/fresh/third-party native 관찰, device semantic/content truth, broad iteration-host-copy-zero, persistent/signed provenance, external `gfx1100`, 이 receipt 자체의 ResultIR/recovery/energy, O(N), speedup, commercial/promotion은 false다. |
| HIP FGMRES recurrence launch/fence rolling ordinal audit v0.2.41 | Phase 0 | implemented | contract_only | Exact process-local `HipRtcFgmresV2Kernel`의 sealed `hipMemsetAsync`, `hipModuleLaunchKernel`, `hipStreamSynchronize` choke point에 receipt-eligible healthy path용 단조 operation ordinal과 fixed-binary SHA-256 rolling predecessor chain을 추가한다. Canonical enqueue 전 start에서 owned8 memset `8`회, canonical fixed descriptor, fence, sealed checkpoint `4` launch, fence, global continuation, terminal fence를 재생하고 completion-export child open 전 end head와 exact 일치해야 receipt를 발행한다. Ordinary audit begin/finish 오류는 ledger를 비가역 poison해 clean receipt만 차단하고 valid solver memset/launch 및 safety fence는 ticket 없는 degraded mode로 유지한다. Invalid descriptor와 enqueue-begin `BaseException`은 native 전 rollback한다. Fence-begin `BaseException`은 exact native safety fence 성공 뒤 전파하고 native failure/non-exact/rejection 결과를 우선한다. Same kernel/loaded runtime/checkpoint token/device/stream 및 canonical/sealed/global schedule hash를 retained expected-context authority로 교차결속하고 raw pointer/token/object identity는 직렬화하지 않는다. Event 배열 없이 counter/head만 유지하는 ledger 메모리는 `O(1)`이고 replay는 `O(L)`이지만 이는 FE solver의 end-to-end `O(N)` 증거가 아니다. Focused `11 passed`, public/capability `16 passed`, RTC 전체 `134 passed`; current-snapshot actual local `gfx1030` required gate는 `1 passed in 37.22s`로 owned memset `8/8`, full launch chain, fence `3/3` 및 동일 solve/export lineage를 확인했다. 이 actual 결과는 비영속 작업 세션 관찰이다. 후속 token-hardened current-source actual local `gfx1030` integrated 10-slot gate도 CPU fallback 없이 `1 passed in 7820.35s (2:10:20)`로 v0.2.42 composition 안에서 owned memset `80/80/0/0/0`, launch `1,230/1,230/0/0/0`, fence `30/30/0/0/0`를 재생했다. Raw/fresh native binding, process-wide ROCm, 모든 device operation, hostile same-process mutation, device 실행/내용/수치 parity, query-recovered ambiguous fence의 clean relabel, broad iteration-host-copy-zero, persistent hardware receipt/외부 로그, external `gfx1100`, signed truth, 10/10 ResultIRV2 ready, end-to-end O(N), speedup, commercial/promotion은 계속 false다. |
| HIP FGMRES model-family host-transfer audit composition v0.2.40 | Phase 0 | implemented | contract_only | 기존 family-v2와 audit-v1 receipt를 변경하지 않는 additive contract다. Exact package registry `gfx1030` 10-slot family result와 각 `context.result` read 순간에 `exported`인 같은 프로세스의 10개 audit context를 요구하고, model-case parity와 audit이 동일 completion-export context/result 객체를 사용했는지 결속한다. Completion/global/recurrence/kernel/device/F/R 및 source family·retained·composition registry identity를 교차검증하고 detached context 중복과 architecture device/runtime/kernel 불일치를 거부한다. Slot별 recurrence bound-runtime copy API attempt `0`, export blocking D2H `3/3/0`, `16F+192+72R`; 고정 suite 합계 `30/30/0`, `4,408` bytes다. 현재 트리 focused/public/capability는 `24 passed`, exact historical family-v2/audit-v1 두 파일은 `21 passed`다. 최종 트리 actual local `gfx1030` 10-slot required gate는 `1 passed in 2004.37s (0:33:24)`로 family local `10/10`(전체 `10/20`), recurrence attempt `0`, export `30/30/0`, `4,408` bytes와 same export identity를 한 composition에서 확인했다. 하드웨어 helper는 slot당 recurrence 1회와 audit-owned export 1회를 downstream에 재사용하고 composition factory는 solve/export API 호출 없이 retained export identity만 소비한다. 전체 프로세스의 관계없는 추가 device solve/export 부재는 false다. Audit-v1 하위 validator는 retained closed publication을 재생할 수 있지만 full composition은 family case source authority까지 요구하므로 cleanup 전에만 검증한다. Detached logical/matrix/family-observation hash는 opaque commitment이며 receipt 단독은 provenance가 아니다. 이 actual 결과는 작업 세션 관찰이며 별도 영속 receipt/log, external `gfx1100`, process-wide/pre-window DMA, setup/teardown/case 사이 transfer, same runtime/stream, broad iteration-host-copy-zero, full family/multiarchitecture, signed evidence, ResultIR, O(N), speedup, commercial/promotion은 false다. |
| HIP FGMRES bound-runtime recurrence host-transfer audit v0.2.39 | Phase 0 | implemented | contract_only | Exact bound Engine-v2 runtime의 H2D async/D2H async/blocking D2H API를 단조 증가 attempt/success/failure/byte ledger로 계측한다. Audit은 canonical context-ready·predecessor enqueue 전 원자적 start boundary에서 canonical·sealed·global recurrence terminal fence까지 동일 lineage/runtime/device/stream의 신규 copy API attempt delta가 모두 `0`인지 검증하고, fence 뒤 기존 exporter의 `solution_x → true_residual → solve_record` blocking D2H 정확히 `3`회와 `16F+192+72R` bytes를 별도 phase로 검증한다. 세 경로 각각의 성공·native error·예외, reentrant start mutation, valid-payload nested strict schema, exact-type alias, exporter backend/binding 위조, cached foreign input, public resource와 AST 휴리스틱 owner guard를 회귀한다. HIP context `12`, completion export `11` 회귀와 작업 세션 actual local ISA `gfx1030` single-case `1 passed in 36.34s`—recurrence delta `0`, export `3`/`360` bytes—를 확인했으나 별도 영속 receipt/log는 없고 RX 6900 XT는 계약 결속값이 아닌 운영자 관찰값이다. 이는 exact bound-runtime copy API의 process-local unsigned 비승격 증거일 뿐 창 이전 async copy 완료/device DMA, process-wide ROCm, raw CDLL/fresh binding, direct ctypes/dlsym/C extension, third-party DMA, hostile same-process interposition를 관찰하지 않는다. Detached receipt provenance도 `expected_context` 또는 서명 없이는 false다. Sync/host scheduling/full setup/source apply/전체 iteration host-copy-zero, `gfx1030` 10-case family/external `gfx1100`, parity·ResultIR·O(N)·speedup·commercial·promotion은 false다. |
| HIP FGMRES reviewer-root bootstrap/new-trust-genesis v0.2.38 | Phase 0 | implemented | contract_only | v0.2.37의 reviewer `0` empty registry-v2 schema/raw/registry/head/time/reviewer-commitment/receipt identity를 actual package resource에서 exact replay하고, 동일 lineage append나 권한 연속성을 주장하지 않는 별도 registry-v3 ID/generation을 정의했다. Fixed reviewer policy는 3 roots, bootstrap 3-of-3, 미래 event 2-of-3, exact target-genesis activation 별도 3-of-3과 plan/receipt hash 결속, lineage-bound enrollment를 요구한다. Canonical non-identity exact-prime-order Ed25519 root, source-head strict-later/반개구간 validity, full ceremony/source/policy/root/lineage/plan/receipt hash chain과 domain-separated signature를 bounded preflight로 검증한다. 신규 집중/public/capability `54/61/69`, 인접 trust/signed/ledger `66/58/26`, scoped 비중복 `219 passed`; 세 독립 감사 모두 `0/0/0/0`이다. Exact offline PEP 517 smoke 2회는 `1180847` bytes/`224` members/동일 `sha256:e7d9cfa4790185d5590c4fb30e7a81d30d84422acd49afd5d5f4e58be0718f5a`였고 wheel verifier·격리 import를 통과했다. 다만 dirty uncommitted source와 build-system/runtime wheelhouse conflation 때문에 authoritative v1 publication receipt는 불가능하며 별도 build-system lock/wheelhouse 계약이 필요하다. Package loader는 code-pinned `pending_independent_reviewer_root_material`만 반환하고 root/서명 material과 v3 genesis는 없다. Detached synthetic 성공은 세 public key 아래의 exact-plan private-key possession signature만 증명한다. 현재 control/인간 consent, reviewer 신원·독립성·HSM, 이전 authority continuity, nonce entropy/global uniqueness, hostile same-process mutation, authoritative publication, package inclusion/target activation, monotonic/historical recovery, runner key, signed/ledger v3, actual `gfx1100`, promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거가 아니다. |
| HIP FGMRES reviewer-root registry-v3 runner-key lifecycle v0.2.56 | Phase 0 | implemented | contract_only | Exact detached registry-v3 genesis receipt와 runner-key v1 PoP를 재검증하고 genesis registry hash에 결박된 epoch-2 enrollment, epoch-3 activation을 구성한다. 각 event는 immutable 세 root 중 ordered unique `2..3-of-3` reviewer quorum, event 시각의 root validity, runner activation의 strict order와 key half-open validity를 검증한다. Detached receipt는 constructed `3 reviewer/1 enrolled/1 active` state와 transition hash chain을 보존하고 attached result는 bootstrap source까지 replay한다. Strict outer schema와 nested 원본 validator, public identity, wrong-key/domain-replay/foreign-predecessor/source-substitution/promotion-claim/bool-int 적대 경계를 검증했다. Focused unit/public/registry-public `26 passed in 34.97s`, adjacent trust chain `144 passed in 58.66s`, capability `18 passed`, public symbols `1152/960/66`이다. 이는 synthetic detached contract이며 actual package count는 `0/0/0`; 실제 reviewer/isolated runner/HSM, package activation, trusted clock/monotonic ledger, external `gfx1100`, signed TraceIR, promotion/commercial은 false 또는 미검증이다. |
| HIP FGMRES reviewed trust-anchor lifecycle v0.2.37 | Phase 0 | implemented | contract_only | Detached domain-separated Ed25519 PoP challenge/receipt, canonical non-identity exact-prime-order runner/reviewer key와 signature `R` 검증, `S < L`, bounded preflight, package-owned reviewer commitment, quorum-signed append-only event history, fixed-shape rolling registry hash chain, key enroll/activate/atomic rotate/retire/revoke replay를 구현했다. Reviewer↔runner를 포함한 전역 public-key 재사용을 거부하고 reviewer activation 시각을 authorization lower bound로 강제한다. Rolling prefix와 runner별 predecessor/active-key index로 registry replay는 `O(E+K)`지만 FE solver/end-to-end `O(N)` 증거는 아니다. Signed-evidence v2와 durable-ledger v2는 exact registry-v2 type/current package hash만 소비하고 v1 downgrade, inactive/revoked key, pre-activation evidence, registry drift recovery를 fail-closed한다. 비중복 `172 passed`; 2회 byte-reproducible candidate wheel `1163595` bytes/`220` members, `sha256:a53f7d7c836e304ba6a8ef0026910095b6e4b0e7c1df643c5585bc6082c2c7da`의 격리 설치·resource 검증과 private-key 표식 부재를 확인했다. PoP는 private-key possession만 증명하며 HSM origin/non-exportability을 증명하지 않는다. Package init fixture는 reviewer `0`, runner key `0`, active key `0`이고 operational reviewer bootstrap은 false다. 따라서 hostile same-process Python module mutation 저항, 실제 HSM/reviewer 운영, external `gfx1100` `10/10`, historical resolver/monotonic anchor, same-artifact two-architecture, promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거가 아니다. |
| HIP FGMRES signed release-identity binding v0.2.36 | Phase 0 | implemented | contract_only | v1과 분리된 challenge/payload/envelope/receipt schema 및 Ed25519 domain에서 fresh-replayed verified release의 전체 identity receipt schema/hash를 서명 대상에 직접 포함한다. Release-binding hash와 identity receipt hash chain을 exact equality로 검증하고 v1/v2 downgrade·cross-protocol을 거부한다. 수치 replay 뒤 release artifact를 최종 재검증하고, detached receipt가 아니라 consume 뒤 mint되는 process-local verified wrapper만 public authority로 반환한다. 전용 v2 owner-private local POSIX SQLite namespace는 challenge/acceptance/recovery에도 동일 identity를 보존한다. 비중복 `240 passed`, 독립 감사 `BLOCKER/HIGH/MEDIUM 0`, candidate wheel `1137224` bytes/`214` members, `sha256:00e547a10cc5813be31cbd87f99efec061f1801ce507b6a3787fbbcba1ecfea3`를 확인했다. Public registry의 active key는 `0`, actual external `gfx1100`은 `0/10`이며 합성 10-slot 회귀만 성공 경로를 실행한다. Schema-only nested receipt validation, trust-key 운영/rotation, hardware truth/root, cross-host·rollback 저항, non-POSIX/NFS/FUSE·false fsync, wall-clock 진위·TTL 이전 물리 fsync 증명, package/trust/fixture registry rotation 간 historical recovery, later append 뒤 current-head attestation, promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거는 아니다. |
| HIP FGMRES external durable replay ledger v0.2.35 | Phase 0 | implemented | contract_only | 명시적으로 초기화한 단일 owner-private local POSIX SQLite ledger에서 pinned namespace/ledger ID, directory·database inode, `BEGIN IMMEDIATE`, `journal_mode=DELETE`, `synchronous=EXTRA`, strict canonical stored payload/schema와 append-only event hash chain을 고정한다. Challenge/request/runner-sequence와 envelope/signed receipt 재사용을 프로세스 재시작·경쟁 뒤에도 차단하고, acceptance commit 전에 release artifact replay와 Ed25519·exact 10-slot 수치 검증을 완료한다. Commit 후 응답 전 crash는 recovery lookup이 저장 시점 권위를 다시 검증해 복원한다. Receipt의 `acceptance_commit_head_event_*`는 acceptance transaction commit 시점의 head이자 acceptance event의 sequence/hash이며, 후속 event append 뒤의 current head 증명이 아니고 기존 receipt를 stale로 만들지 않는다. 이는 단일 configured ledger의 cross-process at-most-once acceptance 경계일 뿐 exactly-once, cross-host/multi-ledger, 동일 UID/root/storage rollback, cryptographic log/TPM, non-POSIX/NFS/FUSE·false sync, wall-clock 진위·TTL 이전 물리 fsync 증명, package/trust/fixture registry rotation 간 historical recovery, full identity receipt signed binding, runner/hardware truth, active key, 실제 `gfx1100` 실행, promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거가 아니다. Active key `0`, actual external `gfx1100` `0/10` |
| HIP FGMRES external release identity v0.2.34 | Phase 0 | implemented | contract_only | Candidate wheel의 same-open-FD bytes/ZIP/full `RECORD`, 현재 설치 distribution `RECORD`/declared scripts, clean Git commit/tree/index/worktree·exact `git archive`·runner/build/lock hash, exact declared-target-environment runtime dependency wheelhouse closure와 fixed canonical build-recipe policy를 독립 재생한다. 핵심 입력을 compile 내부에서 두 번 순차 재생하고 challenge 발급과 signed verification 직전에 fresh replay해, 검증된 identity에서 기존 v1 release binding을 파생하고 process-local capability로 signed verifier와 결합한다. 이는 atomic multi-artifact snapshot, build 실행/재현성, remote commit authenticity, build-system dependency closure, dependency 설치 실행/current-interpreter wheel-tag 호환성, bounded total source-artifact memory, hostile same-process mint isolation, identity receipt hash의 serialized signed binding, durable ledger, runner honesty, hardware attestation/실행 증거가 아니다. Active key `0`, actual external `gfx1100` `0/10`, same-artifact two-architecture·promotion·ResultIR·iteration host-copy-zero·O(N)·speedup·commercial은 false |
| HIP FGMRES external signed evidence verifier v0.2.33 | Phase 0 | implemented | contract_only | Package-owned Ed25519 trust-anchor registry, strict canonical envelope, process-local single-use challenge, release/runtime/kernel/device 결속과 exact `gfx1100` 10-slot raw `solution_x`/`true_residual`/`solve_record` 수치·semantic replay를 구현했다. Caller가 사전 계산해 제공한 expected wheel/source identity를 challenge·서명으로 보호하고, 설치 package의 schema manifest와 fixture registry/replay는 verifier가 직접 재계산한다. 합성 10-slot signed path를 포함한 집중 `10 passed`, quick suite `23 passed`와 1034513-byte wheel(`sha256:4031426ee32b973e1e702f2947cc4dfebaaaf4be1f5d39043fabb11b5b7318e4`)을 확인했다. Package 활성 키는 `0`, 공개 verifier는 `trust_anchor_not_found`로 fail-closed하며 실제 external `gfx1100`은 `0/10`이다. Wheel/source 자체 재해시, hardware-root·악성 runner 부재, durable replay ledger, same-artifact two-architecture, multiarchitecture·release promotion, ResultIR·iteration host-copy-zero·O(N)·speedup·commercial 증거는 없음 |
| HIP FGMRES registry-bound model-family parity v0.2.32 | Phase 0 | implemented | non_promoting | Package registry v1만 case authority로 소비해 exact 10-slot replay와 live process-local CPU/HIP model-case result를 fixed `gfx1030/gfx1100` 20-cell 행렬에 결합한다. Detached validator도 fresh registry/slot/derived key/order와 duplicate·device·UUID/PCI 각각의 cross-base 충돌을 fail-closed한다. Actual RX 6900 XT `gfx1030` 전체 10-case chain은 최종 source에서 `1 passed in 1088.47s (0:18:08)`로 local `10/10`, matrix `10/20`을 확인했다. Rotated-local-axis 최초 실패는 tolerance 완화 없이 registry policy/history를 교정해 재통과했다. External actual `gfx1100`은 `0/10`이며 multiarchitecture·signed promotion·ResultIR·iteration host-copy-zero·O(N)·speedup·commercial 증거는 없음 |
| HIP FGMRES package fixture registry v0.2.31 | Phase 0 | implemented | contract_only | Package-owned exact ModelIR 10개를 strict JSON/schema, code-anchored raw bytes SHA, canonical registry/slot hash, unique full fingerprint로 봉인하고 ModelIR→buffers→ExecutionPlan→free-space/FGMRES/recurrence plan→deterministic CPU FGMRES와 독립 bounded dense direct oracle을 재생한다. Registration `10/10`, canonical hash `sha256:0f9fb841c2ed6bfe2aef43024d5a496485f06d3d00b95892c7304b7e0dab7eb6`, receipt `sha256:c265bd5f8e465fa2605c5c56f78159656ef714647dfd2d098b3a747420d7c324`다. Registry receipt 자체에는 hardware·multiarchitecture·signed promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거가 없음 |
| HIP FGMRES fixed-suite model-family parity local matrix v0.2.30 | Phase 0 | implemented | contract_only | Exact live single-case result를 detached validated snapshot에서 replay하고 authoritative ExecutionPlan metadata descriptor를 package-owned 10-slot × fixed `gfx1030/gfx1100` 20-cell 행렬에 unique exact-match로만 분류한다. Normalized architecture base만 ISA key로 쓰고 duplicate cell과 동일 UUID/PCI의 cross-base 충돌을 거부한다. Unit `16`, case 인접 `37`, focused aggregate `214 passed in 40.59s`, actual `gfx1030` family replay `1 passed in 141.24s`를 통과했다. 현재 registered slot `0/10`, covered cell `0/20`, status `pending_model_cases_and_external_architecture`이므로 full family·multiarchitecture·signed promotion·ResultIR·host-copy-zero·O(N)·speedup·commercial 증거는 없음 |
| HIP FGMRES exact single-model-case CPU/HIP parity v0.2.29 | Phase 0 | implemented | contract_only | Exact process-local terminal observation, deterministic CPU replay, compiler-replayed retained plan lineage, loader-issued runtime/device identity, solution/exported residual/independent `b-Ax`를 고정 `rtol=1e-8`/`atol=1e-12`로 검증한다. Identity 8-symbol은 private fresh fixed binding이고 loader/query/publication은 exact library-field·callable-state snapshot을 봉인한다. Same-open-FD hash/`dlopen` 뒤 resolved path inode/hash를 재확인한다. Actual RX 6900 XT `gfx1030` 전체 체인과 family replay `1 passed in 141.24s`, unit `21`, device `55`, RTC `112 passed`, 독립 감사 잔여 BLOCKER/HIGH/MEDIUM 0이다. 선택 DSO vendor authenticity·전이 의존성·mapped state와 pathname 영구 불변은 증명하지 않으며 단일 모델·단일 ISA·unsigned/non-promoting이다. model-family·multi-architecture·signed promotion·ResultIR·iteration host-copy-zero·O(N)·speedup·commercial readiness 증거는 없음 |
| ModelIR v2 contract | Phase 0 | implemented | contract_only | strict schema/semantic validator와 golden fixture 완료; solver parity는 별도 미완료 |
| MGT → ModelIR v2 Phase 0 adapter | Phase 0 | implemented | contract_only | MGT 9.3.0 explicit linear-frame subset의 lossless audit/SI/semantic roundtrip; MIDAS 전체 호환 아님 |
| SolverModelBuffers v1 | Phase 0 | implemented | contract_only | 공통 dtype/shape/code/unit/entity/hash ABI; device allocation은 미구현 |
| ExecutionPlan v1 | Phase 0 | implemented | contract_only | node-major 6DOF, canonical full/reduced CSR, exact 7-stage graph, K/F/recovery hash; HIP 실행 아님 |
| Sparse-only ExecutionPlan v2 + CPU direct CSR | Phase 0 | implemented | contract_only | global dense K 없이 sorted full/reduced CSR을 직접 결정론 조립하고 shared public frame/truss semantics로 sparse residual/JVP·direct solve·reaction/recovery/energy와 exact same-runtime replay를 검증; CPU-private element dependency는 제거했지만 retained-array memory slope `1.0219`만 관찰했고 peak memory·direct/end-to-end O(N)·HIP·ResultIR 연결은 미완료 |
| CPU fixed-restart FGMRES reference v1 | Phase 0 | implemented | cpu_reference | `r0=b-Ax0`, positive unshifted Jacobi right preconditioning, DGKS MGS, incremental Givens, true-residual dual gate와 scale-relative breakdown을 독립 구현하고 strict immutable receipt·mandatory deterministic replay를 검증; SciPy iterative/fallback은 없으며 HIP recurrence·iteration host-copy 0·AMG/DD·end-to-end O(N)·속도·ResultIR·상용 증거는 없음 |
| CPU FGMRES fixed-rank sparse coarse preconditioner v1 v0.2.55 | Phase 0 | implemented | non_promoting | `ExecutionPlanV2` reduced CSR에 결속된 physical candidate를 Jacobi square-root-energy 좌표에서 deterministic two-pass MGS로 직교화하고 `Z=DQ`, retained `AZ`, SPD `E=Z^TAZ=LL^T`를 구성한다. 실제 FGMRES 우측 전처리 호출은 `z_c=ZE^-1Z^Tr`, `z=z_c+diag(A)^-1(r-AZc)`를 사용하며 추가 CSR apply와 dense `N×N` projector는 0이다. `k<=16`, build `O(nnz*k+N*k^2+k^3)`, apply `O(N*k+k^2)` receipt를 재생한다. Teacher-mode frame 회귀는 Jacobi `2`회 대비 coarse `1`회 iteration과 sparse-direct 변위 parity를 보였고 focused/public/schema `21 passed in 7.53s`, 기존 CPU FGMRES 인접 회귀 포함 `52 passed in 120.60s`, wall `121.19s`, peak RSS `132,432 KiB`, public symbols `1152/960/66`이다. 이는 CPU 진단이며 HIP device residency, 실제 AMG/DD hierarchy, mesh-independent iteration, end-to-end O(N), speedup, ResultIR/promotion/commercial은 미증명이다. |
| StateIR v1 | Phase 0 | implemented | contract_only | immutable initial/trial/commit 및 exact rollback; stateless linear elastic 한정 |
| ResultIR v1 CPU receipt | Phase 0 | implemented | cpu_reference | residual/reaction/recovery/energy 재검증과 전체 receipt chain; HIP/상용 증거 아님 |
| CPU reference operator | Phase 0 | implemented | cpu_reference | zero-offset/release 6DOF EB frame 및 linear truss narrow scope; buffer 무결성 검증 포함, shell/nonlinear/HIP parity 미구현 |
| HIP DeviceExecutionContext v1 | Phase 0 | implemented | contract_only | native runtime probe, 16개 ModelBuffer 지속 적재·실측 전송계수와 failed-open cleanup-only owner; operator/state/solver는 미결합 |
| HIP owner-minted allocation lineage foundation v1 | Phase 0 | implemented | contract_only | process-local typed extent·range·generation capability, atomic multi-owner borrow, immutable free/orphan target와 terminal quarantine, fresh exact-token owner control·publication history·controlled borrow admission을 제공; solver/device-content/signed promotion 증거는 없음 |
| HIP FreeSpace/Krylov allocation-lineage integration v1 | Phase 0 | implemented | contract_only | FreeSpace 12개·Krylov 9개 owned 실제 malloc/free와 parent 5-capability atomic borrow, context-v2 byte/stage conservation 및 중단 안전 RTC owner handoff를 검증; FGMRES predecessor·host-copy-zero·O(N)·속도·상용 증거는 없음 |
| HIP frame/truss device assembly v1 | Phase 0 | implemented | contract_only | host axis code와 symbolic reverse map만 H2D하고 HIPRTC 요소 기여도·결정론적 CSR gather로 수치 CSR을 장치에서 생성; `gfx1030` 컴파일·symbol 확인만 완료했으며 fresh native launch/parity·resident consumer·Krylov·속도·O(N)·상용 증거는 없음 |
| HIP assembly-resident CSR consumer v1 | Phase 0 | implemented | contract_only | live assembly owner의 CSR row/column/value와 foundation load를 exclusive lease로 같은 runtime/device/stream에서 빌리고 state/direction/residual/JVP 네 벡터만 소유; test-double full/free/constrained parity, zero-transfer enqueue, pre-lease kernel 회수성, atomic lifetime/cleanup, concurrent receipt snapshot과 strict telemetry/stage/receipt 상태기계를 검증했다. Resident 단독 API에는 device producer가 없고 아래 free-space child가 별도 제공하며, Krylov·iteration host-copy 0·native hardware parity·O(N)·speedup·상용 증거는 없음 |
| HIP free-space device-direction operator apply v1 | Phase 0 | implemented | contract_only | 5개 symbolic 배열만 H2D하고 borrowed full CSR/state/load에서 장치 내 `K_ff`·축약 state/load를 물질화한 뒤 `F-Ku` direction→resident residual/JVP→reduced JVP gather를 단일 세대 token으로 연결; test-double FP64 parity, `gfx1030` HIPRTC 3-symbol compile/확인, native hardware gate만 있으며 Krylov/CG/FGMRES/PCG·선조건기·linear solver·Newton·iteration host-copy 0·end-to-end O(N)·speedup·signed promotion·상용 준비 증거는 없음 |
| HIP Krylov vector/reduction + positive Jacobi primitives v1 | Phase 0 | implemented | contract_only | exact latest free-space apply 재검사와 child lease를 원자화한 same-stream context가 reduced CSR/direction/JVP 5개를 빌리고 9개 workspace를 소유해 positive unshifted Jacobi, affine, deterministic fixed-tree dot, scale-first LASSQ norm의 고정 diagnostic batch를 enqueue한다. Raw batch H2D/D2H/allocation/sync/fallback 0, strict schema/live witness, parity-failure shared poison, ack-failure retry owner, test-double FP64 parity, `gfx1030` HIPRTC 9-symbol compile과 조건부 native gate까지이며 recurrence·CG/FGMRES/PCG·SPD 증명·solver-integrated preconditioner·iteration host-copy 0·end-to-end O(N)·speedup·상용 증거는 없음 |
| HIP fixed-restart FGMRES allocation/policy plan v1 | Phase 0 | implemented | contract_only | exact ExecutionPlan/free-space overlay/FGMRES policy와 finite-positive Jacobi inverse를 결박하고 7 borrowed + 9 owned buffer, `M<=16`, global `I<=4096`, `P=ceil(F/512)`, dense `M²+5M+1`, little-endian field/code/flag가 명시된 solve record `192+72R` 및 DGKS/Givens/right-Jacobi ABI를 strict schema+mandatory semantic replay로 고정; compile-time HIP plan뿐이며 allocation·live apply/primitive lineage·HIPRTC recurrence·iteration host-copy 0·parity·O(N)·속도·상용 증거는 없음 |
| HIPRTC FGMRES 7-symbol recurrence substrate v1 | Phase 0 | implemented | contract_only | plan과 공유하는 solve-record field/status/reason/hint/flag ABI, fixed-source active-mask CSR SpMV·residual·host-scalar copy/scale·Jacobi·terminal control·restart record, gfx/code-object identity와 pending-stream unload fence를 구현하고 실제 `gfx1030` HIPRTC compile/7-symbol을 확인했다. 후속 resource-only live context는 별도 구현됐지만 이 v1 substrate 자체의 L2/L∞ producer·device-scalar recurrence·MGS/DGKS·Givens·backsolve·solution update·live solver receipt·native numerical parity·iteration host-copy 0·O(N)·속도·상용 증거는 없음 |
| HIP FGMRES recurrence allocation/control plan v2 | Phase 0 | implemented | contract_only | 7 borrowed+10 owned extent/256-byte control에 valid-predecessor column-0 `DECIDE -> non-advancing PREFLIGHT -> gated COMMIT -> FINALIZE` schedule을 결박했다. Current checkpoint contract `sha256:0583f66e5faa848da734ff8fbcc430d8bb71ef9fc854fab49121be3f61691e5d`는 exact full-final-cycle의 mandatory `FINAL_GUARD` handoff와 partial-final-cycle checkpoint terminalization을 분리하고, malformed mandatory prestate는 row publish 전 code 47로 fail-closed한다. Runtime allocation·authoritative transaction·solver·host-copy-zero·성능·상용 증거는 없음 |
| HIPRTC FGMRES initial + first-column checkpoint v0.2.15 | Phase 0 | implemented | contract_only | `E/Q=26+14S/14S`의 valid predecessor에서 raw `x_scale=trial+committed`와 `DECIDE -> gated COMMIT -> FINALIZE`를 실행해 `29+14S/14S`로 끝내고 mask 0/1792/7936을 finalizer까지 보존한다. Plan 58, RTC 57, oracle 95, focused 222, hardware 12, FGMRES 289, broad 1019와 actual HIPRTC를 통과했다. Raw launch owner만 true이며 invalid-source all-or-nothing, shifted/range overlap, same-buffer atomic 3-launch owner와 duplicate native policy는 미증명이다 |
| HIP FGMRES caller-attested checkpoint transaction context v0.2.16 | Phase 0 | implemented | contract_only | exact 11-role f64/u8 extent·alignment·uintptr/range registry, process/device native runtime domain, loader-minted runtime와 sealed native callable, actual `hipGetDevice`, compiler-issued handle/callable witness, atomic raw lease, single-use nonconstructible predecessor와 ambiguity poison, exact-runtime fence·atomic pending consume·retry cleanup을 구현했다. Historical three-row context/raw/HIP-context/FGMRES/broad/native `246/60/258/538/1268/12`; current binding은 fixed four-row지만 계속 caller-attested다. Canonical capability 소비·authoritative transaction, later recurrence·host-copy-zero·상용 증거는 없음 |
| HIP FGMRES live checkpoint resource context v0.2.19 | Phase 0 | implemented | contract_only | exact Krylov parent3와 fresh/exclusive allocator-owned8을 exact11 lease, internal RTC v2 module/checkpoint token, same runtime/device/stream과 semantic-last cleanup에 결속했다. Publication count exactly 8, controlled borrow, failed-open/interrupt recovery, actual `gfx1030` resource gate를 검증했다. Owned content, authoritative predecessor/mask validator, checkpoint transaction, solver/solution, host-copy-zero, O(N), speedup과 상용 증거는 없음 |
| HIP FGMRES canonical predecessor producer v0.2.20 | Phase 0 | implemented | contract_only | live exact11과 delegated CSR3/scratch2를 same runtime/device/stream의 allocation 추가 없는 exact16 projection으로 결속하고, owned8을 sealed `hipMemsetAsync`로 zero-initialize한 다음 exact `27+14S` canonical first-column kernel schedule과 device mask-domain validator gate를 단일 exact-runtime fence로 종료한다. Source-apply/Jacobi completion·owned initialization·device gate는 bound되지만 mask/verdict를 host에서 관찰하지 않으며 authoritative predecessor, checkpoint transaction, invalid-source atomicity, solver/solution, iteration host-copy 0, O(N), speedup, commercial readiness와 promotion은 모두 false인 non-promoting 계약이다 |
| HIP FGMRES checkpoint invalid-source atomicity v0.2.21 | Phase 0 | implemented | contract_only | source-only mode 9 preflight와 state 3 ticket을 추가한 fixed four-row raw transaction으로 late invalid lane에서도 `solution_x`/`true_residual` 전체 bytes를 보존한다. Plan/RTC/context-focused/native `63/100/77/13`, full context `261 passed in 523.33s`, race stress `5/5`와 final High/Medium 0을 확인했다. True 범위는 exact registered nonoverlap allocation, same stream, exclusive source ownership과 fixed owner sequence뿐이다. Canonical capability 소비·authoritative predecessor/transaction, arbitrary duplicate/external writer, later recurrence, host-copy-zero, O(N), speedup과 상용 증거는 없음 |
| HIP FGMRES canonical-capability-consuming sealed checkpoint transaction v0.2.22 | Phase 0 | implemented | contract_only | still-open canonical context의 single non-owning child가 conditional predecessor capability를 reserve 후 enqueue에서 single-use consume하고, 기존 live kernel/checkpoint token/stream/direct11과 physical16 projection에 fixed four-row transaction을 제출해 transaction-owned final fence 1회로 닫는다. Canonical parent 포함 total fence 2, 추가 allocation/device bytes/borrow/checkpoint owner/module/H2D/D2H/intermediate sync/fallback 0이다. Unit/legacy `23/56`, actual `gfx1030` valid/late-invalid scoped cases `2 passed`를 확인했다. Consume-return interruption reconciliation과 closed current-binding claim release를 포함하고, standalone receipt는 semantic consistency만 검증하므로 provenance authenticity에는 `expected_context` 또는 서명이 필요하다. Invalid predecessor state `{2,3}`와 snapshot 보존을 허용하며 terminal gate clear는 future action 차단일 뿐 과거 no-commit/rollback 단독 증거가 아니다. Product receipt는 actual mask/verdict/commit/device outcome을 관찰하지 않아 conditional continuation만 발행하며 authoritative predecessor/numerical transaction/solver/solution·later recurrence·host-copy-zero·O(N)·speedup·commercial/promotion은 false다 |
| HIP FGMRES sealed-continuation global recurrence owner v0.2.26 | Phase 0 | implemented | contract_only | v0.2.24의 flat exact-type/atomic pending/immutable dispatch `O(L)` host-control gate와 v0.2.25 process-local recovery를 유지한다. v0.2.26 actual `gfx1030` `F=24,nnz=360,M=2,I=4,R=2`에서 full/prefix/suffix `156/45/111`, checkpoint `E/Q=147/48`, active `FINAL_GUARD` `E/Q=148/48`, CPU iteration/restart `4/2` 및 operator/preconditioner `7/4`, solution/residual allclose를 확인했다. `I=5` partial-final-cycle은 기존 checkpoint-terminal `E/Q=179/58`과 inactive guard를 보존한다. Mandatory handoff required/validity를 분리해 malformed `next_expected_restart`를 error/status/code `1/6/47`, restart-row 미발행으로 fail-closed한 actual native 회귀도 통과했다. Global product path allocation/H2D/D2H/intermediate sync/fallback/live-read/host-branch 0, fence 1이지만 receipt는 outcome/parity를 계속 관찰하지 않는다. Global-owner receipt 자체에는 kernel/solver speedup·일반 N-DOF O(N)·terminal observation·completion export·full parity·host-copy-zero·상용 증거가 없음 |
| HIP FGMRES completion-only raw export v0.2.27 | Phase 0 | implemented | contract_only | exact `recurrence_fenced` completion capability를 reserve하고 host staging 3개를 먼저 할당한 뒤 첫 read 직전에 single-use consume한다. Loader-bound blocking `hipMemcpyDeviceToHost`와 immutable source pointer snapshot으로 `solution_x(8F) -> true_residual(8F) -> solve_record(192+72R)`를 정확히 3회, 총 `16F+192+72R` bytes 복사하고 detached immutable bytes/read-only views 및 payload/bundle/receipt hash를 발행한다. 반복·동시 retrieval은 추가 copy 없이 같은 result identity로 수렴하며 부분 publication은 없다. Export 전후 global receipt/hash는 불변이고 export owner의 device allocation/borrow·H2D·kernel·explicit stream sync·fallback·numerical-content branch는 0이다. Actual `gfx1030` `F=6,M=1,I=1`에서 `48/48/264`, 총 360 bytes의 exact 3 copy를 확인했지만 CPU 비교는 verification-only다. `solve_record`는 opaque이며 terminal outcome/status·수치 parity·solution-ready·ResultIR·iteration host-copy-zero·일반 N-DOF O(N)·속도·상용/promotion 증거는 모두 없음 |
| HIP FGMRES terminal-outcome observation v0.2.28 | Phase 0 | implemented | contract_only | exact final export result와 process-local context/policy seal을 요구하고 little-endian `192+72R` record의 17 terminal code, active/error/counter/metric/restart row, gate/flag/stagnation/divergence priority를 별도 receipt에서 해석한다. Numerical failure header metric은 stale/inert로 숨기고 committed row prefix만 유지하며, nonfailure true-residual bytes는 deterministic tree metric과 일치해야 한다. Actual `gfx1030` later-column convergence와 active final-guard max iteration을 검증했고 observer의 device allocation/H2D/D2H/kernel/sync는 0이다. Raw export/global receipt는 outcome-free로 남고 numerical parity·solution-ready·ResultIR·iteration host-copy-zero·O(N)·speedup·commercial/promotion 증거는 없음 |
| HIP FGMRES exact stream query + abandoned global-owner recovery v0.2.25 | Phase 0 | implemented | contract_only | Checkpoint lease에 loader-sealed exact `hipStreamQuery`를 결속하고 status `0=complete`, `600=not ready`, 그 외·예외는 fail-closed로 처리한다. Parent-owned weak-liveness recovery cell은 child/context/lease를 strong-reference하지 않고 GC callback에서 HIP을 호출하지 않으며, abandoned consumed/pending child를 `query -> 필요 시 exact sync 1회 -> query -> pending ack -> terminal release`로 process-local 회수한다. Exact bool, frozen authority, stale-pending/partial-close, 중단 안전 단조 release를 강제한다. Actual `gfx1030` lifecycle gate는 query `not-ready -> complete`, sync `1`, pending `39 -> 0`, product malloc/H2D/D2H/runtime-facade sync·verification D2H `0`, `1 passed, 2 deselected in 37.42s`; RTC `111 passed in 34.77s`, checkpoint context `261 passed in 248.58s`, global owner full `54 passed in 1387.12s`, sealed transaction full `30 passed in 507.23s`, independent audit `BLOCKER/HIGH/MEDIUM/LOW=0`이다. Wheel/sdist는 `875235`/`823734` bytes, SHA-256 `e6522f810af2a4a0f6d62c770f510bcab57278e64cec4e0070b8fbec2eb2b8e2`/`8094a8bcaf30d3aaf954d5c5f0183baaf03881ff96ae62b33b6832276b2b3d3c`이며 wheel 격리 설치에서 global public API/schema/HIP kernel resource import를 확인했다. 이는 process crash/GPU reset/cross-process 회수, 수치·completion·parity·solution·O(N)·속도·상용 promotion 증거가 아닌 lifecycle 계약이다 |
| HIP FGMRES full recurrence ABI v2 | Phase 0 | in_progress | unavailable | later columns/restarts·terminal padding·final guard kernel과 conditional suffix owner를 구현했고, integrated owner에서 active later column, restart `1->2->3`, exact full-final-cycle checkpoint handoff 및 active final-guard epoch claim까지 actual hardware로 검증했다. Completion-only export와 context-bound terminal observer 뒤 registry-bound local `gfx1030` fixed-suite CPU/HIP parity도 `10/10` 완료했다. 그러나 external actual `gfx1100`은 `0/10`이고 authoritative ResultIR, multiarchitecture·signed promotion, iteration host-copy 0·일반 N-DOF O(N)·kernel/solver speedup·상용 증거는 없음 |
| HIP canonical-CSR AOT artifact v1 | Phase 0 | implemented | contract_only | 단일 source·versioned C ABI·동일 ROCm-root toolchain·hash/target 검증형 prebuilt loader; 실행/parity/speed 증거 아님 |
| HIP canonical-CSR residual/JVP replay v1 | Phase 0 | implemented | contract_only | plan/committed-state 상주, fused `Ku-F`/`Kv`, exact attempt/success telemetry와 test-double CPU oracle; 현재 native 실행 아님 |
| HIPRTC canonical-CSR residual/JVP v1 | Phase 0 | implemented | contract_only | RX 6900 XT `gfx1030`에서 fixed-source HIPRTC fused `Ku-F`/`Kv` 실행과 full/free/constrained FP64 parity·fallback 0 관찰; unsigned v1은 항상 non-promoting, 솔버/성능/O(N) 증거 아님 |
| HIPRTC fixed-degree-3 kernel scaling v1 | Phase 0 | implemented | non_promoting | 사전 고정한 off-cache 5개/4x family에서 HIP-event OLS `1.0089` (R² `0.99999`), Theil-Sen `1.0090`, bootstrap 95% CI `[1.0084, 1.0098]`과 정합성·timed transfer/allocation·fallback 0을 관찰; fixed synthetic fused kernel 전용이며 솔버/end-to-end O(N)·speedup·상용 증거 아님 |
| HIP AOT-native CSR residual/JVP parity | Phase 0 | in_progress | unavailable | AOT lane은 matching ROCm device-library build/run receipt가 없어 native parity 미증명; 별도 HIPRTC lane 관찰을 AOT release 증거로 승격하지 않음 |
| HIP resident residual | Phase 0 | research_asset | non_promoting | batch residual/FFI 자산은 있으나 full device solver 아님 |
| Device-resident Newton/Krylov | Phase 1 | planned | unavailable | iteration당 host copy 0 증거 없음 |
| E(3) spatial runtime | Phase 0/1 | planned | unavailable | 문서 요구사항만 존재 |
| Fixed-rank projection v1 | Phase 0 | implemented | contract_only | Jacobi square-root-energy scaling·2-pass MGS·implicit `Q(Qᵀv)`와 bounded complexity receipt; 솔버 해 아님 |
| AI correction·physics gate·shadow v1 | Phase 0 | implemented | shadow | immutable `DQy`, CPU full `Ku-F`/`||DRf||₂`/energy/BC/linear-constitutive replay, exact rollback, AI-off/observed bit parity; OOD 미평가로 reject, proposal 미소비 |
| Solver-approved fixed-rank QR memory v1 | Phase 0 | implemented | contract_only | validated ready CPU run만 teacher, rank≤16 FIFO/provenance/projection rebuild; RLS/Kalman/학습 update 아님 |
| Temporal no-attention runtime | Phase 0/1 | research_asset | non_promoting | 기존 T-GNN은 backprop/attention 구성 포함 |
| No-backprop local learning | Phase 0/1 | in_progress | unavailable | solver-approved QR memory substrate는 있으나 RLS/Kalman/readout parameter update는 없음 |
| Solver-gated AI proposal | Phase 0/1 | in_progress | shadow | Phase 0 safety contract는 구현; calibrated OOD/UQ, eligible proposal, iterative solver warm-start 소비와 가속 검증은 미구현 |
| Solver-verified optimization | Phase 1/2 | legacy_asset | non_promoting | 축약/proxy loop를 full FE 검증으로 교체 필요 |
| Independent commercial V&V | Phase 2/3 | blocked_external | unavailable | 외부/실프로젝트 증거 필요 |

v0.2.24 global owner의 immutable-dispatch TOCTOU 수정 후 required F12 later-column/F24 active-restart hardware 두 case는 combined `2 passed in 96.11s`로 재통과했다. Final independent linear re-audit는 transient drift/alias/registry forgery/signed-zero를 포함한 요청 범위에서 remaining defect 0으로 종료했다. 이는 numerical/performance/commercial promotion이 아니다.

v0.2.25는 v0.2.24의 C++/HIP/public schema/ABI identity를 변경하지 않고 exact stream-completion query와 process-local abandoned-owner lifecycle recovery만 추가한다. Recovery의 stream completion은 device numerical outcome이나 solver completion을 의미하지 않으며, abandoned path는 completion capability을 발행하지 않는다.

v0.2.26 current checkpoint/global/combined/source identity는 `0583f66e…` / `7c18ba91…` / `6a361ccf…` / `a5b39fb9…`다. Recurrence semantic payload/schema와 HIP source는 변경됐지만 public global receipt/completion schema는 변경되지 않았다. Active guard 실행 증거는 receipt에 actual outcome/parity 관찰을 추가하지 않는다.

v0.2.35 publication validation은 low-level ledger `41`, high-level durable replay `13`, 기존 signed evidence `16`, release-identity/wheel/dependency/public API/capability 지원 회귀 `104`의 비중복 `174 passed`를 확인했다. Candidate wheel은 `1112675` bytes/`209` members, `sha256:db284e292f22626d1f7e2e65b6fa494b1d55e790374e432bb1127efb22c3ba7f`이며 격리 설치 후 신규 schema/public API와 ledger close·pinned reopen audit replay를 통과했다. 이는 local contract/package 증거이며 hardware·promotion·commercial claim을 추가하지 않는다.

## 갱신 규칙

- `implementation_state` 변경에는 코드 또는 schema path가 필요하다.
- `promotion_state` 변경에는 focused test와 authoritative evidence path가 모두 필요하다.
- 빈 `evidence_paths`로 promotion할 수 없다.
- `partial`, `proxy`, `fallback`, `external_blocked` 표시는 제거하지 않는다.
- release-facing capabilities surface와 연결하기 전 별도 claim-boundary review를 수행한다.

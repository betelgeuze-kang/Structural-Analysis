# Lee-frame candidate integration context

The candidate generator is validated against main containing:

- MaterialStateBundle and checkpoint/material-state ancestry;
- nonlinear numerical ResultIR and non-authoritative recovery candidate;
- SolverEpisodeIR;
- J1–J4 fiber-frame topology, physical scaling, kinematic state, and combined
  execution-state binding;
- bounded public nonlinear two-bar analysis;
- deterministic concrete-damage evidence regeneration.

These merged contracts do not change the Lee-frame candidate's formal evidence
status. The generated receipt identifies local generated bytes, while the DOI
remains publisher metadata. Publisher/table bytes, source-use approval,
independent clean-runner reproduction, formal operator approval, and complete
Level 2 evidence remain required before hierarchy credit is possible.

The candidate continues to write only the `.candidate.json` operator-manifest
path and cannot overwrite or promote the canonical formal evidence manifest.

{
  "schema_version": "native-capabilities.v1",
  "capabilities": {
    "abi_v1_base": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-ffi",
      "claim": "base descriptors, error buffers, layout, overflow and function table only"
    },
    "backend_selector": {
      "status": "implemented",
      "cutover_gate": "C3",
      "owner": "structural-ffi",
      "claim": "ABI v1.12 appends one versioned backend_get_api slot to sa_get_api_v1; the CPU full-residual context owns validated CSR/operator state, deterministic FP64 execution, caller-owned results and lifecycle telemetry behind an opaque non-Send Rust RAII wrapper with concurrent immutable queries and exclusive mutable execution, while the frozen seven-symbol Rust compatibility adapter resolves only sa_get_api_v1 and delegates to the same table; HIP is explicit opt-in and fails closed in CPU-only builds, and protected-runner HIP C2, broader operator families, ROCm packaging and C6 remain open"
    },
    "modelir_v2_rust_wire": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural-contracts",
      "claim": "strict UTF-8 JSON decode, duplicate-key and schema rejection, Python-compatible canonical bytes and content/semantic/provenance hashes only"
    },
    "modelir_v2_cpp_core": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_model_ir",
      "claim": "ABI v1.1 typed descriptor deep copy, C++ semantic validation, deterministic report/snapshot and Python oracle issue/readiness/blocker parity only"
    },
    "modelir_v2": {
      "status": "implemented",
      "cutover_gate": "C3",
      "owner": "structural-contracts",
      "claim": "strict Rust wire identity, typed C++ semantic validation, safe ABI v1.1 RAII round-trip and structural-cli model validate only; no solver, restart, ResultIR or backend readiness"
    },
    "modelir_ndtha_adapter": {
      "status": "implemented",
      "cutover_gate": "C3",
      "owner": "structural_model_ir",
      "claim": "one exact fixed-guided one-story frame3d global-X reduction only: ABI v1.6 derives 12*E*Iy/L^3 stiffness, rho*A*L/2 mass and one selected floor FX load from immutable typed ModelIR, accepts semantically equivalent zero prescribed values and validated provenance-only roundtrip mappings from the bounded MGT normalizer, combines them with explicitly supplied damping ratio, elastic guard, solver configuration and acceleration, publishes seven caller-owned FP64 buffers atomically, and crosses a safe Rust wrapper with closed-form Python C1 parity and zero-fallback CPU execution; arbitrary topology reduction, P-delta derivation, nonlinear material inference, HIP C2 and C6 remain open; public run/restart is promoted only as the separate exact-profile C5 capability"
    },
    "modelir_ndtha_product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "the exact fixed-guided one-story frame3d global-X ModelIR profile only: Rust verifies strict ModelIR and explicit adapter-request identities, C++ derives the bounded native NDTHA problem through ABI v1.6, an outer canonical checkpoint binds content/semantic/provenance, adapter-request, generated-request and inner-state identities, and public model-run/model-resume emit bitwise-identical canonical model/request/checkpoint/ResultIR/ReportIR/Markdown/self-hashed receipt artifacts in a Python/Node-free environment; arbitrary topology, HIP C2, broader solver authority, Workbench and C6 remain open"
    },
    "mgt_import_health": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded MGT import-health C5 only: Rust owns bounded original bytes, strict UTF-8/BOM disposition, source SHA-256, section/row inventory and explicit mapped/preserved_only/dropped/unsupported diagnostics; the exact numeric frame/truss subset alone becomes canonical ModelIR and crosses the existing C++ semantic validator/snapshot through public import mgt, while four existing foundation fixtures remain honestly blocked where material/section/support/load data or shell support is absent; Python C1 raw-parser parity is frozen for all seven fixtures; the exact fixed-guided normalized profile reaches the durable NDTHA Workbench flow, and the exact cantilever normalized profile reaches the model_ir_linear_cpu_v1 Import -> Validate -> Run -> Resume -> Compare -> Report flow with original MGT bytes, deterministic import health, C++ snapshot, real PCG checkpoint, typed recovery, comparison and PDF bound through restart; clean-environment artifacts are deterministic, while general MGT grammar, CP949, shell/load-combination/writeback, broader MGT/solver authority and C6 remain open"
    },
    "reference_materials_elements_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_elements",
      "claim": "bounded CPU C1 reference slice only: C++20 owns an explicit elastic-isotropic material, an epoch-checked bilinear uniaxial trial/commit/rollback point, linear truss3d and Euler-Bernoulli frame3d elements, and a three-node plane-stress membrane embedded in 3D; one independent NumPy oracle compares every tangent, consistent-mass, residual, JVP and recovery value, ABI v1.7 publishes disjoint caller-owned buffers atomically through a safe reentrant Rust wrapper with fallback 0; a product-owned FP64 HIP C2-candidate batch now has zero-error local live element/assembly parity, deterministic repeat, resident operator buffers and fallback 0, but the sequential gate remains C1 until a native-hip-approved protected-runner receipt exists; frame offsets/releases, geometric and material nonlinearity, shell bending/drilling, general topology, checkpoint, product E2E and C6 remain open"
    },
    "dense_assembly_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_assembly",
      "claim": "bounded dense, constraint-reduced canonical-CSR and typed ModelIR linear-graph CPU C1 assembly slice only: C++20 validates unique stable element order, local shapes, finite values and global DOF references, then deterministically scatters tangent, consistent mass, residual and JVP and emits a sorted active-DOF map with canonical CSR rows independent of contribution and constraint input order; a separate composition target resolves every element in a bounded linear-elastic frame3d/truss3d ModelIR graph, maps six canonical DOFs per node, selects direct nodal loads, retains content/semantic/provenance identities and emits internal/external/equilibrium residual plus element recovery from the same source; ABI v1.13 appends an immutable exact-sizes query and a failure-atomic 16-buffer execute operation behind a safe Rust sizes/allocation/CSR/recovery/identity revalidation wrapper, providing a C3 integration candidate without promoting the sequential numerical gate; the dense three-DOF result, irregular constrained three-element CSR graph and mixed three-node ModelIR graph with seven active DOFs and 43 structural entries match independent NumPy oracles; the product-owned HIP C2-candidate batch uses non-atomic stable-order FP64 accumulation, keeps element/operator buffers resident between two kernels and has zero-error local live parity with fallback 0, but the sequential gate remains C1 until a native-hip-approved protected-runner receipt exists; nonzero prescribed constraints, offsets/releases, self-weight, combinations/stages, shell/nonlinear ModelIR graphs, state epochs, sparse resident operators and C6 remain open, while exact CPU restart and product E2E are tracked only as separate bounded C4/C5 implementation capabilities"
    },
    "modelir_linear_checkpoint": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded typed-ModelIR frame3d/truss3d CPU linear checkpoint C4 implementation only: Rust strictly binds content/semantic/provenance, analysis-request, ABI v1.13 assembly, generated canonical-CSR request and the complete ABI v1.10 real PCG iteration state in one pointer-free SAMLPC01 little-endian envelope; every single-byte mutation and request/configuration drift fails with checkpoint-mismatch taxonomy, and resumed terminal artifacts are byte-identical to direct execution; this is a separate CPU implementation capability and does not promote the sequential numerical family past C1 without the approved protected-runner HIP C2 receipt; constrained reactions, shell/nonlinear graphs and C6 remain open, while durable job/service orchestration is tracked only as separate bounded C5 capabilities"
    },
    "modelir_linear_product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded typed-ModelIR frame3d/truss3d CPU linear product E2E C5 implementation only: public model-linear-run/model-linear-resume commands strictly bind exact ModelIR and analysis identities, cross ABI v1.13 C++ assembly, derive canonical CSR, advance ABI v1.10 PCG, publish the exact C4 checkpoint, self-hashed ResultIR/ReportIR/Markdown and a terminal ABI v1.13 active-DOF plus element recovery artifact; clean-environment direct and real-iteration resumed 14-artifact directories are byte-identical with no Python or Node lookup and fallback 0; this separate CPU implementation capability does not promote sequential C2/C3 authority, while constrained reactions, shell/nonlinear graphs, approved protected-runner HIP C2, engineering acceptance and C6 remain open; the durable job/service path is tracked only as separate bounded C5 capabilities"
    },
    "modelir_linear_durable_jobs": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-runtime",
      "claim": "bounded typed-ModelIR frame3d/truss3d CPU linear durable-job C5 implementation only: one strict language-neutral envelope binds canonical ModelIR plus exact analysis control and all three model identities, Rust dispatches its immutable profile through an append-only self-hashed event chain, expired-lease reconciliation and exact completion re-projection of ABI v1.13 assembly, ABI v1.10 PCG checkpoint, ResultIR, typed recovery IR, ReportIR and Markdown; clean-process real-iteration restart/export is byte-identical to the direct public product with no Python or Node lookup; this separate orchestration capability cannot promote the sequential numerical families beyond C1 or supply C2/C3 authority, while tenant authorization, distributed claims, native Workbench integration, approved protected-runner HIP C2, engineering acceptance and C6 remain open"
    },
    "modelir_linear_service_api": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded loopback single-host single-tenant typed-ModelIR frame3d/truss3d CPU linear service C5 implementation only: Rust accepts the strict durable envelope at POST /v1/model-linear-jobs, dispatches through the existing distinct hashed client/worker role credentials and synchronous durable worker, serves profile-specific checkpoint bytes and verified recovery through /v1/jobs/{job_id}/result-recovery-ir, and proves one real-iteration resume plus exact projection in a socket-free service test with no Python or Node; this separate transport capability cannot promote numerical C2/C3 authority, while live process-kill evidence remains with the original NDTHA service profile and TLS, tenant isolation, distributed workers, native Workbench integration, approved protected-runner HIP C2, engineering acceptance and C6 remain open"
    },
    "sparse_linear_solver_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++20 sparse linear C1 reference only: strict canonical CSR validation, symmetry enforcement, Jacobi-preconditioned conjugate gradients, true-residual postcheck, deterministic repetition, fallback 0 and fixed singularity/indefinite/nonconvergence/increment/residual taxonomy cover four SPD profiles and failure paths against an independent NumPy direct-solve oracle; a product-owned fixed-tree FP64 HIP C2 candidate keeps all PCG vectors and iteration control resident, has bitwise repeats, CPU/HIP status and iteration parity, local live maximum solution error 4.4408920985006262e-16 and fallback 0; append-only ABI v1.8 one-shot plus ABI v1.10 complete-state begin/advance and a safe reentrant Rust wrapper implement failure-atomic caller-owned C3 integration, but the sequential gate remains C1 until the native-hip-approved protected-runner C2 receipt exists; general constraints/reordering/preconditioners, indefinite/direct solves and C6 remain open, while exact CPU iterative checkpoint and public CLI are tracked as separate bounded C4/C5 implementation capabilities without promoting this numerical family past C1"
    },
    "sparse_linear_checkpoint": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded canonical-CSR CPU PCG checkpoint C4 implementation only: ABI v1.10 exposes complete real iteration state, and a pointer-free SAPCGC01 artifact binds the exact strict request plus model, iterative state, execution and aggregate checkpoint SHA-256 identities; every single-byte mutation and request/configuration drift fails with checkpoint-mismatch taxonomy, while direct and segmented execution produce bitwise-identical terminal checkpoint and ResultIR bytes; protected-runner HIP C2, durable queued-job integration, arbitrary ModelIR sparse assembly beyond the separately tracked bounded frame3d/truss3d composition and C6 remain open"
    },
    "sparse_linear_product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded canonical-CSR CPU PCG product E2E C5 implementation only: strict Rust request parsing crosses ABI v1.10 real-iteration begin/advance and exact C4 checkpoint, then public linear-run/linear-resume commands atomically publish typed active/failure receipts or self-hashed ResultIR, ReportIR and deterministic Markdown; direct and real-iteration resumed terminal artifact directories are byte-identical in a clean environment with no Python or Node lookup and fallback 0; protected-runner HIP C2, arbitrary ModelIR sparse assembly beyond the separately tracked bounded frame3d/truss3d composition, durable service jobs, engineering acceptance, PDF rendering and C6 remain open"
    },
    "generalized_eigen_solver_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded dense symmetric FP64 C++20 modal and linear-buckling C1 reference only: strict positive-definite/semidefinite matrix contracts, deterministic cyclic-Jacobi decomposition, rigid/infinite-mode filtering, complete-cluster selection, coordinate-axis canonical mode bases, mass/stiffness normalization, residual/orthogonality gates and fallback 0 cover six profiles against an independent SciPy generalized-eigen oracle; a product-owned bounded single-thread HIP C2 candidate covers eight modal/buckling profiles with bitwise repeats, resident eigensolve/canonicalization/result recovery, local live maximum relative eigenvalue error 1.3706125276112035e-16 and fallback 0; append-only ABI v1.9 and a safe reentrant Rust wrapper implement the ABI C3 integration candidate, but the sequential gate remains C1 until the native-hip-approved protected-runner C2 receipt exists; sparse/subspace extraction and C6 remain open, while the CPU phase-boundary checkpoint and public CLI are tracked as separate bounded C4/C5 implementation capabilities without promoting this numerical family past C1"
    },
    "generalized_eigen_checkpoint": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded dense CPU modal/linear-buckling phase-boundary checkpoint C4 implementation only: a canonical pointer-free SAEIGC01 artifact binds the exact strict request plus model, ready-state, execution and aggregate checkpoint SHA-256 identities, rejects every single-byte mutation and request drift with checkpoint-mismatch taxonomy, and resumes to byte-identical ResultIR; the atomic eigensolve exposes no honest mid-Jacobi state, so this boundary is validated-ready-before-dispatch rather than an invented iterative snapshot; protected-runner HIP C2, durable queued-job integration, sparse whole-model authority and C6 remain open"
    },
    "generalized_eigen_product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded dense CPU modal/linear-buckling product E2E C5 implementation only: strict Rust request parsing crosses ABI v1.9 C++ execution and the exact C4 phase checkpoint, then emits self-hashed ResultIR, ReportIR, deterministic Markdown and an atomic run receipt through public eigen-run/eigen-resume commands; modal and buckling direct/resume artifact directories are byte-identical in a clean environment with no Python or Node lookup and fallback 0; protected-runner HIP C2, ModelIR/sparse assembly adaptation, durable service jobs, engineering acceptance, PDF rendering for this profile and C6 remain open"
    },
    "track_point_load_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ track point-load kernel with Python C1 oracle parity for the 9-node midpoint-load support/theory matrix, ABI v1.2 caller-owned output and safe Rust integration; the frozen legacy Rust endpoint-only divergence is retained, while broader input-space parity, HIP C2, restart and product E2E remain open"
    },
    "nonlinear_static_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ story-frame Newton C1 family with independent dense-matrix Python C1 oracle parity for the five-case 1/3-story topology, elastic/plastic, mixed-sign load, P-delta and backtracking matrix; a product-owned bounded single-thread FP64 HIP C2 candidate keeps model, Newton state, tangent solve and recovery resident, has bitwise repeats, exact local CPU/HIP status/iteration/plastic/backtrack parity, zero measured result error and fallback 0; ABI v1.3 one-shot plus ABI v1.11 complete caller-owned real-iteration begin/advance state and a safe reentrant Rust wrapper provide failure-atomic C3 implementation evidence, but the sequential numerical gate remains C1 until the native-hip-approved protected-runner C2 receipt exists; broader nonlinear input-space parity, parallel/general ModelIR topology and transient HIP execution remain open, while restart and product E2E are tracked as separate bounded CPU C4/C5 implementation capabilities and C6 remains open"
    },
    "nonlinear_static_checkpoint": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded story-frame CPU Newton checkpoint C4 implementation only: ABI v1.11 exposes complete real iteration state, and a pointer-free SASTAC01 artifact binds the exact strict request plus model, Newton state, execution and aggregate checkpoint SHA-256 identities; every single-byte mutation and request/input/configuration drift fails with checkpoint-mismatch taxonomy, while direct and segmented execution produce byte-identical terminal checkpoint and ResultIR artifacts; HIP C2, durable queued-job integration, arbitrary ModelIR assembly and C6 remain open"
    },
    "nonlinear_static_product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded story-frame CPU Newton product E2E C5 implementation only: strict Rust request parsing crosses ABI v1.11 real-iteration begin/advance and exact C4 checkpoint, then public static-run/static-resume commands atomically publish typed active/failure receipts or self-hashed ResultIR, ReportIR and deterministic Markdown; direct and real-iteration resumed terminal artifact directories are byte-identical in a clean environment with no Python or Node lookup and fallback 0; HIP C2, arbitrary ModelIR assembly, durable service jobs, engineering acceptance, PDF rendering and C6 remain open"
    },
    "nonlinear_ndtha_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ Newmark/Newton story-frame kernel with shared constitutive assembly and independent dense-matrix Python C1 full-result parity for the five-case 1/2/3-story Newmark parameter, elastic/plastic, mixed-sign acceleration, P-delta, damping-cap, adaptive retry, line-search and collapse matrix; a product-owned bounded single-thread FP64 HIP C2 candidate keeps the model, time-step state, adaptive/Newmark/Newton control, tangent solve and eleven-channel result recovery resident, has bitwise repeats, exact local CPU/HIP status/iteration/plastic/backtrack/result parity and fallback 0; ABI v1.4 nested caller-owned inputs/outputs, safe Rust integration and the frozen legacy Rust boundary provide C3 evidence, but the sequential gate remains C1 until the native-hip-approved protected-runner C2 receipt exists; broader dynamic input-space parity, arbitrary ModelIR assembly, ROCm package authority and C6 remain open, while restart and product E2E are tracked as separate bounded CPU C4/C5 capabilities"
    },
    "checkpoint_restart": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded CPU nonlinear-NDTHA checkpoint C4 only: ABI v1.5 validates complete caller-owned inter-step state; structural-runtime emits a canonical pointer-free little-endian artifact bound to independent model, state and execution SHA-256 identities, publishes it by same-directory write/sync/rename/directory-sync, rejects corruption and binding mismatch, and resumes bitwise-identically; job-state crash recovery and product E2E are promoted only as separate bounded CPU C5 capabilities, while HIP C2, broader solver coverage and release authority remain open"
    },
    "product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded CPU nonlinear-NDTHA C5 only: a strict canonical request crosses Rust to ABI v1.5 C++ execution, C4 checkpoint identity, deterministic ResultIR, ReportIR, Markdown document source and a self-hashed artifact receipt through public CLI run/resume; direct and resumed outputs are byte-identical in an environment with no Python or Node lookup, and authority remains bounded_candidate; HIP C2, arbitrary ModelIR-to-analysis adaptation, distributed durable jobs/API, Workbench and C6 remain open; the exact fixed-guided ModelIR profile, external comparison and PDF rendering are promoted only as separate bounded C5 capabilities"
    },
    "durable_jobs": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-runtime",
      "claim": "bounded single-host CPU nonlinear-NDTHA durable-job C5 only: Rust owns strict idempotent submit/poll/cancel, append-only self-hashed event chains, content-addressed artifacts, OS-lock worker leases, expired-lease crash reconciliation, checkpoint resume, deterministic completion re-projection and public CLI work/export; clean-environment direct and restarted artifacts are byte-identical with no Python or Node lookup; tenant authorization, distributed consensus/claims, broader service API, HIP C2, broader solver families and C6 remain open; loopback transport is promoted only as a separate bounded C5 capability"
    },
    "service_api": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded loopback single-host single-tenant nonlinear-NDTHA HTTP API C5 only: Rust serves exact HTTP/1.1 submit/poll/cancel/work-once and immutable checkpoint/ResultIR/ReportIR/document retrieval, rejects duplicate headers, transfer encoding, ambiguous paths and oversized bodies, uses distinct hashed client/worker bearer credentials from mode-restricted files, emits no-store/nosniff responses, and survives a clean-environment process kill after checkpoint before reopening the same durable store and returning artifacts byte-identical to direct native execution; TLS, non-loopback exposure, tenant isolation, remote/distributed workers, concurrent cancellation, rate limits/quotas, broader solver families, HIP C2 and C6 remain open"
    },
    "external_comparison": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded global nonlinear-NDTHA external comparison C5 only: Rust strictly ingests one hash-bound language-neutral external-result contract, verifies source artifact bytes and any declared executable bytes, requires exact model/case/frame/unit/location/ResultIR mapping, emits self-hashed comparison and receipt artifacts, preserves divergence, and runs through a clean-environment CLI with no Python or Node lookup; Python C1 golden parity covers three global quantities, while live MIDAS/OpenSees/CalculiX execution, same-mesh proof, node/member mapping, broader quantities, HIP C2 and C6 remain open"
    },
    "pdf_report": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-report",
      "claim": "bounded single-page nonlinear-NDTHA and sparse-linear PDF report C5 only: Rust re-projects and byte-verifies exact ResultIR, ReportIR and Markdown bindings; the absent-locale nonlinear-NDTHA and sparse-linear v1 paths remain byte-identical with ASCII standard fonts and distinct typed CLI receipts, while explicit en-US or ko-KR v2 for either source profile embeds one renamed OFL-1.1 TrueType subset as Type0/CIDFont2 with Identity-H and ToUnicode for fixed labels plus printable-ASCII dynamic values and emits a profile-typed self-hashed receipt; each path emits a deterministic A4 PDF 1.7, validates its own xref/object/trailer and v2 embedded-font bytes, and repeats bitwise-identically through clean-environment CLI and Workbench entrypoints with no Python, Node or external renderer lookup; arbitrary Unicode, PDF/A, tagged accessibility, assistive-technology conformance, multipage tables/charts, broader report profiles, HIP C2 and C6 remain open"
    },
    "native_workbench": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-workbench",
      "claim": "bounded terminal Rust-native Workbench C5 only: one exact fixed-guided ModelIR NDTHA profile owns Import -> Validate -> Run -> Resume -> Compare -> Report plus Inspect -> Report-view -> Result-view -> Result-deformed-view -> Review -> Export through direct Rust product-library calls, either from strict ModelIR or from the exact normalized MGT profile with original MGT bytes, import diagnostics and C++ snapshot retained; a separate general ModelIR terminal topology view strictly parses every current positive profile, crosses Rust -> C ABI -> C++ semantic validation, renders the canonical snapshot through fixed deterministic isometric/XY/XZ/YZ ANSI-free projections, exposes analysis types and explicit blockers, and never equates visibility with solver readiness; its closed `en-US`/`ko-KR` paths translate only fixed labels while retaining the exact canvas, topology, SI values, machine tokens and provenance, and the compatibility path preserves the original English bytes; a separate provenance-bound ModelIR node-coordinate edit changes one existing node in the canonical C++ snapshot, retains upstream provenance, degrades any matching exact or canonicalized round-trip row to approximated, strictly reparses and C++-revalidates the result, refuses no-op or invalid geometry, and atomically publishes a new model plus self-hashed receipt; a sibling provenance-bound nodal-load edit replaces the six finite SI components of one existing load inside one named pattern, binds both identities plus previous and edited values, conservatively degrades a matching load-pattern round-trip row, refuses no-op or invalid semantics, and follows the same create-new C++ revalidation boundary without creating, deleting, retargeting or combining loads; a third provenance-bound constraint-value editor changes one finite prescribed metre/radian value only for a DOF already restrained by an existing named constraint, binds the constraint, DOF, unit, previous and edited value plus source hashes, conservatively degrades a matching constraint round-trip row, and follows the same create-new C++ revalidation boundary without changing restraint masks or node targets; immutable hash-bound inputs, Rust -> C ABI -> C++ validation, a real checkpoint, ResultIR/ReportIR, external comparison and deterministic PDF are bound into atomically published self-hashed stage inventories that reconcile a process death after checkpoint publication; the deterministic operator view and immutable explicit human disposition are bound to the exact session/result/comparison/PDF and never inferred from solver or comparison success; one English/Korean UTF-8 linear report view re-verifies that artifact chain, carries the same values and Unicode review text without ANSI/color/layout meaning, escapes directional-spoofing controls and self-hashes its bytes; an English/Korean bounded self-hashed NDTHA response-history view exposes exact completed top-displacement, drift-ratio, base-shear and residual-infinity channels through stable windows without inferring time; an English/Korean exact-profile deformed-shape view revalidates the C++ snapshot and overlays one selected global-X top displacement through deterministic isometric/XY/XZ/YZ projections without synthesizing a general nodal field; a profile-typed English/Korean embedded-font PDF export re-verifies the stored nonlinear-NDTHA or sparse-linear v1 report, leaves the durable workspace unchanged, and publishes exact en-US or ko-KR Type0/ToUnicode PDF v2 bytes to a new directory; neither surface is WCAG, PDF/UA, assistive-technology, general localization, or arbitrary-Unicode certification; Catalog/Catalog-show strictly browse the native-owned 26-case language-neutral benchmark catalog without promoting geometry-only or missing-runner cases, while Evidence/Evidence-show validate bounded non-symlink copied bundles, checksums and source commits and conservatively expose ready/blocked/unavailable without reading protected sources or generating approval; clean-environment default/explicit-English/Korean model-view, model-edit-node, model-edit-nodal-load, model-edit-constraint-value, result-view, result-deformed-view, stage-by-stage, one-shot, localized report-view/result-view/result-deformed-view/PDF export, review/export and read-only catalog workflows are byte-identical with no Python, Node, browser, CLI subprocess or external renderer lookup, and the same bounded surfaces execute from verified CPU static/shared installs with update/rollback; general visual model editing and arbitrary-nodal-field/modal/contour result exploration, general accessibility/localization, arbitrary-Unicode or tagged PDF output, broader MGT and solver profiles, live MIDAS/OpenSees/CalculiX, broader language-neutral fixture ownership, React/TypeScript removal, approved HIP C2 and C6 remain open"
    },
    "modelir_linear_workbench": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-workbench",
      "claim": "bounded typed-ModelIR frame3d/truss3d CPU linear Workbench C5 composition only: the explicit model_ir_linear_cpu_v1 profile owns Import -> Validate -> Run -> Resume -> Compare -> Report plus Inspect -> Report-view -> Review -> Export through direct Rust product-library calls; strict canonical ModelIR or one exact bounded normalized MGT cantilever with original MGT bytes, deterministic import-health diagnostics and C++ snapshot, analysis request, content/semantic/provenance identities, language-neutral external source evidence, a real PCG checkpoint.mlpcp, sparse ResultIR, typed recovered global-DOF and element ResultIR, ReportIR, PDF-ready Markdown, deterministic single-page sparse PDF, embedded-font localized sparse PDF export in exact en-US or ko-KR, global-DOF external comparison and human review are bound into atomic self-hashed stages or create-new exports; every recovered vector, DOF mapping, SI unit/frame, source ResultIR hash, summary and fallback 0 is revalidated, divergence remains explicit, and a clean-environment process restart after one real iteration is byte-identical to direct execution with no Python, Node, browser, CLI subprocess or external renderer lookup; absence of the optional profile preserves the existing fixed-guided NDTHA session and receipt bytes, while NDTHA-only response and deformed-result operations fail closed for this profile; PDF/A/accessibility, broader MGT linear ingestion, live MIDAS/OpenSees/CalculiX execution, general nonlinear-static/modal/buckling/transient Workbench profiles, arbitrary result visualization, React/TypeScript removal, approved protected-runner HIP C2, authoritative numerical C2/C3 and C6 remain open"
    },
    "native_evidence_bundle": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-evidence",
      "claim": "bounded Rust-native evidence-bundle C5 only: an embedded language-neutral source map replaces the former Node source list; structural-evidence check/build reads only the five mapped bounded regular non-symlink strict JSON sources, requires one exact lowercase source commit, rejects duplicate keys, mixed commits, unsafe paths, sensitive-data signals and existing output, preserves exact source bytes, atomically publishes a deterministic workbench-evidence-manifest.v1 bundle for an explicit RFC 3339 timestamp, and emits a canonical self-hashed receipt without inferring readiness or approval; synthetic clean-environment Cargo and installed-package E2E own the contract with Python/Node lookup 0, while protected source freshness, signatures, broader language-neutral fixture/oracle ownership, approved HIP C2 and C6 remain open"
    },
    "native_benchmark_catalog": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-catalog",
      "claim": "bounded Rust-native benchmark-catalog C5 only: a language-neutral source map replaces the former Node directory and first-target rules; structural-catalog check/build reads sorted bounded regular non-symlink strict JSON from the 21 tracked open-data reports and five PEER specimen snapshots, rejects duplicate keys, invalid checksums, duplicate IDs, unsafe paths and nondeterministic output, preserves unverified license/truth/reference/runner fields, reproduces all 26 prior case projections exactly, atomically publishes for an explicit RFC 3339 timestamp, and emits a canonical self-hashed receipt with network and command execution counts 0; Cargo and installed-package E2E own generation parity with Python/Node lookup 0, while acquisition freshness, license verification, runner/reference attachment, broader language-neutral fixture/oracle ownership, approved HIP C2 and C6 remain open"
    },
    "native_frontend_contract": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 contract only: a language-neutral source map and Rust-native structural-frontend-contract check/smoke/delivery/prototype/prototype-browser-smoke/workbench-v2-browser-smoke/browser-smoke/viewer-performance-probe/viewer-report-pdf-smoke/serve/viewer-manifest commands replace the former Node static package, build-smoke wrapper, built Vite delivery, offline prototype DOM shim, source Viewer HTTP server, source Viewer and Workbench prototype browser-smoke wrappers, the Workbench v2 Node build/browser wrapper, the Viewer report PDF verification wrapper, the direct Viewer performance verification entrypoint, and Viewer project-manifest verifiers; strict checks read duplicate-key-safe package, lock, prototype demo, and neutral Viewer manifest JSON plus bounded non-symlink required-file and emitted-asset inventories, require an exact neutral-JSON-to-JavaScript runtime projection, conservatively project all six prototype states without positive demo status, reject innerHTML/eval source markers, confine Viewer artifact paths to the declared repo, enforce dependency/script pins, Viewer shell/fallback markers, Workbench Viewer target, eager/lazy legacy chunk separation, project/drawing/variant minimums, release triples and locally present artifact-count parity; the source server implementation binds only fixed IPv4 loopback and serves GET/HEAD from explicit Viewer/data prefixes with traversal, dotfile and symlink rejection, while server dry-run binds 0 listeners; the prototype server is scoped to prototype/structural-workbench and the Workbench v2 server is scoped to the verified dist tree with a confined SPA fallback, and both route policies are product-tested without sockets, but this sandbox denies loopback bind with EPERM so hosted/clean-machine live-listener evidence remains open; the smoke command validates first, then Rust directly owns the frozen stop-on-failure npm ci and npm run build child-process sequence, rejects package/lock contract mutation, and performs a post-build delivery check, while smoke dry-run spawns 0 processes; the Workbench v2 command hashes the JSON loader and six specifications, directly owns the fixed VITE_BASE_PATH=/ npm build, rejects package/lock mutation, verifies delivery, replaces inherited NODE_OPTIONS with the exact loader value for its direct browser child and workers, and publishes only after both direct child exits are zero and the server has no request errors; all three browser-smoke dry-runs bind 0 listeners and spawn 0 processes, while Viewer and prototype live modes each own one ephemeral loopback server plus one direct Node child running the pinned Playwright CLI and publish only after exit code zero; the Viewer PDF command hashes the retained exporter, owns its direct Node child and temporary/explicit-output cleanup, rejects package/lock/exporter mutation, verifies bounded PDF/HTML hashes, header, size and five HTML markers, optionally requires three pdftotext markers when installed, and dry-run creates no output and spawns 0 processes; the Viewer performance command hashes four frozen probe inputs, owns its direct Node child and temporary/explicit-output cleanup, rejects input mutation, strictly decodes the bounded artifact, independently rechecks source-row identities, loopback URL, viewport, browser errors, canvas framing, ready-time and RAF budgets, and dry-run creates no output and spawns 0 processes; every receipt is canonical and self-hashed, read-only checks record command and network execution counts 0, smoke records direct child exits while explicitly leaving npm executable resolution and registry/cache access uninstrumented, and browser/PDF/performance commands explicitly leave browser page requests uninstrumented; missing gitignored release artifacts remain warnings, the package check runs without node_modules, and delivery consumes an already-built tree; Node, Playwright, Chromium, the static Viewer runtime, prototype JavaScript, the retained Viewer PDF exporter, and the retained Viewer performance probe still own JavaScript rendering, browser/PDF generation, performance sampling and runtime DOM/input/export/accessibility evidence, while npm/Vite/TypeScript and React/TypeScript remain required for the Workbench v2 build and application behavior; this sandbox cannot produce the live browser-smoke, Viewer PDF, or Viewer performance receipts because loopback bind is denied, so hosted/clean-machine browser evidence remains open, and these checks do not prove engineering result validity, provide native UI feature parity, authorize legacy deletion, or close C5/C6"
    },
    "native_frontend_build": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 build orchestration only: package build enters through structural-frontend-contract frontend-build; Rust freezes package/lock/source-map identity plus a bounded complete inventory of the configured frontend source files and roots, hashes installed TypeScript and Vite CLI entrypoint bytes for live execution, removes inherited NODE_OPTIONS, owns the two direct Node children, rejects source or CLI entrypoint mutation, validates the resulting bounded delivery tree, and emits a canonical self-hashed receipt, while dry-run requires no node_modules and spawns no process; Node executable identity, transitive npm package bytes, TypeScript/Vite internals, plugins, build environment and build-time network behavior remain outside Rust ownership, and no browser behavior, clean-machine publication, native UI parity, C5, or C6 is claimed"
    },
    "native_frontend_dev": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 development process ownership only: package dev enters through structural-frontend-contract frontend-dev; Rust validates the launch-time frontend contract, hashes the installed Vite CLI entrypoint, removes inherited NODE_OPTIONS, fixes IPv4 loopback and strict-port arguments, directly owns one Node child, records a successful terminal exit, and emits a canonical self-hashed receipt, while dry-run requires no node_modules and spawns no process; source mutation remains deliberately allowed for HMR and is not revalidated after launch, and Node executable identity, Vite internals and transitive packages, plugin and environment loading, HMR, retained listener readiness, page requests, rendered behavior, clean-machine publication, native UI parity, C5 and C6 remain open"
    },
    "native_frontend_audit": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 dependency-audit orchestration only: frontend-web invokes structural-frontend-contract frontend-audit directly instead of retaining direct npm audit shell authority; Rust validates the package/lock/source-map contract, removes inherited NODE_OPTIONS, owns one exact npm audit --audit-level high direct child, rejects repository-contract mutation, records every numeric exit in a canonical self-hashed receipt, and preserves the hosted workflow's non-blocking numeric-exit policy; exit zero is only npm's audit-clean exit and numeric nonzero is deliberately advisory_or_tool_failure because Rust does not parse or independently classify vulnerability, registry, network, configuration or tool failure, while direct dry-run resolves neither npm nor Node and spawns no child; live registry/advisory/cache access, npm configuration/environment, executable and transitive-process identities, external-cache mutation, finding counts and identities, dependency/license clearance, clean-machine publication, native UI parity, C5 and C6 remain open"
    },
    "native_frontend_audit_report": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 dependency-audit evidence authority only: scripts/build_frontend_dependency_audit_report.py launches one direct Cargo structural-frontend-contract frontend-audit-report command with direct Python npm, npx and Node entrypoints 0; Rust freezes the package, lock, source map and required inventory, removes inherited NODE_OPTIONS, owns one exact npm audit --json child, concurrently drains stdout and stderr with bounded retained memory, strictly rejects duplicate-key, non-finite, oversized and structurally invalid audit JSON plus metadata/finding-count contradictions as blocked evidence, projects the retained frontend-dependency-audit-report.v1 schema, rejects frontend-contract or destination mutation, and publishes verified report bytes through staging, backup rename and rollback before emitting a canonical self-hashed receipt; --fail-blocked changes only the final exit after report and receipt publication, while Python strictly validates their identities and retains only CLI/output compatibility; npm remains the advisory oracle, and live registry/advisory/cache access, npm configuration/environment, executable and transitive-process identities, external-cache mutation, independent advisory verification, dependency/license clearance, clean-machine publication, native UI parity, C5 and C6 remain open"
    },
    "native_quality_gate_frontend_entrypoints": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 quality-gate frontend-entrypoint ownership only: scripts/verify_quality_gate.py retains overall Python sequencing but invokes frontend install, strict dependency audit, contract check, build, Viewer manifest, browser smoke, sample workflow, PDF smoke, performance probe and visual regression through direct Cargo structural-frontend-contract commands with npm and npm package-script entrypoints 0; strict audit publishes the same canonical advisory_or_tool_failure receipt and then fails the Rust command on numeric nonzero, preserving the prior quality-gate stop-on-failure behavior, while frontend-web retains its separate non-blocking audit policy; npm, Node, TypeScript, Vite, Playwright, Chromium, Python sequencing, registry/network/configuration effects and rendered behavior remain retained, and clean-machine live evidence, native UI parity, C5 and C6 remain open"
    },
    "native_phase5_task_browser_smoke": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 Phase 5 task-smoke orchestration only: scripts/run_phase5_task_based_ux_browser_smoke.py invokes one direct Cargo structural-frontend-contract phase5-task-browser-smoke command with direct npm, npx, Node, preview-server and socket-readiness entrypoints 0; Rust freezes the complete frontend build plan, exact developer-preview Playwright specification, fixed five-step workflow vocabulary, fixed 127.0.0.1:4173 SPA route and direct Node command, owns the two build children unless --skip-build is explicit, validates delivery, owns the listener and Playwright child, rejects input or delivery mutation, and emits a canonical self-hashed receipt only after all child exits are zero and request errors are 0, while dry-run requires no dist, runtime, listener, browser or process; Python strictly validates that receipt but retains Cargo process launch plus legacy release-receipt metadata and publication, and Node, TypeScript, Vite, Playwright, Chromium, React/TypeScript rendered behavior, browser requests, human usability observation, hosted live evidence, native UI parity, C5 and C6 remain open"
    },
    "native_frontend_install": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 dependency-install orchestration only: all five hosted frontend/browser workflows invoke structural-frontend-contract frontend-install directly instead of direct npm ci or an npm package-script launcher; Rust validates the package/lock/source-map contract, removes inherited NODE_OPTIONS, owns one exact npm ci direct child, rejects contract mutation, records the successful exit, and emits a canonical self-hashed receipt; the direct Rust dry-run requires no node_modules, network or filesystem mutation, resolves neither npm nor Node and spawns no child, while optional package install:dependencies remains only a local launcher convenience outside that receipt; live npm registry/cache access, lifecycle scripts, configuration/environment, npm/Node executable identity, transitive processes, extracted package bytes, node_modules contents and rollback remain outside Rust ownership, and clean-machine publication, native UI parity, C5 and C6 remain open"
    },
    "native_frontend_ci_entrypoints": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 hosted-entrypoint ownership only: frontend-web, nightly-full-quality, runtime-input-viewer and viewer-browser workflows invoke every frontend product check, dependency audit, build, browser verifier and runtime installer through direct Cargo structural-frontend-contract commands, retain only the native benchmark-catalog and evidence-bundle Bash wrappers for repository-root and source-commit timestamp projection, and contain npm run entrypoints 0, npx entrypoints 0, direct Node entrypoints 0 and direct npm audit entrypoints 0; setup-node, npm, Node, TypeScript, Vite, Playwright, Chromium, JavaScript application behavior, package-script local conveniences and inner runtime/network/filesystem effects remain retained, while the separate AI worker contract workflow is intentionally outside this product-entrypoint slice; clean-machine live evidence, native UI parity, C5 and C6 remain open"
    },
    "native_frontend_preview": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 local serving only: package preview enters through structural-frontend-contract frontend-preview; Rust validates the pinned frontend contract and already-built delivery receipt, confines the delivery root and SPA fallback, permits only one fixed IPv4 loopback listener plus bounded GET/HEAD requests, rejects traversal, dotfiles and symlinks, spawns zero child processes, and emits a canonical self-hashed startup receipt, while dry-run validates the delivery and binds zero listeners; the npm package script remains a launcher convenience but the preview server requires no Node, Vite, browser or Python, and rendered behavior, clean-machine publication, native UI parity, C5 and C6 remain open"
    },
    "native_playwright_install": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 browser-install orchestration only: all five hosted browser workflows invoke structural-frontend-contract playwright-install directly instead of npx or an npm package-script launcher, while package install:browser-runtime remains a local convenience; Rust validates the frontend contract, hashes the installed Playwright CLI entrypoint, removes inherited NODE_OPTIONS, owns one exact node CLI.js install --with-deps chromium child, rejects package/source-map/CLI mutation, records the successful direct-child exit, and emits a canonical self-hashed receipt, while dry-run requires no node_modules, network, host mutation, browser or Rust-owned child process; the retained Playwright installer owns browser and operating-system dependency resolution, downloads, caches, elevation and host-package mutation, and downloaded byte identities, transitive processes, rollback, clean-machine publication, native UI parity, C5 and C6 remain outside this claim"
    },
    "native_viewer_report_pdf_export": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 product-publication authority only: package export:viewer-report-pdf enters through structural-frontend-contract viewer-report-pdf-export; Rust resolves and hashes bounded existing regular-file destinations, rejects symlinks and PDF/HTML aliasing before launch, owns the retained exporter child through the strict PDF smoke verifier, verifies PDF and HTML identities, detects destination mutation during generation, and stages verified bytes before backup/rename publication with rollback, while dry-run creates no output or process and optional --html-out preserves the legacy interface; the retained Node exporter, Playwright, Chromium, internal loopback server, Viewer JavaScript rendering, browser requests and raw PDF generation remain outside Rust ownership, so browser/PDF-generation parity, clean-machine live execution, native UI parity, C5 and C6 remain open"
    },
    "native_viewer_readme_capture": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 product-publication authority only: package capture:readme-viewer-image enters through structural-frontend-contract viewer-readme-capture; Rust hashes the pinned package/lock and three capture sources, replaces inherited camera environment variables with explicit bounded values, owns one direct retained Node child and temporary PNG, rejects source and output mutation, independently validates bounded CRC-correct PNG chunks plus the exact 1600x900 IHDR, and publishes verified bytes through the shared staging/backup/rename rollback contract, while dry-run creates no output, listener or process; the retained capture script, Playwright, Chromium, internal loopback server, Viewer JavaScript rendering, camera behavior, browser requests and screenshot generation remain outside Rust ownership, so human visual approval, customer device parity, clean-machine live execution, native UI parity, C5 and C6 remain open"
    },
    "native_viewer_js_syntax": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 CI-gate orchestration only: runtime-input-viewer CI and package verify:viewer-js-syntax enter through structural-frontend-contract viewer-js-syntax; Rust freezes the package/lock/source map and exact ordered SHA-256 identities of ten Viewer JavaScript sources, owns each direct child lifetime, rejects input mutation, requires every retained Node --check invocation to exit zero, and emits canonical self-hashed evidence, while dry-run spawns no process; the retained Node parser and executable identity remain outside Rust ownership, and this gate does not execute Viewer behavior, start a listener, access a network, require a browser, prove browser parity, close C5, or authorize C6 removal"
    },
    "native_viewer_visual_regression": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 verification only: structural-frontend-contract viewer-visual-regression validates and hashes the tracked baseline plus four frozen source inputs, owns one direct retained Node child and temporary or explicit report cleanup, rejects baseline or source mutation, strictly decodes bounded duplicate-key-free JSON, independently checks all 11 ordered workflow cases, IPv4 loopback URLs, browser-error absence, canvas geometry and signature hashes, exact workflow markers, source identities, recomputed baseline deltas and requested tolerances, and emits a canonical self-hashed receipt; dry-run creates no output, listener or process; the retained Node probe still owns its internal loopback server, Playwright, Chromium, Viewer JavaScript state manipulation and rendering, browser page requests, screenshots, canvas sampling and raw report construction, while explicit baseline refresh remains a direct operator Node command; customer browser/device matrix evidence, pixel-perfect rendering, clean-machine live execution, native UI parity and C6 remain open"
    },
    "native_viewer_sample_workflow": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-frontend-contract",
      "claim": "bounded transitional C0 verification only: structural-frontend-contract viewer-sample-workflow validates and hashes the pinned package/lock plus three frozen Viewer workflow inputs, owns one direct retained Node child and temporary or explicit artifact cleanup, rejects source mutation, strictly decodes bounded duplicate-key-free JSON, independently checks the exact four ordered MIDAS33/real-drawing rehearsal steps, recomputes completion-time and browser warning/error aggregates, requires nonblank canvases and significant-pixel evidence, and emits a canonical self-hashed receipt; scripts/build_ux_release_readiness_report.py --run-browser-smoke now defaults to that direct Cargo command with no outer npm package-script launcher, then consumes and re-hashes the receipt, while Python retains readiness-report assembly and publication; dry-run creates no output, listener or process; the retained Node probe still owns its internal loopback server, Playwright, Chromium, Viewer JavaScript navigation/input/rendering, browser page requests, canvas inspection and raw artifact construction; this automated smoke is not human new-user observation, engineering acceptance or release approval, and hosted live execution, native UI parity and C6 remain open"
    },
    "native_distribution": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-distribution",
      "claim": "bounded Linux CPU distribution C5 only: deterministic immutable static/shared directory bundles bind release, source, ABI, backend, linkage, compiler/build and every payload byte; they contain installed C/C++ headers and libraries plus structural-cli, structural-catalog, structural-evidence, structural-workbench and structural-installer, while shared Rust products use an origin-relative RUNPATH to the bundle's single-export sa_get_api_v1 library; exclusive locked, journaled and fsync/rename-published install/update/rollback recovers at prepared, materialized and activated crash boundaries and rejects release-ID reuse, symlinks, extra files and hash drift; installed strict ModelIR, exact-profile MGT import health, ModelIR linear and exact normalized-MGT-to-ModelIR-linear Import -> Validate -> Run -> Resume -> Compare -> Report paths execute in an empty PATH with Python/Node lookup 0 and bitwise direct/restart parity, followed by deterministic inspect, explicit non-promoting review, review reopen and handoff export; the installed ModelIR linear path binds ResultIR, typed recovery, external comparison, deterministic single-page PDF and both PDF/report receipts in the append-only v14 receipt, append-only v15 additionally binds repeated en-US/ko-KR embedded-font sparse PDF and typed receipt identities plus durable-workspace nonmutation, append-only v16 additionally binds original MGT bytes, normalized import health, typed recovery, PDF/receipt and review/export identities for the exact MGT-linear profile, append-only v17 binds repeated provenance-bound existing-nodal-load component edits with source nonmutation, C++ revalidation and exact model/receipt identities, and append-only v18 binds repeated existing-constraint prescribed-value edits with exact constraint/DOF/unit/value identities and the same source-nonmutation/C++ boundary; CMake consumers, structural-catalog check/build, structural-evidence check/build, benchmark catalog and built-evidence browsing are also installed and verified; installed nonlinear-NDTHA and ModelIR-linear en-US/ko-KR embedded-font PDF exports prove repeated bytes, locale distinction, durable-workspace nonmutation and exact redistributed TTF/OFL/provenance bindings; the installed C++-verified general ModelIR topology view proves repeated ANSI-free bytes and distinct isometric/XY/XZ/YZ projection identities plus default/explicit-English byte identity and deterministic Korean identity separation; the installed provenance-bound node-coordinate, existing-nodal-load component and existing-constraint prescribed-value edits prove repeated canonical model and receipt bytes, source nonmutation, C++ revalidation and deterministic edited-model rendering; all four installed NDTHA response-history channels prove repeated ANSI-free bytes, distinct identities, a bounded explicit window and session nonmutation; four exact-profile deformed-shape projections plus an explicit step/scale view prove C++ snapshot and terminal-adapter binding, deterministic ANSI-free bytes, distinct identities and session nonmutation; installed Korean topology, response and deformed views prove UTF-8 determinism, English/Korean identity separation and session nonmutation, while frozen v1 through v17 receipts retain their narrower authority; the protected ROCm lane builds the separate rocm profile whose product ABI install RUNPATH binds the declared host ROCm runtime directory and rejects unresolved libamdhip64 dependencies, then requires an installed-device receipt bound to the existing C2 receipt, but no authoritative ROCm distribution receipt is present locally; code signing, platform installers, remote update transport, retention/garbage collection, React/TypeScript removal and C6 remain open"
    },
    "native_deployment": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-workbench",
      "claim": "bounded CPU deployment-authority C5 cutover only: the active on-prem Containerfile builds the verified cpu-only static native distribution and runs structural-workbench as a non-root process with a read-only root, operator-mounted workspace, no network namespace, listener, port, secret, Python, Node or React runtime; the installed payload carries the C++-verified general ModelIR topology view, provenance-bound node-coordinate, existing-nodal-load component and existing-constraint prescribed-value edits, bounded English/Korean ModelIR topology views, bounded English/Korean NDTHA response-history and exact-profile deformed-shape views; CPU static/shared distribution v18 E2E additionally proves the installed ModelIR-linear and exact normalized-MGT-linear staged/restart/direct flows, typed recovery, external comparison, deterministic PDF/report receipts, normalized import health, original MGT source binding, repeated provenance-bound existing-nodal-load component and existing-constraint prescribed-value edits and repeated en-US/ko-KR embedded-font sparse PDF exports with empty PATH and fallback 0; a Docker-independent unprivileged Linux harness runs the verified ModelIR/MGT/ModelIR-linear/normalized-MGT-linear flows plus canonical inspect, explicit non-promoting review, review reopen, handoff export, benchmark catalog, copied-evidence browsing and repeated localized sparse PDF export as UID/GID 65532 with empty PATH, read-only root/payload, writable workspace and loopback-only networking, then the Rust installer verifies the operator artifact self-hashes, conservative catalog/evidence projections, ResultIR/recovery/comparison/PDF/document/receipt bindings, normalized MGT source/import-health bindings, localized PDF/receipt identities, exact installed font/license/provenance, locale separation and durable-session nonmutation before emitting and validating a v6 self-hashed local_rootfs_diagnostic_c5 receipt that explicitly is not an OCI/customer image receipt, generated approval evidence, or engineering approval; frozen v1 through v5 rootfs receipts preserve their original narrower authority; the former React Pages workflow is outside .github/workflows and the former Python project-ops container is rollback-only under deployment/legacy-python-onprem; customer image build/scan/sign/import and native publication receipts, general GUI parity, approved HIP C2, deprecation completion, source/test deletion and final C6 remain open"
    },
    "native_automation_cutover": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-distribution",
      "claim": "bounded production-automation C5 cutover only: the former Python evidence branch writer and Python/Node GitHub Release publisher are outside .github/workflows, their Python mutation helpers are outside scripts, and active contents:write and branch-push authority 0 is enforced fail-closed; the exact legacy workflows and helpers remain rollback-only deprecation material, while read-only Python oracle/technical receipt workflows, native signing/import publication, protected HIP C2, source/test deletion and final C6 remain open"
    },
    "hip_backend": {
      "status": "planned",
      "cutover_gate": null,
      "owner": "structural_c_abi_v1",
      "claim": "the hosted and CPU-only packages contain no HIP dependency and reject HIP selection through the ABI v1.12 backend table; an opt-in product-owned full-residual context plus reference-element/assembly, resident sparse-PCG, generalized-eigen, bounded resident nonlinear-static Newton and nonlinear-NDTHA Newmark/Newton C2 candidates exist only behind STRUCTURAL_ENABLE_HIP=ON; a separate deterministic ROCm distribution build/install/device-E2E lane is wired to bind its installed backend receipt to the full-residual C2 receipt, while protected native-hip-approved execution receipts, local ROCm package authority, parallel/general solver authority and C6 remain open"
    }
  }
}

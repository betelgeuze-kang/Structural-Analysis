{
  "schema_version": "native-capabilities.v1",
  "capabilities": {
    "abi_v1_base": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-ffi",
      "claim": "base descriptors, error buffers, layout, overflow and function table only"
    },
    "modelir_v2_rust_wire": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural-contracts",
      "claim": "strict UTF-8 JSON decode, duplicate-key and schema rejection, Python-compatible canonical bytes and content/semantic/provenance hashes only"
    },
    "modelir_v2_cpp_core": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_model_ir",
      "claim": "ABI v1.1 typed descriptor deep copy, C++ semantic validation, deterministic report/snapshot and Python oracle issue/readiness/blocker parity only"
    },
    "modelir_v2": {
      "status": "implemented",
      "cutover_gate": "C3",
      "owner": "structural-contracts",
      "claim": "strict Rust wire identity, typed C++ semantic validation, safe ABI v1.1 RAII round-trip and structural-cli model validate only; no solver, restart, ResultIR or backend readiness"
    },
    "linear_frame3d_cpu_alpha": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_c_abi_v1",
      "claim": "ABI v1.5 bounded CPU-only linear-elastic Timoshenko Frame3D compile/load-case solve for nodal loads, uniform initial-member-local QX/QY/QZ force loads, RX/RY/RZ member-end releases with stiffness and fixed-end-load static condensation, and finite node-to-member-end rigid offsets in global coordinates, plus a strict ModelIR v2 engine_v2_phase0_linear_3d adapter that derives standard-gravity self weight from finite material density and section area and deterministically flattens up to 256 nested linear load combinations and 4096 expanded terms before the existing load-case path, and a hash-bound bounded-candidate ResultIR projection, 2-16 nodes, 1-32 members, at most 128 source uniform member-load rows and at most 60 free equations; C++ boundary/lifetime tests, independent Python six-mode, rotated multi-member, rigid-offset transform, closed-form QX/QY/QZ uniform-load cantilever, and released-member static-condensation displacement/reaction/local-force parity, Rust closed-form self-weight and independent nested-combination superposition tests, and fail-closed independent Rust member-force recovery replay; a separate loopback Workbench composition reaches this exact solver without promoting its authority, while there is no nonuniform or member-point load, no translational release, no zero-effective-length offset, no support for singular or globally unstable release sets, no raw-ABI combination or self-weight descriptor, no HIP parity, no checkpoint and no release authority"
    },
    "linear_frame3d_result_report_alpha": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded structural-cli ModelIR-to-ResultIR/ReportIR/standalone-HTML flow plus strict persisted ResultIR-to-ReportIR/HTML replay for the exact CPU linear Timoshenko Frame Alpha profile with explicit pattern-or-combination selection, nullable exclusive load source bindings, nested linear load-combination superposition, nodal and uniform initial-member-local force loads, finite standard-gravity self weight derived from material density and section area, RX/RY/RZ member-end releases, and finite global rigid end offsets, strict schema and canonical hash replay, equilibrium plus independent Rust recovery-replay-gated bounded_candidate result authority, deterministic source-bound report extrema, explicit not_evaluated comparison authority, and a no-overwrite completed Workbench artifact bundle with manifest-last byte/hash binding; the separate loopback Workbench host can now submit/run this path, but there is no envelope/nonlinear combination, no translational release, no nonuniform or member-point load, no native-binary or portable-distribution PDF, no durable or packaged Workbench execution, no external comparison authority, no HIP parity, and no design or release authority"
    },
    "linear_frame3d_external_comparison_alpha": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded structural-cli ResultIR plus strict external ReferenceIR to hash-bound ComparisonIR or deterministic standalone HTML path with exact model/load/entity coverage, fixed global-node and member-local axis/sign declarations, m/mm and N/kN/N*m/kN*m normalization, component-level rows, fixed displacement/reaction 0.5% and member-end-force 1% scaled-difference gates, nonzero exit on evaluated gate failure, explicit operator-attached-or-synthetic provenance, a same-origin Workbench source-replay consumer and optional source-tree PDF presentation; the external export hash and same-model mapping are operator declarations, base ReportIR comparison remains not_evaluated, and no SAP2000/MIDAS/OpenSees/CalculiX execution receipt, independent validation, design, commercial or release authority is established"
    },
    "linear_frame3d_pdf_report_alpha": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-report-tooling",
      "claim": "source-tree ReportLab renderer that requires an explicit structural-cli binary, invokes strict persisted ResultIR-to-ReportIR replay and optional ReferenceIR-to-ComparisonIR replay before presentation, emits a same-runtime byte-deterministic invariant A4 ASCII PDF plus canonical no-overwrite receipt bound to PDF, ResultIR, ReportIR and optional ComparisonIR hashes, preserves evaluated comparison PASS/CHECK separately from external_validation=not_established, and visually verified multipage headers, footers and component rows; the workstation delivery builder packages only a parseable PDF with that receipt and its exact schema, replays schema/hash/bytes/page-count/authority during restore and no longer fabricates a placeholder report, but no native-binary PDF backend, portable CLI distribution or Workbench packaging/action, cross-ReportLab-version byte identity, external solver execution receipt, independent validation, design, commercial or release authority is established"
    },
    "linear_frame3d_workbench_consumer_alpha": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "workbench-v2",
      "claim": "same-origin typed ResultIR/ReportIR consumer with direct-pair, completed CLI bundle, or bounded native job-view loading plus an optional atomic external ReferenceIR/ComparisonIR pair; when an explicit same-origin loopback submission endpoint is configured, its separate run panel uploads exact ModelIR text, submits one immutable job, starts a synchronous bounded child structural-cli run request, polls the strictly replayed materialized view through concurrent same-origin requests and hands only the terminal job URL to the unchanged strict consumer; duplicate-key, job event/view and terminal bundle binding, manifest byte/hash, schema, source, equilibrium/recovery, extrema, authority and comparison mapping/unit/tolerance/row/summary/hash replay still run before display; queued/running jobs remain pending, a worker timeout/process/status failure after strict revision-1 Running is append-only terminalized as Failed without bundle authority, other states are not rewritten, and invalid or partial comparisons expose neither comparison artifact; this is live polling and bounded failure finalization rather than a background queue, cancellation, retry or recovery, the child-worker boundary is not a privilege sandbox or CPU/memory resource limit, and there is no resume, crash recovery, browser-side solver recovery reconstruction, actual external execution receipt, independent external validation, PDF action, packaged Workbench, design, commercial or release authority"
    },
    "linear_frame3d_job_alpha": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-runtime",
      "claim": "bounded filesystem_append_only_single_host.v1 Frame3D job request, append-only self-hashed event chain, atomic materialized view, one-attempt lock, terminal Workbench bundle publication and structural-cli submit/run/inspect commands, plus a loopback-only same-origin HTTP host with a strict browser submission envelope, at most 16 concurrent requests, strict view polling while a synchronous bounded-time child structural-cli process run request is in flight, duplicate-active-worker rejection and normal bounded-shutdown request joining before strict terminal bundle replay; a worker timeout/process/status failure after strict revision-1 Running appends a revision-2 Failed event/view without bundle authority, while queued, terminal, corrupt and partial states are not rewritten; this live polling and bounded failure finalization is not a background queue, cancellation, retry, resume, stale-lock cleanup or durable crash recovery, and the solver-process crash boundary is not a privilege sandbox or CPU/memory resource limit; there is no authentication, multi-host scheduling, packaged Workbench, design, commercial or release authority"
    },
    "linear_frame3d_cli_distribution_alpha": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-cli",
      "claim": "deterministic source-bound portable ZIP candidate containing the release structural-cli binary, strict distribution, smoke and bounded external comparison schemas plus the loopback job-submission schema, one analysis-ready ModelIR example, license and bounded workflow instructions, with duplicate/path/hash validation and extracted same-runner validate-to-Workbench-bundle smoke; the binary exposes comparison and workstation hosting but no operator-attached external reference, comparison receipt or Workbench static build is packaged; no installer, code signing, SBOM, clean-machine or offline dependency audit; no retained Linux/Windows passing receipt, Workbench application package, PDF, design, commercial or release authority"
    },
    "checkpoint_restart": {
      "status": "planned",
      "cutover_gate": null,
      "owner": "structural-runtime",
      "claim": "no checkpoint or restart authority in Slice A"
    },
    "product_e2e": {
      "status": "planned",
      "cutover_gate": null,
      "owner": "structural-cli",
      "claim": "bounded linear Frame Alpha CLI analysis-to-ResultIR/ReportIR/HTML, strict persisted ResultIR report replay, strict operator-declared external ReferenceIR-to-ComparisonIR/HTML evaluation, a source-tree deterministic PDF/receipt projection with fail-closed workstation delivery-package binding, filesystem single-host one-attempt jobs, a loopback same-origin Workbench ModelIR submit/bounded-child-worker synchronous-run/live-view-poll/terminal-bundle replay path with duplicate-active-worker rejection and append-only strict-Running worker failure finalization, and a same-runner portable CLI distribution smoke exist, but no real external comparison receipt is attached and background queue/cancellation/retry/resume/durable crash recovery, privilege sandbox/resource limits, clean-machine installation, packaged Workbench execution, native-binary or portable-CLI PDF and general product E2E remain unimplemented"
    },
    "hip_backend": {
      "status": "planned",
      "cutover_gate": null,
      "owner": "structural_c_abi_v1",
      "claim": "hosted build contains no HIP backend"
    }
  }
}

{
  "schema_version": "native-capabilities.v1",
  "capabilities": {
    "abi_v1_base": {
      "status": "implemented",
      "cutover_gate": "C0",
      "owner": "structural-ffi",
      "claim": "base descriptors, error buffers, layout, overflow and function table only"
    },
    "modelir_v2_rust_wire": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural-contracts",
      "claim": "strict UTF-8 JSON decode, duplicate-key and schema rejection, Python-compatible canonical bytes and content/semantic/provenance hashes only"
    },
    "modelir_v2_cpp_core": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_model_ir",
      "claim": "ABI v1.1 typed descriptor deep copy, C++ semantic validation, deterministic report/snapshot and Python oracle issue/readiness/blocker parity only"
    },
    "modelir_v2": {
      "status": "implemented",
      "cutover_gate": "C3",
      "owner": "structural-contracts",
      "claim": "strict Rust wire identity, typed C++ semantic validation, safe ABI v1.1 RAII round-trip and structural-cli model validate only; no solver, restart, ResultIR or backend readiness"
    },
    "track_point_load_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ track point-load kernel with Python C1 oracle parity for the 9-node midpoint-load support/theory matrix, ABI v1.2 caller-owned output and safe Rust integration; the frozen legacy Rust endpoint-only divergence is retained, while broader input-space parity, HIP C2, restart and product E2E remain open"
    },
    "nonlinear_static_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ story-frame Newton kernel with independent dense-matrix Python C1 oracle parity for the five-case 1/3-story topology, elastic/plastic, mixed-sign load, P-delta and backtracking matrix, ABI v1.3 caller-owned inputs/output and safe Rust integration; broader nonlinear input-space parity, HIP C2, restart and product E2E remain open"
    },
    "nonlinear_ndtha_cpu": {
      "status": "implemented",
      "cutover_gate": "C1",
      "owner": "structural_solver_cpu",
      "claim": "bounded serial FP64 C++ Newmark/Newton story-frame kernel with shared constitutive assembly and independent dense-matrix Python C1 full-result parity for the five-case 1/2/3-story Newmark parameter, elastic/plastic, mixed-sign acceleration, P-delta, damping-cap, adaptive retry, line-search and collapse matrix, plus ABI v1.4 nested caller-owned inputs/outputs, safe Rust integration and the frozen legacy Rust boundary; broader dynamic input-space parity, HIP C2 and product E2E remain open, while restart is promoted only as the separate bounded CPU checkpoint capability"
    },
    "checkpoint_restart": {
      "status": "implemented",
      "cutover_gate": "C4",
      "owner": "structural-runtime",
      "claim": "bounded CPU nonlinear-NDTHA checkpoint C4 only: ABI v1.5 validates complete caller-owned inter-step state; structural-runtime emits a canonical pointer-free little-endian artifact bound to independent model, state and execution SHA-256 identities, publishes it by same-directory write/sync/rename/directory-sync, rejects corruption and binding mismatch, and resumes bitwise-identically; job-state crash recovery and product E2E are promoted only as separate bounded CPU C5 capabilities, while HIP C2, broader solver coverage and release authority remain open"
    },
    "product_e2e": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-cli",
      "claim": "bounded CPU nonlinear-NDTHA C5 only: a strict canonical request crosses Rust to ABI v1.5 C++ execution, C4 checkpoint identity, deterministic ResultIR, ReportIR, Markdown document source and a self-hashed artifact receipt through public CLI run/resume; direct and resumed outputs are byte-identical in an environment with no Python or Node lookup, and authority remains bounded_candidate; HIP C2, ModelIR-to-analysis adaptation, distributed durable jobs/API, Workbench, external comparison, PDF rendering and C6 remain open"
    },
    "durable_jobs": {
      "status": "implemented",
      "cutover_gate": "C5",
      "owner": "structural-runtime",
      "claim": "bounded single-host CPU nonlinear-NDTHA durable-job C5 only: Rust owns strict idempotent submit/poll/cancel, append-only self-hashed event chains, content-addressed artifacts, OS-lock worker leases, expired-lease crash reconciliation, checkpoint resume, deterministic completion re-projection and public CLI work/export; clean-environment direct and restarted artifacts are byte-identical with no Python or Node lookup; tenant authorization, distributed consensus/claims, service API, HIP C2, broader solver families and C6 remain open"
    },
    "hip_backend": {
      "status": "planned",
      "cutover_gate": null,
      "owner": "structural_c_abi_v1",
      "claim": "hosted build contains no HIP backend"
    }
  }
}
